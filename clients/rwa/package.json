{
  "name": "@yamale/rwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The investor-facing app for Yamale's real-world-asset vehicles: what a vehicle is, what a holder owns, and what could take it from them.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/*.test.ts"
  },
  "dependencies": {
    "@cosmjs/proto-signing": "^0.32.4",
    "@tanstack/react-query": "^5.62.0",
    "@yamale/chain": "*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
