// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/netting/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Cycle, Obligation, Position, Reserve } from "./netting.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.netting.v1";

/**
 * GenesisState defines the netting module's genesis state.
 *
 * Two pieces of the module's state are deliberately absent from it: the locked
 * figure that limits what a participant may owe, and the index from a
 * participant to its obligations. Both are derived in InitGenesis from the
 * records below.
 *
 * The locked figure is the one that matters. It is the collateral check, so a
 * figure imported independently of the positions that justify it would be a
 * number the chain believes over its own records — and the failure that
 * follows is a participant withdrawing reserve it has already committed. Made
 * underivable, that is unrepresentable.
 */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params:
    | Params
    | undefined;
  /** cycles is every netting window, open, settled or held. */
  cycles: Cycle[];
  /** obligations is every obligation ever submitted. */
  obligations: Obligation[];
  /**
   * positions is every non-zero net position. A settled cycle keeps its
   * positions: they are the record of what each participant actually paid or
   * received, which is what a participant reconciles its own books against.
   */
  positions: Position[];
  /**
   * reserves is what each participant has prefunded, per currency. The sum of
   * these must equal what the module account holds, or the chain is claiming
   * custody of money that is not there.
   */
  reserves: Reserve[];
  /**
   * current_cycle is the open window. There is always exactly one, written by
   * InitGenesis in every case including a fresh chain, so that the end blocker
   * can never read a cycle that was never created.
   */
  currentCycle: string;
  /**
   * cycle_count and obligation_count are the next ids to be issued, never the
   * number issued so far. Neither may be zero: an id of zero is
   * indistinguishable from an unset field in proto3, and this repository has
   * been bitten by that three times. A fresh chain starts obligation_count at
   * 1 and cycle_count at 2, because cycle 1 is the window it opens with.
   */
  cycleCount: string;
  obligationCount: string;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    cycles: [],
    obligations: [],
    positions: [],
    reserves: [],
    currentCycle: "0",
    cycleCount: "0",
    obligationCount: "0",
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.cycles) {
      Cycle.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.obligations) {
      Obligation.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.positions) {
      Position.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.reserves) {
      Reserve.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.currentCycle !== "0") {
      writer.uint32(48).uint64(message.currentCycle);
    }
    if (message.cycleCount !== "0") {
      writer.uint32(56).uint64(message.cycleCount);
    }
    if (message.obligationCount !== "0") {
      writer.uint32(64).uint64(message.obligationCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.cycles.push(Cycle.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.obligations.push(Obligation.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.positions.push(Position.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.reserves.push(Reserve.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.currentCycle = longToString(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.cycleCount = longToString(reader.uint64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.obligationCount = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      cycles: globalThis.Array.isArray(object?.cycles) ? object.cycles.map((e: any) => Cycle.fromJSON(e)) : [],
      obligations: globalThis.Array.isArray(object?.obligations)
        ? object.obligations.map((e: any) => Obligation.fromJSON(e))
        : [],
      positions: globalThis.Array.isArray(object?.positions)
        ? object.positions.map((e: any) => Position.fromJSON(e))
        : [],
      reserves: globalThis.Array.isArray(object?.reserves) ? object.reserves.map((e: any) => Reserve.fromJSON(e)) : [],
      currentCycle: isSet(object.currentCycle) ? globalThis.String(object.currentCycle) : "0",
      cycleCount: isSet(object.cycleCount) ? globalThis.String(object.cycleCount) : "0",
      obligationCount: isSet(object.obligationCount) ? globalThis.String(object.obligationCount) : "0",
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.cycles?.length) {
      obj.cycles = message.cycles.map((e) => Cycle.toJSON(e));
    }
    if (message.obligations?.length) {
      obj.obligations = message.obligations.map((e) => Obligation.toJSON(e));
    }
    if (message.positions?.length) {
      obj.positions = message.positions.map((e) => Position.toJSON(e));
    }
    if (message.reserves?.length) {
      obj.reserves = message.reserves.map((e) => Reserve.toJSON(e));
    }
    if (message.currentCycle !== "0") {
      obj.currentCycle = message.currentCycle;
    }
    if (message.cycleCount !== "0") {
      obj.cycleCount = message.cycleCount;
    }
    if (message.obligationCount !== "0") {
      obj.obligationCount = message.obligationCount;
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.cycles = object.cycles?.map((e) => Cycle.fromPartial(e)) || [];
    message.obligations = object.obligations?.map((e) => Obligation.fromPartial(e)) || [];
    message.positions = object.positions?.map((e) => Position.fromPartial(e)) || [];
    message.reserves = object.reserves?.map((e) => Reserve.fromPartial(e)) || [];
    message.currentCycle = object.currentCycle ?? "0";
    message.cycleCount = object.cycleCount ?? "0";
    message.obligationCount = object.obligationCount ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
