// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/netting/v1/tx.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { SettlementMode, settlementModeFromJSON, settlementModeToJSON } from "./netting.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.netting.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /** NOTE: All parameters must be supplied. */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/**
 * MsgPostReserve moves coins from a participant's own balance into the
 * module account, where they become that participant's settlement reserve.
 *
 * The reserve is what makes deferred settlement safe here. A participant may
 * only run a net debit up to what it has already prefunded, so by the time a
 * cycle closes every debit in it is covered by money the chain is already
 * holding. Settlement is then a rearrangement of balances the module already
 * custodies rather than a request to institutions to pay — and a request is
 * the thing that can be refused.
 */
export interface MsgPostReserve {
  /**
   * participant is the approved institution posting the reserve, and the only
   * valid signer.
   */
  participant: string;
  /**
   * amount may name several currencies at once; each is reserved separately,
   * because netting never crosses currencies.
   */
  amount: Coin[];
}

/** MsgPostReserveResponse defines the MsgPostReserve response. */
export interface MsgPostReserveResponse {
}

/**
 * MsgWithdrawReserve returns reserve to the participant's own balance.
 *
 * Only the uncommitted part can leave. Anything backing a position in a cycle
 * that has not settled stays where it is — otherwise a participant could
 * submit its obligations, withdraw the collateral behind them, and leave its
 * counterparties holding a claim on an empty account, which is the exact
 * failure the reserve exists to make impossible.
 */
export interface MsgWithdrawReserve {
  /** participant is the institution withdrawing, and the only valid signer. */
  participant: string;
  /** amount is what to withdraw, per currency. */
  amount: Coin[];
}

/** MsgWithdrawReserveResponse defines the MsgWithdrawReserve response. */
export interface MsgWithdrawReserveResponse {
}

/**
 * MsgSubmitObligation records what one participant owes another, as the net
 * result of the retail activity it settled on its own books.
 *
 * Whether it nets or settles gross is decided by the chain from the amount and
 * the currency's policy, never by the sender. A participant that could choose
 * would put its largest items into the deferred window, which is precisely
 * backwards: the threshold exists so that the exposures nobody would want
 * deferred are the ones that cannot be.
 */
export interface MsgSubmitObligation {
  /** from_participant owes and signs; to_participant is owed. */
  fromParticipant: string;
  toParticipant: string;
  /** denom and amount are the currency and the figure owed. */
  denom: string;
  amount: string;
  /**
   * batch_hash is SHA-256 over the salted retail batch this figure summarises.
   * Required, 32 bytes; see Obligation.batch_hash.
   */
  batchHash: Uint8Array;
}

/**
 * MsgSubmitObligationResponse tells the sender what became of the obligation.
 *
 * It carries the mode because the sender does not decide it and cannot compute
 * it without reading the parameters: an institution needs to know from the
 * response whether the money has already moved or whether it is now carrying
 * an exposure until the cycle closes.
 */
export interface MsgSubmitObligationResponse {
  /**
   * id is the obligation's chain-wide number, cycle_id the window it landed
   * in, and mode whether it settled gross on the spot or joined the netting.
   */
  id: string;
  cycleId: string;
  mode: SettlementMode;
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams = {
  encode(message: MsgUpdateParams, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse = {
  encode(_: MsgUpdateParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgPostReserve(): MsgPostReserve {
  return { participant: "", amount: [] };
}

export const MsgPostReserve = {
  encode(message: MsgPostReserve, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.participant !== "") {
      writer.uint32(10).string(message.participant);
    }
    for (const v of message.amount) {
      Coin.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgPostReserve {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPostReserve();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.participant = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.amount.push(Coin.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPostReserve {
    return {
      participant: isSet(object.participant) ? globalThis.String(object.participant) : "",
      amount: globalThis.Array.isArray(object?.amount) ? object.amount.map((e: any) => Coin.fromJSON(e)) : [],
    };
  },

  toJSON(message: MsgPostReserve): unknown {
    const obj: any = {};
    if (message.participant !== "") {
      obj.participant = message.participant;
    }
    if (message.amount?.length) {
      obj.amount = message.amount.map((e) => Coin.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MsgPostReserve>): MsgPostReserve {
    return MsgPostReserve.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgPostReserve>): MsgPostReserve {
    const message = createBaseMsgPostReserve();
    message.participant = object.participant ?? "";
    message.amount = object.amount?.map((e) => Coin.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMsgPostReserveResponse(): MsgPostReserveResponse {
  return {};
}

export const MsgPostReserveResponse = {
  encode(_: MsgPostReserveResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgPostReserveResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPostReserveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgPostReserveResponse {
    return {};
  },

  toJSON(_: MsgPostReserveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgPostReserveResponse>): MsgPostReserveResponse {
    return MsgPostReserveResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgPostReserveResponse>): MsgPostReserveResponse {
    const message = createBaseMsgPostReserveResponse();
    return message;
  },
};

function createBaseMsgWithdrawReserve(): MsgWithdrawReserve {
  return { participant: "", amount: [] };
}

export const MsgWithdrawReserve = {
  encode(message: MsgWithdrawReserve, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.participant !== "") {
      writer.uint32(10).string(message.participant);
    }
    for (const v of message.amount) {
      Coin.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgWithdrawReserve {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWithdrawReserve();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.participant = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.amount.push(Coin.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgWithdrawReserve {
    return {
      participant: isSet(object.participant) ? globalThis.String(object.participant) : "",
      amount: globalThis.Array.isArray(object?.amount) ? object.amount.map((e: any) => Coin.fromJSON(e)) : [],
    };
  },

  toJSON(message: MsgWithdrawReserve): unknown {
    const obj: any = {};
    if (message.participant !== "") {
      obj.participant = message.participant;
    }
    if (message.amount?.length) {
      obj.amount = message.amount.map((e) => Coin.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MsgWithdrawReserve>): MsgWithdrawReserve {
    return MsgWithdrawReserve.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgWithdrawReserve>): MsgWithdrawReserve {
    const message = createBaseMsgWithdrawReserve();
    message.participant = object.participant ?? "";
    message.amount = object.amount?.map((e) => Coin.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMsgWithdrawReserveResponse(): MsgWithdrawReserveResponse {
  return {};
}

export const MsgWithdrawReserveResponse = {
  encode(_: MsgWithdrawReserveResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgWithdrawReserveResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWithdrawReserveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgWithdrawReserveResponse {
    return {};
  },

  toJSON(_: MsgWithdrawReserveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgWithdrawReserveResponse>): MsgWithdrawReserveResponse {
    return MsgWithdrawReserveResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgWithdrawReserveResponse>): MsgWithdrawReserveResponse {
    const message = createBaseMsgWithdrawReserveResponse();
    return message;
  },
};

function createBaseMsgSubmitObligation(): MsgSubmitObligation {
  return { fromParticipant: "", toParticipant: "", denom: "", amount: "", batchHash: new Uint8Array(0) };
}

export const MsgSubmitObligation = {
  encode(message: MsgSubmitObligation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.fromParticipant !== "") {
      writer.uint32(10).string(message.fromParticipant);
    }
    if (message.toParticipant !== "") {
      writer.uint32(18).string(message.toParticipant);
    }
    if (message.denom !== "") {
      writer.uint32(26).string(message.denom);
    }
    if (message.amount !== "") {
      writer.uint32(34).string(message.amount);
    }
    if (message.batchHash.length !== 0) {
      writer.uint32(42).bytes(message.batchHash);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSubmitObligation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSubmitObligation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.fromParticipant = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.toParticipant = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.amount = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.batchHash = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSubmitObligation {
    return {
      fromParticipant: isSet(object.fromParticipant) ? globalThis.String(object.fromParticipant) : "",
      toParticipant: isSet(object.toParticipant) ? globalThis.String(object.toParticipant) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      batchHash: isSet(object.batchHash) ? bytesFromBase64(object.batchHash) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgSubmitObligation): unknown {
    const obj: any = {};
    if (message.fromParticipant !== "") {
      obj.fromParticipant = message.fromParticipant;
    }
    if (message.toParticipant !== "") {
      obj.toParticipant = message.toParticipant;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.batchHash.length !== 0) {
      obj.batchHash = base64FromBytes(message.batchHash);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSubmitObligation>): MsgSubmitObligation {
    return MsgSubmitObligation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSubmitObligation>): MsgSubmitObligation {
    const message = createBaseMsgSubmitObligation();
    message.fromParticipant = object.fromParticipant ?? "";
    message.toParticipant = object.toParticipant ?? "";
    message.denom = object.denom ?? "";
    message.amount = object.amount ?? "";
    message.batchHash = object.batchHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgSubmitObligationResponse(): MsgSubmitObligationResponse {
  return { id: "0", cycleId: "0", mode: 0 };
}

export const MsgSubmitObligationResponse = {
  encode(message: MsgSubmitObligationResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.cycleId !== "0") {
      writer.uint32(16).uint64(message.cycleId);
    }
    if (message.mode !== 0) {
      writer.uint32(24).int32(message.mode);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSubmitObligationResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSubmitObligationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.cycleId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSubmitObligationResponse {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      cycleId: isSet(object.cycleId) ? globalThis.String(object.cycleId) : "0",
      mode: isSet(object.mode) ? settlementModeFromJSON(object.mode) : 0,
    };
  },

  toJSON(message: MsgSubmitObligationResponse): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.cycleId !== "0") {
      obj.cycleId = message.cycleId;
    }
    if (message.mode !== 0) {
      obj.mode = settlementModeToJSON(message.mode);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSubmitObligationResponse>): MsgSubmitObligationResponse {
    return MsgSubmitObligationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSubmitObligationResponse>): MsgSubmitObligationResponse {
    const message = createBaseMsgSubmitObligationResponse();
    message.id = object.id ?? "0";
    message.cycleId = object.cycleId ?? "0";
    message.mode = object.mode ?? 0;
    return message;
  },
};

/**
 * Msg defines the Msg service.
 *
 * Four messages, and there is deliberately no fifth. Nothing here can reduce,
 * reassign, cancel or recompute an obligation — not a participant, not the
 * supervisor, not governance. That absence is the module's central safety
 * property, and it is enforced by there being no code that could do it. A
 * settlement system whose administrator can rewrite what was already owed has
 * unwinding risk whatever its documentation says.
 */
export interface Msg {
  /**
   * UpdateParams defines a (governance) operation for updating the module
   * parameters. The authority defaults to the x/gov module account.
   */
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
  /** PostReserve prefunds a participant's settlement reserve. */
  PostReserve(request: MsgPostReserve): Promise<MsgPostReserveResponse>;
  /** WithdrawReserve takes back the part of a reserve that is not committed. */
  WithdrawReserve(request: MsgWithdrawReserve): Promise<MsgWithdrawReserveResponse>;
  /** SubmitObligation records what one participant owes another. */
  SubmitObligation(request: MsgSubmitObligation): Promise<MsgSubmitObligationResponse>;
}

export const MsgServiceName = "blockchain.netting.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.UpdateParams = this.UpdateParams.bind(this);
    this.PostReserve = this.PostReserve.bind(this);
    this.WithdrawReserve = this.WithdrawReserve.bind(this);
    this.SubmitObligation = this.SubmitObligation.bind(this);
  }
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(_m0.Reader.create(data)));
  }

  PostReserve(request: MsgPostReserve): Promise<MsgPostReserveResponse> {
    const data = MsgPostReserve.encode(request).finish();
    const promise = this.rpc.request(this.service, "PostReserve", data);
    return promise.then((data) => MsgPostReserveResponse.decode(_m0.Reader.create(data)));
  }

  WithdrawReserve(request: MsgWithdrawReserve): Promise<MsgWithdrawReserveResponse> {
    const data = MsgWithdrawReserve.encode(request).finish();
    const promise = this.rpc.request(this.service, "WithdrawReserve", data);
    return promise.then((data) => MsgWithdrawReserveResponse.decode(_m0.Reader.create(data)));
  }

  SubmitObligation(request: MsgSubmitObligation): Promise<MsgSubmitObligationResponse> {
    const data = MsgSubmitObligation.encode(request).finish();
    const promise = this.rpc.request(this.service, "SubmitObligation", data);
    return promise.then((data) => MsgSubmitObligationResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
