// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/netting/v1/netting.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.netting.v1";

/** SettlementMode is how one obligation was disposed of. */
export const SettlementMode = {
  /** SETTLEMENT_MODE_UNSPECIFIED - SETTLEMENT_MODE_UNSPECIFIED is the unset default and is never valid. */
  SETTLEMENT_MODE_UNSPECIFIED: 0,
  /**
   * SETTLEMENT_MODE_GROSS - SETTLEMENT_MODE_GROSS means the amount moved between the two participants
   * in the block the obligation was submitted in, as an ordinary transfer.
   * Nothing about it is deferred and it takes no part in any cycle's netting.
   */
  SETTLEMENT_MODE_GROSS: 1,
  /**
   * SETTLEMENT_MODE_NET - SETTLEMENT_MODE_NET means the obligation joined the open cycle and altered
   * both participants' running positions. It settles when the cycle closes.
   */
  SETTLEMENT_MODE_NET: 2,
  UNRECOGNIZED: -1,
} as const;

export type SettlementMode = typeof SettlementMode[keyof typeof SettlementMode];

export namespace SettlementMode {
  export type SETTLEMENT_MODE_UNSPECIFIED = typeof SettlementMode.SETTLEMENT_MODE_UNSPECIFIED;
  export type SETTLEMENT_MODE_GROSS = typeof SettlementMode.SETTLEMENT_MODE_GROSS;
  export type SETTLEMENT_MODE_NET = typeof SettlementMode.SETTLEMENT_MODE_NET;
  export type UNRECOGNIZED = typeof SettlementMode.UNRECOGNIZED;
}

export function settlementModeFromJSON(object: any): SettlementMode {
  switch (object) {
    case 0:
    case "SETTLEMENT_MODE_UNSPECIFIED":
      return SettlementMode.SETTLEMENT_MODE_UNSPECIFIED;
    case 1:
    case "SETTLEMENT_MODE_GROSS":
      return SettlementMode.SETTLEMENT_MODE_GROSS;
    case 2:
    case "SETTLEMENT_MODE_NET":
      return SettlementMode.SETTLEMENT_MODE_NET;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SettlementMode.UNRECOGNIZED;
  }
}

export function settlementModeToJSON(object: SettlementMode): string {
  switch (object) {
    case SettlementMode.SETTLEMENT_MODE_UNSPECIFIED:
      return "SETTLEMENT_MODE_UNSPECIFIED";
    case SettlementMode.SETTLEMENT_MODE_GROSS:
      return "SETTLEMENT_MODE_GROSS";
    case SettlementMode.SETTLEMENT_MODE_NET:
      return "SETTLEMENT_MODE_NET";
    case SettlementMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** CycleStatus is where a netting window has got to. */
export const CycleStatus = {
  /** CYCLE_STATUS_UNSPECIFIED - CYCLE_STATUS_UNSPECIFIED is the unset default and is never valid. */
  CYCLE_STATUS_UNSPECIFIED: 0,
  /**
   * CYCLE_STATUS_OPEN - CYCLE_STATUS_OPEN is the window obligations are currently joining. Exactly
   * one cycle is open at a time.
   */
  CYCLE_STATUS_OPEN: 1,
  /** CYCLE_STATUS_SETTLED - CYCLE_STATUS_SETTLED means every currency in the window cleared. */
  CYCLE_STATUS_SETTLED: 2,
  /**
   * CYCLE_STATUS_HELD - CYCLE_STATUS_HELD means at least one currency in the window did not clear
   * and is waiting to be retried. The obligations in it are untouched and
   * still owed at their original amounts — see DenomOutcome.hold_reason.
   */
  CYCLE_STATUS_HELD: 3,
  UNRECOGNIZED: -1,
} as const;

export type CycleStatus = typeof CycleStatus[keyof typeof CycleStatus];

export namespace CycleStatus {
  export type CYCLE_STATUS_UNSPECIFIED = typeof CycleStatus.CYCLE_STATUS_UNSPECIFIED;
  export type CYCLE_STATUS_OPEN = typeof CycleStatus.CYCLE_STATUS_OPEN;
  export type CYCLE_STATUS_SETTLED = typeof CycleStatus.CYCLE_STATUS_SETTLED;
  export type CYCLE_STATUS_HELD = typeof CycleStatus.CYCLE_STATUS_HELD;
  export type UNRECOGNIZED = typeof CycleStatus.UNRECOGNIZED;
}

export function cycleStatusFromJSON(object: any): CycleStatus {
  switch (object) {
    case 0:
    case "CYCLE_STATUS_UNSPECIFIED":
      return CycleStatus.CYCLE_STATUS_UNSPECIFIED;
    case 1:
    case "CYCLE_STATUS_OPEN":
      return CycleStatus.CYCLE_STATUS_OPEN;
    case 2:
    case "CYCLE_STATUS_SETTLED":
      return CycleStatus.CYCLE_STATUS_SETTLED;
    case 3:
    case "CYCLE_STATUS_HELD":
      return CycleStatus.CYCLE_STATUS_HELD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CycleStatus.UNRECOGNIZED;
  }
}

export function cycleStatusToJSON(object: CycleStatus): string {
  switch (object) {
    case CycleStatus.CYCLE_STATUS_UNSPECIFIED:
      return "CYCLE_STATUS_UNSPECIFIED";
    case CycleStatus.CYCLE_STATUS_OPEN:
      return "CYCLE_STATUS_OPEN";
    case CycleStatus.CYCLE_STATUS_SETTLED:
      return "CYCLE_STATUS_SETTLED";
    case CycleStatus.CYCLE_STATUS_HELD:
      return "CYCLE_STATUS_HELD";
    case CycleStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** DenomStatus is the outcome for one currency inside one cycle. */
export const DenomStatus = {
  /** DENOM_STATUS_UNSPECIFIED - DENOM_STATUS_UNSPECIFIED is the unset default and is never valid. */
  DENOM_STATUS_UNSPECIFIED: 0,
  /** DENOM_STATUS_OPEN - DENOM_STATUS_OPEN means the cycle has not closed yet. */
  DENOM_STATUS_OPEN: 1,
  /**
   * DENOM_STATUS_SETTLED - DENOM_STATUS_SETTLED means every position in this currency was discharged
   * against the participants' reserves, in one indivisible step.
   */
  DENOM_STATUS_SETTLED: 2,
  /** DENOM_STATUS_HELD - DENOM_STATUS_HELD means the settlement was refused and nothing moved. */
  DENOM_STATUS_HELD: 3,
  UNRECOGNIZED: -1,
} as const;

export type DenomStatus = typeof DenomStatus[keyof typeof DenomStatus];

export namespace DenomStatus {
  export type DENOM_STATUS_UNSPECIFIED = typeof DenomStatus.DENOM_STATUS_UNSPECIFIED;
  export type DENOM_STATUS_OPEN = typeof DenomStatus.DENOM_STATUS_OPEN;
  export type DENOM_STATUS_SETTLED = typeof DenomStatus.DENOM_STATUS_SETTLED;
  export type DENOM_STATUS_HELD = typeof DenomStatus.DENOM_STATUS_HELD;
  export type UNRECOGNIZED = typeof DenomStatus.UNRECOGNIZED;
}

export function denomStatusFromJSON(object: any): DenomStatus {
  switch (object) {
    case 0:
    case "DENOM_STATUS_UNSPECIFIED":
      return DenomStatus.DENOM_STATUS_UNSPECIFIED;
    case 1:
    case "DENOM_STATUS_OPEN":
      return DenomStatus.DENOM_STATUS_OPEN;
    case 2:
    case "DENOM_STATUS_SETTLED":
      return DenomStatus.DENOM_STATUS_SETTLED;
    case 3:
    case "DENOM_STATUS_HELD":
      return DenomStatus.DENOM_STATUS_HELD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DenomStatus.UNRECOGNIZED;
  }
}

export function denomStatusToJSON(object: DenomStatus): string {
  switch (object) {
    case DenomStatus.DENOM_STATUS_UNSPECIFIED:
      return "DENOM_STATUS_UNSPECIFIED";
    case DenomStatus.DENOM_STATUS_OPEN:
      return "DENOM_STATUS_OPEN";
    case DenomStatus.DENOM_STATUS_SETTLED:
      return "DENOM_STATUS_SETTLED";
    case DenomStatus.DENOM_STATUS_HELD:
      return "DENOM_STATUS_HELD";
    case DenomStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Cycle is one netting window: the period obligations join, and the record of
 * what happened when it closed.
 */
export interface Cycle {
  /**
   * id is the cycle number, from 1. Zero is never a cycle, because in proto3
   * an id of zero is indistinguishable from an unset field and "settled in
   * cycle 0" would be unreadable to every client that received it.
   */
  id: string;
  /**
   * opened_at_height is the first block whose obligations join this cycle, and
   * closed_at_height the block its end blocker settled it in. closed_at_height
   * is zero while the cycle is open.
   */
  openedAtHeight: string;
  closedAtHeight: string;
  /**
   * status is the cycle's outcome, taken from the per-currency outcomes below:
   * settled only when every one of them settled.
   */
  status: CycleStatus;
  /**
   * outcomes carries one entry per currency that saw activity, created when
   * the first obligation in that currency arrives and completed at close.
   *
   * Kept on the cycle rather than in its own store because it is a record
   * rather than a control: a wrong figure here misreports how much compression
   * a cycle achieved, and can never let anybody withdraw money they owe.
   */
  outcomes: DenomOutcome[];
}

/**
 * DenomOutcome is the per-currency slice of a cycle. Currencies settle
 * independently: there is no cross-currency netting on this chain, because
 * offsetting a euro debit against a naira credit is an FX trade, and an FX
 * trade priced by a settlement system is a position that system is taking.
 */
export interface DenomOutcome {
  /** denom is the currency this slice covers. */
  denom: string;
  /** status is what became of it. */
  status: DenomStatus;
  /**
   * gross_amount is the sum of every netted obligation submitted in this
   * currency, and net_amount the sum actually discharged when the cycle
   * settled. The ratio between them is the compression the cycle achieved, and
   * it is the number that says whether tiering was worth doing.
   *
   * net_amount stays zero while the cycle is open and while it is held.
   */
  grossAmount: string;
  netAmount: string;
  /** obligation_count is how many netted obligations made up gross_amount. */
  obligationCount: string;
  /**
   * hold_reason records why a held slice was refused, in words an operator can
   * act on. Empty unless status is held.
   */
  holdReason: string;
}

/**
 * Obligation is one participant's admitted debt to another, as submitted.
 *
 * It is an interbank figure, never a customer payment. A participant runs its
 * own retail ledger, matches its customers' instructions there, and submits to
 * this chain only what it owes another institution as a result. That is the
 * whole privacy argument of the tiered design: a customer payment that never
 * reaches the chain cannot be read off it by a competitor bank, and no amount
 * of cryptography is needed to keep a record that was never written.
 */
export interface Obligation {
  /**
   * cycle_id is the window this obligation joined. A gross obligation records
   * the cycle that was open when it settled, so it can be found alongside the
   * netted ones, but it took no part in that cycle's positions.
   */
  cycleId: string;
  /** id is the obligation number, from 1, unique across the chain. */
  id: string;
  /**
   * from_participant owes, to_participant is owed. Both must be approved
   * participants at the moment of submission, and from_participant is the only
   * valid signer: an institution's debt is not something a third party may
   * declare on its behalf.
   */
  fromParticipant: string;
  toParticipant: string;
  /**
   * denom and amount are the currency and the figure owed. Amount is strictly
   * positive; a negative obligation would be the opposite obligation wearing a
   * disguise, and would let one participant move another's position.
   */
  denom: string;
  amount: string;
  /**
   * batch_hash is SHA-256 over the retail batch this figure summarises, held
   * off-chain by the submitting participant.
   *
   * Required, and required at 32 bytes. An obligation with no link to what it
   * summarises is a number an institution can neither be held to nor reconcile
   * against, which is exactly the auditability that gets given up when items
   * stop being recorded individually. This is a new module with no history to
   * replay, so the rule can be absolute rather than parameterised.
   *
   * The batch must carry a random salt before it is hashed, for the same
   * reason x/paymsg's metadata hash does: a small, guessable payload hashed
   * without one is not a fingerprint, it is a lookup table.
   */
  batchHash: Uint8Array;
  /** mode is whether this settled gross on submission or joined the netting. */
  mode: SettlementMode;
  /** submitted_at_height is the block it arrived in. */
  submittedAtHeight: string;
}

/**
 * Position is one participant's running multilateral net position in one
 * currency for one cycle. Negative means the participant owes the system,
 * positive that the system owes it, and the two always sum to zero across
 * every participant in a currency.
 */
export interface Position {
  /** cycle_id, denom and participant are the position's identity. */
  cycleId: string;
  denom: string;
  participant: string;
  /**
   * amount is signed. A position of exactly zero is never stored — it is
   * removed instead — so that state derived by replaying obligations and state
   * restored from a genesis file are the same bytes. A zero written where an
   * import writes nothing is how import and export stop agreeing.
   */
  amount: string;
}

/**
 * Reserve is what a participant has prefunded into the module account, per
 * currency. It is the collateral behind everything it is allowed to owe.
 */
export interface Reserve {
  /**
   * participant is the institution that posted it, denom the currency, amount
   * the balance held on its behalf.
   */
  participant: string;
  denom: string;
  amount: string;
}

function createBaseCycle(): Cycle {
  return { id: "0", openedAtHeight: "0", closedAtHeight: "0", status: 0, outcomes: [] };
}

export const Cycle = {
  encode(message: Cycle, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.openedAtHeight !== "0") {
      writer.uint32(16).int64(message.openedAtHeight);
    }
    if (message.closedAtHeight !== "0") {
      writer.uint32(24).int64(message.closedAtHeight);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    for (const v of message.outcomes) {
      DenomOutcome.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Cycle {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCycle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.openedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.closedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.outcomes.push(DenomOutcome.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Cycle {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      openedAtHeight: isSet(object.openedAtHeight) ? globalThis.String(object.openedAtHeight) : "0",
      closedAtHeight: isSet(object.closedAtHeight) ? globalThis.String(object.closedAtHeight) : "0",
      status: isSet(object.status) ? cycleStatusFromJSON(object.status) : 0,
      outcomes: globalThis.Array.isArray(object?.outcomes)
        ? object.outcomes.map((e: any) => DenomOutcome.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Cycle): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.openedAtHeight !== "0") {
      obj.openedAtHeight = message.openedAtHeight;
    }
    if (message.closedAtHeight !== "0") {
      obj.closedAtHeight = message.closedAtHeight;
    }
    if (message.status !== 0) {
      obj.status = cycleStatusToJSON(message.status);
    }
    if (message.outcomes?.length) {
      obj.outcomes = message.outcomes.map((e) => DenomOutcome.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Cycle>): Cycle {
    return Cycle.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Cycle>): Cycle {
    const message = createBaseCycle();
    message.id = object.id ?? "0";
    message.openedAtHeight = object.openedAtHeight ?? "0";
    message.closedAtHeight = object.closedAtHeight ?? "0";
    message.status = object.status ?? 0;
    message.outcomes = object.outcomes?.map((e) => DenomOutcome.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDenomOutcome(): DenomOutcome {
  return { denom: "", status: 0, grossAmount: "", netAmount: "", obligationCount: "0", holdReason: "" };
}

export const DenomOutcome = {
  encode(message: DenomOutcome, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.denom !== "") {
      writer.uint32(10).string(message.denom);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.grossAmount !== "") {
      writer.uint32(26).string(message.grossAmount);
    }
    if (message.netAmount !== "") {
      writer.uint32(34).string(message.netAmount);
    }
    if (message.obligationCount !== "0") {
      writer.uint32(40).uint64(message.obligationCount);
    }
    if (message.holdReason !== "") {
      writer.uint32(50).string(message.holdReason);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DenomOutcome {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDenomOutcome();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.grossAmount = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.netAmount = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.obligationCount = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.holdReason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DenomOutcome {
    return {
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      status: isSet(object.status) ? denomStatusFromJSON(object.status) : 0,
      grossAmount: isSet(object.grossAmount) ? globalThis.String(object.grossAmount) : "",
      netAmount: isSet(object.netAmount) ? globalThis.String(object.netAmount) : "",
      obligationCount: isSet(object.obligationCount) ? globalThis.String(object.obligationCount) : "0",
      holdReason: isSet(object.holdReason) ? globalThis.String(object.holdReason) : "",
    };
  },

  toJSON(message: DenomOutcome): unknown {
    const obj: any = {};
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.status !== 0) {
      obj.status = denomStatusToJSON(message.status);
    }
    if (message.grossAmount !== "") {
      obj.grossAmount = message.grossAmount;
    }
    if (message.netAmount !== "") {
      obj.netAmount = message.netAmount;
    }
    if (message.obligationCount !== "0") {
      obj.obligationCount = message.obligationCount;
    }
    if (message.holdReason !== "") {
      obj.holdReason = message.holdReason;
    }
    return obj;
  },

  create(base?: DeepPartial<DenomOutcome>): DenomOutcome {
    return DenomOutcome.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DenomOutcome>): DenomOutcome {
    const message = createBaseDenomOutcome();
    message.denom = object.denom ?? "";
    message.status = object.status ?? 0;
    message.grossAmount = object.grossAmount ?? "";
    message.netAmount = object.netAmount ?? "";
    message.obligationCount = object.obligationCount ?? "0";
    message.holdReason = object.holdReason ?? "";
    return message;
  },
};

function createBaseObligation(): Obligation {
  return {
    cycleId: "0",
    id: "0",
    fromParticipant: "",
    toParticipant: "",
    denom: "",
    amount: "",
    batchHash: new Uint8Array(0),
    mode: 0,
    submittedAtHeight: "0",
  };
}

export const Obligation = {
  encode(message: Obligation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.cycleId !== "0") {
      writer.uint32(8).uint64(message.cycleId);
    }
    if (message.id !== "0") {
      writer.uint32(16).uint64(message.id);
    }
    if (message.fromParticipant !== "") {
      writer.uint32(26).string(message.fromParticipant);
    }
    if (message.toParticipant !== "") {
      writer.uint32(34).string(message.toParticipant);
    }
    if (message.denom !== "") {
      writer.uint32(42).string(message.denom);
    }
    if (message.amount !== "") {
      writer.uint32(50).string(message.amount);
    }
    if (message.batchHash.length !== 0) {
      writer.uint32(58).bytes(message.batchHash);
    }
    if (message.mode !== 0) {
      writer.uint32(64).int32(message.mode);
    }
    if (message.submittedAtHeight !== "0") {
      writer.uint32(72).int64(message.submittedAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Obligation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObligation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.cycleId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.fromParticipant = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.toParticipant = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.amount = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.batchHash = reader.bytes();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.submittedAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Obligation {
    return {
      cycleId: isSet(object.cycleId) ? globalThis.String(object.cycleId) : "0",
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      fromParticipant: isSet(object.fromParticipant) ? globalThis.String(object.fromParticipant) : "",
      toParticipant: isSet(object.toParticipant) ? globalThis.String(object.toParticipant) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      batchHash: isSet(object.batchHash) ? bytesFromBase64(object.batchHash) : new Uint8Array(0),
      mode: isSet(object.mode) ? settlementModeFromJSON(object.mode) : 0,
      submittedAtHeight: isSet(object.submittedAtHeight) ? globalThis.String(object.submittedAtHeight) : "0",
    };
  },

  toJSON(message: Obligation): unknown {
    const obj: any = {};
    if (message.cycleId !== "0") {
      obj.cycleId = message.cycleId;
    }
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.fromParticipant !== "") {
      obj.fromParticipant = message.fromParticipant;
    }
    if (message.toParticipant !== "") {
      obj.toParticipant = message.toParticipant;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.batchHash.length !== 0) {
      obj.batchHash = base64FromBytes(message.batchHash);
    }
    if (message.mode !== 0) {
      obj.mode = settlementModeToJSON(message.mode);
    }
    if (message.submittedAtHeight !== "0") {
      obj.submittedAtHeight = message.submittedAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<Obligation>): Obligation {
    return Obligation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Obligation>): Obligation {
    const message = createBaseObligation();
    message.cycleId = object.cycleId ?? "0";
    message.id = object.id ?? "0";
    message.fromParticipant = object.fromParticipant ?? "";
    message.toParticipant = object.toParticipant ?? "";
    message.denom = object.denom ?? "";
    message.amount = object.amount ?? "";
    message.batchHash = object.batchHash ?? new Uint8Array(0);
    message.mode = object.mode ?? 0;
    message.submittedAtHeight = object.submittedAtHeight ?? "0";
    return message;
  },
};

function createBasePosition(): Position {
  return { cycleId: "0", denom: "", participant: "", amount: "" };
}

export const Position = {
  encode(message: Position, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.cycleId !== "0") {
      writer.uint32(8).uint64(message.cycleId);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.participant !== "") {
      writer.uint32(26).string(message.participant);
    }
    if (message.amount !== "") {
      writer.uint32(34).string(message.amount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Position {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePosition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.cycleId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.participant = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.amount = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Position {
    return {
      cycleId: isSet(object.cycleId) ? globalThis.String(object.cycleId) : "0",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      participant: isSet(object.participant) ? globalThis.String(object.participant) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
    };
  },

  toJSON(message: Position): unknown {
    const obj: any = {};
    if (message.cycleId !== "0") {
      obj.cycleId = message.cycleId;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.participant !== "") {
      obj.participant = message.participant;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    return obj;
  },

  create(base?: DeepPartial<Position>): Position {
    return Position.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Position>): Position {
    const message = createBasePosition();
    message.cycleId = object.cycleId ?? "0";
    message.denom = object.denom ?? "";
    message.participant = object.participant ?? "";
    message.amount = object.amount ?? "";
    return message;
  },
};

function createBaseReserve(): Reserve {
  return { participant: "", denom: "", amount: "" };
}

export const Reserve = {
  encode(message: Reserve, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.participant !== "") {
      writer.uint32(10).string(message.participant);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.amount !== "") {
      writer.uint32(26).string(message.amount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Reserve {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserve();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.participant = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.amount = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Reserve {
    return {
      participant: isSet(object.participant) ? globalThis.String(object.participant) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
    };
  },

  toJSON(message: Reserve): unknown {
    const obj: any = {};
    if (message.participant !== "") {
      obj.participant = message.participant;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    return obj;
  },

  create(base?: DeepPartial<Reserve>): Reserve {
    return Reserve.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Reserve>): Reserve {
    const message = createBaseReserve();
    message.participant = object.participant ?? "";
    message.denom = object.denom ?? "";
    message.amount = object.amount ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
