// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/constitution/v1/query.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination.ts";
import { Amendment, Invariants, Ratification } from "./constitution.ts";

export const protobufPackage = "blockchain.constitution.v1";

/** QueryInvariantsRequest is request type for the Query/Invariants RPC method. */
export interface QueryInvariantsRequest {
}

/** QueryInvariantsResponse is response type for the Query/Invariants RPC method. */
export interface QueryInvariantsResponse {
  invariants: Invariants | undefined;
}

/** QueryAmendmentRequest defines the QueryAmendmentRequest message. */
export interface QueryAmendmentRequest {
  id: string;
}

/** QueryAmendmentResponse defines the QueryAmendmentResponse message. */
export interface QueryAmendmentResponse {
  amendment: Amendment | undefined;
}

/** QueryAllAmendmentRequest defines the QueryAllAmendmentRequest message. */
export interface QueryAllAmendmentRequest {
  pagination: PageRequest | undefined;
}

/** QueryAllAmendmentResponse defines the QueryAllAmendmentResponse message. */
export interface QueryAllAmendmentResponse {
  amendment: Amendment[];
  pagination: PageResponse | undefined;
}

/** QueryRatificationsRequest defines the QueryRatificationsRequest message. */
export interface QueryRatificationsRequest {
  amendmentId: string;
}

/** QueryRatificationsResponse defines the QueryRatificationsResponse message. */
export interface QueryRatificationsResponse {
  ratifications: Ratification[];
  /**
   * required_power is what the amendment still has to reach, restated here so
   * that a reader of this response never has to recompute a threshold from a
   * basis-point figure held in another query's answer.
   */
  requiredPower: string;
}

function createBaseQueryInvariantsRequest(): QueryInvariantsRequest {
  return {};
}

export const QueryInvariantsRequest = {
  encode(_: QueryInvariantsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryInvariantsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryInvariantsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryInvariantsRequest {
    return {};
  },

  toJSON(_: QueryInvariantsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryInvariantsRequest>): QueryInvariantsRequest {
    return QueryInvariantsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryInvariantsRequest>): QueryInvariantsRequest {
    const message = createBaseQueryInvariantsRequest();
    return message;
  },
};

function createBaseQueryInvariantsResponse(): QueryInvariantsResponse {
  return { invariants: undefined };
}

export const QueryInvariantsResponse = {
  encode(message: QueryInvariantsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.invariants !== undefined) {
      Invariants.encode(message.invariants, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryInvariantsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryInvariantsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.invariants = Invariants.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryInvariantsResponse {
    return { invariants: isSet(object.invariants) ? Invariants.fromJSON(object.invariants) : undefined };
  },

  toJSON(message: QueryInvariantsResponse): unknown {
    const obj: any = {};
    if (message.invariants !== undefined) {
      obj.invariants = Invariants.toJSON(message.invariants);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryInvariantsResponse>): QueryInvariantsResponse {
    return QueryInvariantsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryInvariantsResponse>): QueryInvariantsResponse {
    const message = createBaseQueryInvariantsResponse();
    message.invariants = (object.invariants !== undefined && object.invariants !== null)
      ? Invariants.fromPartial(object.invariants)
      : undefined;
    return message;
  },
};

function createBaseQueryAmendmentRequest(): QueryAmendmentRequest {
  return { id: "0" };
}

export const QueryAmendmentRequest = {
  encode(message: QueryAmendmentRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAmendmentRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAmendmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAmendmentRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "0" };
  },

  toJSON(message: QueryAmendmentRequest): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAmendmentRequest>): QueryAmendmentRequest {
    return QueryAmendmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAmendmentRequest>): QueryAmendmentRequest {
    const message = createBaseQueryAmendmentRequest();
    message.id = object.id ?? "0";
    return message;
  },
};

function createBaseQueryAmendmentResponse(): QueryAmendmentResponse {
  return { amendment: undefined };
}

export const QueryAmendmentResponse = {
  encode(message: QueryAmendmentResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.amendment !== undefined) {
      Amendment.encode(message.amendment, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAmendmentResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAmendmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.amendment = Amendment.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAmendmentResponse {
    return { amendment: isSet(object.amendment) ? Amendment.fromJSON(object.amendment) : undefined };
  },

  toJSON(message: QueryAmendmentResponse): unknown {
    const obj: any = {};
    if (message.amendment !== undefined) {
      obj.amendment = Amendment.toJSON(message.amendment);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAmendmentResponse>): QueryAmendmentResponse {
    return QueryAmendmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAmendmentResponse>): QueryAmendmentResponse {
    const message = createBaseQueryAmendmentResponse();
    message.amendment = (object.amendment !== undefined && object.amendment !== null)
      ? Amendment.fromPartial(object.amendment)
      : undefined;
    return message;
  },
};

function createBaseQueryAllAmendmentRequest(): QueryAllAmendmentRequest {
  return { pagination: undefined };
}

export const QueryAllAmendmentRequest = {
  encode(message: QueryAllAmendmentRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAllAmendmentRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAllAmendmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAllAmendmentRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryAllAmendmentRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAllAmendmentRequest>): QueryAllAmendmentRequest {
    return QueryAllAmendmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAllAmendmentRequest>): QueryAllAmendmentRequest {
    const message = createBaseQueryAllAmendmentRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryAllAmendmentResponse(): QueryAllAmendmentResponse {
  return { amendment: [], pagination: undefined };
}

export const QueryAllAmendmentResponse = {
  encode(message: QueryAllAmendmentResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.amendment) {
      Amendment.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAllAmendmentResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAllAmendmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.amendment.push(Amendment.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAllAmendmentResponse {
    return {
      amendment: globalThis.Array.isArray(object?.amendment)
        ? object.amendment.map((e: any) => Amendment.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryAllAmendmentResponse): unknown {
    const obj: any = {};
    if (message.amendment?.length) {
      obj.amendment = message.amendment.map((e) => Amendment.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAllAmendmentResponse>): QueryAllAmendmentResponse {
    return QueryAllAmendmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAllAmendmentResponse>): QueryAllAmendmentResponse {
    const message = createBaseQueryAllAmendmentResponse();
    message.amendment = object.amendment?.map((e) => Amendment.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryRatificationsRequest(): QueryRatificationsRequest {
  return { amendmentId: "0" };
}

export const QueryRatificationsRequest = {
  encode(message: QueryRatificationsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.amendmentId !== "0") {
      writer.uint32(8).uint64(message.amendmentId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRatificationsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRatificationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.amendmentId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRatificationsRequest {
    return { amendmentId: isSet(object.amendmentId) ? globalThis.String(object.amendmentId) : "0" };
  },

  toJSON(message: QueryRatificationsRequest): unknown {
    const obj: any = {};
    if (message.amendmentId !== "0") {
      obj.amendmentId = message.amendmentId;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRatificationsRequest>): QueryRatificationsRequest {
    return QueryRatificationsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRatificationsRequest>): QueryRatificationsRequest {
    const message = createBaseQueryRatificationsRequest();
    message.amendmentId = object.amendmentId ?? "0";
    return message;
  },
};

function createBaseQueryRatificationsResponse(): QueryRatificationsResponse {
  return { ratifications: [], requiredPower: "0" };
}

export const QueryRatificationsResponse = {
  encode(message: QueryRatificationsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.ratifications) {
      Ratification.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.requiredPower !== "0") {
      writer.uint32(16).int64(message.requiredPower);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRatificationsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRatificationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ratifications.push(Ratification.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.requiredPower = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRatificationsResponse {
    return {
      ratifications: globalThis.Array.isArray(object?.ratifications)
        ? object.ratifications.map((e: any) => Ratification.fromJSON(e))
        : [],
      requiredPower: isSet(object.requiredPower) ? globalThis.String(object.requiredPower) : "0",
    };
  },

  toJSON(message: QueryRatificationsResponse): unknown {
    const obj: any = {};
    if (message.ratifications?.length) {
      obj.ratifications = message.ratifications.map((e) => Ratification.toJSON(e));
    }
    if (message.requiredPower !== "0") {
      obj.requiredPower = message.requiredPower;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRatificationsResponse>): QueryRatificationsResponse {
    return QueryRatificationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRatificationsResponse>): QueryRatificationsResponse {
    const message = createBaseQueryRatificationsResponse();
    message.ratifications = object.ratifications?.map((e) => Ratification.fromPartial(e)) || [];
    message.requiredPower = object.requiredPower ?? "0";
    return message;
  },
};

/** Query defines the gRPC querier service. */
export interface Query {
  /**
   * Invariants returns the values in force. This is the query a supervisor runs
   * to find out what the chain has promised not to change, so it answers with
   * the whole settlement rather than a field at a time.
   */
  Invariants(request: QueryInvariantsRequest): Promise<QueryInvariantsResponse>;
  /** Amendment queries one amendment by id. */
  Amendment(request: QueryAmendmentRequest): Promise<QueryAmendmentResponse>;
  /**
   * ListAmendment queries every amendment ever opened, lapsed and withdrawn
   * ones included.
   */
  ListAmendment(request: QueryAllAmendmentRequest): Promise<QueryAllAmendmentResponse>;
  /**
   * Ratifications lists who has ratified one amendment, which is the record
   * that makes a validator answerable for a change it agreed to.
   */
  Ratifications(request: QueryRatificationsRequest): Promise<QueryRatificationsResponse>;
}

export const QueryServiceName = "blockchain.constitution.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Invariants = this.Invariants.bind(this);
    this.Amendment = this.Amendment.bind(this);
    this.ListAmendment = this.ListAmendment.bind(this);
    this.Ratifications = this.Ratifications.bind(this);
  }
  Invariants(request: QueryInvariantsRequest): Promise<QueryInvariantsResponse> {
    const data = QueryInvariantsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Invariants", data);
    return promise.then((data) => QueryInvariantsResponse.decode(_m0.Reader.create(data)));
  }

  Amendment(request: QueryAmendmentRequest): Promise<QueryAmendmentResponse> {
    const data = QueryAmendmentRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Amendment", data);
    return promise.then((data) => QueryAmendmentResponse.decode(_m0.Reader.create(data)));
  }

  ListAmendment(request: QueryAllAmendmentRequest): Promise<QueryAllAmendmentResponse> {
    const data = QueryAllAmendmentRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListAmendment", data);
    return promise.then((data) => QueryAllAmendmentResponse.decode(_m0.Reader.create(data)));
  }

  Ratifications(request: QueryRatificationsRequest): Promise<QueryRatificationsResponse> {
    const data = QueryRatificationsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Ratifications", data);
    return promise.then((data) => QueryRatificationsResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
