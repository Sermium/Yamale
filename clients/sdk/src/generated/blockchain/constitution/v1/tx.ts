// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/constitution/v1/tx.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Invariants } from "./constitution.ts";

export const protobufPackage = "blockchain.constitution.v1";

/**
 * MsgProposeAmendment opens an amendment to the invariants.
 *
 * Amendment is possible at all, rather than forbidden outright, because
 * "forbidden" would be a lie. A chain can be hard-forked and an upgrade handler
 * can rewrite any store, so a constitution with no amendment path does not
 * become unamendable — it relocates its amendments into a binary release, which
 * is a change with less public notice and fewer signatures than a proposal. The
 * path exists so that it is the cheapest one, and it is made slow and loud so
 * that taking it is a thing people find out about.
 *
 * Two independent conditions, both required: a governance proposal to open it,
 * and a supermajority of the voting power recorded at that moment to ratify it,
 * with weeks between the two. Neither alone is enough.
 */
export interface MsgProposeAmendment {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /**
   * invariants is the complete replacement set, not a delta. All fields must be
   * supplied and all of them are validated, so an amendment that meant to move
   * one ceiling cannot silently zero another by omitting it — and a zero here
   * would be a divisor or a missing destination somewhere else.
   */
  invariants:
    | Invariants
    | undefined;
  /** reason is the stated grounds, in plain words. */
  reason: string;
}

/** MsgProposeAmendmentResponse returns the id of the amendment that was opened. */
export interface MsgProposeAmendmentResponse {
  amendmentId: string;
}

/**
 * MsgRatifyAmendment is one validator agreeing to a pending amendment.
 *
 * Signed by the operator account, at the weight that operator carries when it
 * signs, and countable only once. A validator that changes its mind cannot
 * un-ratify: the amendment's protection is the delay and the threshold, not the
 * ability to run the vote backwards, and a withdrawable ratification would let
 * a set hold an amendment at one vote short indefinitely.
 */
export interface MsgRatifyAmendment {
  /** validator is the operator address in its account form. */
  validator: string;
  amendmentId: string;
}

/**
 * MsgRatifyAmendmentResponse defines the response structure for executing a
 * MsgRatifyAmendment message.
 */
export interface MsgRatifyAmendmentResponse {
}

/**
 * MsgWithdrawAmendment takes a pending amendment back before it takes effect.
 *
 * Authority-gated like the proposal, because withdrawing one is a decision of
 * the same kind. It exists so that an amendment found to be wrong during its
 * own delay can be stopped by the body that opened it, rather than left to run
 * its weeks out while everybody waits to see whether it fails to ratify.
 */
export interface MsgWithdrawAmendment {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  amendmentId: string;
}

/**
 * MsgWithdrawAmendmentResponse defines the response structure for executing a
 * MsgWithdrawAmendment message.
 */
export interface MsgWithdrawAmendmentResponse {
}

function createBaseMsgProposeAmendment(): MsgProposeAmendment {
  return { authority: "", invariants: undefined, reason: "" };
}

export const MsgProposeAmendment = {
  encode(message: MsgProposeAmendment, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.invariants !== undefined) {
      Invariants.encode(message.invariants, writer.uint32(18).fork()).ldelim();
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgProposeAmendment {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgProposeAmendment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.invariants = Invariants.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgProposeAmendment {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      invariants: isSet(object.invariants) ? Invariants.fromJSON(object.invariants) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: MsgProposeAmendment): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.invariants !== undefined) {
      obj.invariants = Invariants.toJSON(message.invariants);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgProposeAmendment>): MsgProposeAmendment {
    return MsgProposeAmendment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgProposeAmendment>): MsgProposeAmendment {
    const message = createBaseMsgProposeAmendment();
    message.authority = object.authority ?? "";
    message.invariants = (object.invariants !== undefined && object.invariants !== null)
      ? Invariants.fromPartial(object.invariants)
      : undefined;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseMsgProposeAmendmentResponse(): MsgProposeAmendmentResponse {
  return { amendmentId: "0" };
}

export const MsgProposeAmendmentResponse = {
  encode(message: MsgProposeAmendmentResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.amendmentId !== "0") {
      writer.uint32(8).uint64(message.amendmentId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgProposeAmendmentResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgProposeAmendmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.amendmentId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgProposeAmendmentResponse {
    return { amendmentId: isSet(object.amendmentId) ? globalThis.String(object.amendmentId) : "0" };
  },

  toJSON(message: MsgProposeAmendmentResponse): unknown {
    const obj: any = {};
    if (message.amendmentId !== "0") {
      obj.amendmentId = message.amendmentId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgProposeAmendmentResponse>): MsgProposeAmendmentResponse {
    return MsgProposeAmendmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgProposeAmendmentResponse>): MsgProposeAmendmentResponse {
    const message = createBaseMsgProposeAmendmentResponse();
    message.amendmentId = object.amendmentId ?? "0";
    return message;
  },
};

function createBaseMsgRatifyAmendment(): MsgRatifyAmendment {
  return { validator: "", amendmentId: "0" };
}

export const MsgRatifyAmendment = {
  encode(message: MsgRatifyAmendment, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.validator !== "") {
      writer.uint32(10).string(message.validator);
    }
    if (message.amendmentId !== "0") {
      writer.uint32(16).uint64(message.amendmentId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRatifyAmendment {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRatifyAmendment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.amendmentId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRatifyAmendment {
    return {
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      amendmentId: isSet(object.amendmentId) ? globalThis.String(object.amendmentId) : "0",
    };
  },

  toJSON(message: MsgRatifyAmendment): unknown {
    const obj: any = {};
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.amendmentId !== "0") {
      obj.amendmentId = message.amendmentId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRatifyAmendment>): MsgRatifyAmendment {
    return MsgRatifyAmendment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRatifyAmendment>): MsgRatifyAmendment {
    const message = createBaseMsgRatifyAmendment();
    message.validator = object.validator ?? "";
    message.amendmentId = object.amendmentId ?? "0";
    return message;
  },
};

function createBaseMsgRatifyAmendmentResponse(): MsgRatifyAmendmentResponse {
  return {};
}

export const MsgRatifyAmendmentResponse = {
  encode(_: MsgRatifyAmendmentResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRatifyAmendmentResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRatifyAmendmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRatifyAmendmentResponse {
    return {};
  },

  toJSON(_: MsgRatifyAmendmentResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRatifyAmendmentResponse>): MsgRatifyAmendmentResponse {
    return MsgRatifyAmendmentResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRatifyAmendmentResponse>): MsgRatifyAmendmentResponse {
    const message = createBaseMsgRatifyAmendmentResponse();
    return message;
  },
};

function createBaseMsgWithdrawAmendment(): MsgWithdrawAmendment {
  return { authority: "", amendmentId: "0" };
}

export const MsgWithdrawAmendment = {
  encode(message: MsgWithdrawAmendment, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.amendmentId !== "0") {
      writer.uint32(16).uint64(message.amendmentId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgWithdrawAmendment {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWithdrawAmendment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.amendmentId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgWithdrawAmendment {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      amendmentId: isSet(object.amendmentId) ? globalThis.String(object.amendmentId) : "0",
    };
  },

  toJSON(message: MsgWithdrawAmendment): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.amendmentId !== "0") {
      obj.amendmentId = message.amendmentId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgWithdrawAmendment>): MsgWithdrawAmendment {
    return MsgWithdrawAmendment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgWithdrawAmendment>): MsgWithdrawAmendment {
    const message = createBaseMsgWithdrawAmendment();
    message.authority = object.authority ?? "";
    message.amendmentId = object.amendmentId ?? "0";
    return message;
  },
};

function createBaseMsgWithdrawAmendmentResponse(): MsgWithdrawAmendmentResponse {
  return {};
}

export const MsgWithdrawAmendmentResponse = {
  encode(_: MsgWithdrawAmendmentResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgWithdrawAmendmentResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWithdrawAmendmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgWithdrawAmendmentResponse {
    return {};
  },

  toJSON(_: MsgWithdrawAmendmentResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgWithdrawAmendmentResponse>): MsgWithdrawAmendmentResponse {
    return MsgWithdrawAmendmentResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgWithdrawAmendmentResponse>): MsgWithdrawAmendmentResponse {
    const message = createBaseMsgWithdrawAmendmentResponse();
    return message;
  },
};

/**
 * Msg defines the Msg service.
 *
 * There is no UpdateParams here, and there never will be. Every other module on
 * this chain has one; this module's whole purpose is to hold the values that
 * message must not reach, so giving it its own would be a door cut into the
 * wall it is.
 */
export interface Msg {
  /**
   * ProposeAmendment opens a change to the invariants and starts its public
   * delay. It is authority-gated, so a proposal is the only way in.
   */
  ProposeAmendment(request: MsgProposeAmendment): Promise<MsgProposeAmendmentResponse>;
  /** RatifyAmendment records one validator agreeing to a pending amendment. */
  RatifyAmendment(request: MsgRatifyAmendment): Promise<MsgRatifyAmendmentResponse>;
  /** WithdrawAmendment takes a pending amendment back before it takes effect. */
  WithdrawAmendment(request: MsgWithdrawAmendment): Promise<MsgWithdrawAmendmentResponse>;
}

export const MsgServiceName = "blockchain.constitution.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.ProposeAmendment = this.ProposeAmendment.bind(this);
    this.RatifyAmendment = this.RatifyAmendment.bind(this);
    this.WithdrawAmendment = this.WithdrawAmendment.bind(this);
  }
  ProposeAmendment(request: MsgProposeAmendment): Promise<MsgProposeAmendmentResponse> {
    const data = MsgProposeAmendment.encode(request).finish();
    const promise = this.rpc.request(this.service, "ProposeAmendment", data);
    return promise.then((data) => MsgProposeAmendmentResponse.decode(_m0.Reader.create(data)));
  }

  RatifyAmendment(request: MsgRatifyAmendment): Promise<MsgRatifyAmendmentResponse> {
    const data = MsgRatifyAmendment.encode(request).finish();
    const promise = this.rpc.request(this.service, "RatifyAmendment", data);
    return promise.then((data) => MsgRatifyAmendmentResponse.decode(_m0.Reader.create(data)));
  }

  WithdrawAmendment(request: MsgWithdrawAmendment): Promise<MsgWithdrawAmendmentResponse> {
    const data = MsgWithdrawAmendment.encode(request).finish();
    const promise = this.rpc.request(this.service, "WithdrawAmendment", data);
    return promise.then((data) => MsgWithdrawAmendmentResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
