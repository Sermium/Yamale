// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/constitution/v1/constitution.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.constitution.v1";

/** AmendmentStatus is where an amendment has got to. */
export const AmendmentStatus = {
  /** AMENDMENT_STATUS_UNSPECIFIED - AMENDMENT_STATUS_UNSPECIFIED is the unset default and is never valid. */
  AMENDMENT_STATUS_UNSPECIFIED: 0,
  /**
   * AMENDMENT_STATUS_PENDING - AMENDMENT_STATUS_PENDING is proposed, in public, and counting down. It is
   * collecting ratifications and has changed nothing.
   */
  AMENDMENT_STATUS_PENDING: 1,
  /**
   * AMENDMENT_STATUS_ENACTED - AMENDMENT_STATUS_ENACTED means the delay ran out with enough ratified
   * power behind it and the invariants were replaced.
   */
  AMENDMENT_STATUS_ENACTED: 2,
  /**
   * AMENDMENT_STATUS_LAPSED - AMENDMENT_STATUS_LAPSED means the delay ran out without enough ratified
   * power. It is deliberately not the same status as withdrawn: lapsed is the
   * record of an amendment the validator set declined to ratify, and that is
   * precisely the history worth keeping.
   */
  AMENDMENT_STATUS_LAPSED: 3,
  /**
   * AMENDMENT_STATUS_WITHDRAWN - AMENDMENT_STATUS_WITHDRAWN means governance took it back before the delay
   * ran out.
   */
  AMENDMENT_STATUS_WITHDRAWN: 4,
  UNRECOGNIZED: -1,
} as const;

export type AmendmentStatus = typeof AmendmentStatus[keyof typeof AmendmentStatus];

export namespace AmendmentStatus {
  export type AMENDMENT_STATUS_UNSPECIFIED = typeof AmendmentStatus.AMENDMENT_STATUS_UNSPECIFIED;
  export type AMENDMENT_STATUS_PENDING = typeof AmendmentStatus.AMENDMENT_STATUS_PENDING;
  export type AMENDMENT_STATUS_ENACTED = typeof AmendmentStatus.AMENDMENT_STATUS_ENACTED;
  export type AMENDMENT_STATUS_LAPSED = typeof AmendmentStatus.AMENDMENT_STATUS_LAPSED;
  export type AMENDMENT_STATUS_WITHDRAWN = typeof AmendmentStatus.AMENDMENT_STATUS_WITHDRAWN;
  export type UNRECOGNIZED = typeof AmendmentStatus.UNRECOGNIZED;
}

export function amendmentStatusFromJSON(object: any): AmendmentStatus {
  switch (object) {
    case 0:
    case "AMENDMENT_STATUS_UNSPECIFIED":
      return AmendmentStatus.AMENDMENT_STATUS_UNSPECIFIED;
    case 1:
    case "AMENDMENT_STATUS_PENDING":
      return AmendmentStatus.AMENDMENT_STATUS_PENDING;
    case 2:
    case "AMENDMENT_STATUS_ENACTED":
      return AmendmentStatus.AMENDMENT_STATUS_ENACTED;
    case 3:
    case "AMENDMENT_STATUS_LAPSED":
      return AmendmentStatus.AMENDMENT_STATUS_LAPSED;
    case 4:
    case "AMENDMENT_STATUS_WITHDRAWN":
      return AmendmentStatus.AMENDMENT_STATUS_WITHDRAWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AmendmentStatus.UNRECOGNIZED;
  }
}

export function amendmentStatusToJSON(object: AmendmentStatus): string {
  switch (object) {
    case AmendmentStatus.AMENDMENT_STATUS_UNSPECIFIED:
      return "AMENDMENT_STATUS_UNSPECIFIED";
    case AmendmentStatus.AMENDMENT_STATUS_PENDING:
      return "AMENDMENT_STATUS_PENDING";
    case AmendmentStatus.AMENDMENT_STATUS_ENACTED:
      return "AMENDMENT_STATUS_ENACTED";
    case AmendmentStatus.AMENDMENT_STATUS_LAPSED:
      return "AMENDMENT_STATUS_LAPSED";
    case AmendmentStatus.AMENDMENT_STATUS_WITHDRAWN:
      return "AMENDMENT_STATUS_WITHDRAWN";
    case AmendmentStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Invariants are the values this chain fixes at genesis and will not let an
 * ordinary governance proposal change.
 *
 * Every other parameter on this chain is a mutable gov param, and that was not
 * a theoretical weakness: recovery_destination — the single address seized
 * assets may be sent to — was found empty on the running devnet, which means a
 * seizure carried by two thirds of the validator set would have had nowhere to
 * send what it took, and nobody noticed until a console happened to print it.
 * A parameter that nothing refuses to change is a parameter nothing is
 * checking.
 *
 * What belongs here is narrow and deliberate: the numbers a majority would have
 * an interest in moving in the moment it wanted to act. A chain that can vote
 * to lower its own seizure threshold does not have one, and a cap enforced at
 * an epoch the same vote can lengthen is not enforced at every epoch — which is
 * why the epoch length is in this message and not in x/validatorgov's params.
 *
 * The values are not duplicated state. x/enforcement still stores its own
 * Params; what changed is that both its MsgUpdateParams and its InitGenesis
 * refuse anything that disagrees with the copy here. Divergence is therefore
 * unrepresentable rather than merely unlikely, and the modules that read these
 * values at speed keep reading their own store.
 *
 * Zero is "unset" for every field. That is why InitGenesis refuses a zero
 * rather than substituting a default: an address compiled into a binary is not
 * the foundation on somebody else's network, and a cap that defaulted itself
 * into existence would be a cap nobody chose.
 */
export interface Invariants {
  /**
   * max_entity_power_bps is the share of voting power one legal entity may
   * hold, in basis points.
   *
   * Under equal seats this is countable rather than arithmetic: with one seat
   * per admitted validator, 2000 is "no entity holds more than a fifth of the
   * seats", which a supervisor can check against a list without recomputing
   * anything.
   */
  maxEntityPowerBps: string;
  /**
   * max_beneficial_owner_power_bps is the same ceiling applied to the ultimate
   * beneficial owner behind the entities, which is the number that actually
   * matters. Two subsidiaries of one state bank are two entities and one owner,
   * and it is the owner that decides how they both vote.
   */
  maxBeneficialOwnerPowerBps: string;
  /**
   * max_jurisdiction_power_bps is the ceiling on the power answering to any one
   * national authority. It is what makes open accession possible: a sovereign
   * is never refused, only admitted at a bounded weight, so balance is kept by
   * arithmetic instead of by a refusal that would be read as exclusion.
   */
  maxJurisdictionPowerBps: string;
  /**
   * concentration_epoch_blocks is how often the ceilings above are checked and
   * breaches corrected.
   *
   * It is constitutional for the same reason the ceilings are. Concentration is
   * a state, not an event — it changes when a bank acquires a participant, when
   * two members merge, when an operator is nationalised — so the only thing
   * standing between a cap and a dead letter is that the check keeps running.
   * A proposal able to set this to a billion blocks would repeal the caps
   * without appearing to touch them.
   */
  concentrationEpochBlocks: string;
  /**
   * min_active_validators is the size below which the epoch check stops
   * demoting and starts merely reporting.
   *
   * A cap can be arithmetically unsatisfiable: three validators under three
   * owners cannot all sit below a fifth of the power, and a check that kept
   * demoting until the ceiling held would demote the chain to a halt. So the
   * floor is explicit and a breach it cannot correct is recorded and emitted as
   * an event instead. Enforcement must never be the thing that stops the chain
   * — an uncorrectable breach that everyone can see is strictly better than a
   * network that no longer produces blocks.
   */
  minActiveValidators: number;
  /**
   * enforcement_threshold_bps is x/enforcement's seizure threshold: the share
   * of voting power that must vote yes before assets are taken. It is fixed
   * here because it is the first number a set that wanted to seize something
   * would have an interest in lowering, and lowering it is exactly the move
   * that would be indistinguishable from a routine parameter change.
   */
  enforcementThresholdBps: string;
  /**
   * enforcement_recovery_destination is the only address seized assets may be
   * sent to. Fixed here because a mutable destination is the difference between
   * a recovery mechanism and a theft mechanism: the threshold decides whether
   * funds move, this decides who ends up with them.
   */
  enforcementRecoveryDestination: string;
  /**
   * enforcement_voting_period_blocks and
   * enforcement_provisional_freeze_blocks are the two delays that make a
   * seizure answerable. Shortening the voting period is how a supermajority
   * becomes whoever was awake; shortening the freeze is how an account is
   * handed back mid-case. Both are the sort of change that reads as
   * housekeeping and is not.
   */
  enforcementVotingPeriodBlocks: string;
  enforcementProvisionalFreezeBlocks: string;
  /**
   * amendment_delay_blocks is how long a proposed amendment sits in public
   * before it can take effect. Weeks, not hours: the delay is the whole
   * protection, because it is the window in which anyone who would be harmed by
   * the change finds out it is coming while they can still act on it.
   */
  amendmentDelayBlocks: string;
  /**
   * amendment_threshold_bps is the share of the voting power recorded when the
   * amendment was proposed that must separately ratify it, in basis points.
   *
   * Separately, because a gov proposal passes on whatever threshold x/gov
   * happens to carry, and x/gov's thresholds are ordinary mutable params — so
   * an amendment ratified only through gov could be ratified by first voting
   * gov's own numbers down. The ratification here is measured against a
   * snapshot taken when the amendment opened, so it cannot be passed by
   * shrinking the electorate either.
   */
  amendmentThresholdBps: string;
}

/**
 * Amendment is one attempt to change the invariants, and everything the chain
 * did about it. Amendments are never deleted: an amendment that lapsed is the
 * record of somebody having tried to move a ceiling, and a set of amendments
 * filtered down to the ones that succeeded would hide exactly the pattern worth
 * noticing.
 */
export interface Amendment {
  /**
   * id is numbered from one. Zero is reserved because in proto3 an id of 0 is
   * indistinguishable from an unset field, and "amendment 0" would be a record
   * nobody could look up.
   */
  id: string;
  /**
   * proposed is the complete replacement set. Whole rather than a delta, so
   * that what the chain will hold after enactment is on the record from the day
   * it was proposed, and nobody has to reconstruct it from a diff against a
   * state that may itself have been amended in between.
   */
  proposed:
    | Invariants
    | undefined;
  /**
   * reason is the stated grounds, in plain words. Required: a proposal to move
   * the numbers this chain promised not to move should have to say why.
   */
  reason: string;
  proposedAtHeight: string;
  /**
   * effective_at_height is the earliest height the amendment can take effect.
   * It is fixed when the amendment opens, from the delay in force at that
   * moment, so an amendment that shortens the delay cannot shorten its own.
   */
  effectiveAtHeight: string;
  /**
   * snapshot_power is the total voting power at the moment the amendment
   * opened. Ratification is measured against this and not against the power
   * bonded when the delay runs out, because a threshold measured against the
   * set that remains is passed by jailing everyone who would have voted no.
   */
  snapshotPower: string;
  /**
   * ratified_power is the power that has ratified so far, accumulated as each
   * ratification arrives at the weight the validator carried then. Kept as a
   * running total rather than recomputed at enactment for the same reason: a
   * tally recomputed at the end is a tally that can be changed by changing the
   * set.
   */
  ratifiedPower: string;
  status: AmendmentStatus;
  resolvedAtHeight: string;
}

/**
 * Ratification is one validator agreeing to one amendment, at the weight it
 * carried when it agreed. Recorded individually rather than only as a total so
 * that who ratified a change to the seizure threshold is answerable for it
 * afterwards, which is the only sanction available against a set that votes
 * itself more power.
 */
export interface Ratification {
  amendmentId: string;
  /**
   * validator is the operator address in its account form, matching how every
   * other record in this repository keys a validator.
   */
  validator: string;
  power: string;
  height: string;
}

function createBaseInvariants(): Invariants {
  return {
    maxEntityPowerBps: "0",
    maxBeneficialOwnerPowerBps: "0",
    maxJurisdictionPowerBps: "0",
    concentrationEpochBlocks: "0",
    minActiveValidators: 0,
    enforcementThresholdBps: "0",
    enforcementRecoveryDestination: "",
    enforcementVotingPeriodBlocks: "0",
    enforcementProvisionalFreezeBlocks: "0",
    amendmentDelayBlocks: "0",
    amendmentThresholdBps: "0",
  };
}

export const Invariants = {
  encode(message: Invariants, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.maxEntityPowerBps !== "0") {
      writer.uint32(8).uint64(message.maxEntityPowerBps);
    }
    if (message.maxBeneficialOwnerPowerBps !== "0") {
      writer.uint32(16).uint64(message.maxBeneficialOwnerPowerBps);
    }
    if (message.maxJurisdictionPowerBps !== "0") {
      writer.uint32(24).uint64(message.maxJurisdictionPowerBps);
    }
    if (message.concentrationEpochBlocks !== "0") {
      writer.uint32(32).uint64(message.concentrationEpochBlocks);
    }
    if (message.minActiveValidators !== 0) {
      writer.uint32(40).uint32(message.minActiveValidators);
    }
    if (message.enforcementThresholdBps !== "0") {
      writer.uint32(48).uint64(message.enforcementThresholdBps);
    }
    if (message.enforcementRecoveryDestination !== "") {
      writer.uint32(58).string(message.enforcementRecoveryDestination);
    }
    if (message.enforcementVotingPeriodBlocks !== "0") {
      writer.uint32(64).uint64(message.enforcementVotingPeriodBlocks);
    }
    if (message.enforcementProvisionalFreezeBlocks !== "0") {
      writer.uint32(72).uint64(message.enforcementProvisionalFreezeBlocks);
    }
    if (message.amendmentDelayBlocks !== "0") {
      writer.uint32(80).uint64(message.amendmentDelayBlocks);
    }
    if (message.amendmentThresholdBps !== "0") {
      writer.uint32(88).uint64(message.amendmentThresholdBps);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Invariants {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInvariants();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.maxEntityPowerBps = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.maxBeneficialOwnerPowerBps = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.maxJurisdictionPowerBps = longToString(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.concentrationEpochBlocks = longToString(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.minActiveValidators = reader.uint32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.enforcementThresholdBps = longToString(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.enforcementRecoveryDestination = reader.string();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.enforcementVotingPeriodBlocks = longToString(reader.uint64() as Long);
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.enforcementProvisionalFreezeBlocks = longToString(reader.uint64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.amendmentDelayBlocks = longToString(reader.uint64() as Long);
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.amendmentThresholdBps = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Invariants {
    return {
      maxEntityPowerBps: isSet(object.maxEntityPowerBps) ? globalThis.String(object.maxEntityPowerBps) : "0",
      maxBeneficialOwnerPowerBps: isSet(object.maxBeneficialOwnerPowerBps)
        ? globalThis.String(object.maxBeneficialOwnerPowerBps)
        : "0",
      maxJurisdictionPowerBps: isSet(object.maxJurisdictionPowerBps)
        ? globalThis.String(object.maxJurisdictionPowerBps)
        : "0",
      concentrationEpochBlocks: isSet(object.concentrationEpochBlocks)
        ? globalThis.String(object.concentrationEpochBlocks)
        : "0",
      minActiveValidators: isSet(object.minActiveValidators) ? globalThis.Number(object.minActiveValidators) : 0,
      enforcementThresholdBps: isSet(object.enforcementThresholdBps)
        ? globalThis.String(object.enforcementThresholdBps)
        : "0",
      enforcementRecoveryDestination: isSet(object.enforcementRecoveryDestination)
        ? globalThis.String(object.enforcementRecoveryDestination)
        : "",
      enforcementVotingPeriodBlocks: isSet(object.enforcementVotingPeriodBlocks)
        ? globalThis.String(object.enforcementVotingPeriodBlocks)
        : "0",
      enforcementProvisionalFreezeBlocks: isSet(object.enforcementProvisionalFreezeBlocks)
        ? globalThis.String(object.enforcementProvisionalFreezeBlocks)
        : "0",
      amendmentDelayBlocks: isSet(object.amendmentDelayBlocks) ? globalThis.String(object.amendmentDelayBlocks) : "0",
      amendmentThresholdBps: isSet(object.amendmentThresholdBps)
        ? globalThis.String(object.amendmentThresholdBps)
        : "0",
    };
  },

  toJSON(message: Invariants): unknown {
    const obj: any = {};
    if (message.maxEntityPowerBps !== "0") {
      obj.maxEntityPowerBps = message.maxEntityPowerBps;
    }
    if (message.maxBeneficialOwnerPowerBps !== "0") {
      obj.maxBeneficialOwnerPowerBps = message.maxBeneficialOwnerPowerBps;
    }
    if (message.maxJurisdictionPowerBps !== "0") {
      obj.maxJurisdictionPowerBps = message.maxJurisdictionPowerBps;
    }
    if (message.concentrationEpochBlocks !== "0") {
      obj.concentrationEpochBlocks = message.concentrationEpochBlocks;
    }
    if (message.minActiveValidators !== 0) {
      obj.minActiveValidators = Math.round(message.minActiveValidators);
    }
    if (message.enforcementThresholdBps !== "0") {
      obj.enforcementThresholdBps = message.enforcementThresholdBps;
    }
    if (message.enforcementRecoveryDestination !== "") {
      obj.enforcementRecoveryDestination = message.enforcementRecoveryDestination;
    }
    if (message.enforcementVotingPeriodBlocks !== "0") {
      obj.enforcementVotingPeriodBlocks = message.enforcementVotingPeriodBlocks;
    }
    if (message.enforcementProvisionalFreezeBlocks !== "0") {
      obj.enforcementProvisionalFreezeBlocks = message.enforcementProvisionalFreezeBlocks;
    }
    if (message.amendmentDelayBlocks !== "0") {
      obj.amendmentDelayBlocks = message.amendmentDelayBlocks;
    }
    if (message.amendmentThresholdBps !== "0") {
      obj.amendmentThresholdBps = message.amendmentThresholdBps;
    }
    return obj;
  },

  create(base?: DeepPartial<Invariants>): Invariants {
    return Invariants.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Invariants>): Invariants {
    const message = createBaseInvariants();
    message.maxEntityPowerBps = object.maxEntityPowerBps ?? "0";
    message.maxBeneficialOwnerPowerBps = object.maxBeneficialOwnerPowerBps ?? "0";
    message.maxJurisdictionPowerBps = object.maxJurisdictionPowerBps ?? "0";
    message.concentrationEpochBlocks = object.concentrationEpochBlocks ?? "0";
    message.minActiveValidators = object.minActiveValidators ?? 0;
    message.enforcementThresholdBps = object.enforcementThresholdBps ?? "0";
    message.enforcementRecoveryDestination = object.enforcementRecoveryDestination ?? "";
    message.enforcementVotingPeriodBlocks = object.enforcementVotingPeriodBlocks ?? "0";
    message.enforcementProvisionalFreezeBlocks = object.enforcementProvisionalFreezeBlocks ?? "0";
    message.amendmentDelayBlocks = object.amendmentDelayBlocks ?? "0";
    message.amendmentThresholdBps = object.amendmentThresholdBps ?? "0";
    return message;
  },
};

function createBaseAmendment(): Amendment {
  return {
    id: "0",
    proposed: undefined,
    reason: "",
    proposedAtHeight: "0",
    effectiveAtHeight: "0",
    snapshotPower: "0",
    ratifiedPower: "0",
    status: 0,
    resolvedAtHeight: "0",
  };
}

export const Amendment = {
  encode(message: Amendment, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.proposed !== undefined) {
      Invariants.encode(message.proposed, writer.uint32(18).fork()).ldelim();
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.proposedAtHeight !== "0") {
      writer.uint32(32).int64(message.proposedAtHeight);
    }
    if (message.effectiveAtHeight !== "0") {
      writer.uint32(40).int64(message.effectiveAtHeight);
    }
    if (message.snapshotPower !== "0") {
      writer.uint32(48).int64(message.snapshotPower);
    }
    if (message.ratifiedPower !== "0") {
      writer.uint32(56).int64(message.ratifiedPower);
    }
    if (message.status !== 0) {
      writer.uint32(64).int32(message.status);
    }
    if (message.resolvedAtHeight !== "0") {
      writer.uint32(72).int64(message.resolvedAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Amendment {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmendment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.proposed = Invariants.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.proposedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.effectiveAtHeight = longToString(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.snapshotPower = longToString(reader.int64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.ratifiedPower = longToString(reader.int64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.resolvedAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Amendment {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      proposed: isSet(object.proposed) ? Invariants.fromJSON(object.proposed) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      proposedAtHeight: isSet(object.proposedAtHeight) ? globalThis.String(object.proposedAtHeight) : "0",
      effectiveAtHeight: isSet(object.effectiveAtHeight) ? globalThis.String(object.effectiveAtHeight) : "0",
      snapshotPower: isSet(object.snapshotPower) ? globalThis.String(object.snapshotPower) : "0",
      ratifiedPower: isSet(object.ratifiedPower) ? globalThis.String(object.ratifiedPower) : "0",
      status: isSet(object.status) ? amendmentStatusFromJSON(object.status) : 0,
      resolvedAtHeight: isSet(object.resolvedAtHeight) ? globalThis.String(object.resolvedAtHeight) : "0",
    };
  },

  toJSON(message: Amendment): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.proposed !== undefined) {
      obj.proposed = Invariants.toJSON(message.proposed);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.proposedAtHeight !== "0") {
      obj.proposedAtHeight = message.proposedAtHeight;
    }
    if (message.effectiveAtHeight !== "0") {
      obj.effectiveAtHeight = message.effectiveAtHeight;
    }
    if (message.snapshotPower !== "0") {
      obj.snapshotPower = message.snapshotPower;
    }
    if (message.ratifiedPower !== "0") {
      obj.ratifiedPower = message.ratifiedPower;
    }
    if (message.status !== 0) {
      obj.status = amendmentStatusToJSON(message.status);
    }
    if (message.resolvedAtHeight !== "0") {
      obj.resolvedAtHeight = message.resolvedAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<Amendment>): Amendment {
    return Amendment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Amendment>): Amendment {
    const message = createBaseAmendment();
    message.id = object.id ?? "0";
    message.proposed = (object.proposed !== undefined && object.proposed !== null)
      ? Invariants.fromPartial(object.proposed)
      : undefined;
    message.reason = object.reason ?? "";
    message.proposedAtHeight = object.proposedAtHeight ?? "0";
    message.effectiveAtHeight = object.effectiveAtHeight ?? "0";
    message.snapshotPower = object.snapshotPower ?? "0";
    message.ratifiedPower = object.ratifiedPower ?? "0";
    message.status = object.status ?? 0;
    message.resolvedAtHeight = object.resolvedAtHeight ?? "0";
    return message;
  },
};

function createBaseRatification(): Ratification {
  return { amendmentId: "0", validator: "", power: "0", height: "0" };
}

export const Ratification = {
  encode(message: Ratification, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.amendmentId !== "0") {
      writer.uint32(8).uint64(message.amendmentId);
    }
    if (message.validator !== "") {
      writer.uint32(18).string(message.validator);
    }
    if (message.power !== "0") {
      writer.uint32(24).int64(message.power);
    }
    if (message.height !== "0") {
      writer.uint32(32).int64(message.height);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Ratification {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRatification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.amendmentId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.power = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.height = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Ratification {
    return {
      amendmentId: isSet(object.amendmentId) ? globalThis.String(object.amendmentId) : "0",
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      power: isSet(object.power) ? globalThis.String(object.power) : "0",
      height: isSet(object.height) ? globalThis.String(object.height) : "0",
    };
  },

  toJSON(message: Ratification): unknown {
    const obj: any = {};
    if (message.amendmentId !== "0") {
      obj.amendmentId = message.amendmentId;
    }
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.power !== "0") {
      obj.power = message.power;
    }
    if (message.height !== "0") {
      obj.height = message.height;
    }
    return obj;
  },

  create(base?: DeepPartial<Ratification>): Ratification {
    return Ratification.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Ratification>): Ratification {
    const message = createBaseRatification();
    message.amendmentId = object.amendmentId ?? "0";
    message.validator = object.validator ?? "";
    message.power = object.power ?? "0";
    message.height = object.height ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
