// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/constitution/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Amendment, Invariants, Ratification } from "./constitution.ts";

export const protobufPackage = "blockchain.constitution.v1";

/**
 * GenesisState defines the constitution module's genesis state.
 *
 * There is no Params field, and its absence is the design. This module holds
 * nothing an ordinary governance proposal may write, so giving it a mutable
 * parameter set would be handing back the property it exists to provide.
 */
export interface GenesisState {
  /**
   * invariants is the founding settlement. Every field of it must be set: a
   * genesis that leaves one at zero is refused at InitGenesis rather than
   * completed from a default, because a ceiling nobody chose is not a ceiling
   * and an address compiled into a binary is not the foundation on somebody
   * else's network.
   */
  invariants:
    | Invariants
    | undefined;
  /**
   * amendments carries every amendment ever opened, pending ones included. A
   * chain that dropped a pending amendment on export would lose a public delay
   * that was already running — at the one moment, an upgrade, when nobody is
   * watching the constitution.
   */
  amendments: Amendment[];
  /**
   * ratifications are exported individually because the running total on the
   * amendment is not enough to reconstruct them, and a chain that kept the
   * total but forgot who agreed would let a validator ratify a second time
   * after an import.
   */
  ratifications: Ratification[];
  /**
   * amendment_count is the next id to hand out. Exported explicitly rather than
   * rebuilt from the highest amendment present, so that a genesis edited by
   * hand says what it means and importing it cannot collide.
   */
  amendmentCount: string;
}

function createBaseGenesisState(): GenesisState {
  return { invariants: undefined, amendments: [], ratifications: [], amendmentCount: "0" };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.invariants !== undefined) {
      Invariants.encode(message.invariants, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.amendments) {
      Amendment.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.ratifications) {
      Ratification.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.amendmentCount !== "0") {
      writer.uint32(32).uint64(message.amendmentCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.invariants = Invariants.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.amendments.push(Amendment.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.ratifications.push(Ratification.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.amendmentCount = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      invariants: isSet(object.invariants) ? Invariants.fromJSON(object.invariants) : undefined,
      amendments: globalThis.Array.isArray(object?.amendments)
        ? object.amendments.map((e: any) => Amendment.fromJSON(e))
        : [],
      ratifications: globalThis.Array.isArray(object?.ratifications)
        ? object.ratifications.map((e: any) => Ratification.fromJSON(e))
        : [],
      amendmentCount: isSet(object.amendmentCount) ? globalThis.String(object.amendmentCount) : "0",
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.invariants !== undefined) {
      obj.invariants = Invariants.toJSON(message.invariants);
    }
    if (message.amendments?.length) {
      obj.amendments = message.amendments.map((e) => Amendment.toJSON(e));
    }
    if (message.ratifications?.length) {
      obj.ratifications = message.ratifications.map((e) => Ratification.toJSON(e));
    }
    if (message.amendmentCount !== "0") {
      obj.amendmentCount = message.amendmentCount;
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.invariants = (object.invariants !== undefined && object.invariants !== null)
      ? Invariants.fromPartial(object.invariants)
      : undefined;
    message.amendments = object.amendments?.map((e) => Amendment.fromPartial(e)) || [];
    message.ratifications = object.ratifications?.map((e) => Ratification.fromPartial(e)) || [];
    message.amendmentCount = object.amendmentCount ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
