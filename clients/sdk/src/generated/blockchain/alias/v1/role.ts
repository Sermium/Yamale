// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/alias/v1/role.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.alias.v1";

/**
 * Role is a power an account may be granted inside a perimeter.
 *
 * The list is short and closed on purpose. A role is only worth having if some
 * module refuses an action without it, so a role nothing consults is a name in
 * a registry pretending to be a control — and the way that happens is by
 * letting the set grow faster than the checks.
 */
export const Role = {
  /**
   * ROLE_UNSPECIFIED - ROLE_UNSPECIFIED is the unset default and is never valid.
   *
   * The zero value is reserved rather than given to a real role, and every
   * path refuses it. Proto3 cannot tell a zero from an absent field, so a
   * grant whose role happened to be the first of the list would be
   * indistinguishable from a grant whose role nobody filled in — and the
   * second must never be honoured.
   */
  ROLE_UNSPECIFIED: 0,
  /**
   * ROLE_REGISTRY_AUTHORITY - ROLE_REGISTRY_AUTHORITY is a lands commission or cadastral office:
   * registering a parcel, validating a transfer, freezing land.
   */
  ROLE_REGISTRY_AUTHORITY: 1,
  /**
   * ROLE_MONETARY_AUTHORITY - ROLE_MONETARY_AUTHORITY is a central bank: admitting the issuer of a
   * currency inside its jurisdiction.
   */
  ROLE_MONETARY_AUTHORITY: 2,
  /**
   * ROLE_PAYMENTS_AUTHORITY - ROLE_PAYMENTS_AUTHORITY admits the institutions that may appear on a
   * payment instruction. Separate from the monetary authority because
   * licensing a payment service provider and issuing money are different
   * offices in most of the deployments this chain is built for, and collapsing
   * them here would force one key to hold both.
   */
  ROLE_PAYMENTS_AUTHORITY: 3,
  /**
   * ROLE_ENFORCEMENT_AUTHORITY - ROLE_ENFORCEMENT_AUTHORITY may stop an account: opening a case against it,
   * or freezing it outright.
   */
  ROLE_ENFORCEMENT_AUTHORITY: 4,
  /**
   * ROLE_SUPERVISOR - ROLE_SUPERVISOR is oversight without the power to act — the role held by
   * an auditor or a regulator that watches a perimeter it does not administer.
   * It is granted through the same registry as the rest so that "who is
   * watching this country" has one answer in one place.
   */
  ROLE_SUPERVISOR: 5,
  UNRECOGNIZED: -1,
} as const;

export type Role = typeof Role[keyof typeof Role];

export namespace Role {
  export type ROLE_UNSPECIFIED = typeof Role.ROLE_UNSPECIFIED;
  export type ROLE_REGISTRY_AUTHORITY = typeof Role.ROLE_REGISTRY_AUTHORITY;
  export type ROLE_MONETARY_AUTHORITY = typeof Role.ROLE_MONETARY_AUTHORITY;
  export type ROLE_PAYMENTS_AUTHORITY = typeof Role.ROLE_PAYMENTS_AUTHORITY;
  export type ROLE_ENFORCEMENT_AUTHORITY = typeof Role.ROLE_ENFORCEMENT_AUTHORITY;
  export type ROLE_SUPERVISOR = typeof Role.ROLE_SUPERVISOR;
  export type UNRECOGNIZED = typeof Role.UNRECOGNIZED;
}

export function roleFromJSON(object: any): Role {
  switch (object) {
    case 0:
    case "ROLE_UNSPECIFIED":
      return Role.ROLE_UNSPECIFIED;
    case 1:
    case "ROLE_REGISTRY_AUTHORITY":
      return Role.ROLE_REGISTRY_AUTHORITY;
    case 2:
    case "ROLE_MONETARY_AUTHORITY":
      return Role.ROLE_MONETARY_AUTHORITY;
    case 3:
    case "ROLE_PAYMENTS_AUTHORITY":
      return Role.ROLE_PAYMENTS_AUTHORITY;
    case 4:
    case "ROLE_ENFORCEMENT_AUTHORITY":
      return Role.ROLE_ENFORCEMENT_AUTHORITY;
    case 5:
    case "ROLE_SUPERVISOR":
      return Role.ROLE_SUPERVISOR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Role.UNRECOGNIZED;
  }
}

export function roleToJSON(object: Role): string {
  switch (object) {
    case Role.ROLE_UNSPECIFIED:
      return "ROLE_UNSPECIFIED";
    case Role.ROLE_REGISTRY_AUTHORITY:
      return "ROLE_REGISTRY_AUTHORITY";
    case Role.ROLE_MONETARY_AUTHORITY:
      return "ROLE_MONETARY_AUTHORITY";
    case Role.ROLE_PAYMENTS_AUTHORITY:
      return "ROLE_PAYMENTS_AUTHORITY";
    case Role.ROLE_ENFORCEMENT_AUTHORITY:
      return "ROLE_ENFORCEMENT_AUTHORITY";
    case Role.ROLE_SUPERVISOR:
      return "ROLE_SUPERVISOR";
    case Role.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * RoleGrant is the triple the whole perimeter is built from: who, what role,
 * where.
 *
 * It is created and removed by governance and by nobody else. A role its own
 * holder could grant is not a perimeter, and neither is one another holder of
 * the same role could hand out — buying one office would buy the power to
 * manufacture the rest.
 */
export interface RoleGrant {
  /**
   * holder is the granted account. Expected to be an x/group account wherever
   * the decision the role carries should be M-of-N rather than one official's
   * key; that expectation is checked at grant time rather than trusted, for
   * the same reason x/land refuses a registry office that is a plain key.
   */
  holder: string;
  /** role is what the holder may do. Never ROLE_UNSPECIFIED. */
  role: Role;
  /**
   * jurisdiction is where the holder may do it: an ISO 3166-1 alpha-2 country
   * code from the assigned list, or "*" for chain-wide.
   *
   * The chain-wide form is the exception and is meant to stay rare. It has its
   * own query endpoint so a governance console can list every one of them
   * without having to know how to spell the wildcard — an exception nobody can
   * enumerate is an exception nobody audits.
   *
   * Note what cannot appear here: the foundation's reserved code. That code
   * marks the *absence* of a national perimeter, so a grant naming it would be
   * a grant over nowhere, and it would read to a human as chain-wide authority
   * while conferring none. Chain-wide is spelled "*" and only "*".
   */
  jurisdiction: string;
  /**
   * granted_by is the authority that made the grant. Kept because "who says
   * this account may freeze Nigerian accounts" is the question asked when the
   * answer turns out to be wrong, and an unattributed grant cannot answer it.
   */
  grantedBy: string;
  /**
   * granted_at_height is when it was written, so a widening of somebody's
   * powers is visible as a change rather than as a fact that was always there.
   */
  grantedAtHeight: string;
}

function createBaseRoleGrant(): RoleGrant {
  return { holder: "", role: 0, jurisdiction: "", grantedBy: "", grantedAtHeight: "0" };
}

export const RoleGrant = {
  encode(message: RoleGrant, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.holder !== "") {
      writer.uint32(10).string(message.holder);
    }
    if (message.role !== 0) {
      writer.uint32(16).int32(message.role);
    }
    if (message.jurisdiction !== "") {
      writer.uint32(26).string(message.jurisdiction);
    }
    if (message.grantedBy !== "") {
      writer.uint32(34).string(message.grantedBy);
    }
    if (message.grantedAtHeight !== "0") {
      writer.uint32(40).int64(message.grantedAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RoleGrant {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoleGrant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.holder = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.jurisdiction = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.grantedBy = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.grantedAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoleGrant {
    return {
      holder: isSet(object.holder) ? globalThis.String(object.holder) : "",
      role: isSet(object.role) ? roleFromJSON(object.role) : 0,
      jurisdiction: isSet(object.jurisdiction) ? globalThis.String(object.jurisdiction) : "",
      grantedBy: isSet(object.grantedBy) ? globalThis.String(object.grantedBy) : "",
      grantedAtHeight: isSet(object.grantedAtHeight) ? globalThis.String(object.grantedAtHeight) : "0",
    };
  },

  toJSON(message: RoleGrant): unknown {
    const obj: any = {};
    if (message.holder !== "") {
      obj.holder = message.holder;
    }
    if (message.role !== 0) {
      obj.role = roleToJSON(message.role);
    }
    if (message.jurisdiction !== "") {
      obj.jurisdiction = message.jurisdiction;
    }
    if (message.grantedBy !== "") {
      obj.grantedBy = message.grantedBy;
    }
    if (message.grantedAtHeight !== "0") {
      obj.grantedAtHeight = message.grantedAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<RoleGrant>): RoleGrant {
    return RoleGrant.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoleGrant>): RoleGrant {
    const message = createBaseRoleGrant();
    message.holder = object.holder ?? "";
    message.role = object.role ?? 0;
    message.jurisdiction = object.jurisdiction ?? "";
    message.grantedBy = object.grantedBy ?? "";
    message.grantedAtHeight = object.grantedAtHeight ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
