// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/alias/v1/query.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination.ts";
import { Alias } from "./alias.ts";
import { Jurisdiction } from "./jurisdiction.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.alias.v1";

/** QueryParamsRequest is the request for the module parameters. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse carries the module parameters. */
export interface QueryParamsResponse {
  params: Params | undefined;
}

/** QueryAliasRequest asks which address an identifier resolves to. */
export interface QueryAliasRequest {
  /**
   * id in any form the client has it: hyphenated, lower case, with the
   * confusable characters unfolded. The module normalises before looking up.
   */
  id: string;
}

/** QueryAliasResponse carries the binding, or a not-found error when the identifier was never issued. */
export interface QueryAliasResponse {
  alias: Alias | undefined;
}

/** QueryAliasOfRequest asks which identifier an address holds. */
export interface QueryAliasOfRequest {
  address: string;
}

/** QueryAliasOfResponse carries that binding. */
export interface QueryAliasOfResponse {
  alias: Alias | undefined;
}

/** QueryRetiredRequest asks whether an identifier has been tombstoned. */
export interface QueryRetiredRequest {
  id: string;
}

/** QueryRetiredResponse distinguishes "retired" from "never existed" — different facts for somebody about to send money. */
export interface QueryRetiredResponse {
  retired: boolean;
}

/** QueryJurisdictionRequest asks which country an account is recorded in. */
export interface QueryJurisdictionRequest {
  address: string;
}

/**
 * QueryJurisdictionResponse carries that record, or a not-found error.
 *
 * Not-found is a real answer here rather than a gap: outside the foundation
 * administrators, an account with no jurisdiction holds no identifier and never
 * will.
 */
export interface QueryJurisdictionResponse {
  jurisdiction: Jurisdiction | undefined;
}

/** QueryPerimeterRequest asks for the accounts recorded in one country. */
export interface QueryPerimeterRequest {
  /** country is an ISO 3166-1 alpha-2 code, in any case. */
  country: string;
  pagination: PageRequest | undefined;
}

/** QueryPerimeterResponse carries one page of that country's accounts. */
export interface QueryPerimeterResponse {
  jurisdictions: Jurisdiction[];
  pagination: PageResponse | undefined;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest = {
  encode(_: QueryParamsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse = {
  encode(message: QueryParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryAliasRequest(): QueryAliasRequest {
  return { id: "" };
}

export const QueryAliasRequest = {
  encode(message: QueryAliasRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAliasRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAliasRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAliasRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: QueryAliasRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAliasRequest>): QueryAliasRequest {
    return QueryAliasRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAliasRequest>): QueryAliasRequest {
    const message = createBaseQueryAliasRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseQueryAliasResponse(): QueryAliasResponse {
  return { alias: undefined };
}

export const QueryAliasResponse = {
  encode(message: QueryAliasResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.alias !== undefined) {
      Alias.encode(message.alias, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAliasResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAliasResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.alias = Alias.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAliasResponse {
    return { alias: isSet(object.alias) ? Alias.fromJSON(object.alias) : undefined };
  },

  toJSON(message: QueryAliasResponse): unknown {
    const obj: any = {};
    if (message.alias !== undefined) {
      obj.alias = Alias.toJSON(message.alias);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAliasResponse>): QueryAliasResponse {
    return QueryAliasResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAliasResponse>): QueryAliasResponse {
    const message = createBaseQueryAliasResponse();
    message.alias = (object.alias !== undefined && object.alias !== null) ? Alias.fromPartial(object.alias) : undefined;
    return message;
  },
};

function createBaseQueryAliasOfRequest(): QueryAliasOfRequest {
  return { address: "" };
}

export const QueryAliasOfRequest = {
  encode(message: QueryAliasOfRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAliasOfRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAliasOfRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAliasOfRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryAliasOfRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAliasOfRequest>): QueryAliasOfRequest {
    return QueryAliasOfRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAliasOfRequest>): QueryAliasOfRequest {
    const message = createBaseQueryAliasOfRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryAliasOfResponse(): QueryAliasOfResponse {
  return { alias: undefined };
}

export const QueryAliasOfResponse = {
  encode(message: QueryAliasOfResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.alias !== undefined) {
      Alias.encode(message.alias, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAliasOfResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAliasOfResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.alias = Alias.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAliasOfResponse {
    return { alias: isSet(object.alias) ? Alias.fromJSON(object.alias) : undefined };
  },

  toJSON(message: QueryAliasOfResponse): unknown {
    const obj: any = {};
    if (message.alias !== undefined) {
      obj.alias = Alias.toJSON(message.alias);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAliasOfResponse>): QueryAliasOfResponse {
    return QueryAliasOfResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAliasOfResponse>): QueryAliasOfResponse {
    const message = createBaseQueryAliasOfResponse();
    message.alias = (object.alias !== undefined && object.alias !== null) ? Alias.fromPartial(object.alias) : undefined;
    return message;
  },
};

function createBaseQueryRetiredRequest(): QueryRetiredRequest {
  return { id: "" };
}

export const QueryRetiredRequest = {
  encode(message: QueryRetiredRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRetiredRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRetiredRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRetiredRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: QueryRetiredRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRetiredRequest>): QueryRetiredRequest {
    return QueryRetiredRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRetiredRequest>): QueryRetiredRequest {
    const message = createBaseQueryRetiredRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseQueryRetiredResponse(): QueryRetiredResponse {
  return { retired: false };
}

export const QueryRetiredResponse = {
  encode(message: QueryRetiredResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.retired !== false) {
      writer.uint32(8).bool(message.retired);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRetiredResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRetiredResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.retired = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRetiredResponse {
    return { retired: isSet(object.retired) ? globalThis.Boolean(object.retired) : false };
  },

  toJSON(message: QueryRetiredResponse): unknown {
    const obj: any = {};
    if (message.retired !== false) {
      obj.retired = message.retired;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRetiredResponse>): QueryRetiredResponse {
    return QueryRetiredResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRetiredResponse>): QueryRetiredResponse {
    const message = createBaseQueryRetiredResponse();
    message.retired = object.retired ?? false;
    return message;
  },
};

function createBaseQueryJurisdictionRequest(): QueryJurisdictionRequest {
  return { address: "" };
}

export const QueryJurisdictionRequest = {
  encode(message: QueryJurisdictionRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryJurisdictionRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryJurisdictionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryJurisdictionRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryJurisdictionRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryJurisdictionRequest>): QueryJurisdictionRequest {
    return QueryJurisdictionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryJurisdictionRequest>): QueryJurisdictionRequest {
    const message = createBaseQueryJurisdictionRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryJurisdictionResponse(): QueryJurisdictionResponse {
  return { jurisdiction: undefined };
}

export const QueryJurisdictionResponse = {
  encode(message: QueryJurisdictionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.jurisdiction !== undefined) {
      Jurisdiction.encode(message.jurisdiction, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryJurisdictionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryJurisdictionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.jurisdiction = Jurisdiction.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryJurisdictionResponse {
    return { jurisdiction: isSet(object.jurisdiction) ? Jurisdiction.fromJSON(object.jurisdiction) : undefined };
  },

  toJSON(message: QueryJurisdictionResponse): unknown {
    const obj: any = {};
    if (message.jurisdiction !== undefined) {
      obj.jurisdiction = Jurisdiction.toJSON(message.jurisdiction);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryJurisdictionResponse>): QueryJurisdictionResponse {
    return QueryJurisdictionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryJurisdictionResponse>): QueryJurisdictionResponse {
    const message = createBaseQueryJurisdictionResponse();
    message.jurisdiction = (object.jurisdiction !== undefined && object.jurisdiction !== null)
      ? Jurisdiction.fromPartial(object.jurisdiction)
      : undefined;
    return message;
  },
};

function createBaseQueryPerimeterRequest(): QueryPerimeterRequest {
  return { country: "", pagination: undefined };
}

export const QueryPerimeterRequest = {
  encode(message: QueryPerimeterRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.country !== "") {
      writer.uint32(10).string(message.country);
    }
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryPerimeterRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPerimeterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.country = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPerimeterRequest {
    return {
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryPerimeterRequest): unknown {
    const obj: any = {};
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryPerimeterRequest>): QueryPerimeterRequest {
    return QueryPerimeterRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryPerimeterRequest>): QueryPerimeterRequest {
    const message = createBaseQueryPerimeterRequest();
    message.country = object.country ?? "";
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryPerimeterResponse(): QueryPerimeterResponse {
  return { jurisdictions: [], pagination: undefined };
}

export const QueryPerimeterResponse = {
  encode(message: QueryPerimeterResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.jurisdictions) {
      Jurisdiction.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryPerimeterResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPerimeterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.jurisdictions.push(Jurisdiction.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPerimeterResponse {
    return {
      jurisdictions: globalThis.Array.isArray(object?.jurisdictions)
        ? object.jurisdictions.map((e: any) => Jurisdiction.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryPerimeterResponse): unknown {
    const obj: any = {};
    if (message.jurisdictions?.length) {
      obj.jurisdictions = message.jurisdictions.map((e) => Jurisdiction.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryPerimeterResponse>): QueryPerimeterResponse {
    return QueryPerimeterResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryPerimeterResponse>): QueryPerimeterResponse {
    const message = createBaseQueryPerimeterResponse();
    message.jurisdictions = object.jurisdictions?.map((e) => Jurisdiction.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

/**
 * Query defines the alias query service.
 *
 * There is deliberately no list endpoint. A typable identifier space is
 * enumerable in principle — eight characters is 2^40, which is hours of GPU
 * time — and no storage trick changes that. What the module can do is refuse to
 * hand out the directory as a convenience: walking it should cost an indexer's
 * effort, not one paginated request.
 *
 * The constraint that actually limits the damage is elsewhere: an identifier
 * maps to an address and to nothing else. No name, no telephone number, no
 * document number lives in this module, so somebody who does enumerate it
 * learns which addresses have identifiers — visible on a public chain
 * already — and nothing about who owns them.
 */
export interface Query {
  /** Params returns the module parameters. */
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse>;
  /** Alias resolves an identifier to an address. The hot path. */
  Alias(request: QueryAliasRequest): Promise<QueryAliasResponse>;
  /**
   * AliasOf returns the identifier held by an address, for interfaces that
   * need to show a person their own handle.
   */
  AliasOf(request: QueryAliasOfRequest): Promise<QueryAliasOfResponse>;
  /**
   * Retired reports whether an identifier has been tombstoned. A client that
   * resolves nothing should be able to tell "never existed" from "existed and
   * was retired", because those mean very different things to somebody about
   * to send money.
   */
  Retired(request: QueryRetiredRequest): Promise<QueryRetiredResponse>;
  /** Jurisdiction reports the country recorded against one account. */
  Jurisdiction(request: QueryJurisdictionRequest): Promise<QueryJurisdictionResponse>;
  /**
   * Perimeter lists the accounts recorded in one country, so an authority can
   * see the accounts it may act on and no others.
   *
   * This is a list endpoint in a module that deliberately has none, so note
   * what it returns and what it does not: jurisdiction records, never
   * identifiers. Walking it tells you which addresses are recorded in a
   * country — a supervisory fact, and the reason the endpoint exists — and
   * gives no shortcut to the alias directory that is withheld above.
   */
  Perimeter(request: QueryPerimeterRequest): Promise<QueryPerimeterResponse>;
}

export const QueryServiceName = "blockchain.alias.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Params = this.Params.bind(this);
    this.Alias = this.Alias.bind(this);
    this.AliasOf = this.AliasOf.bind(this);
    this.Retired = this.Retired.bind(this);
    this.Jurisdiction = this.Jurisdiction.bind(this);
    this.Perimeter = this.Perimeter.bind(this);
  }
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse> {
    const data = QueryParamsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Params", data);
    return promise.then((data) => QueryParamsResponse.decode(_m0.Reader.create(data)));
  }

  Alias(request: QueryAliasRequest): Promise<QueryAliasResponse> {
    const data = QueryAliasRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Alias", data);
    return promise.then((data) => QueryAliasResponse.decode(_m0.Reader.create(data)));
  }

  AliasOf(request: QueryAliasOfRequest): Promise<QueryAliasOfResponse> {
    const data = QueryAliasOfRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "AliasOf", data);
    return promise.then((data) => QueryAliasOfResponse.decode(_m0.Reader.create(data)));
  }

  Retired(request: QueryRetiredRequest): Promise<QueryRetiredResponse> {
    const data = QueryRetiredRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Retired", data);
    return promise.then((data) => QueryRetiredResponse.decode(_m0.Reader.create(data)));
  }

  Jurisdiction(request: QueryJurisdictionRequest): Promise<QueryJurisdictionResponse> {
    const data = QueryJurisdictionRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Jurisdiction", data);
    return promise.then((data) => QueryJurisdictionResponse.decode(_m0.Reader.create(data)));
  }

  Perimeter(request: QueryPerimeterRequest): Promise<QueryPerimeterResponse> {
    const data = QueryPerimeterRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Perimeter", data);
    return promise.then((data) => QueryPerimeterResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
