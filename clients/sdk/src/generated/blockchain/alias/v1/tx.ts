// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/alias/v1/tx.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.alias.v1";

/**
 * MsgRegisterAlias claims an identifier for the sending account.
 *
 * It carries no identifier field on purpose, and no country field either. The
 * chain derives the identifier from the address and takes the country prefix
 * from the jurisdiction already recorded against the account, so there is
 * nothing for the sender to choose, nothing for anyone else to squat, and no
 * way to be issued a prefix that says somewhere you are not. An account with no
 * recorded jurisdiction is refused rather than given a default.
 */
export interface MsgRegisterAlias {
  account: string;
}

/** MsgRegisterAliasResponse returns the identifier the chain assigned. */
export interface MsgRegisterAliasResponse {
  /**
   * id is the identifier that was assigned, normalised. The sender cannot know
   * it in advance of the transaction landing, so it comes back here and in the
   * event.
   */
  id: string;
}

/**
 * MsgRotateAlias retires the sender's current identifier and issues a new one.
 *
 * For an account whose key was compromised: the old identifier is tombstoned
 * and never issued again, so a payment sent to the handle somebody memorised
 * arrives nowhere rather than in a stranger's account. That is the whole reason
 * retiring is allowed and repointing is not.
 */
export interface MsgRotateAlias {
  account: string;
}

/** MsgRotateAliasResponse returns what was tombstoned and what replaced it. */
export interface MsgRotateAliasResponse {
  /** retired is the identifier that will never be issued again. */
  retired: string;
  /** id is the replacement. */
  id: string;
}

/**
 * MsgSetJurisdiction records or corrects the country an account belongs to.
 *
 * The first recording is made by the approved participant that onboarded the
 * account, because that is the party that performed the KYC and therefore the
 * only one that knows the answer. It is never self-declared: an account that
 * could name its own perimeter could name the one with no authority watching
 * it.
 *
 * A correction — somebody moves, a firm redomiciles — is a foundation
 * administrator's act, not the participant's. A participant that could rewrite
 * a country it had already recorded could move a customer out from under an
 * authority mid-investigation.
 */
export interface MsgSetJurisdiction {
  /**
   * recorder is the approved participant that onboarded the account, or a
   * foundation administrator, or governance.
   */
  recorder: string;
  /** account is whose perimeter this is. */
  account: string;
  /**
   * country is an ISO 3166-1 alpha-2 code. It must be one ISO has actually
   * assigned: the reserved codes, ZZ among them, are refused here so that the
   * marker the foundation administrators carry cannot be handed to an ordinary
   * account and read as chain-wide authority.
   */
  country: string;
}

/**
 * MsgSetJurisdictionResponse reports the identifier consequences of the change.
 *
 * Correcting the country of an account that already holds an identifier retires
 * that identifier and issues a replacement in the same message. It has to: the
 * prefix would otherwise name a country the chain no longer records, and a
 * prefix that can go stale is a prefix that can lie. Rotating it here keeps
 * every property intact — the old identifier is tombstoned rather than
 * repointed, and is never issued again.
 */
export interface MsgSetJurisdictionResponse {
  /**
   * retired is the identifier the correction gave up, empty when the account
   * held none.
   */
  retired: string;
  /**
   * id is the replacement identifier, carrying the new country. Empty when the
   * account held none, in which case it will get one when it registers.
   */
  id: string;
}

/**
 * MsgRegisterViewingKey publishes the sending account's X25519 public key.
 *
 * Self-signed, unlike the jurisdiction beside it, and the difference is not an
 * oversight. A jurisdiction is a claim about somebody that they must not be
 * able to make for themselves; a viewing key is a claim only about which key
 * can decrypt payloads addressed to the sender, and an account that publishes a
 * key it does not hold has locked itself out of its own payment detail and
 * nobody else out of anything.
 *
 * Sending it again rotates: the previous version stays queryable so historical
 * envelopes remain openable, and new envelopes wrap to the new version. See
 * ViewingKey.version.
 */
export interface MsgRegisterViewingKey {
  account: string;
  /**
   * public_key is 32 bytes of X25519. The private half never leaves the holder,
   * and nothing on this chain has any use for it.
   */
  publicKey: Uint8Array;
}

/** MsgRegisterViewingKeyResponse returns the version that was assigned. */
export interface MsgRegisterViewingKeyResponse {
  /**
   * version is what an envelope will name when it wraps to this key. The sender
   * cannot know it before the transaction lands, so it comes back here.
   */
  version: string;
}

/**
 * MsgRevokeViewingKey marks one of the sender's key versions compromised.
 *
 * It does not delete the key and does not pretend the payloads wrapped to it
 * became unreadable — ciphertext that has been distributed cannot be recalled,
 * and a message that implied otherwise would be worse than none. What it does
 * is stop senders wrapping to it and let a reader see that a payload they are
 * holding was addressed to a key somebody else may also hold. Erasing the
 * payload itself is the store's job, not the chain's.
 */
export interface MsgRevokeViewingKey {
  account: string;
  /**
   * version is which registration to mark. Named explicitly rather than
   * defaulting to the newest, because the key an operator wants to revoke is
   * usually the old one they have just rotated away from.
   */
  version: string;
}

/** MsgRevokeViewingKeyResponse is empty. */
export interface MsgRevokeViewingKeyResponse {
}

/**
 * MsgAppointRegulator names the authority that holds the third viewing key over
 * payments settling in one country.
 *
 * Authority-gated, because this is the single most powerful grant the
 * confidentiality design makes: the appointee can read the ISO 20022 detail of
 * every payment that names their country from the moment they are appointed.
 * An account that could appoint itself regulator of Ghana would have granted
 * itself that by sending one message.
 */
export interface MsgAppointRegulator {
  authority: string;
  /**
   * country is an ISO 3166-1 alpha-2 code. Checked against the assigned list
   * for the same reason a jurisdiction is: a mistyped NX would appoint a
   * regulator of nowhere, and every payment declaring NG would go on being
   * encrypted to nobody while the appointment sat in state looking done.
   */
  country: string;
  /**
   * address is the appointed authority, expected to be an x/group account
   * wherever the decision to open a payload should be M-of-N rather than one
   * official. The chain does not require that; it records who was named.
   */
  address: string;
}

/** MsgAppointRegulatorResponse is empty. */
export interface MsgAppointRegulatorResponse {
}

/**
 * MsgGrantAuditor grants the time-boxed cross-account reading role.
 *
 * Authority-gated and bounded in three ways at once — it expires by height, the
 * number of live grants is capped, and every grant is recorded with who made
 * it. All three exist because this role is the one that reads payment detail
 * belonging to people who have no relationship with the holder.
 */
export interface MsgGrantAuditor {
  authority: string;
  address: string;
  /**
   * expires_at_height must be in the future. There is no unbounded form of this
   * grant, and no zero-means-forever: a role that can become permanent by
   * leaving a field unset is not time-boxed, it is time-boxed by convention.
   */
  expiresAtHeight: string;
}

/** MsgGrantAuditorResponse is empty. */
export interface MsgGrantAuditorResponse {
}

/** MsgUpdateParams sets the module parameters. Governance only. */
export interface MsgUpdateParams {
  authority: string;
  params: Params | undefined;
}

/** MsgUpdateParamsResponse is empty. */
export interface MsgUpdateParamsResponse {
}

function createBaseMsgRegisterAlias(): MsgRegisterAlias {
  return { account: "" };
}

export const MsgRegisterAlias = {
  encode(message: MsgRegisterAlias, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.account !== "") {
      writer.uint32(10).string(message.account);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRegisterAlias {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterAlias();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.account = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterAlias {
    return { account: isSet(object.account) ? globalThis.String(object.account) : "" };
  },

  toJSON(message: MsgRegisterAlias): unknown {
    const obj: any = {};
    if (message.account !== "") {
      obj.account = message.account;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterAlias>): MsgRegisterAlias {
    return MsgRegisterAlias.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterAlias>): MsgRegisterAlias {
    const message = createBaseMsgRegisterAlias();
    message.account = object.account ?? "";
    return message;
  },
};

function createBaseMsgRegisterAliasResponse(): MsgRegisterAliasResponse {
  return { id: "" };
}

export const MsgRegisterAliasResponse = {
  encode(message: MsgRegisterAliasResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRegisterAliasResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterAliasResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterAliasResponse {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: MsgRegisterAliasResponse): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterAliasResponse>): MsgRegisterAliasResponse {
    return MsgRegisterAliasResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterAliasResponse>): MsgRegisterAliasResponse {
    const message = createBaseMsgRegisterAliasResponse();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseMsgRotateAlias(): MsgRotateAlias {
  return { account: "" };
}

export const MsgRotateAlias = {
  encode(message: MsgRotateAlias, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.account !== "") {
      writer.uint32(10).string(message.account);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRotateAlias {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRotateAlias();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.account = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRotateAlias {
    return { account: isSet(object.account) ? globalThis.String(object.account) : "" };
  },

  toJSON(message: MsgRotateAlias): unknown {
    const obj: any = {};
    if (message.account !== "") {
      obj.account = message.account;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRotateAlias>): MsgRotateAlias {
    return MsgRotateAlias.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRotateAlias>): MsgRotateAlias {
    const message = createBaseMsgRotateAlias();
    message.account = object.account ?? "";
    return message;
  },
};

function createBaseMsgRotateAliasResponse(): MsgRotateAliasResponse {
  return { retired: "", id: "" };
}

export const MsgRotateAliasResponse = {
  encode(message: MsgRotateAliasResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.retired !== "") {
      writer.uint32(10).string(message.retired);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRotateAliasResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRotateAliasResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.retired = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRotateAliasResponse {
    return {
      retired: isSet(object.retired) ? globalThis.String(object.retired) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
    };
  },

  toJSON(message: MsgRotateAliasResponse): unknown {
    const obj: any = {};
    if (message.retired !== "") {
      obj.retired = message.retired;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRotateAliasResponse>): MsgRotateAliasResponse {
    return MsgRotateAliasResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRotateAliasResponse>): MsgRotateAliasResponse {
    const message = createBaseMsgRotateAliasResponse();
    message.retired = object.retired ?? "";
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseMsgSetJurisdiction(): MsgSetJurisdiction {
  return { recorder: "", account: "", country: "" };
}

export const MsgSetJurisdiction = {
  encode(message: MsgSetJurisdiction, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.recorder !== "") {
      writer.uint32(10).string(message.recorder);
    }
    if (message.account !== "") {
      writer.uint32(18).string(message.account);
    }
    if (message.country !== "") {
      writer.uint32(26).string(message.country);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSetJurisdiction {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSetJurisdiction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.recorder = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.account = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.country = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSetJurisdiction {
    return {
      recorder: isSet(object.recorder) ? globalThis.String(object.recorder) : "",
      account: isSet(object.account) ? globalThis.String(object.account) : "",
      country: isSet(object.country) ? globalThis.String(object.country) : "",
    };
  },

  toJSON(message: MsgSetJurisdiction): unknown {
    const obj: any = {};
    if (message.recorder !== "") {
      obj.recorder = message.recorder;
    }
    if (message.account !== "") {
      obj.account = message.account;
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSetJurisdiction>): MsgSetJurisdiction {
    return MsgSetJurisdiction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSetJurisdiction>): MsgSetJurisdiction {
    const message = createBaseMsgSetJurisdiction();
    message.recorder = object.recorder ?? "";
    message.account = object.account ?? "";
    message.country = object.country ?? "";
    return message;
  },
};

function createBaseMsgSetJurisdictionResponse(): MsgSetJurisdictionResponse {
  return { retired: "", id: "" };
}

export const MsgSetJurisdictionResponse = {
  encode(message: MsgSetJurisdictionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.retired !== "") {
      writer.uint32(10).string(message.retired);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSetJurisdictionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSetJurisdictionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.retired = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSetJurisdictionResponse {
    return {
      retired: isSet(object.retired) ? globalThis.String(object.retired) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
    };
  },

  toJSON(message: MsgSetJurisdictionResponse): unknown {
    const obj: any = {};
    if (message.retired !== "") {
      obj.retired = message.retired;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSetJurisdictionResponse>): MsgSetJurisdictionResponse {
    return MsgSetJurisdictionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSetJurisdictionResponse>): MsgSetJurisdictionResponse {
    const message = createBaseMsgSetJurisdictionResponse();
    message.retired = object.retired ?? "";
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseMsgRegisterViewingKey(): MsgRegisterViewingKey {
  return { account: "", publicKey: new Uint8Array(0) };
}

export const MsgRegisterViewingKey = {
  encode(message: MsgRegisterViewingKey, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.account !== "") {
      writer.uint32(10).string(message.account);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(18).bytes(message.publicKey);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRegisterViewingKey {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterViewingKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.account = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterViewingKey {
    return {
      account: isSet(object.account) ? globalThis.String(object.account) : "",
      publicKey: isSet(object.publicKey) ? bytesFromBase64(object.publicKey) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgRegisterViewingKey): unknown {
    const obj: any = {};
    if (message.account !== "") {
      obj.account = message.account;
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterViewingKey>): MsgRegisterViewingKey {
    return MsgRegisterViewingKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterViewingKey>): MsgRegisterViewingKey {
    const message = createBaseMsgRegisterViewingKey();
    message.account = object.account ?? "";
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRegisterViewingKeyResponse(): MsgRegisterViewingKeyResponse {
  return { version: "0" };
}

export const MsgRegisterViewingKeyResponse = {
  encode(message: MsgRegisterViewingKeyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.version !== "0") {
      writer.uint32(8).uint64(message.version);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRegisterViewingKeyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterViewingKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.version = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterViewingKeyResponse {
    return { version: isSet(object.version) ? globalThis.String(object.version) : "0" };
  },

  toJSON(message: MsgRegisterViewingKeyResponse): unknown {
    const obj: any = {};
    if (message.version !== "0") {
      obj.version = message.version;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterViewingKeyResponse>): MsgRegisterViewingKeyResponse {
    return MsgRegisterViewingKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterViewingKeyResponse>): MsgRegisterViewingKeyResponse {
    const message = createBaseMsgRegisterViewingKeyResponse();
    message.version = object.version ?? "0";
    return message;
  },
};

function createBaseMsgRevokeViewingKey(): MsgRevokeViewingKey {
  return { account: "", version: "0" };
}

export const MsgRevokeViewingKey = {
  encode(message: MsgRevokeViewingKey, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.account !== "") {
      writer.uint32(10).string(message.account);
    }
    if (message.version !== "0") {
      writer.uint32(16).uint64(message.version);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRevokeViewingKey {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeViewingKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.account = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.version = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRevokeViewingKey {
    return {
      account: isSet(object.account) ? globalThis.String(object.account) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
    };
  },

  toJSON(message: MsgRevokeViewingKey): unknown {
    const obj: any = {};
    if (message.account !== "") {
      obj.account = message.account;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRevokeViewingKey>): MsgRevokeViewingKey {
    return MsgRevokeViewingKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRevokeViewingKey>): MsgRevokeViewingKey {
    const message = createBaseMsgRevokeViewingKey();
    message.account = object.account ?? "";
    message.version = object.version ?? "0";
    return message;
  },
};

function createBaseMsgRevokeViewingKeyResponse(): MsgRevokeViewingKeyResponse {
  return {};
}

export const MsgRevokeViewingKeyResponse = {
  encode(_: MsgRevokeViewingKeyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRevokeViewingKeyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeViewingKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRevokeViewingKeyResponse {
    return {};
  },

  toJSON(_: MsgRevokeViewingKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRevokeViewingKeyResponse>): MsgRevokeViewingKeyResponse {
    return MsgRevokeViewingKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRevokeViewingKeyResponse>): MsgRevokeViewingKeyResponse {
    const message = createBaseMsgRevokeViewingKeyResponse();
    return message;
  },
};

function createBaseMsgAppointRegulator(): MsgAppointRegulator {
  return { authority: "", country: "", address: "" };
}

export const MsgAppointRegulator = {
  encode(message: MsgAppointRegulator, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.country !== "") {
      writer.uint32(18).string(message.country);
    }
    if (message.address !== "") {
      writer.uint32(26).string(message.address);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgAppointRegulator {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAppointRegulator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.country = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.address = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAppointRegulator {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
    };
  },

  toJSON(message: MsgAppointRegulator): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgAppointRegulator>): MsgAppointRegulator {
    return MsgAppointRegulator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgAppointRegulator>): MsgAppointRegulator {
    const message = createBaseMsgAppointRegulator();
    message.authority = object.authority ?? "";
    message.country = object.country ?? "";
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseMsgAppointRegulatorResponse(): MsgAppointRegulatorResponse {
  return {};
}

export const MsgAppointRegulatorResponse = {
  encode(_: MsgAppointRegulatorResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgAppointRegulatorResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAppointRegulatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgAppointRegulatorResponse {
    return {};
  },

  toJSON(_: MsgAppointRegulatorResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgAppointRegulatorResponse>): MsgAppointRegulatorResponse {
    return MsgAppointRegulatorResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgAppointRegulatorResponse>): MsgAppointRegulatorResponse {
    const message = createBaseMsgAppointRegulatorResponse();
    return message;
  },
};

function createBaseMsgGrantAuditor(): MsgGrantAuditor {
  return { authority: "", address: "", expiresAtHeight: "0" };
}

export const MsgGrantAuditor = {
  encode(message: MsgGrantAuditor, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.expiresAtHeight !== "0") {
      writer.uint32(24).int64(message.expiresAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgGrantAuditor {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgGrantAuditor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.address = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.expiresAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgGrantAuditor {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      expiresAtHeight: isSet(object.expiresAtHeight) ? globalThis.String(object.expiresAtHeight) : "0",
    };
  },

  toJSON(message: MsgGrantAuditor): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.expiresAtHeight !== "0") {
      obj.expiresAtHeight = message.expiresAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgGrantAuditor>): MsgGrantAuditor {
    return MsgGrantAuditor.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgGrantAuditor>): MsgGrantAuditor {
    const message = createBaseMsgGrantAuditor();
    message.authority = object.authority ?? "";
    message.address = object.address ?? "";
    message.expiresAtHeight = object.expiresAtHeight ?? "0";
    return message;
  },
};

function createBaseMsgGrantAuditorResponse(): MsgGrantAuditorResponse {
  return {};
}

export const MsgGrantAuditorResponse = {
  encode(_: MsgGrantAuditorResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgGrantAuditorResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgGrantAuditorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgGrantAuditorResponse {
    return {};
  },

  toJSON(_: MsgGrantAuditorResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgGrantAuditorResponse>): MsgGrantAuditorResponse {
    return MsgGrantAuditorResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgGrantAuditorResponse>): MsgGrantAuditorResponse {
    const message = createBaseMsgGrantAuditorResponse();
    return message;
  },
};

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams = {
  encode(message: MsgUpdateParams, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse = {
  encode(_: MsgUpdateParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

/**
 * Msg defines the alias Msg service.
 *
 * There is no MsgSetAlias, because identifiers are assigned rather than chosen:
 * a chosen handle brings squatting, a resale market, and — the one that costs
 * money — impersonation, where YAMALE-PAY and YAMALE-PAY1 look identical in a
 * confirmation dialog.
 *
 * There is no MsgReleaseAlias either. Releasing and re-registering as two
 * messages would leave an account unreachable between them, so rotation is one
 * atomic message.
 *
 * There is no MsgClearJurisdiction. Removing a country would put an account
 * back into the unclaimed state the perimeter exists to abolish, holding an
 * identifier whose prefix names a country the chain no longer records. A
 * jurisdiction is corrected, never emptied.
 */
export interface Msg {
  /** RegisterAlias issues an identifier to the sending account. */
  RegisterAlias(request: MsgRegisterAlias): Promise<MsgRegisterAliasResponse>;
  /** RotateAlias retires the sender's identifier and issues a new one. */
  RotateAlias(request: MsgRotateAlias): Promise<MsgRotateAliasResponse>;
  /** SetJurisdiction records or corrects the country an account belongs to. */
  SetJurisdiction(request: MsgSetJurisdiction): Promise<MsgSetJurisdictionResponse>;
  /** RegisterViewingKey publishes the sender's X25519 public key, or rotates it. */
  RegisterViewingKey(request: MsgRegisterViewingKey): Promise<MsgRegisterViewingKeyResponse>;
  /** RevokeViewingKey marks one of the sender's key versions compromised. */
  RevokeViewingKey(request: MsgRevokeViewingKey): Promise<MsgRevokeViewingKeyResponse>;
  /** AppointRegulator names the authority holding the viewing key for a country. */
  AppointRegulator(request: MsgAppointRegulator): Promise<MsgAppointRegulatorResponse>;
  /** GrantAuditor grants the time-boxed cross-account reading role. */
  GrantAuditor(request: MsgGrantAuditor): Promise<MsgGrantAuditorResponse>;
  /** UpdateParams sets the module parameters. Governance only. */
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
}

export const MsgServiceName = "blockchain.alias.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.RegisterAlias = this.RegisterAlias.bind(this);
    this.RotateAlias = this.RotateAlias.bind(this);
    this.SetJurisdiction = this.SetJurisdiction.bind(this);
    this.RegisterViewingKey = this.RegisterViewingKey.bind(this);
    this.RevokeViewingKey = this.RevokeViewingKey.bind(this);
    this.AppointRegulator = this.AppointRegulator.bind(this);
    this.GrantAuditor = this.GrantAuditor.bind(this);
    this.UpdateParams = this.UpdateParams.bind(this);
  }
  RegisterAlias(request: MsgRegisterAlias): Promise<MsgRegisterAliasResponse> {
    const data = MsgRegisterAlias.encode(request).finish();
    const promise = this.rpc.request(this.service, "RegisterAlias", data);
    return promise.then((data) => MsgRegisterAliasResponse.decode(_m0.Reader.create(data)));
  }

  RotateAlias(request: MsgRotateAlias): Promise<MsgRotateAliasResponse> {
    const data = MsgRotateAlias.encode(request).finish();
    const promise = this.rpc.request(this.service, "RotateAlias", data);
    return promise.then((data) => MsgRotateAliasResponse.decode(_m0.Reader.create(data)));
  }

  SetJurisdiction(request: MsgSetJurisdiction): Promise<MsgSetJurisdictionResponse> {
    const data = MsgSetJurisdiction.encode(request).finish();
    const promise = this.rpc.request(this.service, "SetJurisdiction", data);
    return promise.then((data) => MsgSetJurisdictionResponse.decode(_m0.Reader.create(data)));
  }

  RegisterViewingKey(request: MsgRegisterViewingKey): Promise<MsgRegisterViewingKeyResponse> {
    const data = MsgRegisterViewingKey.encode(request).finish();
    const promise = this.rpc.request(this.service, "RegisterViewingKey", data);
    return promise.then((data) => MsgRegisterViewingKeyResponse.decode(_m0.Reader.create(data)));
  }

  RevokeViewingKey(request: MsgRevokeViewingKey): Promise<MsgRevokeViewingKeyResponse> {
    const data = MsgRevokeViewingKey.encode(request).finish();
    const promise = this.rpc.request(this.service, "RevokeViewingKey", data);
    return promise.then((data) => MsgRevokeViewingKeyResponse.decode(_m0.Reader.create(data)));
  }

  AppointRegulator(request: MsgAppointRegulator): Promise<MsgAppointRegulatorResponse> {
    const data = MsgAppointRegulator.encode(request).finish();
    const promise = this.rpc.request(this.service, "AppointRegulator", data);
    return promise.then((data) => MsgAppointRegulatorResponse.decode(_m0.Reader.create(data)));
  }

  GrantAuditor(request: MsgGrantAuditor): Promise<MsgGrantAuditorResponse> {
    const data = MsgGrantAuditor.encode(request).finish();
    const promise = this.rpc.request(this.service, "GrantAuditor", data);
    return promise.then((data) => MsgGrantAuditorResponse.decode(_m0.Reader.create(data)));
  }

  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
