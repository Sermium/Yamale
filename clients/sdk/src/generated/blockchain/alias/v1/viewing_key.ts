// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/alias/v1/viewing_key.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.alias.v1";

/**
 * ViewingKey is the X25519 public key a payment payload is encrypted to.
 *
 * It lives in this module for the same reason the jurisdiction does. The
 * payload is encrypted at the moment the payment is sent, so the sender has to
 * be able to look up every recipient's key *then* — the payer's, the payee's,
 * the key of the regulator of the declared settlement jurisdiction, and every
 * live auditor's. A registry that only covered accounts which happen to be
 * somebody's payment customer would leave the regulator and the auditor
 * unresolvable, which are two of the three parties the design exists to serve.
 *
 * Only the public half is ever recorded. That is not a convention that could be
 * relaxed later: a private key on an append-only ledger is a private key
 * published to everyone forever, and there is no erasure path that takes it
 * back.
 */
export interface ViewingKey {
  /** address is the account that holds the matching private key. */
  address: string;
  /**
   * version climbs by one per registration and is never reused.
   *
   * Every envelope names the version it wrapped a content key to, so a payload
   * encrypted years ago still says which key opens it. Reusing a number would
   * make that reference ambiguous, and the party holding the wrong half would
   * see an authentication failure rather than a resolvable "you need the older
   * key" — indistinguishable from a corrupted payload.
   */
  version: string;
  /**
   * public_key is 32 bytes of X25519. Checked for length rather than assumed:
   * a shorter value is not a key, and a sender that wrapped a content key to it
   * would produce an envelope nobody can ever open, discovered only by the
   * party who needed to read it.
   */
  publicKey: Uint8Array;
  /**
   * registered_at_height is when this version was published.
   *
   * Senders must not encrypt to a key that was not yet published at the height
   * they are sending, and a reader auditing an old envelope needs to know which
   * keys were available when it was written.
   */
  registeredAtHeight: string;
  /**
   * revoked marks a key whose private half is believed compromised.
   *
   * Revocation is not rotation, and conflating them loses the record. A revoked
   * key must not be wrapped to again — but the envelopes already wrapped to it
   * stay wrapped to it, because ciphertext that exists cannot be recalled. The
   * flag is therefore a warning to senders and an exposure marker for readers,
   * never a claim that old payloads became unreadable.
   *
   * A boolean beside the height rather than "height != 0", which is what this
   * field was first. Proto3 cannot tell a height of zero from an unset field,
   * so a key revoked at height zero — a genesis-seeded revocation, or any
   * revocation on a chain that has not produced a block — read back as live.
   * That failure is silent and points the wrong way: senders go on sealing
   * payment detail to a key its holder has already declared compromised.
   */
  revoked: boolean;
  /**
   * revoked_at_height is when it happened, and is meaningful only when revoked
   * is set. Kept because "when did this key stop being trustworthy" decides
   * which stored payloads are exposed, and a bare boolean cannot answer it.
   */
  revokedAtHeight: string;
}

/**
 * RegulatorAppointment names the authority that holds the third viewing key
 * over every payment declaring one country as its settlement jurisdiction.
 *
 * One per country, deliberately. The settlement jurisdiction is the field that
 * settles which authority may act on a cross-border payment, and a country with
 * two appointed regulators would reintroduce exactly the contest over standing
 * that the single declaration exists to end.
 */
export interface RegulatorAppointment {
  /** country is an ISO 3166-1 alpha-2 code from the assigned list. */
  country: string;
  /**
   * address is the appointed authority. Its viewing key is looked up
   * separately, so a regulator can rotate keys without being reappointed.
   */
  address: string;
  /**
   * appointed_by is the authority that made the appointment. Kept because "who
   * says this account regulates Nigeria" is the question asked when the answer
   * turns out to be wrong, and an unattributed appointment cannot answer it.
   */
  appointedBy: string;
  /**
   * appointed_at_height is when it was last written, so a change to who
   * regulates a country is visible as a change rather than as a fact that was
   * always there.
   */
  appointedAtHeight: string;
}

/**
 * AuditorGrant is the time-boxed role that may read payment payloads across
 * accounts, for aggregate checks no single party's own keys can perform.
 *
 * It expires by itself. A grant that has to be revoked to end is a grant that
 * stays live when the person who would have revoked it moves on, and this one
 * reads the detail of payments belonging to people who never dealt with the
 * holder — so the failure mode of forgetting is the whole population's
 * remittance text, indefinitely.
 */
export interface AuditorGrant {
  /** address is the granted account. */
  address: string;
  /** granted_by is the authority that granted it. */
  grantedBy: string;
  /** granted_at_height is when the grant was made. */
  grantedAtHeight: string;
  /**
   * expires_at_height is the first height at which the grant no longer holds.
   *
   * Required and strictly in the future, never zero. Zero would read as "no
   * expiry" to anyone who did not check, which is the one thing a time-boxed
   * role must not be able to become by omission.
   */
  expiresAtHeight: string;
}

function createBaseViewingKey(): ViewingKey {
  return {
    address: "",
    version: "0",
    publicKey: new Uint8Array(0),
    registeredAtHeight: "0",
    revoked: false,
    revokedAtHeight: "0",
  };
}

export const ViewingKey = {
  encode(message: ViewingKey, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.version !== "0") {
      writer.uint32(16).uint64(message.version);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(26).bytes(message.publicKey);
    }
    if (message.registeredAtHeight !== "0") {
      writer.uint32(32).int64(message.registeredAtHeight);
    }
    if (message.revoked !== false) {
      writer.uint32(40).bool(message.revoked);
    }
    if (message.revokedAtHeight !== "0") {
      writer.uint32(48).int64(message.revokedAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ViewingKey {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseViewingKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.version = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.registeredAtHeight = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.revoked = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.revokedAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ViewingKey {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
      publicKey: isSet(object.publicKey) ? bytesFromBase64(object.publicKey) : new Uint8Array(0),
      registeredAtHeight: isSet(object.registeredAtHeight) ? globalThis.String(object.registeredAtHeight) : "0",
      revoked: isSet(object.revoked) ? globalThis.Boolean(object.revoked) : false,
      revokedAtHeight: isSet(object.revokedAtHeight) ? globalThis.String(object.revokedAtHeight) : "0",
    };
  },

  toJSON(message: ViewingKey): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    if (message.registeredAtHeight !== "0") {
      obj.registeredAtHeight = message.registeredAtHeight;
    }
    if (message.revoked !== false) {
      obj.revoked = message.revoked;
    }
    if (message.revokedAtHeight !== "0") {
      obj.revokedAtHeight = message.revokedAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<ViewingKey>): ViewingKey {
    return ViewingKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ViewingKey>): ViewingKey {
    const message = createBaseViewingKey();
    message.address = object.address ?? "";
    message.version = object.version ?? "0";
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.registeredAtHeight = object.registeredAtHeight ?? "0";
    message.revoked = object.revoked ?? false;
    message.revokedAtHeight = object.revokedAtHeight ?? "0";
    return message;
  },
};

function createBaseRegulatorAppointment(): RegulatorAppointment {
  return { country: "", address: "", appointedBy: "", appointedAtHeight: "0" };
}

export const RegulatorAppointment = {
  encode(message: RegulatorAppointment, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.country !== "") {
      writer.uint32(10).string(message.country);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.appointedBy !== "") {
      writer.uint32(26).string(message.appointedBy);
    }
    if (message.appointedAtHeight !== "0") {
      writer.uint32(32).int64(message.appointedAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RegulatorAppointment {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegulatorAppointment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.country = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.address = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.appointedBy = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.appointedAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegulatorAppointment {
    return {
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      appointedBy: isSet(object.appointedBy) ? globalThis.String(object.appointedBy) : "",
      appointedAtHeight: isSet(object.appointedAtHeight) ? globalThis.String(object.appointedAtHeight) : "0",
    };
  },

  toJSON(message: RegulatorAppointment): unknown {
    const obj: any = {};
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.appointedBy !== "") {
      obj.appointedBy = message.appointedBy;
    }
    if (message.appointedAtHeight !== "0") {
      obj.appointedAtHeight = message.appointedAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<RegulatorAppointment>): RegulatorAppointment {
    return RegulatorAppointment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegulatorAppointment>): RegulatorAppointment {
    const message = createBaseRegulatorAppointment();
    message.country = object.country ?? "";
    message.address = object.address ?? "";
    message.appointedBy = object.appointedBy ?? "";
    message.appointedAtHeight = object.appointedAtHeight ?? "0";
    return message;
  },
};

function createBaseAuditorGrant(): AuditorGrant {
  return { address: "", grantedBy: "", grantedAtHeight: "0", expiresAtHeight: "0" };
}

export const AuditorGrant = {
  encode(message: AuditorGrant, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.grantedBy !== "") {
      writer.uint32(18).string(message.grantedBy);
    }
    if (message.grantedAtHeight !== "0") {
      writer.uint32(24).int64(message.grantedAtHeight);
    }
    if (message.expiresAtHeight !== "0") {
      writer.uint32(32).int64(message.expiresAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AuditorGrant {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditorGrant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.grantedBy = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.grantedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.expiresAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditorGrant {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      grantedBy: isSet(object.grantedBy) ? globalThis.String(object.grantedBy) : "",
      grantedAtHeight: isSet(object.grantedAtHeight) ? globalThis.String(object.grantedAtHeight) : "0",
      expiresAtHeight: isSet(object.expiresAtHeight) ? globalThis.String(object.expiresAtHeight) : "0",
    };
  },

  toJSON(message: AuditorGrant): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.grantedBy !== "") {
      obj.grantedBy = message.grantedBy;
    }
    if (message.grantedAtHeight !== "0") {
      obj.grantedAtHeight = message.grantedAtHeight;
    }
    if (message.expiresAtHeight !== "0") {
      obj.expiresAtHeight = message.expiresAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<AuditorGrant>): AuditorGrant {
    return AuditorGrant.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuditorGrant>): AuditorGrant {
    const message = createBaseAuditorGrant();
    message.address = object.address ?? "";
    message.grantedBy = object.grantedBy ?? "";
    message.grantedAtHeight = object.grantedAtHeight ?? "0";
    message.expiresAtHeight = object.expiresAtHeight ?? "0";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
