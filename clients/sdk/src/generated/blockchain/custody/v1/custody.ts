// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/custody/v1/custody.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.custody.v1";

/** DepositStatus is where a deposit stands. */
export const DepositStatus = {
  /** DEPOSIT_STATUS_UNSPECIFIED - DEPOSIT_STATUS_UNSPECIFIED is the unset zero value. */
  DEPOSIT_STATUS_UNSPECIFIED: 0,
  /** DEPOSIT_STATUS_PENDING - waiting for enough attestors. */
  DEPOSIT_STATUS_PENDING: 1,
  /** DEPOSIT_STATUS_CREDITED - threshold reached and the claim minted. */
  DEPOSIT_STATUS_CREDITED: 2,
  /** DEPOSIT_STATUS_REJECTED - refused by governance before crediting. */
  DEPOSIT_STATUS_REJECTED: 3,
  UNRECOGNIZED: -1,
} as const;

export type DepositStatus = typeof DepositStatus[keyof typeof DepositStatus];

export namespace DepositStatus {
  export type DEPOSIT_STATUS_UNSPECIFIED = typeof DepositStatus.DEPOSIT_STATUS_UNSPECIFIED;
  export type DEPOSIT_STATUS_PENDING = typeof DepositStatus.DEPOSIT_STATUS_PENDING;
  export type DEPOSIT_STATUS_CREDITED = typeof DepositStatus.DEPOSIT_STATUS_CREDITED;
  export type DEPOSIT_STATUS_REJECTED = typeof DepositStatus.DEPOSIT_STATUS_REJECTED;
  export type UNRECOGNIZED = typeof DepositStatus.UNRECOGNIZED;
}

export function depositStatusFromJSON(object: any): DepositStatus {
  switch (object) {
    case 0:
    case "DEPOSIT_STATUS_UNSPECIFIED":
      return DepositStatus.DEPOSIT_STATUS_UNSPECIFIED;
    case 1:
    case "DEPOSIT_STATUS_PENDING":
      return DepositStatus.DEPOSIT_STATUS_PENDING;
    case 2:
    case "DEPOSIT_STATUS_CREDITED":
      return DepositStatus.DEPOSIT_STATUS_CREDITED;
    case 3:
    case "DEPOSIT_STATUS_REJECTED":
      return DepositStatus.DEPOSIT_STATUS_REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DepositStatus.UNRECOGNIZED;
  }
}

export function depositStatusToJSON(object: DepositStatus): string {
  switch (object) {
    case DepositStatus.DEPOSIT_STATUS_UNSPECIFIED:
      return "DEPOSIT_STATUS_UNSPECIFIED";
    case DepositStatus.DEPOSIT_STATUS_PENDING:
      return "DEPOSIT_STATUS_PENDING";
    case DepositStatus.DEPOSIT_STATUS_CREDITED:
      return "DEPOSIT_STATUS_CREDITED";
    case DepositStatus.DEPOSIT_STATUS_REJECTED:
      return "DEPOSIT_STATUS_REJECTED";
    case DepositStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** RedemptionStatus is where a redemption stands. */
export const RedemptionStatus = {
  /** REDEMPTION_STATUS_UNSPECIFIED - REDEMPTION_STATUS_UNSPECIFIED is the unset zero value. */
  REDEMPTION_STATUS_UNSPECIFIED: 0,
  /** REDEMPTION_STATUS_PENDING - REDEMPTION_STATUS_PENDING is burned and waiting out the delay. */
  REDEMPTION_STATUS_PENDING: 1,
  /** REDEMPTION_STATUS_SETTLED - REDEMPTION_STATUS_SETTLED is paid on the source chain. */
  REDEMPTION_STATUS_SETTLED: 2,
  /** REDEMPTION_STATUS_CANCELLED - cancelled before payout; the claim is minted back to the redeemer. */
  REDEMPTION_STATUS_CANCELLED: 3,
  UNRECOGNIZED: -1,
} as const;

export type RedemptionStatus = typeof RedemptionStatus[keyof typeof RedemptionStatus];

export namespace RedemptionStatus {
  export type REDEMPTION_STATUS_UNSPECIFIED = typeof RedemptionStatus.REDEMPTION_STATUS_UNSPECIFIED;
  export type REDEMPTION_STATUS_PENDING = typeof RedemptionStatus.REDEMPTION_STATUS_PENDING;
  export type REDEMPTION_STATUS_SETTLED = typeof RedemptionStatus.REDEMPTION_STATUS_SETTLED;
  export type REDEMPTION_STATUS_CANCELLED = typeof RedemptionStatus.REDEMPTION_STATUS_CANCELLED;
  export type UNRECOGNIZED = typeof RedemptionStatus.UNRECOGNIZED;
}

export function redemptionStatusFromJSON(object: any): RedemptionStatus {
  switch (object) {
    case 0:
    case "REDEMPTION_STATUS_UNSPECIFIED":
      return RedemptionStatus.REDEMPTION_STATUS_UNSPECIFIED;
    case 1:
    case "REDEMPTION_STATUS_PENDING":
      return RedemptionStatus.REDEMPTION_STATUS_PENDING;
    case 2:
    case "REDEMPTION_STATUS_SETTLED":
      return RedemptionStatus.REDEMPTION_STATUS_SETTLED;
    case 3:
    case "REDEMPTION_STATUS_CANCELLED":
      return RedemptionStatus.REDEMPTION_STATUS_CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RedemptionStatus.UNRECOGNIZED;
  }
}

export function redemptionStatusToJSON(object: RedemptionStatus): string {
  switch (object) {
    case RedemptionStatus.REDEMPTION_STATUS_UNSPECIFIED:
      return "REDEMPTION_STATUS_UNSPECIFIED";
    case RedemptionStatus.REDEMPTION_STATUS_PENDING:
      return "REDEMPTION_STATUS_PENDING";
    case RedemptionStatus.REDEMPTION_STATUS_SETTLED:
      return "REDEMPTION_STATUS_SETTLED";
    case RedemptionStatus.REDEMPTION_STATUS_CANCELLED:
      return "REDEMPTION_STATUS_CANCELLED";
    case RedemptionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Asset is one externally-custodied thing this chain can issue a claim on.
 *
 * The claim and the asset are the same unit, always. Deposit one ETH, hold one
 * yeth, redeem one ETH. That identity is the whole design: where the asset and
 * the liability are the same unit there is no price in the arrangement, so
 * there is no position for the treasury to be wrong about.
 *
 * The alternative — minting the native token against foreign collateral — is a
 * written put the treasury is short, permanently and unhedged. See
 * docs/guides/custody.md.
 */
export interface Asset {
  /** denom on this chain, e.g. "yeth". */
  denom: string;
  /**
   * chain the real asset sits on, e.g. "ethereum". Informational: this module
   * never talks to it.
   */
  sourceChain: string;
  /**
   * symbol and exponent for display. ETH needs 18, which x/stablecoin already
   * permits.
   */
  symbol: string;
  exponent: number;
  /**
   * paused stops new issuance without stopping redemption. Redemption must
   * never be pausable by the operator — an issuer who can stop you leaving is
   * not a custodian.
   */
  paused: boolean;
}

/**
 * Attestation is one attestor's statement that a deposit happened.
 *
 * Held individually rather than as a counter so that who said what stays on the
 * record. A threshold reached by two attestors is a fact about those two, and
 * if one is later found compromised, every mint they contributed to is
 * identifiable.
 */
export interface Attestation {
  depositId: string;
  attestor: string;
  attestedAtHeight: string;
}

/** Deposit is an external payment being turned into a claim on this chain. */
export interface Deposit {
  id: string;
  denom: string;
  /** recipient of the minted claim. */
  recipient: string;
  amount: string;
  /**
   * external_ref is the transaction on the source chain. Unique per asset: the
   * same deposit must never be creditable twice, and this is what makes a
   * replay detectable rather than merely unlikely.
   */
  externalRef: string;
  status: DepositStatus;
  createdAtHeight: string;
}

/**
 * Redemption is a claim being burned to release the real asset.
 *
 * The burn happens when the redemption is requested, not when it is paid.
 * Leaving the claim in circulation while the asset is being sent would let the
 * same claim be spent again and redeemed twice.
 */
export interface Redemption {
  id: string;
  denom: string;
  redeemer: string;
  amount: string;
  /** destination on the source chain. Opaque to this module. */
  destination: string;
  status: RedemptionStatus;
  requestedAtHeight: string;
  /**
   * payable_at_height is when the delay expires. Stored rather than computed so
   * a parameter change cannot retroactively move an existing redemption.
   */
  payableAtHeight: string;
  /** external_ref of the payout, once settled. */
  settledRef: string;
}

/**
 * Reserve is what the custodian says it holds against an asset.
 *
 * Attested rather than derived: the chain cannot see another chain's balances,
 * so this is a signed statement, and it is only as good as the attestors. What
 * the chain *can* do is compare it with what it has issued, which is what makes
 * "are we solvent" answerable by anyone rather than by the operator.
 */
export interface Reserve {
  denom: string;
  held: string;
  asOfHeight: string;
  attestor: string;
}

/**
 * Solvency is the answer to the only question that matters, computed rather
 * than asserted: issued versus held, per asset.
 */
export interface Solvency {
  denom: string;
  /**
   * issued is the chain's own total supply of the claim. The chain knows this
   * for certain.
   */
  issued: string;
  /** held is the last attested reserve. The chain takes this on trust. */
  held: string;
  /**
   * reserve_age_blocks is how stale that statement is. A reserve figure with no
   * age is a number somebody can leave unchanged while the money leaves.
   */
  reserveAgeBlocks: string;
  solvent: boolean;
}

function createBaseAsset(): Asset {
  return { denom: "", sourceChain: "", symbol: "", exponent: 0, paused: false };
}

export const Asset = {
  encode(message: Asset, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.denom !== "") {
      writer.uint32(10).string(message.denom);
    }
    if (message.sourceChain !== "") {
      writer.uint32(18).string(message.sourceChain);
    }
    if (message.symbol !== "") {
      writer.uint32(26).string(message.symbol);
    }
    if (message.exponent !== 0) {
      writer.uint32(32).uint32(message.exponent);
    }
    if (message.paused !== false) {
      writer.uint32(40).bool(message.paused);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Asset {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAsset();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.sourceChain = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.symbol = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.exponent = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.paused = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Asset {
    return {
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      sourceChain: isSet(object.sourceChain) ? globalThis.String(object.sourceChain) : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      exponent: isSet(object.exponent) ? globalThis.Number(object.exponent) : 0,
      paused: isSet(object.paused) ? globalThis.Boolean(object.paused) : false,
    };
  },

  toJSON(message: Asset): unknown {
    const obj: any = {};
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.sourceChain !== "") {
      obj.sourceChain = message.sourceChain;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.exponent !== 0) {
      obj.exponent = Math.round(message.exponent);
    }
    if (message.paused !== false) {
      obj.paused = message.paused;
    }
    return obj;
  },

  create(base?: DeepPartial<Asset>): Asset {
    return Asset.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Asset>): Asset {
    const message = createBaseAsset();
    message.denom = object.denom ?? "";
    message.sourceChain = object.sourceChain ?? "";
    message.symbol = object.symbol ?? "";
    message.exponent = object.exponent ?? 0;
    message.paused = object.paused ?? false;
    return message;
  },
};

function createBaseAttestation(): Attestation {
  return { depositId: "", attestor: "", attestedAtHeight: "0" };
}

export const Attestation = {
  encode(message: Attestation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.depositId !== "") {
      writer.uint32(10).string(message.depositId);
    }
    if (message.attestor !== "") {
      writer.uint32(18).string(message.attestor);
    }
    if (message.attestedAtHeight !== "0") {
      writer.uint32(24).int64(message.attestedAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Attestation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.depositId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.attestor = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.attestedAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Attestation {
    return {
      depositId: isSet(object.depositId) ? globalThis.String(object.depositId) : "",
      attestor: isSet(object.attestor) ? globalThis.String(object.attestor) : "",
      attestedAtHeight: isSet(object.attestedAtHeight) ? globalThis.String(object.attestedAtHeight) : "0",
    };
  },

  toJSON(message: Attestation): unknown {
    const obj: any = {};
    if (message.depositId !== "") {
      obj.depositId = message.depositId;
    }
    if (message.attestor !== "") {
      obj.attestor = message.attestor;
    }
    if (message.attestedAtHeight !== "0") {
      obj.attestedAtHeight = message.attestedAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<Attestation>): Attestation {
    return Attestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Attestation>): Attestation {
    const message = createBaseAttestation();
    message.depositId = object.depositId ?? "";
    message.attestor = object.attestor ?? "";
    message.attestedAtHeight = object.attestedAtHeight ?? "0";
    return message;
  },
};

function createBaseDeposit(): Deposit {
  return { id: "", denom: "", recipient: "", amount: "", externalRef: "", status: 0, createdAtHeight: "0" };
}

export const Deposit = {
  encode(message: Deposit, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.recipient !== "") {
      writer.uint32(26).string(message.recipient);
    }
    if (message.amount !== "") {
      writer.uint32(34).string(message.amount);
    }
    if (message.externalRef !== "") {
      writer.uint32(42).string(message.externalRef);
    }
    if (message.status !== 0) {
      writer.uint32(48).int32(message.status);
    }
    if (message.createdAtHeight !== "0") {
      writer.uint32(56).int64(message.createdAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Deposit {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeposit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.recipient = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.amount = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.externalRef = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.createdAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Deposit {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      recipient: isSet(object.recipient) ? globalThis.String(object.recipient) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      externalRef: isSet(object.externalRef) ? globalThis.String(object.externalRef) : "",
      status: isSet(object.status) ? depositStatusFromJSON(object.status) : 0,
      createdAtHeight: isSet(object.createdAtHeight) ? globalThis.String(object.createdAtHeight) : "0",
    };
  },

  toJSON(message: Deposit): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.recipient !== "") {
      obj.recipient = message.recipient;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.externalRef !== "") {
      obj.externalRef = message.externalRef;
    }
    if (message.status !== 0) {
      obj.status = depositStatusToJSON(message.status);
    }
    if (message.createdAtHeight !== "0") {
      obj.createdAtHeight = message.createdAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<Deposit>): Deposit {
    return Deposit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Deposit>): Deposit {
    const message = createBaseDeposit();
    message.id = object.id ?? "";
    message.denom = object.denom ?? "";
    message.recipient = object.recipient ?? "";
    message.amount = object.amount ?? "";
    message.externalRef = object.externalRef ?? "";
    message.status = object.status ?? 0;
    message.createdAtHeight = object.createdAtHeight ?? "0";
    return message;
  },
};

function createBaseRedemption(): Redemption {
  return {
    id: "",
    denom: "",
    redeemer: "",
    amount: "",
    destination: "",
    status: 0,
    requestedAtHeight: "0",
    payableAtHeight: "0",
    settledRef: "",
  };
}

export const Redemption = {
  encode(message: Redemption, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.redeemer !== "") {
      writer.uint32(26).string(message.redeemer);
    }
    if (message.amount !== "") {
      writer.uint32(34).string(message.amount);
    }
    if (message.destination !== "") {
      writer.uint32(42).string(message.destination);
    }
    if (message.status !== 0) {
      writer.uint32(48).int32(message.status);
    }
    if (message.requestedAtHeight !== "0") {
      writer.uint32(56).int64(message.requestedAtHeight);
    }
    if (message.payableAtHeight !== "0") {
      writer.uint32(64).int64(message.payableAtHeight);
    }
    if (message.settledRef !== "") {
      writer.uint32(74).string(message.settledRef);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Redemption {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRedemption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.redeemer = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.amount = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.destination = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.requestedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.payableAtHeight = longToString(reader.int64() as Long);
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.settledRef = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Redemption {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      redeemer: isSet(object.redeemer) ? globalThis.String(object.redeemer) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      destination: isSet(object.destination) ? globalThis.String(object.destination) : "",
      status: isSet(object.status) ? redemptionStatusFromJSON(object.status) : 0,
      requestedAtHeight: isSet(object.requestedAtHeight) ? globalThis.String(object.requestedAtHeight) : "0",
      payableAtHeight: isSet(object.payableAtHeight) ? globalThis.String(object.payableAtHeight) : "0",
      settledRef: isSet(object.settledRef) ? globalThis.String(object.settledRef) : "",
    };
  },

  toJSON(message: Redemption): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.redeemer !== "") {
      obj.redeemer = message.redeemer;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.destination !== "") {
      obj.destination = message.destination;
    }
    if (message.status !== 0) {
      obj.status = redemptionStatusToJSON(message.status);
    }
    if (message.requestedAtHeight !== "0") {
      obj.requestedAtHeight = message.requestedAtHeight;
    }
    if (message.payableAtHeight !== "0") {
      obj.payableAtHeight = message.payableAtHeight;
    }
    if (message.settledRef !== "") {
      obj.settledRef = message.settledRef;
    }
    return obj;
  },

  create(base?: DeepPartial<Redemption>): Redemption {
    return Redemption.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Redemption>): Redemption {
    const message = createBaseRedemption();
    message.id = object.id ?? "";
    message.denom = object.denom ?? "";
    message.redeemer = object.redeemer ?? "";
    message.amount = object.amount ?? "";
    message.destination = object.destination ?? "";
    message.status = object.status ?? 0;
    message.requestedAtHeight = object.requestedAtHeight ?? "0";
    message.payableAtHeight = object.payableAtHeight ?? "0";
    message.settledRef = object.settledRef ?? "";
    return message;
  },
};

function createBaseReserve(): Reserve {
  return { denom: "", held: "", asOfHeight: "0", attestor: "" };
}

export const Reserve = {
  encode(message: Reserve, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.denom !== "") {
      writer.uint32(10).string(message.denom);
    }
    if (message.held !== "") {
      writer.uint32(18).string(message.held);
    }
    if (message.asOfHeight !== "0") {
      writer.uint32(24).int64(message.asOfHeight);
    }
    if (message.attestor !== "") {
      writer.uint32(34).string(message.attestor);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Reserve {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserve();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.held = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.asOfHeight = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.attestor = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Reserve {
    return {
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      held: isSet(object.held) ? globalThis.String(object.held) : "",
      asOfHeight: isSet(object.asOfHeight) ? globalThis.String(object.asOfHeight) : "0",
      attestor: isSet(object.attestor) ? globalThis.String(object.attestor) : "",
    };
  },

  toJSON(message: Reserve): unknown {
    const obj: any = {};
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.held !== "") {
      obj.held = message.held;
    }
    if (message.asOfHeight !== "0") {
      obj.asOfHeight = message.asOfHeight;
    }
    if (message.attestor !== "") {
      obj.attestor = message.attestor;
    }
    return obj;
  },

  create(base?: DeepPartial<Reserve>): Reserve {
    return Reserve.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Reserve>): Reserve {
    const message = createBaseReserve();
    message.denom = object.denom ?? "";
    message.held = object.held ?? "";
    message.asOfHeight = object.asOfHeight ?? "0";
    message.attestor = object.attestor ?? "";
    return message;
  },
};

function createBaseSolvency(): Solvency {
  return { denom: "", issued: "", held: "", reserveAgeBlocks: "0", solvent: false };
}

export const Solvency = {
  encode(message: Solvency, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.denom !== "") {
      writer.uint32(10).string(message.denom);
    }
    if (message.issued !== "") {
      writer.uint32(18).string(message.issued);
    }
    if (message.held !== "") {
      writer.uint32(26).string(message.held);
    }
    if (message.reserveAgeBlocks !== "0") {
      writer.uint32(32).int64(message.reserveAgeBlocks);
    }
    if (message.solvent !== false) {
      writer.uint32(40).bool(message.solvent);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Solvency {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolvency();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.issued = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.held = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.reserveAgeBlocks = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.solvent = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Solvency {
    return {
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      issued: isSet(object.issued) ? globalThis.String(object.issued) : "",
      held: isSet(object.held) ? globalThis.String(object.held) : "",
      reserveAgeBlocks: isSet(object.reserveAgeBlocks) ? globalThis.String(object.reserveAgeBlocks) : "0",
      solvent: isSet(object.solvent) ? globalThis.Boolean(object.solvent) : false,
    };
  },

  toJSON(message: Solvency): unknown {
    const obj: any = {};
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.issued !== "") {
      obj.issued = message.issued;
    }
    if (message.held !== "") {
      obj.held = message.held;
    }
    if (message.reserveAgeBlocks !== "0") {
      obj.reserveAgeBlocks = message.reserveAgeBlocks;
    }
    if (message.solvent !== false) {
      obj.solvent = message.solvent;
    }
    return obj;
  },

  create(base?: DeepPartial<Solvency>): Solvency {
    return Solvency.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Solvency>): Solvency {
    const message = createBaseSolvency();
    message.denom = object.denom ?? "";
    message.issued = object.issued ?? "";
    message.held = object.held ?? "";
    message.reserveAgeBlocks = object.reserveAgeBlocks ?? "0";
    message.solvent = object.solvent ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
