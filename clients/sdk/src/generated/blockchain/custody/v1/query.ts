// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/custody/v1/query.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Asset, Deposit, Redemption, Solvency } from "./custody.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.custody.v1";

/** QueryParamsRequest is the request for the module parameters. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse carries the module parameters. */
export interface QueryParamsResponse {
  params: Params | undefined;
}

/** QueryAssetsRequest asks for the registered assets. */
export interface QueryAssetsRequest {
}

/** QueryAssetsResponse carries them. */
export interface QueryAssetsResponse {
  assets: Asset[];
}

/** QuerySolvencyRequest asks whether the custodian holds what it owes. */
export interface QuerySolvencyRequest {
}

/** QuerySolvencyResponse carries one line per asset. */
export interface QuerySolvencyResponse {
  solvency: Solvency[];
}

/** QueryDepositRequest asks for one deposit. */
export interface QueryDepositRequest {
  id: string;
}

/** QueryDepositResponse carries it. */
export interface QueryDepositResponse {
  deposit: Deposit | undefined;
}

/** QueryRedemptionRequest asks for one redemption. */
export interface QueryRedemptionRequest {
  id: string;
}

/** QueryRedemptionResponse carries it. */
export interface QueryRedemptionResponse {
  redemption: Redemption | undefined;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest = {
  encode(_: QueryParamsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse = {
  encode(message: QueryParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryAssetsRequest(): QueryAssetsRequest {
  return {};
}

export const QueryAssetsRequest = {
  encode(_: QueryAssetsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAssetsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAssetsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryAssetsRequest {
    return {};
  },

  toJSON(_: QueryAssetsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryAssetsRequest>): QueryAssetsRequest {
    return QueryAssetsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryAssetsRequest>): QueryAssetsRequest {
    const message = createBaseQueryAssetsRequest();
    return message;
  },
};

function createBaseQueryAssetsResponse(): QueryAssetsResponse {
  return { assets: [] };
}

export const QueryAssetsResponse = {
  encode(message: QueryAssetsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.assets) {
      Asset.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryAssetsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAssetsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.assets.push(Asset.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAssetsResponse {
    return { assets: globalThis.Array.isArray(object?.assets) ? object.assets.map((e: any) => Asset.fromJSON(e)) : [] };
  },

  toJSON(message: QueryAssetsResponse): unknown {
    const obj: any = {};
    if (message.assets?.length) {
      obj.assets = message.assets.map((e) => Asset.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryAssetsResponse>): QueryAssetsResponse {
    return QueryAssetsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryAssetsResponse>): QueryAssetsResponse {
    const message = createBaseQueryAssetsResponse();
    message.assets = object.assets?.map((e) => Asset.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQuerySolvencyRequest(): QuerySolvencyRequest {
  return {};
}

export const QuerySolvencyRequest = {
  encode(_: QuerySolvencyRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QuerySolvencyRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySolvencyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QuerySolvencyRequest {
    return {};
  },

  toJSON(_: QuerySolvencyRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QuerySolvencyRequest>): QuerySolvencyRequest {
    return QuerySolvencyRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QuerySolvencyRequest>): QuerySolvencyRequest {
    const message = createBaseQuerySolvencyRequest();
    return message;
  },
};

function createBaseQuerySolvencyResponse(): QuerySolvencyResponse {
  return { solvency: [] };
}

export const QuerySolvencyResponse = {
  encode(message: QuerySolvencyResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.solvency) {
      Solvency.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QuerySolvencyResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySolvencyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.solvency.push(Solvency.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySolvencyResponse {
    return {
      solvency: globalThis.Array.isArray(object?.solvency) ? object.solvency.map((e: any) => Solvency.fromJSON(e)) : [],
    };
  },

  toJSON(message: QuerySolvencyResponse): unknown {
    const obj: any = {};
    if (message.solvency?.length) {
      obj.solvency = message.solvency.map((e) => Solvency.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QuerySolvencyResponse>): QuerySolvencyResponse {
    return QuerySolvencyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuerySolvencyResponse>): QuerySolvencyResponse {
    const message = createBaseQuerySolvencyResponse();
    message.solvency = object.solvency?.map((e) => Solvency.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryDepositRequest(): QueryDepositRequest {
  return { id: "" };
}

export const QueryDepositRequest = {
  encode(message: QueryDepositRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryDepositRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryDepositRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryDepositRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: QueryDepositRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryDepositRequest>): QueryDepositRequest {
    return QueryDepositRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryDepositRequest>): QueryDepositRequest {
    const message = createBaseQueryDepositRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseQueryDepositResponse(): QueryDepositResponse {
  return { deposit: undefined };
}

export const QueryDepositResponse = {
  encode(message: QueryDepositResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.deposit !== undefined) {
      Deposit.encode(message.deposit, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryDepositResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryDepositResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.deposit = Deposit.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryDepositResponse {
    return { deposit: isSet(object.deposit) ? Deposit.fromJSON(object.deposit) : undefined };
  },

  toJSON(message: QueryDepositResponse): unknown {
    const obj: any = {};
    if (message.deposit !== undefined) {
      obj.deposit = Deposit.toJSON(message.deposit);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryDepositResponse>): QueryDepositResponse {
    return QueryDepositResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryDepositResponse>): QueryDepositResponse {
    const message = createBaseQueryDepositResponse();
    message.deposit = (object.deposit !== undefined && object.deposit !== null)
      ? Deposit.fromPartial(object.deposit)
      : undefined;
    return message;
  },
};

function createBaseQueryRedemptionRequest(): QueryRedemptionRequest {
  return { id: "" };
}

export const QueryRedemptionRequest = {
  encode(message: QueryRedemptionRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRedemptionRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRedemptionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRedemptionRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: QueryRedemptionRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRedemptionRequest>): QueryRedemptionRequest {
    return QueryRedemptionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRedemptionRequest>): QueryRedemptionRequest {
    const message = createBaseQueryRedemptionRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseQueryRedemptionResponse(): QueryRedemptionResponse {
  return { redemption: undefined };
}

export const QueryRedemptionResponse = {
  encode(message: QueryRedemptionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.redemption !== undefined) {
      Redemption.encode(message.redemption, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRedemptionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRedemptionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.redemption = Redemption.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRedemptionResponse {
    return { redemption: isSet(object.redemption) ? Redemption.fromJSON(object.redemption) : undefined };
  },

  toJSON(message: QueryRedemptionResponse): unknown {
    const obj: any = {};
    if (message.redemption !== undefined) {
      obj.redemption = Redemption.toJSON(message.redemption);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRedemptionResponse>): QueryRedemptionResponse {
    return QueryRedemptionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRedemptionResponse>): QueryRedemptionResponse {
    const message = createBaseQueryRedemptionResponse();
    message.redemption = (object.redemption !== undefined && object.redemption !== null)
      ? Redemption.fromPartial(object.redemption)
      : undefined;
    return message;
  },
};

/** Query defines the custody query service. */
export interface Query {
  /** Params returns the module parameters. */
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse>;
  /** Assets lists everything this chain issues claims on. */
  Assets(request: QueryAssetsRequest): Promise<QueryAssetsResponse>;
  /**
   * Solvency answers "are we holding what we owe", per asset.
   *
   * Deliberately public and deliberately computed rather than reported: if the
   * number cannot be checked by anyone, the whole arrangement rests on trusting
   * the operator, and this is a chain whose purpose is not having to.
   */
  Solvency(request: QuerySolvencyRequest): Promise<QuerySolvencyResponse>;
  /** Deposit returns one deposit by id. */
  Deposit(request: QueryDepositRequest): Promise<QueryDepositResponse>;
  /** Redemption returns one redemption by id. */
  Redemption(request: QueryRedemptionRequest): Promise<QueryRedemptionResponse>;
}

export const QueryServiceName = "blockchain.custody.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Params = this.Params.bind(this);
    this.Assets = this.Assets.bind(this);
    this.Solvency = this.Solvency.bind(this);
    this.Deposit = this.Deposit.bind(this);
    this.Redemption = this.Redemption.bind(this);
  }
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse> {
    const data = QueryParamsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Params", data);
    return promise.then((data) => QueryParamsResponse.decode(_m0.Reader.create(data)));
  }

  Assets(request: QueryAssetsRequest): Promise<QueryAssetsResponse> {
    const data = QueryAssetsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Assets", data);
    return promise.then((data) => QueryAssetsResponse.decode(_m0.Reader.create(data)));
  }

  Solvency(request: QuerySolvencyRequest): Promise<QuerySolvencyResponse> {
    const data = QuerySolvencyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Solvency", data);
    return promise.then((data) => QuerySolvencyResponse.decode(_m0.Reader.create(data)));
  }

  Deposit(request: QueryDepositRequest): Promise<QueryDepositResponse> {
    const data = QueryDepositRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Deposit", data);
    return promise.then((data) => QueryDepositResponse.decode(_m0.Reader.create(data)));
  }

  Redemption(request: QueryRedemptionRequest): Promise<QueryRedemptionResponse> {
    const data = QueryRedemptionRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Redemption", data);
    return promise.then((data) => QueryRedemptionResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
