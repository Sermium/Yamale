// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/custody/v1/params.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.custody.v1";

/** Params defines the parameters for the custody module. */
export interface Params {
  /**
   * attestation_threshold is how many appointed attestors must agree that an
   * external deposit happened before anything is minted.
   *
   * One attestor is one compromised key away from unlimited issuance, which is
   * the failure that has emptied every bridge that ever mattered. This is the
   * number that decides whether custody is a security model or a promise.
   */
  attestationThreshold: number;
  /**
   * redemption_delay_blocks is how long a redemption waits before it can be
   * paid out.
   *
   * Not friction for its own sake: it is the window in which an attestor
   * compromise or a mistaken mint can be caught before the asset leaves. Set to
   * zero it is an instant withdrawal, which is what an attacker wants.
   */
  redemptionDelayBlocks: string;
  /**
   * fee_bps is charged on issuance and redemption, in basis points.
   *
   * Fees are the revenue model on purpose. Every custodian failure in this
   * industry ran the same way: a reserve pushed into riskier assets to fund
   * operations. A custodian that earns from flow does not need to earn from the
   * float.
   */
  feeBps: number;
}

function createBaseParams(): Params {
  return { attestationThreshold: 0, redemptionDelayBlocks: "0", feeBps: 0 };
}

export const Params = {
  encode(message: Params, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.attestationThreshold !== 0) {
      writer.uint32(8).uint32(message.attestationThreshold);
    }
    if (message.redemptionDelayBlocks !== "0") {
      writer.uint32(16).uint64(message.redemptionDelayBlocks);
    }
    if (message.feeBps !== 0) {
      writer.uint32(24).uint32(message.feeBps);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Params {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.attestationThreshold = reader.uint32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.redemptionDelayBlocks = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.feeBps = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      attestationThreshold: isSet(object.attestationThreshold) ? globalThis.Number(object.attestationThreshold) : 0,
      redemptionDelayBlocks: isSet(object.redemptionDelayBlocks)
        ? globalThis.String(object.redemptionDelayBlocks)
        : "0",
      feeBps: isSet(object.feeBps) ? globalThis.Number(object.feeBps) : 0,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.attestationThreshold !== 0) {
      obj.attestationThreshold = Math.round(message.attestationThreshold);
    }
    if (message.redemptionDelayBlocks !== "0") {
      obj.redemptionDelayBlocks = message.redemptionDelayBlocks;
    }
    if (message.feeBps !== 0) {
      obj.feeBps = Math.round(message.feeBps);
    }
    return obj;
  },

  create(base?: DeepPartial<Params>): Params {
    return Params.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.attestationThreshold = object.attestationThreshold ?? 0;
    message.redemptionDelayBlocks = object.redemptionDelayBlocks ?? "0";
    message.feeBps = object.feeBps ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
