// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/custody/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Asset, Deposit, Redemption, Reserve } from "./custody.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.custody.v1";

/** GenesisState defines the custody module's genesis state. */
export interface GenesisState {
  params: Params | undefined;
  assets: Asset[];
  attestors: string[];
  deposits: Deposit[];
  redemptions: Redemption[];
  reserves: Reserve[];
  /**
   * next_deposit and next_redemption carry the sequences, so an exported chain
   * resumes numbering rather than reissuing ids that already exist.
   */
  nextDeposit: string;
  nextRedemption: string;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    assets: [],
    attestors: [],
    deposits: [],
    redemptions: [],
    reserves: [],
    nextDeposit: "0",
    nextRedemption: "0",
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.assets) {
      Asset.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.attestors) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.deposits) {
      Deposit.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.redemptions) {
      Redemption.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.reserves) {
      Reserve.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    if (message.nextDeposit !== "0") {
      writer.uint32(56).uint64(message.nextDeposit);
    }
    if (message.nextRedemption !== "0") {
      writer.uint32(64).uint64(message.nextRedemption);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.assets.push(Asset.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.attestors.push(reader.string());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.deposits.push(Deposit.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.redemptions.push(Redemption.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.reserves.push(Reserve.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.nextDeposit = longToString(reader.uint64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.nextRedemption = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      assets: globalThis.Array.isArray(object?.assets) ? object.assets.map((e: any) => Asset.fromJSON(e)) : [],
      attestors: globalThis.Array.isArray(object?.attestors)
        ? object.attestors.map((e: any) => globalThis.String(e))
        : [],
      deposits: globalThis.Array.isArray(object?.deposits) ? object.deposits.map((e: any) => Deposit.fromJSON(e)) : [],
      redemptions: globalThis.Array.isArray(object?.redemptions)
        ? object.redemptions.map((e: any) => Redemption.fromJSON(e))
        : [],
      reserves: globalThis.Array.isArray(object?.reserves) ? object.reserves.map((e: any) => Reserve.fromJSON(e)) : [],
      nextDeposit: isSet(object.nextDeposit) ? globalThis.String(object.nextDeposit) : "0",
      nextRedemption: isSet(object.nextRedemption) ? globalThis.String(object.nextRedemption) : "0",
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.assets?.length) {
      obj.assets = message.assets.map((e) => Asset.toJSON(e));
    }
    if (message.attestors?.length) {
      obj.attestors = message.attestors;
    }
    if (message.deposits?.length) {
      obj.deposits = message.deposits.map((e) => Deposit.toJSON(e));
    }
    if (message.redemptions?.length) {
      obj.redemptions = message.redemptions.map((e) => Redemption.toJSON(e));
    }
    if (message.reserves?.length) {
      obj.reserves = message.reserves.map((e) => Reserve.toJSON(e));
    }
    if (message.nextDeposit !== "0") {
      obj.nextDeposit = message.nextDeposit;
    }
    if (message.nextRedemption !== "0") {
      obj.nextRedemption = message.nextRedemption;
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.assets = object.assets?.map((e) => Asset.fromPartial(e)) || [];
    message.attestors = object.attestors?.map((e) => e) || [];
    message.deposits = object.deposits?.map((e) => Deposit.fromPartial(e)) || [];
    message.redemptions = object.redemptions?.map((e) => Redemption.fromPartial(e)) || [];
    message.reserves = object.reserves?.map((e) => Reserve.fromPartial(e)) || [];
    message.nextDeposit = object.nextDeposit ?? "0";
    message.nextRedemption = object.nextRedemption ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
