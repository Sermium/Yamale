// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/land/v1/parcel.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.land.v1";

/**
 * Status is where a parcel is in its life. A parcel that is not REGISTERED
 * cannot begin a transfer, which is what makes FROZEN and DISPUTED effective:
 * they are not warnings, they are stops.
 */
export const Status = {
  /**
   * STATUS_UNSPECIFIED - Never set on a stored parcel; the zero value only appears on a message
   * that forgot to set one, and the keeper rejects it.
   */
  STATUS_UNSPECIFIED: 0,
  /** STATUS_REGISTERED - Held by exactly one account, and movable. */
  STATUS_REGISTERED: 1,
  /** STATUS_TRANSFER_PENDING - A transfer is under way. No second transfer may start. */
  STATUS_TRANSFER_PENDING: 2,
  /**
   * STATUS_DISPUTED - Somebody objected. Terminal for the transfer, and an entry point for a
   * court. The chain preserves the evidence and does not adjudicate.
   */
  STATUS_DISPUTED: 3,
  /** STATUS_FROZEN - Stopped by an authority plus quorum — a court order, a fraud inquiry. */
  STATUS_FROZEN: 4,
  UNRECOGNIZED: -1,
} as const;

export type Status = typeof Status[keyof typeof Status];

export namespace Status {
  export type STATUS_UNSPECIFIED = typeof Status.STATUS_UNSPECIFIED;
  export type STATUS_REGISTERED = typeof Status.STATUS_REGISTERED;
  export type STATUS_TRANSFER_PENDING = typeof Status.STATUS_TRANSFER_PENDING;
  export type STATUS_DISPUTED = typeof Status.STATUS_DISPUTED;
  export type STATUS_FROZEN = typeof Status.STATUS_FROZEN;
  export type UNRECOGNIZED = typeof Status.UNRECOGNIZED;
}

export function statusFromJSON(object: any): Status {
  switch (object) {
    case 0:
    case "STATUS_UNSPECIFIED":
      return Status.STATUS_UNSPECIFIED;
    case 1:
    case "STATUS_REGISTERED":
      return Status.STATUS_REGISTERED;
    case 2:
    case "STATUS_TRANSFER_PENDING":
      return Status.STATUS_TRANSFER_PENDING;
    case 3:
    case "STATUS_DISPUTED":
      return Status.STATUS_DISPUTED;
    case 4:
    case "STATUS_FROZEN":
      return Status.STATUS_FROZEN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Status.UNRECOGNIZED;
  }
}

export function statusToJSON(object: Status): string {
  switch (object) {
    case Status.STATUS_UNSPECIFIED:
      return "STATUS_UNSPECIFIED";
    case Status.STATUS_REGISTERED:
      return "STATUS_REGISTERED";
    case Status.STATUS_TRANSFER_PENDING:
      return "STATUS_TRANSFER_PENDING";
    case Status.STATUS_DISPUTED:
      return "STATUS_DISPUTED";
    case Status.STATUS_FROZEN:
      return "STATUS_FROZEN";
    case Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Parcel is a piece of ground that can be owned exactly once. */
export interface Parcel {
  /** Assigned by the chain. Never reused, never reassigned. */
  id: string;
  /**
   * Hash of the surveyed boundary (GeoJSON or the cadastral document). The
   * survey itself is too large for a block and often too sensitive to publish;
   * the hash proves which survey this title refers to without disclosing it.
   *
   * This field is the uniqueness constraint. The keeper indexes it, and
   * registering a parcel whose hash already exists fails — that refusal is the
   * whole "cannot be owned twice" guarantee.
   */
  geometryHash: string;
  /**
   * The registry's own human reference, so this record can be reconciled with
   * the paper world it has to coexist with for a very long time.
   */
  cadastralRef: string;
  /**
   * Exactly one account. Deliberately not a list: co-ownership is a legal
   * arrangement between people, expressed by the account being a group account,
   * not by the chain holding several owners and having to rank them.
   */
  holder: string;
  /** The registry office whose jurisdiction this parcel falls in. */
  authority: string;
  status: Status;
  /**
   * Mortgages, liens, rights of way. Recorded because a title shown without its
   * encumbrances is a lie that gets somebody's house taken.
   */
  encumbrances: Encumbrance[];
  /**
   * Block height of first registration. The audit trail starts here, including
   * for the initial seeding of the registry, which is a political act and
   * should be as auditable as everything after it.
   */
  registeredAt: string;
  /**
   * The deed documents themselves, as metadata on the token.
   *
   * A parcel is an NFT: one indivisible token, one holder, and everything a
   * reader needs to understand the title travelling with it. Deeds are listed
   * rather than embedded — a scan of a 1974 grant is megabytes and often
   * contains personal data, so the chain carries its hash and a pointer, and
   * the registry serves the document itself.
   */
  deeds: Deed[];
  /**
   * What may lawfully be done with this land, set by the registry.
   *
   * These are the reason the registry stays in the loop after issuance. A
   * parcel can be fractionalised later — an exploitation right sold in shares,
   * rents flowing to holders — but only within these limits, and the land
   * service must be able to see and stop a breach. Without them, tokenisation
   * becomes a way to sell around the law.
   */
  restrictions: Restriction[];
  /**
   * Set when a tokenisation vehicle has been opened over this parcel. The
   * parcel itself never moves into the vehicle: the title stays here, held by
   * the same account, and the vehicle sells rights *referencing* it. That
   * separation is what keeps a fractionalised parcel still governed by the
   * registry rather than by whoever accumulated the tokens.
   */
  vehicleId: string;
  /**
   * Every freeze ever imposed on this parcel, and the grounds given for it.
   *
   * The keeper refuses a freeze without a reason, so recording the status
   * without the reason threw away the only part a court order actually
   * consists of. A holder shown "your land is frozen" with no readable grounds
   * cannot tell an inquiry from an extortion, and cannot argue with either —
   * which is precisely the unaccountable act this module is written against.
   *
   * Lifting marks the entry rather than removing it, like a released
   * encumbrance and a lifted restriction: a freeze that vanishes takes with it
   * the evidence that an office ever stopped this land, and who did it.
   */
  freezes: Freeze[];
}

/**
 * Freeze is one order stopping every dealing with a parcel, and the grounds
 * the office gave for it.
 *
 * Kept as a list on the parcel rather than as a single current entry, because
 * a parcel frozen, released and frozen again by a different office is the exact
 * history somebody contesting the second freeze needs to be able to show.
 */
export interface Freeze {
  /**
   * Why the land was stopped: the court order, the fraud inquiry, the
   * succession dispute. Required by the keeper, and stored because a stop
   * whose grounds cannot be read is indistinguishable from an arbitrary one.
   */
  reason: string;
  /** The office that imposed it, so it can be argued with. */
  imposedBy: string;
  imposedAt: string;
  lifted: boolean;
  /**
   * The office that lifted it and the grounds it gave, recorded for the same
   * reason as the freeze itself: releasing land is as consequential a decision
   * as stopping it, and an unattributed release is one nobody can question.
   */
  liftedBy: string;
  liftReason: string;
  liftedAt: string;
}

/** Deed is one document in the chain of title. */
export interface Deed {
  /** What it is: grant, sale, inheritance, court order, survey. */
  kind: string;
  /**
   * Hash of the document the registry holds. Proves which paper this refers to
   * without publishing a scan that may carry somebody's personal details.
   */
  documentHash: string;
  /** Where the registry serves it from, for anyone entitled to read it. */
  uri: string;
  /** The registry's reference for the document, and its date in the paper world. */
  reference: string;
  issuedOn: string;
  recordedAt: string;
}

/**
 * Restriction is a limit on what may be done with a parcel.
 *
 * Kept as data rather than code because the limits differ by country and by
 * parcel, and a chain that hard-codes one country's land law is a chain only
 * that country can use.
 */
export interface Restriction {
  /**
   * agricultural_use_only, no_fractionalisation, foreign_ownership_capped,
   * heritage_protected, minimum_parcel_size, customary_tenure.
   */
  kind: string;
  /** The limit itself, where one applies — a percentage cap, a minimum size. */
  value: string;
  detail: string;
  /** The office that imposed it, so it can be argued with. */
  imposedBy: string;
  imposedAt: string;
  lifted: boolean;
}

/**
 * FractionalisationAuthority is a registry office's standing permission for a
 * tokenisation vehicle to be opened over one parcel.
 *
 * It is a record of its own rather than a flag on the parcel because
 * x/tokenisation has to be able to answer "may this be issued, and how much of
 * it" at every issuance, long after the grant. A permission that existed only
 * as a message in a past block is a permission nothing can enforce, which is
 * how the ceiling and the expiry come to be decorative.
 *
 * One per parcel: granting again replaces the terms rather than accumulating
 * permissions, because a parcel carrying two ceilings has the higher one.
 */
export interface FractionalisationAuthority {
  parcelId: string;
  /**
   * What may be sold: an exploitation right, a lease, a revenue share. Not the
   * title — the title is not for sale in pieces and never leaves this module.
   */
  right: string;
  /**
   * Ceiling on the fraction that may be issued, in basis points. A holder
   * permitted to sell 40% of the rent has 4000 here and keeps the other 6000.
   */
  maxShareBps: number;
  /**
   * Unix seconds. Past this the authorisation is not live and x/tokenisation
   * refuses to issue against it, so a permission granted for one purpose does
   * not sit open for years.
   */
  expiresAt: string;
  /** The office that granted it, so the decision can be argued with. */
  grantedBy: string;
  grantedAt: string;
  /**
   * Withdrawn rather than deleted, for the same reason a lifted restriction is
   * marked rather than removed: a permission that vanishes takes with it the
   * evidence that the registry ever gave it, and who gave it. Withdrawal stops
   * new issuance and expropriates nobody.
   */
  withdrawn: boolean;
  withdrawnAt: string;
}

/** Encumbrance is a claim against a parcel that is not ownership. */
export interface Encumbrance {
  /** mortgage, lien, right-of-way, caveat */
  kind: string;
  holder: string;
  detail: string;
  recordedAt: string;
  /**
   * Released rather than deleted: an encumbrance that vanishes from the record
   * takes with it the evidence that it ever constrained the title.
   */
  released: boolean;
}

/** Authority is a registry office permitted to register and validate. */
export interface Authority {
  address: string;
  name: string;
  /**
   * Where this office may act. A parcel is validated by the office whose
   * jurisdiction it falls in, and attested by offices elsewhere.
   */
  jurisdiction: string;
  active: boolean;
}

/**
 * Transfer is a proposed change of holder, and the record of who agreed to it.
 *
 * It is kept after completion, not deleted. The history of who signed what and
 * when is the receipt that a dispossessed owner currently does not have, and
 * deleting it to save space would remove the only thing that makes the theft
 * arguable afterwards.
 */
export interface Transfer {
  id: string;
  parcelId: string;
  from: string;
  to: string;
  /**
   * Declared consideration. Recorded for the audit trail; the chain does not
   * move it. Land paid for off-chain is the common case and pretending
   * otherwise would make the record false.
   */
  price: string;
  /** Set when the jurisdiction's own office validates. */
  validated: boolean;
  validatedBy: string;
  /**
   * Independent registrars who have attested, one entry each. The keeper
   * refuses an attestation from the proposing authority: an attestor from the
   * same office is not independent, and allowing it collapses the whole
   * mechanism back to a single bribe.
   */
  attestors: string[];
  /**
   * When quorum was reached. The challenge window runs from here, not from
   * proposal, so the public clock starts only once the transfer is real.
   */
  quorumAt: string;
  /** First objection, if any. One is enough to stop everything. */
  objectedBy: string;
  objectionReason: string;
  proposedAt: string;
  completedAt: string;
}

function createBaseParcel(): Parcel {
  return {
    id: "0",
    geometryHash: "",
    cadastralRef: "",
    holder: "",
    authority: "",
    status: 0,
    encumbrances: [],
    registeredAt: "0",
    deeds: [],
    restrictions: [],
    vehicleId: "0",
    freezes: [],
  };
}

export const Parcel = {
  encode(message: Parcel, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.geometryHash !== "") {
      writer.uint32(18).string(message.geometryHash);
    }
    if (message.cadastralRef !== "") {
      writer.uint32(26).string(message.cadastralRef);
    }
    if (message.holder !== "") {
      writer.uint32(34).string(message.holder);
    }
    if (message.authority !== "") {
      writer.uint32(42).string(message.authority);
    }
    if (message.status !== 0) {
      writer.uint32(48).int32(message.status);
    }
    for (const v of message.encumbrances) {
      Encumbrance.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    if (message.registeredAt !== "0") {
      writer.uint32(64).int64(message.registeredAt);
    }
    for (const v of message.deeds) {
      Deed.encode(v!, writer.uint32(74).fork()).ldelim();
    }
    for (const v of message.restrictions) {
      Restriction.encode(v!, writer.uint32(82).fork()).ldelim();
    }
    if (message.vehicleId !== "0") {
      writer.uint32(88).uint64(message.vehicleId);
    }
    for (const v of message.freezes) {
      Freeze.encode(v!, writer.uint32(98).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Parcel {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParcel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.geometryHash = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.cadastralRef = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.holder = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.encumbrances.push(Encumbrance.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.registeredAt = longToString(reader.int64() as Long);
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.deeds.push(Deed.decode(reader, reader.uint32()));
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.restrictions.push(Restriction.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.vehicleId = longToString(reader.uint64() as Long);
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.freezes.push(Freeze.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Parcel {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      geometryHash: isSet(object.geometryHash) ? globalThis.String(object.geometryHash) : "",
      cadastralRef: isSet(object.cadastralRef) ? globalThis.String(object.cadastralRef) : "",
      holder: isSet(object.holder) ? globalThis.String(object.holder) : "",
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      status: isSet(object.status) ? statusFromJSON(object.status) : 0,
      encumbrances: globalThis.Array.isArray(object?.encumbrances)
        ? object.encumbrances.map((e: any) => Encumbrance.fromJSON(e))
        : [],
      registeredAt: isSet(object.registeredAt) ? globalThis.String(object.registeredAt) : "0",
      deeds: globalThis.Array.isArray(object?.deeds) ? object.deeds.map((e: any) => Deed.fromJSON(e)) : [],
      restrictions: globalThis.Array.isArray(object?.restrictions)
        ? object.restrictions.map((e: any) => Restriction.fromJSON(e))
        : [],
      vehicleId: isSet(object.vehicleId) ? globalThis.String(object.vehicleId) : "0",
      freezes: globalThis.Array.isArray(object?.freezes) ? object.freezes.map((e: any) => Freeze.fromJSON(e)) : [],
    };
  },

  toJSON(message: Parcel): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.geometryHash !== "") {
      obj.geometryHash = message.geometryHash;
    }
    if (message.cadastralRef !== "") {
      obj.cadastralRef = message.cadastralRef;
    }
    if (message.holder !== "") {
      obj.holder = message.holder;
    }
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.status !== 0) {
      obj.status = statusToJSON(message.status);
    }
    if (message.encumbrances?.length) {
      obj.encumbrances = message.encumbrances.map((e) => Encumbrance.toJSON(e));
    }
    if (message.registeredAt !== "0") {
      obj.registeredAt = message.registeredAt;
    }
    if (message.deeds?.length) {
      obj.deeds = message.deeds.map((e) => Deed.toJSON(e));
    }
    if (message.restrictions?.length) {
      obj.restrictions = message.restrictions.map((e) => Restriction.toJSON(e));
    }
    if (message.vehicleId !== "0") {
      obj.vehicleId = message.vehicleId;
    }
    if (message.freezes?.length) {
      obj.freezes = message.freezes.map((e) => Freeze.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Parcel>): Parcel {
    return Parcel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Parcel>): Parcel {
    const message = createBaseParcel();
    message.id = object.id ?? "0";
    message.geometryHash = object.geometryHash ?? "";
    message.cadastralRef = object.cadastralRef ?? "";
    message.holder = object.holder ?? "";
    message.authority = object.authority ?? "";
    message.status = object.status ?? 0;
    message.encumbrances = object.encumbrances?.map((e) => Encumbrance.fromPartial(e)) || [];
    message.registeredAt = object.registeredAt ?? "0";
    message.deeds = object.deeds?.map((e) => Deed.fromPartial(e)) || [];
    message.restrictions = object.restrictions?.map((e) => Restriction.fromPartial(e)) || [];
    message.vehicleId = object.vehicleId ?? "0";
    message.freezes = object.freezes?.map((e) => Freeze.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFreeze(): Freeze {
  return { reason: "", imposedBy: "", imposedAt: "0", lifted: false, liftedBy: "", liftReason: "", liftedAt: "0" };
}

export const Freeze = {
  encode(message: Freeze, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    if (message.imposedBy !== "") {
      writer.uint32(18).string(message.imposedBy);
    }
    if (message.imposedAt !== "0") {
      writer.uint32(24).int64(message.imposedAt);
    }
    if (message.lifted !== false) {
      writer.uint32(32).bool(message.lifted);
    }
    if (message.liftedBy !== "") {
      writer.uint32(42).string(message.liftedBy);
    }
    if (message.liftReason !== "") {
      writer.uint32(50).string(message.liftReason);
    }
    if (message.liftedAt !== "0") {
      writer.uint32(56).int64(message.liftedAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Freeze {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFreeze();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.imposedBy = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.imposedAt = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.lifted = reader.bool();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.liftedBy = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.liftReason = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.liftedAt = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Freeze {
    return {
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      imposedBy: isSet(object.imposedBy) ? globalThis.String(object.imposedBy) : "",
      imposedAt: isSet(object.imposedAt) ? globalThis.String(object.imposedAt) : "0",
      lifted: isSet(object.lifted) ? globalThis.Boolean(object.lifted) : false,
      liftedBy: isSet(object.liftedBy) ? globalThis.String(object.liftedBy) : "",
      liftReason: isSet(object.liftReason) ? globalThis.String(object.liftReason) : "",
      liftedAt: isSet(object.liftedAt) ? globalThis.String(object.liftedAt) : "0",
    };
  },

  toJSON(message: Freeze): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.imposedBy !== "") {
      obj.imposedBy = message.imposedBy;
    }
    if (message.imposedAt !== "0") {
      obj.imposedAt = message.imposedAt;
    }
    if (message.lifted !== false) {
      obj.lifted = message.lifted;
    }
    if (message.liftedBy !== "") {
      obj.liftedBy = message.liftedBy;
    }
    if (message.liftReason !== "") {
      obj.liftReason = message.liftReason;
    }
    if (message.liftedAt !== "0") {
      obj.liftedAt = message.liftedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<Freeze>): Freeze {
    return Freeze.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Freeze>): Freeze {
    const message = createBaseFreeze();
    message.reason = object.reason ?? "";
    message.imposedBy = object.imposedBy ?? "";
    message.imposedAt = object.imposedAt ?? "0";
    message.lifted = object.lifted ?? false;
    message.liftedBy = object.liftedBy ?? "";
    message.liftReason = object.liftReason ?? "";
    message.liftedAt = object.liftedAt ?? "0";
    return message;
  },
};

function createBaseDeed(): Deed {
  return { kind: "", documentHash: "", uri: "", reference: "", issuedOn: "", recordedAt: "0" };
}

export const Deed = {
  encode(message: Deed, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.kind !== "") {
      writer.uint32(10).string(message.kind);
    }
    if (message.documentHash !== "") {
      writer.uint32(18).string(message.documentHash);
    }
    if (message.uri !== "") {
      writer.uint32(26).string(message.uri);
    }
    if (message.reference !== "") {
      writer.uint32(34).string(message.reference);
    }
    if (message.issuedOn !== "") {
      writer.uint32(42).string(message.issuedOn);
    }
    if (message.recordedAt !== "0") {
      writer.uint32(48).int64(message.recordedAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Deed {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.kind = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.documentHash = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.uri = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.reference = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.issuedOn = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.recordedAt = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Deed {
    return {
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      documentHash: isSet(object.documentHash) ? globalThis.String(object.documentHash) : "",
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      reference: isSet(object.reference) ? globalThis.String(object.reference) : "",
      issuedOn: isSet(object.issuedOn) ? globalThis.String(object.issuedOn) : "",
      recordedAt: isSet(object.recordedAt) ? globalThis.String(object.recordedAt) : "0",
    };
  },

  toJSON(message: Deed): unknown {
    const obj: any = {};
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.documentHash !== "") {
      obj.documentHash = message.documentHash;
    }
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.reference !== "") {
      obj.reference = message.reference;
    }
    if (message.issuedOn !== "") {
      obj.issuedOn = message.issuedOn;
    }
    if (message.recordedAt !== "0") {
      obj.recordedAt = message.recordedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<Deed>): Deed {
    return Deed.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Deed>): Deed {
    const message = createBaseDeed();
    message.kind = object.kind ?? "";
    message.documentHash = object.documentHash ?? "";
    message.uri = object.uri ?? "";
    message.reference = object.reference ?? "";
    message.issuedOn = object.issuedOn ?? "";
    message.recordedAt = object.recordedAt ?? "0";
    return message;
  },
};

function createBaseRestriction(): Restriction {
  return { kind: "", value: "", detail: "", imposedBy: "", imposedAt: "0", lifted: false };
}

export const Restriction = {
  encode(message: Restriction, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.kind !== "") {
      writer.uint32(10).string(message.kind);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    if (message.detail !== "") {
      writer.uint32(26).string(message.detail);
    }
    if (message.imposedBy !== "") {
      writer.uint32(34).string(message.imposedBy);
    }
    if (message.imposedAt !== "0") {
      writer.uint32(40).int64(message.imposedAt);
    }
    if (message.lifted !== false) {
      writer.uint32(48).bool(message.lifted);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Restriction {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestriction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.kind = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.detail = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.imposedBy = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.imposedAt = longToString(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.lifted = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Restriction {
    return {
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
      imposedBy: isSet(object.imposedBy) ? globalThis.String(object.imposedBy) : "",
      imposedAt: isSet(object.imposedAt) ? globalThis.String(object.imposedAt) : "0",
      lifted: isSet(object.lifted) ? globalThis.Boolean(object.lifted) : false,
    };
  },

  toJSON(message: Restriction): unknown {
    const obj: any = {};
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    if (message.imposedBy !== "") {
      obj.imposedBy = message.imposedBy;
    }
    if (message.imposedAt !== "0") {
      obj.imposedAt = message.imposedAt;
    }
    if (message.lifted !== false) {
      obj.lifted = message.lifted;
    }
    return obj;
  },

  create(base?: DeepPartial<Restriction>): Restriction {
    return Restriction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Restriction>): Restriction {
    const message = createBaseRestriction();
    message.kind = object.kind ?? "";
    message.value = object.value ?? "";
    message.detail = object.detail ?? "";
    message.imposedBy = object.imposedBy ?? "";
    message.imposedAt = object.imposedAt ?? "0";
    message.lifted = object.lifted ?? false;
    return message;
  },
};

function createBaseFractionalisationAuthority(): FractionalisationAuthority {
  return {
    parcelId: "0",
    right: "",
    maxShareBps: 0,
    expiresAt: "0",
    grantedBy: "",
    grantedAt: "0",
    withdrawn: false,
    withdrawnAt: "0",
  };
}

export const FractionalisationAuthority = {
  encode(message: FractionalisationAuthority, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.parcelId !== "0") {
      writer.uint32(8).uint64(message.parcelId);
    }
    if (message.right !== "") {
      writer.uint32(18).string(message.right);
    }
    if (message.maxShareBps !== 0) {
      writer.uint32(24).uint32(message.maxShareBps);
    }
    if (message.expiresAt !== "0") {
      writer.uint32(32).int64(message.expiresAt);
    }
    if (message.grantedBy !== "") {
      writer.uint32(42).string(message.grantedBy);
    }
    if (message.grantedAt !== "0") {
      writer.uint32(48).int64(message.grantedAt);
    }
    if (message.withdrawn !== false) {
      writer.uint32(56).bool(message.withdrawn);
    }
    if (message.withdrawnAt !== "0") {
      writer.uint32(64).int64(message.withdrawnAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FractionalisationAuthority {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFractionalisationAuthority();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.parcelId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.right = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.maxShareBps = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.expiresAt = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.grantedBy = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.grantedAt = longToString(reader.int64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.withdrawn = reader.bool();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.withdrawnAt = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FractionalisationAuthority {
    return {
      parcelId: isSet(object.parcelId) ? globalThis.String(object.parcelId) : "0",
      right: isSet(object.right) ? globalThis.String(object.right) : "",
      maxShareBps: isSet(object.maxShareBps) ? globalThis.Number(object.maxShareBps) : 0,
      expiresAt: isSet(object.expiresAt) ? globalThis.String(object.expiresAt) : "0",
      grantedBy: isSet(object.grantedBy) ? globalThis.String(object.grantedBy) : "",
      grantedAt: isSet(object.grantedAt) ? globalThis.String(object.grantedAt) : "0",
      withdrawn: isSet(object.withdrawn) ? globalThis.Boolean(object.withdrawn) : false,
      withdrawnAt: isSet(object.withdrawnAt) ? globalThis.String(object.withdrawnAt) : "0",
    };
  },

  toJSON(message: FractionalisationAuthority): unknown {
    const obj: any = {};
    if (message.parcelId !== "0") {
      obj.parcelId = message.parcelId;
    }
    if (message.right !== "") {
      obj.right = message.right;
    }
    if (message.maxShareBps !== 0) {
      obj.maxShareBps = Math.round(message.maxShareBps);
    }
    if (message.expiresAt !== "0") {
      obj.expiresAt = message.expiresAt;
    }
    if (message.grantedBy !== "") {
      obj.grantedBy = message.grantedBy;
    }
    if (message.grantedAt !== "0") {
      obj.grantedAt = message.grantedAt;
    }
    if (message.withdrawn !== false) {
      obj.withdrawn = message.withdrawn;
    }
    if (message.withdrawnAt !== "0") {
      obj.withdrawnAt = message.withdrawnAt;
    }
    return obj;
  },

  create(base?: DeepPartial<FractionalisationAuthority>): FractionalisationAuthority {
    return FractionalisationAuthority.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FractionalisationAuthority>): FractionalisationAuthority {
    const message = createBaseFractionalisationAuthority();
    message.parcelId = object.parcelId ?? "0";
    message.right = object.right ?? "";
    message.maxShareBps = object.maxShareBps ?? 0;
    message.expiresAt = object.expiresAt ?? "0";
    message.grantedBy = object.grantedBy ?? "";
    message.grantedAt = object.grantedAt ?? "0";
    message.withdrawn = object.withdrawn ?? false;
    message.withdrawnAt = object.withdrawnAt ?? "0";
    return message;
  },
};

function createBaseEncumbrance(): Encumbrance {
  return { kind: "", holder: "", detail: "", recordedAt: "0", released: false };
}

export const Encumbrance = {
  encode(message: Encumbrance, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.kind !== "") {
      writer.uint32(10).string(message.kind);
    }
    if (message.holder !== "") {
      writer.uint32(18).string(message.holder);
    }
    if (message.detail !== "") {
      writer.uint32(26).string(message.detail);
    }
    if (message.recordedAt !== "0") {
      writer.uint32(32).int64(message.recordedAt);
    }
    if (message.released !== false) {
      writer.uint32(40).bool(message.released);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Encumbrance {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEncumbrance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.kind = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.holder = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.detail = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.recordedAt = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.released = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Encumbrance {
    return {
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      holder: isSet(object.holder) ? globalThis.String(object.holder) : "",
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
      recordedAt: isSet(object.recordedAt) ? globalThis.String(object.recordedAt) : "0",
      released: isSet(object.released) ? globalThis.Boolean(object.released) : false,
    };
  },

  toJSON(message: Encumbrance): unknown {
    const obj: any = {};
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.holder !== "") {
      obj.holder = message.holder;
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    if (message.recordedAt !== "0") {
      obj.recordedAt = message.recordedAt;
    }
    if (message.released !== false) {
      obj.released = message.released;
    }
    return obj;
  },

  create(base?: DeepPartial<Encumbrance>): Encumbrance {
    return Encumbrance.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Encumbrance>): Encumbrance {
    const message = createBaseEncumbrance();
    message.kind = object.kind ?? "";
    message.holder = object.holder ?? "";
    message.detail = object.detail ?? "";
    message.recordedAt = object.recordedAt ?? "0";
    message.released = object.released ?? false;
    return message;
  },
};

function createBaseAuthority(): Authority {
  return { address: "", name: "", jurisdiction: "", active: false };
}

export const Authority = {
  encode(message: Authority, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.jurisdiction !== "") {
      writer.uint32(26).string(message.jurisdiction);
    }
    if (message.active !== false) {
      writer.uint32(32).bool(message.active);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Authority {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthority();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.jurisdiction = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.active = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Authority {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      jurisdiction: isSet(object.jurisdiction) ? globalThis.String(object.jurisdiction) : "",
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
    };
  },

  toJSON(message: Authority): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.jurisdiction !== "") {
      obj.jurisdiction = message.jurisdiction;
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    return obj;
  },

  create(base?: DeepPartial<Authority>): Authority {
    return Authority.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Authority>): Authority {
    const message = createBaseAuthority();
    message.address = object.address ?? "";
    message.name = object.name ?? "";
    message.jurisdiction = object.jurisdiction ?? "";
    message.active = object.active ?? false;
    return message;
  },
};

function createBaseTransfer(): Transfer {
  return {
    id: "0",
    parcelId: "0",
    from: "",
    to: "",
    price: "",
    validated: false,
    validatedBy: "",
    attestors: [],
    quorumAt: "0",
    objectedBy: "",
    objectionReason: "",
    proposedAt: "0",
    completedAt: "0",
  };
}

export const Transfer = {
  encode(message: Transfer, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.parcelId !== "0") {
      writer.uint32(16).uint64(message.parcelId);
    }
    if (message.from !== "") {
      writer.uint32(26).string(message.from);
    }
    if (message.to !== "") {
      writer.uint32(34).string(message.to);
    }
    if (message.price !== "") {
      writer.uint32(42).string(message.price);
    }
    if (message.validated !== false) {
      writer.uint32(48).bool(message.validated);
    }
    if (message.validatedBy !== "") {
      writer.uint32(58).string(message.validatedBy);
    }
    for (const v of message.attestors) {
      writer.uint32(66).string(v!);
    }
    if (message.quorumAt !== "0") {
      writer.uint32(72).int64(message.quorumAt);
    }
    if (message.objectedBy !== "") {
      writer.uint32(82).string(message.objectedBy);
    }
    if (message.objectionReason !== "") {
      writer.uint32(90).string(message.objectionReason);
    }
    if (message.proposedAt !== "0") {
      writer.uint32(96).int64(message.proposedAt);
    }
    if (message.completedAt !== "0") {
      writer.uint32(104).int64(message.completedAt);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Transfer {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.parcelId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.from = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.to = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.price = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.validated = reader.bool();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.validatedBy = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.attestors.push(reader.string());
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.quorumAt = longToString(reader.int64() as Long);
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.objectedBy = reader.string();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.objectionReason = reader.string();
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.proposedAt = longToString(reader.int64() as Long);
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.completedAt = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Transfer {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      parcelId: isSet(object.parcelId) ? globalThis.String(object.parcelId) : "0",
      from: isSet(object.from) ? globalThis.String(object.from) : "",
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      validated: isSet(object.validated) ? globalThis.Boolean(object.validated) : false,
      validatedBy: isSet(object.validatedBy) ? globalThis.String(object.validatedBy) : "",
      attestors: globalThis.Array.isArray(object?.attestors)
        ? object.attestors.map((e: any) => globalThis.String(e))
        : [],
      quorumAt: isSet(object.quorumAt) ? globalThis.String(object.quorumAt) : "0",
      objectedBy: isSet(object.objectedBy) ? globalThis.String(object.objectedBy) : "",
      objectionReason: isSet(object.objectionReason) ? globalThis.String(object.objectionReason) : "",
      proposedAt: isSet(object.proposedAt) ? globalThis.String(object.proposedAt) : "0",
      completedAt: isSet(object.completedAt) ? globalThis.String(object.completedAt) : "0",
    };
  },

  toJSON(message: Transfer): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.parcelId !== "0") {
      obj.parcelId = message.parcelId;
    }
    if (message.from !== "") {
      obj.from = message.from;
    }
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.validated !== false) {
      obj.validated = message.validated;
    }
    if (message.validatedBy !== "") {
      obj.validatedBy = message.validatedBy;
    }
    if (message.attestors?.length) {
      obj.attestors = message.attestors;
    }
    if (message.quorumAt !== "0") {
      obj.quorumAt = message.quorumAt;
    }
    if (message.objectedBy !== "") {
      obj.objectedBy = message.objectedBy;
    }
    if (message.objectionReason !== "") {
      obj.objectionReason = message.objectionReason;
    }
    if (message.proposedAt !== "0") {
      obj.proposedAt = message.proposedAt;
    }
    if (message.completedAt !== "0") {
      obj.completedAt = message.completedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<Transfer>): Transfer {
    return Transfer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Transfer>): Transfer {
    const message = createBaseTransfer();
    message.id = object.id ?? "0";
    message.parcelId = object.parcelId ?? "0";
    message.from = object.from ?? "";
    message.to = object.to ?? "";
    message.price = object.price ?? "";
    message.validated = object.validated ?? false;
    message.validatedBy = object.validatedBy ?? "";
    message.attestors = object.attestors?.map((e) => e) || [];
    message.quorumAt = object.quorumAt ?? "0";
    message.objectedBy = object.objectedBy ?? "";
    message.objectionReason = object.objectionReason ?? "";
    message.proposedAt = object.proposedAt ?? "0";
    message.completedAt = object.completedAt ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
