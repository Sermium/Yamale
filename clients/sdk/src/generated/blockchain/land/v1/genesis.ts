// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/land/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Params } from "./params.ts";
import { Authority, FractionalisationAuthority, Parcel, Transfer } from "./parcel.ts";

export const protobufPackage = "blockchain.land.v1";

/**
 * GenesisState carries the whole registry across an export and back.
 *
 * The counters are exported explicitly rather than derived from the longest id
 * present. A derived counter changes behaviour after an export whenever the
 * highest-numbered record has been removed, and on this chain the exported
 * state must match byte for byte or the import is not the same registry.
 */
export interface GenesisState {
  params: Params | undefined;
  authorities: Authority[];
  parcels: Parcel[];
  transfers: Transfer[];
  nextParcelId: string;
  nextTransferId: string;
  /**
   * Fractionalisation permissions, including the withdrawn and the expired.
   *
   * Carried rather than dropped because x/tokenisation enforces against them:
   * an export that kept the parcels but lost their authorisations would import
   * a registry in which every vehicle already opened over land had lost the
   * permission that made it lawful.
   */
  fractionalisationAuthorities: FractionalisationAuthority[];
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    authorities: [],
    parcels: [],
    transfers: [],
    nextParcelId: "0",
    nextTransferId: "0",
    fractionalisationAuthorities: [],
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.authorities) {
      Authority.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.parcels) {
      Parcel.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.transfers) {
      Transfer.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message.nextParcelId !== "0") {
      writer.uint32(40).uint64(message.nextParcelId);
    }
    if (message.nextTransferId !== "0") {
      writer.uint32(48).uint64(message.nextTransferId);
    }
    for (const v of message.fractionalisationAuthorities) {
      FractionalisationAuthority.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.authorities.push(Authority.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.parcels.push(Parcel.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.transfers.push(Transfer.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.nextParcelId = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.nextTransferId = longToString(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.fractionalisationAuthorities.push(FractionalisationAuthority.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      authorities: globalThis.Array.isArray(object?.authorities)
        ? object.authorities.map((e: any) => Authority.fromJSON(e))
        : [],
      parcels: globalThis.Array.isArray(object?.parcels) ? object.parcels.map((e: any) => Parcel.fromJSON(e)) : [],
      transfers: globalThis.Array.isArray(object?.transfers)
        ? object.transfers.map((e: any) => Transfer.fromJSON(e))
        : [],
      nextParcelId: isSet(object.nextParcelId) ? globalThis.String(object.nextParcelId) : "0",
      nextTransferId: isSet(object.nextTransferId) ? globalThis.String(object.nextTransferId) : "0",
      fractionalisationAuthorities: globalThis.Array.isArray(object?.fractionalisationAuthorities)
        ? object.fractionalisationAuthorities.map((e: any) => FractionalisationAuthority.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.authorities?.length) {
      obj.authorities = message.authorities.map((e) => Authority.toJSON(e));
    }
    if (message.parcels?.length) {
      obj.parcels = message.parcels.map((e) => Parcel.toJSON(e));
    }
    if (message.transfers?.length) {
      obj.transfers = message.transfers.map((e) => Transfer.toJSON(e));
    }
    if (message.nextParcelId !== "0") {
      obj.nextParcelId = message.nextParcelId;
    }
    if (message.nextTransferId !== "0") {
      obj.nextTransferId = message.nextTransferId;
    }
    if (message.fractionalisationAuthorities?.length) {
      obj.fractionalisationAuthorities = message.fractionalisationAuthorities.map((e) =>
        FractionalisationAuthority.toJSON(e)
      );
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.authorities = object.authorities?.map((e) => Authority.fromPartial(e)) || [];
    message.parcels = object.parcels?.map((e) => Parcel.fromPartial(e)) || [];
    message.transfers = object.transfers?.map((e) => Transfer.fromPartial(e)) || [];
    message.nextParcelId = object.nextParcelId ?? "0";
    message.nextTransferId = object.nextTransferId ?? "0";
    message.fractionalisationAuthorities =
      object.fractionalisationAuthorities?.map((e) => FractionalisationAuthority.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
