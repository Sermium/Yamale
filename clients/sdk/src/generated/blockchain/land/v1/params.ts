// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/land/v1/params.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.land.v1";

/**
 * Params are the dials that decide how expensive it is to steal land here.
 * They belong to governance, because a chain where an official can lower the
 * quorum is a chain with no quorum.
 */
export interface Params {
  /**
   * Independent registrars who must attest before a transfer can complete.
   * Below three, two colluding officials suffice — the whole mechanism rests on
   * this being larger than the number of people a buyer can quietly reach.
   */
  attestationQuorum: number;
  /**
   * Seconds between reaching quorum and being allowed to complete. Long enough
   * for word to reach a family member in another city; short enough that
   * legitimate sales are not strangled. Fourteen days by default.
   */
  challengeWindow: string;
  /**
   * Whether a registrar from the proposing office may count toward quorum.
   * False, and it should stay false: an attestor from the same office is not
   * independent, and allowing it collapses the mechanism back to one bribe.
   */
  sameAuthorityAttestation: boolean;
}

function createBaseParams(): Params {
  return { attestationQuorum: 0, challengeWindow: "0", sameAuthorityAttestation: false };
}

export const Params = {
  encode(message: Params, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.attestationQuorum !== 0) {
      writer.uint32(8).uint32(message.attestationQuorum);
    }
    if (message.challengeWindow !== "0") {
      writer.uint32(16).int64(message.challengeWindow);
    }
    if (message.sameAuthorityAttestation !== false) {
      writer.uint32(24).bool(message.sameAuthorityAttestation);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Params {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.attestationQuorum = reader.uint32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.challengeWindow = longToString(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.sameAuthorityAttestation = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      attestationQuorum: isSet(object.attestationQuorum) ? globalThis.Number(object.attestationQuorum) : 0,
      challengeWindow: isSet(object.challengeWindow) ? globalThis.String(object.challengeWindow) : "0",
      sameAuthorityAttestation: isSet(object.sameAuthorityAttestation)
        ? globalThis.Boolean(object.sameAuthorityAttestation)
        : false,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.attestationQuorum !== 0) {
      obj.attestationQuorum = Math.round(message.attestationQuorum);
    }
    if (message.challengeWindow !== "0") {
      obj.challengeWindow = message.challengeWindow;
    }
    if (message.sameAuthorityAttestation !== false) {
      obj.sameAuthorityAttestation = message.sameAuthorityAttestation;
    }
    return obj;
  },

  create(base?: DeepPartial<Params>): Params {
    return Params.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.attestationQuorum = object.attestationQuorum ?? 0;
    message.challengeWindow = object.challengeWindow ?? "0";
    message.sameAuthorityAttestation = object.sameAuthorityAttestation ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
