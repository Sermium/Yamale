// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/enforcement/v1/query.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination.ts";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { Case, Freeze, Vote } from "./enforcement.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.enforcement.v1";

/** QueryParamsRequest is the request type for the Query/Params RPC method. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is the response type for the Query/Params RPC method. */
export interface QueryParamsResponse {
  /** params holds all the parameters of this module. */
  params: Params | undefined;
}

/** QueryGetCaseRequest asks for one case by id. */
export interface QueryGetCaseRequest {
  id: string;
}

/** QueryGetCaseResponse carries the case and every vote cast on it. */
export interface QueryGetCaseResponse {
  case: Case | undefined;
  votes: Vote[];
}

/** QueryListCaseRequest asks for every case, paginated. */
export interface QueryListCaseRequest {
  pagination: PageRequest | undefined;
}

/** QueryListCaseResponse carries a page of cases, oldest id first. */
export interface QueryListCaseResponse {
  case: Case[];
  pagination: PageResponse | undefined;
}

/** QueryOpenCasesRequest asks for the cases still being voted on. */
export interface QueryOpenCasesRequest {
}

/** QueryOpenCasesResponse carries the cases a validator still has to vote on. */
export interface QueryOpenCasesResponse {
  case: Case[];
}

/** QueryCaseVotesRequest asks how a case has been voted on so far. */
export interface QueryCaseVotesRequest {
  caseId: string;
}

/** QueryCaseVotesResponse carries the votes and the tally they add up to. */
export interface QueryCaseVotesResponse {
  votes: Vote[];
  /**
   * The tallies as they stand, so a caller does not have to add up the votes
   * and reach a different answer from the chain's.
   */
  yesPower: string;
  noPower: string;
  abstainPower: string;
  totalPowerAtOpen: string;
  /** required_power is what yes_power has to reach for the case to pass. */
  requiredPower: string;
}

/** QueryFreezeStatusRequest asks whether one address can send. */
export interface QueryFreezeStatusRequest {
  address: string;
}

/** QueryFreezeStatusResponse says whether it can, and if not, on what grounds. */
export interface QueryFreezeStatusResponse {
  frozen: boolean;
  /**
   * freeze and case are set only when frozen. Together they are the answer to
   * "why", which is the question this query exists for.
   */
  freeze: Freeze | undefined;
  case: Case | undefined;
}

/** QueryListFreezeRequest asks for every frozen address, paginated. */
export interface QueryListFreezeRequest {
  pagination: PageRequest | undefined;
}

/** QueryListFreezeResponse carries a page of freezes. */
export interface QueryListFreezeResponse {
  freeze: Freeze[];
  pagination: PageResponse | undefined;
}

/** QueryRecoveredRequest asks what this module has taken in total. */
export interface QueryRecoveredRequest {
}

/**
 * QueryRecoveredResponse carries the running total and the case counts that
 * put it in proportion.
 */
export interface QueryRecoveredResponse {
  total: Coin[];
  /** cases_passed and cases_opened put that total in proportion. */
  casesOpened: string;
  casesPassed: string;
}

/** QueryHeldCasesRequest asks for the seizures waiting out their delay. */
export interface QueryHeldCasesRequest {
}

/** QueryHeldCasesResponse carries them, soonest to execute first. */
export interface QueryHeldCasesResponse {
  case: Case[];
}

/** QuerySeizureWindowRequest asks how much of the rolling cap is left. */
export interface QuerySeizureWindowRequest {
}

/**
 * QuerySeizureWindowResponse says what has been seized inside the current
 * window and what is still permitted.
 *
 * It reports the window's start height alongside the totals, because "seized
 * this much" means nothing without the period it was measured over, and a
 * caller that computed that period itself from the parameters could compute it
 * differently from the chain.
 */
export interface QuerySeizureWindowResponse {
  windowStartHeight: string;
  currentHeight: string;
  /**
   * seized is the total inside the window; cap is the parameter it is measured
   * against; remaining is cap minus seized, floored at zero and carrying only
   * the denominations the cap names.
   */
  seized: Coin[];
  cap: Coin[];
  remaining: Coin[];
  /**
   * seizure_count and max_seizures are the other half of the cap: the one that
   * binds every denomination, including ones the value cap does not name.
   */
  seizureCount: string;
  maxSeizures: string;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest = {
  encode(_: QueryParamsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse = {
  encode(message: QueryParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryGetCaseRequest(): QueryGetCaseRequest {
  return { id: "0" };
}

export const QueryGetCaseRequest = {
  encode(message: QueryGetCaseRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetCaseRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetCaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetCaseRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "0" };
  },

  toJSON(message: QueryGetCaseRequest): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryGetCaseRequest>): QueryGetCaseRequest {
    return QueryGetCaseRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryGetCaseRequest>): QueryGetCaseRequest {
    const message = createBaseQueryGetCaseRequest();
    message.id = object.id ?? "0";
    return message;
  },
};

function createBaseQueryGetCaseResponse(): QueryGetCaseResponse {
  return { case: undefined, votes: [] };
}

export const QueryGetCaseResponse = {
  encode(message: QueryGetCaseResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.case !== undefined) {
      Case.encode(message.case, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.votes) {
      Vote.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetCaseResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetCaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.case = Case.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.votes.push(Vote.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetCaseResponse {
    return {
      case: isSet(object.case) ? Case.fromJSON(object.case) : undefined,
      votes: globalThis.Array.isArray(object?.votes) ? object.votes.map((e: any) => Vote.fromJSON(e)) : [],
    };
  },

  toJSON(message: QueryGetCaseResponse): unknown {
    const obj: any = {};
    if (message.case !== undefined) {
      obj.case = Case.toJSON(message.case);
    }
    if (message.votes?.length) {
      obj.votes = message.votes.map((e) => Vote.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryGetCaseResponse>): QueryGetCaseResponse {
    return QueryGetCaseResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryGetCaseResponse>): QueryGetCaseResponse {
    const message = createBaseQueryGetCaseResponse();
    message.case = (object.case !== undefined && object.case !== null) ? Case.fromPartial(object.case) : undefined;
    message.votes = object.votes?.map((e) => Vote.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryListCaseRequest(): QueryListCaseRequest {
  return { pagination: undefined };
}

export const QueryListCaseRequest = {
  encode(message: QueryListCaseRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListCaseRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListCaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListCaseRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryListCaseRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryListCaseRequest>): QueryListCaseRequest {
    return QueryListCaseRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryListCaseRequest>): QueryListCaseRequest {
    const message = createBaseQueryListCaseRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryListCaseResponse(): QueryListCaseResponse {
  return { case: [], pagination: undefined };
}

export const QueryListCaseResponse = {
  encode(message: QueryListCaseResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.case) {
      Case.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListCaseResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListCaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.case.push(Case.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListCaseResponse {
    return {
      case: globalThis.Array.isArray(object?.case) ? object.case.map((e: any) => Case.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryListCaseResponse): unknown {
    const obj: any = {};
    if (message.case?.length) {
      obj.case = message.case.map((e) => Case.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryListCaseResponse>): QueryListCaseResponse {
    return QueryListCaseResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryListCaseResponse>): QueryListCaseResponse {
    const message = createBaseQueryListCaseResponse();
    message.case = object.case?.map((e) => Case.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryOpenCasesRequest(): QueryOpenCasesRequest {
  return {};
}

export const QueryOpenCasesRequest = {
  encode(_: QueryOpenCasesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryOpenCasesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryOpenCasesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryOpenCasesRequest {
    return {};
  },

  toJSON(_: QueryOpenCasesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryOpenCasesRequest>): QueryOpenCasesRequest {
    return QueryOpenCasesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryOpenCasesRequest>): QueryOpenCasesRequest {
    const message = createBaseQueryOpenCasesRequest();
    return message;
  },
};

function createBaseQueryOpenCasesResponse(): QueryOpenCasesResponse {
  return { case: [] };
}

export const QueryOpenCasesResponse = {
  encode(message: QueryOpenCasesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.case) {
      Case.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryOpenCasesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryOpenCasesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.case.push(Case.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryOpenCasesResponse {
    return { case: globalThis.Array.isArray(object?.case) ? object.case.map((e: any) => Case.fromJSON(e)) : [] };
  },

  toJSON(message: QueryOpenCasesResponse): unknown {
    const obj: any = {};
    if (message.case?.length) {
      obj.case = message.case.map((e) => Case.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryOpenCasesResponse>): QueryOpenCasesResponse {
    return QueryOpenCasesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryOpenCasesResponse>): QueryOpenCasesResponse {
    const message = createBaseQueryOpenCasesResponse();
    message.case = object.case?.map((e) => Case.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryCaseVotesRequest(): QueryCaseVotesRequest {
  return { caseId: "0" };
}

export const QueryCaseVotesRequest = {
  encode(message: QueryCaseVotesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryCaseVotesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCaseVotesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryCaseVotesRequest {
    return { caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0" };
  },

  toJSON(message: QueryCaseVotesRequest): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryCaseVotesRequest>): QueryCaseVotesRequest {
    return QueryCaseVotesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryCaseVotesRequest>): QueryCaseVotesRequest {
    const message = createBaseQueryCaseVotesRequest();
    message.caseId = object.caseId ?? "0";
    return message;
  },
};

function createBaseQueryCaseVotesResponse(): QueryCaseVotesResponse {
  return { votes: [], yesPower: "0", noPower: "0", abstainPower: "0", totalPowerAtOpen: "0", requiredPower: "0" };
}

export const QueryCaseVotesResponse = {
  encode(message: QueryCaseVotesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.votes) {
      Vote.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.yesPower !== "0") {
      writer.uint32(16).int64(message.yesPower);
    }
    if (message.noPower !== "0") {
      writer.uint32(24).int64(message.noPower);
    }
    if (message.abstainPower !== "0") {
      writer.uint32(32).int64(message.abstainPower);
    }
    if (message.totalPowerAtOpen !== "0") {
      writer.uint32(40).int64(message.totalPowerAtOpen);
    }
    if (message.requiredPower !== "0") {
      writer.uint32(48).int64(message.requiredPower);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryCaseVotesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCaseVotesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.votes.push(Vote.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.yesPower = longToString(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.noPower = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.abstainPower = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.totalPowerAtOpen = longToString(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.requiredPower = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryCaseVotesResponse {
    return {
      votes: globalThis.Array.isArray(object?.votes) ? object.votes.map((e: any) => Vote.fromJSON(e)) : [],
      yesPower: isSet(object.yesPower) ? globalThis.String(object.yesPower) : "0",
      noPower: isSet(object.noPower) ? globalThis.String(object.noPower) : "0",
      abstainPower: isSet(object.abstainPower) ? globalThis.String(object.abstainPower) : "0",
      totalPowerAtOpen: isSet(object.totalPowerAtOpen) ? globalThis.String(object.totalPowerAtOpen) : "0",
      requiredPower: isSet(object.requiredPower) ? globalThis.String(object.requiredPower) : "0",
    };
  },

  toJSON(message: QueryCaseVotesResponse): unknown {
    const obj: any = {};
    if (message.votes?.length) {
      obj.votes = message.votes.map((e) => Vote.toJSON(e));
    }
    if (message.yesPower !== "0") {
      obj.yesPower = message.yesPower;
    }
    if (message.noPower !== "0") {
      obj.noPower = message.noPower;
    }
    if (message.abstainPower !== "0") {
      obj.abstainPower = message.abstainPower;
    }
    if (message.totalPowerAtOpen !== "0") {
      obj.totalPowerAtOpen = message.totalPowerAtOpen;
    }
    if (message.requiredPower !== "0") {
      obj.requiredPower = message.requiredPower;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryCaseVotesResponse>): QueryCaseVotesResponse {
    return QueryCaseVotesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryCaseVotesResponse>): QueryCaseVotesResponse {
    const message = createBaseQueryCaseVotesResponse();
    message.votes = object.votes?.map((e) => Vote.fromPartial(e)) || [];
    message.yesPower = object.yesPower ?? "0";
    message.noPower = object.noPower ?? "0";
    message.abstainPower = object.abstainPower ?? "0";
    message.totalPowerAtOpen = object.totalPowerAtOpen ?? "0";
    message.requiredPower = object.requiredPower ?? "0";
    return message;
  },
};

function createBaseQueryFreezeStatusRequest(): QueryFreezeStatusRequest {
  return { address: "" };
}

export const QueryFreezeStatusRequest = {
  encode(message: QueryFreezeStatusRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryFreezeStatusRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryFreezeStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryFreezeStatusRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryFreezeStatusRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryFreezeStatusRequest>): QueryFreezeStatusRequest {
    return QueryFreezeStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryFreezeStatusRequest>): QueryFreezeStatusRequest {
    const message = createBaseQueryFreezeStatusRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryFreezeStatusResponse(): QueryFreezeStatusResponse {
  return { frozen: false, freeze: undefined, case: undefined };
}

export const QueryFreezeStatusResponse = {
  encode(message: QueryFreezeStatusResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.frozen !== false) {
      writer.uint32(8).bool(message.frozen);
    }
    if (message.freeze !== undefined) {
      Freeze.encode(message.freeze, writer.uint32(18).fork()).ldelim();
    }
    if (message.case !== undefined) {
      Case.encode(message.case, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryFreezeStatusResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryFreezeStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.frozen = reader.bool();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.freeze = Freeze.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.case = Case.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryFreezeStatusResponse {
    return {
      frozen: isSet(object.frozen) ? globalThis.Boolean(object.frozen) : false,
      freeze: isSet(object.freeze) ? Freeze.fromJSON(object.freeze) : undefined,
      case: isSet(object.case) ? Case.fromJSON(object.case) : undefined,
    };
  },

  toJSON(message: QueryFreezeStatusResponse): unknown {
    const obj: any = {};
    if (message.frozen !== false) {
      obj.frozen = message.frozen;
    }
    if (message.freeze !== undefined) {
      obj.freeze = Freeze.toJSON(message.freeze);
    }
    if (message.case !== undefined) {
      obj.case = Case.toJSON(message.case);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryFreezeStatusResponse>): QueryFreezeStatusResponse {
    return QueryFreezeStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryFreezeStatusResponse>): QueryFreezeStatusResponse {
    const message = createBaseQueryFreezeStatusResponse();
    message.frozen = object.frozen ?? false;
    message.freeze = (object.freeze !== undefined && object.freeze !== null)
      ? Freeze.fromPartial(object.freeze)
      : undefined;
    message.case = (object.case !== undefined && object.case !== null) ? Case.fromPartial(object.case) : undefined;
    return message;
  },
};

function createBaseQueryListFreezeRequest(): QueryListFreezeRequest {
  return { pagination: undefined };
}

export const QueryListFreezeRequest = {
  encode(message: QueryListFreezeRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListFreezeRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListFreezeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListFreezeRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryListFreezeRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryListFreezeRequest>): QueryListFreezeRequest {
    return QueryListFreezeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryListFreezeRequest>): QueryListFreezeRequest {
    const message = createBaseQueryListFreezeRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryListFreezeResponse(): QueryListFreezeResponse {
  return { freeze: [], pagination: undefined };
}

export const QueryListFreezeResponse = {
  encode(message: QueryListFreezeResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.freeze) {
      Freeze.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListFreezeResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListFreezeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.freeze.push(Freeze.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListFreezeResponse {
    return {
      freeze: globalThis.Array.isArray(object?.freeze) ? object.freeze.map((e: any) => Freeze.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryListFreezeResponse): unknown {
    const obj: any = {};
    if (message.freeze?.length) {
      obj.freeze = message.freeze.map((e) => Freeze.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryListFreezeResponse>): QueryListFreezeResponse {
    return QueryListFreezeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryListFreezeResponse>): QueryListFreezeResponse {
    const message = createBaseQueryListFreezeResponse();
    message.freeze = object.freeze?.map((e) => Freeze.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryRecoveredRequest(): QueryRecoveredRequest {
  return {};
}

export const QueryRecoveredRequest = {
  encode(_: QueryRecoveredRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRecoveredRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRecoveredRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryRecoveredRequest {
    return {};
  },

  toJSON(_: QueryRecoveredRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryRecoveredRequest>): QueryRecoveredRequest {
    return QueryRecoveredRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryRecoveredRequest>): QueryRecoveredRequest {
    const message = createBaseQueryRecoveredRequest();
    return message;
  },
};

function createBaseQueryRecoveredResponse(): QueryRecoveredResponse {
  return { total: [], casesOpened: "0", casesPassed: "0" };
}

export const QueryRecoveredResponse = {
  encode(message: QueryRecoveredResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.total) {
      Coin.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.casesOpened !== "0") {
      writer.uint32(16).uint64(message.casesOpened);
    }
    if (message.casesPassed !== "0") {
      writer.uint32(24).uint64(message.casesPassed);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryRecoveredResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRecoveredResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.total.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.casesOpened = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.casesPassed = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRecoveredResponse {
    return {
      total: globalThis.Array.isArray(object?.total) ? object.total.map((e: any) => Coin.fromJSON(e)) : [],
      casesOpened: isSet(object.casesOpened) ? globalThis.String(object.casesOpened) : "0",
      casesPassed: isSet(object.casesPassed) ? globalThis.String(object.casesPassed) : "0",
    };
  },

  toJSON(message: QueryRecoveredResponse): unknown {
    const obj: any = {};
    if (message.total?.length) {
      obj.total = message.total.map((e) => Coin.toJSON(e));
    }
    if (message.casesOpened !== "0") {
      obj.casesOpened = message.casesOpened;
    }
    if (message.casesPassed !== "0") {
      obj.casesPassed = message.casesPassed;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRecoveredResponse>): QueryRecoveredResponse {
    return QueryRecoveredResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRecoveredResponse>): QueryRecoveredResponse {
    const message = createBaseQueryRecoveredResponse();
    message.total = object.total?.map((e) => Coin.fromPartial(e)) || [];
    message.casesOpened = object.casesOpened ?? "0";
    message.casesPassed = object.casesPassed ?? "0";
    return message;
  },
};

function createBaseQueryHeldCasesRequest(): QueryHeldCasesRequest {
  return {};
}

export const QueryHeldCasesRequest = {
  encode(_: QueryHeldCasesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryHeldCasesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHeldCasesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryHeldCasesRequest {
    return {};
  },

  toJSON(_: QueryHeldCasesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryHeldCasesRequest>): QueryHeldCasesRequest {
    return QueryHeldCasesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryHeldCasesRequest>): QueryHeldCasesRequest {
    const message = createBaseQueryHeldCasesRequest();
    return message;
  },
};

function createBaseQueryHeldCasesResponse(): QueryHeldCasesResponse {
  return { case: [] };
}

export const QueryHeldCasesResponse = {
  encode(message: QueryHeldCasesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.case) {
      Case.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryHeldCasesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHeldCasesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.case.push(Case.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryHeldCasesResponse {
    return { case: globalThis.Array.isArray(object?.case) ? object.case.map((e: any) => Case.fromJSON(e)) : [] };
  },

  toJSON(message: QueryHeldCasesResponse): unknown {
    const obj: any = {};
    if (message.case?.length) {
      obj.case = message.case.map((e) => Case.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryHeldCasesResponse>): QueryHeldCasesResponse {
    return QueryHeldCasesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryHeldCasesResponse>): QueryHeldCasesResponse {
    const message = createBaseQueryHeldCasesResponse();
    message.case = object.case?.map((e) => Case.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQuerySeizureWindowRequest(): QuerySeizureWindowRequest {
  return {};
}

export const QuerySeizureWindowRequest = {
  encode(_: QuerySeizureWindowRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QuerySeizureWindowRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySeizureWindowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QuerySeizureWindowRequest {
    return {};
  },

  toJSON(_: QuerySeizureWindowRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QuerySeizureWindowRequest>): QuerySeizureWindowRequest {
    return QuerySeizureWindowRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QuerySeizureWindowRequest>): QuerySeizureWindowRequest {
    const message = createBaseQuerySeizureWindowRequest();
    return message;
  },
};

function createBaseQuerySeizureWindowResponse(): QuerySeizureWindowResponse {
  return {
    windowStartHeight: "0",
    currentHeight: "0",
    seized: [],
    cap: [],
    remaining: [],
    seizureCount: "0",
    maxSeizures: "0",
  };
}

export const QuerySeizureWindowResponse = {
  encode(message: QuerySeizureWindowResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.windowStartHeight !== "0") {
      writer.uint32(8).int64(message.windowStartHeight);
    }
    if (message.currentHeight !== "0") {
      writer.uint32(16).int64(message.currentHeight);
    }
    for (const v of message.seized) {
      Coin.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.cap) {
      Coin.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.remaining) {
      Coin.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.seizureCount !== "0") {
      writer.uint32(48).uint64(message.seizureCount);
    }
    if (message.maxSeizures !== "0") {
      writer.uint32(56).uint64(message.maxSeizures);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QuerySeizureWindowResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuerySeizureWindowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.windowStartHeight = longToString(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.currentHeight = longToString(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.seized.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.cap.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.remaining.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.seizureCount = longToString(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.maxSeizures = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuerySeizureWindowResponse {
    return {
      windowStartHeight: isSet(object.windowStartHeight) ? globalThis.String(object.windowStartHeight) : "0",
      currentHeight: isSet(object.currentHeight) ? globalThis.String(object.currentHeight) : "0",
      seized: globalThis.Array.isArray(object?.seized) ? object.seized.map((e: any) => Coin.fromJSON(e)) : [],
      cap: globalThis.Array.isArray(object?.cap) ? object.cap.map((e: any) => Coin.fromJSON(e)) : [],
      remaining: globalThis.Array.isArray(object?.remaining) ? object.remaining.map((e: any) => Coin.fromJSON(e)) : [],
      seizureCount: isSet(object.seizureCount) ? globalThis.String(object.seizureCount) : "0",
      maxSeizures: isSet(object.maxSeizures) ? globalThis.String(object.maxSeizures) : "0",
    };
  },

  toJSON(message: QuerySeizureWindowResponse): unknown {
    const obj: any = {};
    if (message.windowStartHeight !== "0") {
      obj.windowStartHeight = message.windowStartHeight;
    }
    if (message.currentHeight !== "0") {
      obj.currentHeight = message.currentHeight;
    }
    if (message.seized?.length) {
      obj.seized = message.seized.map((e) => Coin.toJSON(e));
    }
    if (message.cap?.length) {
      obj.cap = message.cap.map((e) => Coin.toJSON(e));
    }
    if (message.remaining?.length) {
      obj.remaining = message.remaining.map((e) => Coin.toJSON(e));
    }
    if (message.seizureCount !== "0") {
      obj.seizureCount = message.seizureCount;
    }
    if (message.maxSeizures !== "0") {
      obj.maxSeizures = message.maxSeizures;
    }
    return obj;
  },

  create(base?: DeepPartial<QuerySeizureWindowResponse>): QuerySeizureWindowResponse {
    return QuerySeizureWindowResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuerySeizureWindowResponse>): QuerySeizureWindowResponse {
    const message = createBaseQuerySeizureWindowResponse();
    message.windowStartHeight = object.windowStartHeight ?? "0";
    message.currentHeight = object.currentHeight ?? "0";
    message.seized = object.seized?.map((e) => Coin.fromPartial(e)) || [];
    message.cap = object.cap?.map((e) => Coin.fromPartial(e)) || [];
    message.remaining = object.remaining?.map((e) => Coin.fromPartial(e)) || [];
    message.seizureCount = object.seizureCount ?? "0";
    message.maxSeizures = object.maxSeizures ?? "0";
    return message;
  },
};

/**
 * Query defines the gRPC querier service.
 *
 * The whole of this module's activity is queryable by anyone, including from
 * the accused account's point of view: FreezeStatus answers "why can I not send
 * anything, and who decided that" in one call, because an account that is
 * frozen without being told why is the failure this module has to avoid most.
 */
export interface Query {
  /** Params queries the parameters of the module. */
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse>;
  /** GetCase queries one case, with its votes. */
  GetCase(request: QueryGetCaseRequest): Promise<QueryGetCaseResponse>;
  /** ListCase queries every case ever opened, resolved or not. */
  ListCase(request: QueryListCaseRequest): Promise<QueryListCaseResponse>;
  /** OpenCases queries the cases still being voted on. */
  OpenCases(request: QueryOpenCasesRequest): Promise<QueryOpenCasesResponse>;
  /** CaseVotes queries how each validator voted on a case. */
  CaseVotes(request: QueryCaseVotesRequest): Promise<QueryCaseVotesResponse>;
  /** FreezeStatus answers whether one address can send, and if not, why. */
  FreezeStatus(request: QueryFreezeStatusRequest): Promise<QueryFreezeStatusResponse>;
  /** ListFreeze queries every frozen address. */
  ListFreeze(request: QueryListFreezeRequest): Promise<QueryListFreezeResponse>;
  /**
   * Recovered totals what this module has taken, across every case. It is the
   * number that says how much this power has actually been used.
   */
  Recovered(request: QueryRecoveredRequest): Promise<QueryRecoveredResponse>;
  /**
   * HeldCases queries the seizures that have been agreed and are waiting out
   * their delay. This is the list an ombudsman reads: everything still stoppable
   * at no cost to anybody, and how long there is left to stop it.
   */
  HeldCases(request: QueryHeldCasesRequest): Promise<QueryHeldCasesResponse>;
  /** SeizureWindow queries how much of the rolling cap is left. */
  SeizureWindow(request: QuerySeizureWindowRequest): Promise<QuerySeizureWindowResponse>;
}

export const QueryServiceName = "blockchain.enforcement.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Params = this.Params.bind(this);
    this.GetCase = this.GetCase.bind(this);
    this.ListCase = this.ListCase.bind(this);
    this.OpenCases = this.OpenCases.bind(this);
    this.CaseVotes = this.CaseVotes.bind(this);
    this.FreezeStatus = this.FreezeStatus.bind(this);
    this.ListFreeze = this.ListFreeze.bind(this);
    this.Recovered = this.Recovered.bind(this);
    this.HeldCases = this.HeldCases.bind(this);
    this.SeizureWindow = this.SeizureWindow.bind(this);
  }
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse> {
    const data = QueryParamsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Params", data);
    return promise.then((data) => QueryParamsResponse.decode(_m0.Reader.create(data)));
  }

  GetCase(request: QueryGetCaseRequest): Promise<QueryGetCaseResponse> {
    const data = QueryGetCaseRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetCase", data);
    return promise.then((data) => QueryGetCaseResponse.decode(_m0.Reader.create(data)));
  }

  ListCase(request: QueryListCaseRequest): Promise<QueryListCaseResponse> {
    const data = QueryListCaseRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListCase", data);
    return promise.then((data) => QueryListCaseResponse.decode(_m0.Reader.create(data)));
  }

  OpenCases(request: QueryOpenCasesRequest): Promise<QueryOpenCasesResponse> {
    const data = QueryOpenCasesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "OpenCases", data);
    return promise.then((data) => QueryOpenCasesResponse.decode(_m0.Reader.create(data)));
  }

  CaseVotes(request: QueryCaseVotesRequest): Promise<QueryCaseVotesResponse> {
    const data = QueryCaseVotesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CaseVotes", data);
    return promise.then((data) => QueryCaseVotesResponse.decode(_m0.Reader.create(data)));
  }

  FreezeStatus(request: QueryFreezeStatusRequest): Promise<QueryFreezeStatusResponse> {
    const data = QueryFreezeStatusRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "FreezeStatus", data);
    return promise.then((data) => QueryFreezeStatusResponse.decode(_m0.Reader.create(data)));
  }

  ListFreeze(request: QueryListFreezeRequest): Promise<QueryListFreezeResponse> {
    const data = QueryListFreezeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListFreeze", data);
    return promise.then((data) => QueryListFreezeResponse.decode(_m0.Reader.create(data)));
  }

  Recovered(request: QueryRecoveredRequest): Promise<QueryRecoveredResponse> {
    const data = QueryRecoveredRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Recovered", data);
    return promise.then((data) => QueryRecoveredResponse.decode(_m0.Reader.create(data)));
  }

  HeldCases(request: QueryHeldCasesRequest): Promise<QueryHeldCasesResponse> {
    const data = QueryHeldCasesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "HeldCases", data);
    return promise.then((data) => QueryHeldCasesResponse.decode(_m0.Reader.create(data)));
  }

  SeizureWindow(request: QuerySeizureWindowRequest): Promise<QuerySeizureWindowResponse> {
    const data = QuerySeizureWindowRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SeizureWindow", data);
    return promise.then((data) => QuerySeizureWindowResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
