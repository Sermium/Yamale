// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/enforcement/v1/enforcement.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";

export const protobufPackage = "blockchain.enforcement.v1";

/** CaseAction is what a case asks the chain to do. */
export const CaseAction = {
  /** CASE_ACTION_UNSPECIFIED - CASE_ACTION_UNSPECIFIED is the unset default and is never valid. */
  CASE_ACTION_UNSPECIFIED: 0,
  /**
   * CASE_ACTION_FREEZE - CASE_ACTION_FREEZE stops the account from sending anything. Nothing is
   * taken and nothing is redistributed; the funds stay where they are and stay
   * the account's.
   */
  CASE_ACTION_FREEZE: 1,
  /**
   * CASE_ACTION_SEIZE - CASE_ACTION_SEIZE freezes the account and, if the case passes, moves what
   * it holds to the recovery destination. Delegations are unbonded so that
   * staked funds come back and can be recovered as they mature.
   */
  CASE_ACTION_SEIZE: 2,
  UNRECOGNIZED: -1,
} as const;

export type CaseAction = typeof CaseAction[keyof typeof CaseAction];

export namespace CaseAction {
  export type CASE_ACTION_UNSPECIFIED = typeof CaseAction.CASE_ACTION_UNSPECIFIED;
  export type CASE_ACTION_FREEZE = typeof CaseAction.CASE_ACTION_FREEZE;
  export type CASE_ACTION_SEIZE = typeof CaseAction.CASE_ACTION_SEIZE;
  export type UNRECOGNIZED = typeof CaseAction.UNRECOGNIZED;
}

export function caseActionFromJSON(object: any): CaseAction {
  switch (object) {
    case 0:
    case "CASE_ACTION_UNSPECIFIED":
      return CaseAction.CASE_ACTION_UNSPECIFIED;
    case 1:
    case "CASE_ACTION_FREEZE":
      return CaseAction.CASE_ACTION_FREEZE;
    case 2:
    case "CASE_ACTION_SEIZE":
      return CaseAction.CASE_ACTION_SEIZE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CaseAction.UNRECOGNIZED;
  }
}

export function caseActionToJSON(object: CaseAction): string {
  switch (object) {
    case CaseAction.CASE_ACTION_UNSPECIFIED:
      return "CASE_ACTION_UNSPECIFIED";
    case CaseAction.CASE_ACTION_FREEZE:
      return "CASE_ACTION_FREEZE";
    case CaseAction.CASE_ACTION_SEIZE:
      return "CASE_ACTION_SEIZE";
    case CaseAction.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** CaseStatus is where a case has got to. */
export const CaseStatus = {
  /** CASE_STATUS_UNSPECIFIED - CASE_STATUS_UNSPECIFIED is the unset default and is never valid. */
  CASE_STATUS_UNSPECIFIED: 0,
  /**
   * CASE_STATUS_VOTING - CASE_STATUS_VOTING is open. The target is frozen for as long as it stays
   * that way, or until the provisional freeze expires.
   */
  CASE_STATUS_VOTING: 1,
  /**
   * CASE_STATUS_PASSED - CASE_STATUS_PASSED means the validators agreed. A freeze case ends here
   * with the account frozen indefinitely; a seizure case sweeps and then keeps
   * sweeping as unbonding funds arrive.
   */
  CASE_STATUS_PASSED: 2,
  /** CASE_STATUS_REJECTED - CASE_STATUS_REJECTED means they did not, and the freeze is lifted. */
  CASE_STATUS_REJECTED: 3,
  /**
   * CASE_STATUS_EXPIRED - CASE_STATUS_EXPIRED means the voting period ended without the threshold
   * being reached — including the ordinary case where nobody voted at all. The
   * freeze is lifted. This is deliberately not the same outcome as rejection:
   * silence is not a finding, and the record should not claim it was.
   */
  CASE_STATUS_EXPIRED: 4,
  /**
   * CASE_STATUS_WITHDRAWN - CASE_STATUS_WITHDRAWN means whoever opened it took it back before the vote
   * ended, which also lifts the freeze.
   */
  CASE_STATUS_WITHDRAWN: 5,
  /**
   * CASE_STATUS_REVERSED - CASE_STATUS_REVERSED means an authority overturned the case rather than the
   * validators deciding it: governance reversing one that passed, or the
   * emergency authority releasing an account it or a validator had frozen. Any
   * freeze is lifted.
   *
   * What was already seized is not returned by this module — that takes a
   * transfer from the recovery destination, by whoever controls it, and
   * pretending otherwise would be a lie told by an enum value.
   */
  CASE_STATUS_REVERSED: 6,
  UNRECOGNIZED: -1,
} as const;

export type CaseStatus = typeof CaseStatus[keyof typeof CaseStatus];

export namespace CaseStatus {
  export type CASE_STATUS_UNSPECIFIED = typeof CaseStatus.CASE_STATUS_UNSPECIFIED;
  export type CASE_STATUS_VOTING = typeof CaseStatus.CASE_STATUS_VOTING;
  export type CASE_STATUS_PASSED = typeof CaseStatus.CASE_STATUS_PASSED;
  export type CASE_STATUS_REJECTED = typeof CaseStatus.CASE_STATUS_REJECTED;
  export type CASE_STATUS_EXPIRED = typeof CaseStatus.CASE_STATUS_EXPIRED;
  export type CASE_STATUS_WITHDRAWN = typeof CaseStatus.CASE_STATUS_WITHDRAWN;
  export type CASE_STATUS_REVERSED = typeof CaseStatus.CASE_STATUS_REVERSED;
  export type UNRECOGNIZED = typeof CaseStatus.UNRECOGNIZED;
}

export function caseStatusFromJSON(object: any): CaseStatus {
  switch (object) {
    case 0:
    case "CASE_STATUS_UNSPECIFIED":
      return CaseStatus.CASE_STATUS_UNSPECIFIED;
    case 1:
    case "CASE_STATUS_VOTING":
      return CaseStatus.CASE_STATUS_VOTING;
    case 2:
    case "CASE_STATUS_PASSED":
      return CaseStatus.CASE_STATUS_PASSED;
    case 3:
    case "CASE_STATUS_REJECTED":
      return CaseStatus.CASE_STATUS_REJECTED;
    case 4:
    case "CASE_STATUS_EXPIRED":
      return CaseStatus.CASE_STATUS_EXPIRED;
    case 5:
    case "CASE_STATUS_WITHDRAWN":
      return CaseStatus.CASE_STATUS_WITHDRAWN;
    case 6:
    case "CASE_STATUS_REVERSED":
      return CaseStatus.CASE_STATUS_REVERSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CaseStatus.UNRECOGNIZED;
  }
}

export function caseStatusToJSON(object: CaseStatus): string {
  switch (object) {
    case CaseStatus.CASE_STATUS_UNSPECIFIED:
      return "CASE_STATUS_UNSPECIFIED";
    case CaseStatus.CASE_STATUS_VOTING:
      return "CASE_STATUS_VOTING";
    case CaseStatus.CASE_STATUS_PASSED:
      return "CASE_STATUS_PASSED";
    case CaseStatus.CASE_STATUS_REJECTED:
      return "CASE_STATUS_REJECTED";
    case CaseStatus.CASE_STATUS_EXPIRED:
      return "CASE_STATUS_EXPIRED";
    case CaseStatus.CASE_STATUS_WITHDRAWN:
      return "CASE_STATUS_WITHDRAWN";
    case CaseStatus.CASE_STATUS_REVERSED:
      return "CASE_STATUS_REVERSED";
    case CaseStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** VoteOption is how a validator voted. */
export const VoteOption = {
  /**
   * VOTE_OPTION_UNSPECIFIED - VOTE_OPTION_UNSPECIFIED is the unset default and is never valid. A vote
   * has to say something.
   */
  VOTE_OPTION_UNSPECIFIED: 0,
  /** VOTE_OPTION_YES - VOTE_OPTION_YES supports the case: freeze, or freeze and seize. */
  VOTE_OPTION_YES: 1,
  /** VOTE_OPTION_NO - VOTE_OPTION_NO opposes it. */
  VOTE_OPTION_NO: 2,
  /**
   * VOTE_OPTION_ABSTAIN - VOTE_OPTION_ABSTAIN counts towards nothing. It exists so a validator can
   * put on the record that they saw the case and declined to judge it, which
   * is a different statement from not voting.
   */
  VOTE_OPTION_ABSTAIN: 3,
  UNRECOGNIZED: -1,
} as const;

export type VoteOption = typeof VoteOption[keyof typeof VoteOption];

export namespace VoteOption {
  export type VOTE_OPTION_UNSPECIFIED = typeof VoteOption.VOTE_OPTION_UNSPECIFIED;
  export type VOTE_OPTION_YES = typeof VoteOption.VOTE_OPTION_YES;
  export type VOTE_OPTION_NO = typeof VoteOption.VOTE_OPTION_NO;
  export type VOTE_OPTION_ABSTAIN = typeof VoteOption.VOTE_OPTION_ABSTAIN;
  export type UNRECOGNIZED = typeof VoteOption.UNRECOGNIZED;
}

export function voteOptionFromJSON(object: any): VoteOption {
  switch (object) {
    case 0:
    case "VOTE_OPTION_UNSPECIFIED":
      return VoteOption.VOTE_OPTION_UNSPECIFIED;
    case 1:
    case "VOTE_OPTION_YES":
      return VoteOption.VOTE_OPTION_YES;
    case 2:
    case "VOTE_OPTION_NO":
      return VoteOption.VOTE_OPTION_NO;
    case 3:
    case "VOTE_OPTION_ABSTAIN":
      return VoteOption.VOTE_OPTION_ABSTAIN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return VoteOption.UNRECOGNIZED;
  }
}

export function voteOptionToJSON(object: VoteOption): string {
  switch (object) {
    case VoteOption.VOTE_OPTION_UNSPECIFIED:
      return "VOTE_OPTION_UNSPECIFIED";
    case VoteOption.VOTE_OPTION_YES:
      return "VOTE_OPTION_YES";
    case VoteOption.VOTE_OPTION_NO:
      return "VOTE_OPTION_NO";
    case VoteOption.VOTE_OPTION_ABSTAIN:
      return "VOTE_OPTION_ABSTAIN";
    case VoteOption.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Case is an accusation against one address, and everything the chain did about
 * it. Cases are never deleted: the record of who was frozen, on whose word, on
 * what evidence, and by whose votes is the only thing that makes this power
 * answerable.
 */
export interface Case {
  id: string;
  /** target is the account accused. */
  target: string;
  /**
   * opener is the validator operator that opened the case, or the emergency
   * authority when `emergency` is set. Not typed as a validator address for
   * exactly that reason: an emergency case is opened by a group policy account,
   * and rendering it as an operator address would name a validator that does
   * not exist.
   */
  opener: string;
  action: CaseAction;
  status: CaseStatus;
  /** reason is the stated grounds, in plain words, for whoever is accused. */
  reason: string;
  /**
   * evidence_uri points at the evidence held off-chain; evidence_hash is the
   * SHA-256 of what it pointed at when the case was opened, so a document that
   * is later edited can be shown to have been edited.
   */
  evidenceUri: string;
  evidenceHash: string;
  openedAtHeight: string;
  votingEndsAtHeight: string;
  resolvedAtHeight: string;
  /**
   * total_power_at_open is the bonded power the threshold is measured against.
   * Frozen at open so that power leaving the set mid-vote cannot lower the bar
   * for what is already being decided.
   */
  totalPowerAtOpen: string;
  yesPower: string;
  noPower: string;
  abstainPower: string;
  /**
   * recovered is everything this case has taken so far. It grows as unbonding
   * funds arrive and are swept, which is why it is a running total rather than
   * a single figure written once.
   */
  recovered: Coin[];
  /**
   * sweep_complete is set when a seizure has nothing left to collect: the
   * account is empty and no unbonding remains. Until then the case stays
   * sweepable by anyone.
   */
  sweepComplete: boolean;
  /**
   * emergency marks a case opened by the emergency authority rather than by a
   * validator. It changes nothing about how the case is decided — the
   * validators still confirm or refuse it on the same terms — and exists so
   * that nobody reading the record has to work out from an address whether the
   * founders acted directly.
   */
  emergency: boolean;
}

/**
 * Vote is one validator's judgement on one case, with the power it carried when
 * it was cast.
 */
export interface Vote {
  caseId: string;
  validator: string;
  option: VoteOption;
  /**
   * power is recorded as it was at the moment of voting. Reading it live at
   * resolution instead would let a validator vote and then change what their
   * vote meant.
   */
  power: string;
}

/**
 * Freeze is the fact that stops the money moving.
 *
 * It is kept as its own record rather than being derived by scanning cases,
 * because it is read on every single transfer the chain processes.
 */
export interface Freeze {
  address: string;
  /**
   * case_id is the case that put it there — so an account holder who is
   * refused a transfer can be told which case to read, rather than "no".
   */
  caseId: string;
  /**
   * expires_at_height is the height the freeze lapses at, for the provisional
   * freeze that comes with opening a case. Zero means it does not expire on its
   * own: the validators voted for it, and lifting it takes a decision.
   */
  expiresAtHeight: string;
  frozenAtHeight: string;
}

function createBaseCase(): Case {
  return {
    id: "0",
    target: "",
    opener: "",
    action: 0,
    status: 0,
    reason: "",
    evidenceUri: "",
    evidenceHash: "",
    openedAtHeight: "0",
    votingEndsAtHeight: "0",
    resolvedAtHeight: "0",
    totalPowerAtOpen: "0",
    yesPower: "0",
    noPower: "0",
    abstainPower: "0",
    recovered: [],
    sweepComplete: false,
    emergency: false,
  };
}

export const Case = {
  encode(message: Case, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.opener !== "") {
      writer.uint32(26).string(message.opener);
    }
    if (message.action !== 0) {
      writer.uint32(32).int32(message.action);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.reason !== "") {
      writer.uint32(50).string(message.reason);
    }
    if (message.evidenceUri !== "") {
      writer.uint32(58).string(message.evidenceUri);
    }
    if (message.evidenceHash !== "") {
      writer.uint32(66).string(message.evidenceHash);
    }
    if (message.openedAtHeight !== "0") {
      writer.uint32(72).int64(message.openedAtHeight);
    }
    if (message.votingEndsAtHeight !== "0") {
      writer.uint32(80).int64(message.votingEndsAtHeight);
    }
    if (message.resolvedAtHeight !== "0") {
      writer.uint32(88).int64(message.resolvedAtHeight);
    }
    if (message.totalPowerAtOpen !== "0") {
      writer.uint32(96).int64(message.totalPowerAtOpen);
    }
    if (message.yesPower !== "0") {
      writer.uint32(104).int64(message.yesPower);
    }
    if (message.noPower !== "0") {
      writer.uint32(112).int64(message.noPower);
    }
    if (message.abstainPower !== "0") {
      writer.uint32(120).int64(message.abstainPower);
    }
    for (const v of message.recovered) {
      Coin.encode(v!, writer.uint32(130).fork()).ldelim();
    }
    if (message.sweepComplete !== false) {
      writer.uint32(136).bool(message.sweepComplete);
    }
    if (message.emergency !== false) {
      writer.uint32(144).bool(message.emergency);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Case {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.opener = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.action = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.evidenceUri = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.evidenceHash = reader.string();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.openedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.votingEndsAtHeight = longToString(reader.int64() as Long);
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.resolvedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.totalPowerAtOpen = longToString(reader.int64() as Long);
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.yesPower = longToString(reader.int64() as Long);
          continue;
        case 14:
          if (tag !== 112) {
            break;
          }

          message.noPower = longToString(reader.int64() as Long);
          continue;
        case 15:
          if (tag !== 120) {
            break;
          }

          message.abstainPower = longToString(reader.int64() as Long);
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.recovered.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 17:
          if (tag !== 136) {
            break;
          }

          message.sweepComplete = reader.bool();
          continue;
        case 18:
          if (tag !== 144) {
            break;
          }

          message.emergency = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Case {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      opener: isSet(object.opener) ? globalThis.String(object.opener) : "",
      action: isSet(object.action) ? caseActionFromJSON(object.action) : 0,
      status: isSet(object.status) ? caseStatusFromJSON(object.status) : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      evidenceUri: isSet(object.evidenceUri) ? globalThis.String(object.evidenceUri) : "",
      evidenceHash: isSet(object.evidenceHash) ? globalThis.String(object.evidenceHash) : "",
      openedAtHeight: isSet(object.openedAtHeight) ? globalThis.String(object.openedAtHeight) : "0",
      votingEndsAtHeight: isSet(object.votingEndsAtHeight) ? globalThis.String(object.votingEndsAtHeight) : "0",
      resolvedAtHeight: isSet(object.resolvedAtHeight) ? globalThis.String(object.resolvedAtHeight) : "0",
      totalPowerAtOpen: isSet(object.totalPowerAtOpen) ? globalThis.String(object.totalPowerAtOpen) : "0",
      yesPower: isSet(object.yesPower) ? globalThis.String(object.yesPower) : "0",
      noPower: isSet(object.noPower) ? globalThis.String(object.noPower) : "0",
      abstainPower: isSet(object.abstainPower) ? globalThis.String(object.abstainPower) : "0",
      recovered: globalThis.Array.isArray(object?.recovered) ? object.recovered.map((e: any) => Coin.fromJSON(e)) : [],
      sweepComplete: isSet(object.sweepComplete) ? globalThis.Boolean(object.sweepComplete) : false,
      emergency: isSet(object.emergency) ? globalThis.Boolean(object.emergency) : false,
    };
  },

  toJSON(message: Case): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.opener !== "") {
      obj.opener = message.opener;
    }
    if (message.action !== 0) {
      obj.action = caseActionToJSON(message.action);
    }
    if (message.status !== 0) {
      obj.status = caseStatusToJSON(message.status);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.evidenceUri !== "") {
      obj.evidenceUri = message.evidenceUri;
    }
    if (message.evidenceHash !== "") {
      obj.evidenceHash = message.evidenceHash;
    }
    if (message.openedAtHeight !== "0") {
      obj.openedAtHeight = message.openedAtHeight;
    }
    if (message.votingEndsAtHeight !== "0") {
      obj.votingEndsAtHeight = message.votingEndsAtHeight;
    }
    if (message.resolvedAtHeight !== "0") {
      obj.resolvedAtHeight = message.resolvedAtHeight;
    }
    if (message.totalPowerAtOpen !== "0") {
      obj.totalPowerAtOpen = message.totalPowerAtOpen;
    }
    if (message.yesPower !== "0") {
      obj.yesPower = message.yesPower;
    }
    if (message.noPower !== "0") {
      obj.noPower = message.noPower;
    }
    if (message.abstainPower !== "0") {
      obj.abstainPower = message.abstainPower;
    }
    if (message.recovered?.length) {
      obj.recovered = message.recovered.map((e) => Coin.toJSON(e));
    }
    if (message.sweepComplete !== false) {
      obj.sweepComplete = message.sweepComplete;
    }
    if (message.emergency !== false) {
      obj.emergency = message.emergency;
    }
    return obj;
  },

  create(base?: DeepPartial<Case>): Case {
    return Case.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Case>): Case {
    const message = createBaseCase();
    message.id = object.id ?? "0";
    message.target = object.target ?? "";
    message.opener = object.opener ?? "";
    message.action = object.action ?? 0;
    message.status = object.status ?? 0;
    message.reason = object.reason ?? "";
    message.evidenceUri = object.evidenceUri ?? "";
    message.evidenceHash = object.evidenceHash ?? "";
    message.openedAtHeight = object.openedAtHeight ?? "0";
    message.votingEndsAtHeight = object.votingEndsAtHeight ?? "0";
    message.resolvedAtHeight = object.resolvedAtHeight ?? "0";
    message.totalPowerAtOpen = object.totalPowerAtOpen ?? "0";
    message.yesPower = object.yesPower ?? "0";
    message.noPower = object.noPower ?? "0";
    message.abstainPower = object.abstainPower ?? "0";
    message.recovered = object.recovered?.map((e) => Coin.fromPartial(e)) || [];
    message.sweepComplete = object.sweepComplete ?? false;
    message.emergency = object.emergency ?? false;
    return message;
  },
};

function createBaseVote(): Vote {
  return { caseId: "0", validator: "", option: 0, power: "0" };
}

export const Vote = {
  encode(message: Vote, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.validator !== "") {
      writer.uint32(18).string(message.validator);
    }
    if (message.option !== 0) {
      writer.uint32(24).int32(message.option);
    }
    if (message.power !== "0") {
      writer.uint32(32).int64(message.power);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Vote {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.option = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.power = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Vote {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      option: isSet(object.option) ? voteOptionFromJSON(object.option) : 0,
      power: isSet(object.power) ? globalThis.String(object.power) : "0",
    };
  },

  toJSON(message: Vote): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.option !== 0) {
      obj.option = voteOptionToJSON(message.option);
    }
    if (message.power !== "0") {
      obj.power = message.power;
    }
    return obj;
  },

  create(base?: DeepPartial<Vote>): Vote {
    return Vote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Vote>): Vote {
    const message = createBaseVote();
    message.caseId = object.caseId ?? "0";
    message.validator = object.validator ?? "";
    message.option = object.option ?? 0;
    message.power = object.power ?? "0";
    return message;
  },
};

function createBaseFreeze(): Freeze {
  return { address: "", caseId: "0", expiresAtHeight: "0", frozenAtHeight: "0" };
}

export const Freeze = {
  encode(message: Freeze, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.caseId !== "0") {
      writer.uint32(16).uint64(message.caseId);
    }
    if (message.expiresAtHeight !== "0") {
      writer.uint32(24).int64(message.expiresAtHeight);
    }
    if (message.frozenAtHeight !== "0") {
      writer.uint32(32).int64(message.frozenAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Freeze {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFreeze();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.expiresAtHeight = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.frozenAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Freeze {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      expiresAtHeight: isSet(object.expiresAtHeight) ? globalThis.String(object.expiresAtHeight) : "0",
      frozenAtHeight: isSet(object.frozenAtHeight) ? globalThis.String(object.frozenAtHeight) : "0",
    };
  },

  toJSON(message: Freeze): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.expiresAtHeight !== "0") {
      obj.expiresAtHeight = message.expiresAtHeight;
    }
    if (message.frozenAtHeight !== "0") {
      obj.frozenAtHeight = message.frozenAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<Freeze>): Freeze {
    return Freeze.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Freeze>): Freeze {
    const message = createBaseFreeze();
    message.address = object.address ?? "";
    message.caseId = object.caseId ?? "0";
    message.expiresAtHeight = object.expiresAtHeight ?? "0";
    message.frozenAtHeight = object.frozenAtHeight ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
