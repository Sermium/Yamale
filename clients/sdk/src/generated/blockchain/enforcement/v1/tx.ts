// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/enforcement/v1/tx.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import {
  CaseAction,
  caseActionFromJSON,
  caseActionToJSON,
  VoteOption,
  voteOptionFromJSON,
  voteOptionToJSON,
} from "./enforcement.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.enforcement.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /** NOTE: All parameters must be supplied. */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/**
 * MsgOpenCase opens a case against an address and freezes it immediately.
 *
 * One bonded validator is enough to open one, which is the point: a scam is
 * drained in minutes and a vote takes hours, so the freeze cannot wait for the
 * vote. What stops that being an arbitrary power is that the freeze expires by
 * itself, the case is public from the first block, and taking anything needs
 * the supermajority.
 */
export interface MsgOpenCase {
  /**
   * opener is the account address of a bonded validator — the key it signs
   * with, not its operator address. The chain derives the operator from it and
   * records that on the case, so the accusation is attributed to the validator
   * rather than to an address nobody recognises.
   */
  opener: string;
  target: string;
  action: CaseAction;
  /** reason is the grounds, in words the accused can read. */
  reason: string;
  /**
   * evidence_uri and evidence_hash point at and pin the evidence held
   * off-chain. Required for a seizure unless the parameters say otherwise.
   */
  evidenceUri: string;
  evidenceHash: string;
}

/** MsgOpenCaseResponse returns the new case's id. */
export interface MsgOpenCaseResponse {
  id: string;
}

/** MsgVoteCase records a validator's judgement on an open case. */
export interface MsgVoteCase {
  /**
   * voter is the validator's account address, the key it signs with. Its
   * operator address and its voting power are read from the staking module.
   */
  voter: string;
  caseId: string;
  option: VoteOption;
}

/** MsgVoteCaseResponse defines the response structure. */
export interface MsgVoteCaseResponse {
}

/** MsgWithdrawCase withdraws an open case. Only whoever opened it may. */
export interface MsgWithdrawCase {
  /** opener is the account address of the validator that opened the case. */
  opener: string;
  caseId: string;
}

/** MsgWithdrawCaseResponse defines the response structure. */
export interface MsgWithdrawCaseResponse {
}

/** MsgSweep collects what a passed seizure can reach right now. */
export interface MsgSweep {
  /**
   * sender pays for the transaction and gets nothing for it. Anyone may send
   * this: the destination is fixed by the parameters, so there is nothing to
   * gain by being the one who calls it and nothing to lose by letting somebody
   * else.
   */
  sender: string;
  caseId: string;
}

/**
 * MsgSweepResponse reports what this sweep actually collected, which may be
 * nothing if the unbonding has not matured yet.
 */
export interface MsgSweepResponse {
  collected: Coin[];
  complete: boolean;
}

/** MsgReverseCase overturns a passed case. Governance only. */
export interface MsgReverseCase {
  authority: string;
  caseId: string;
  /** reason is why it was overturned, kept beside the original accusation. */
  reason: string;
}

/** MsgReverseCaseResponse defines the response structure. */
export interface MsgReverseCaseResponse {
}

/**
 * MsgEmergencyFreeze freezes an account on the emergency authority's signature
 * alone.
 *
 * There is no matching emergency seizure, and that is the whole shape of this
 * power. Stopping money is recoverable — the account is released and nothing
 * was lost but time. Taking it is not, so it stays with the validator
 * supermajority no matter who is asking.
 *
 * The freeze this creates is provisional exactly like a validator's: it opens a
 * case, it lapses if nobody confirms it, and the validators can refuse it. The
 * founders are faster than the set; they are not above it.
 */
export interface MsgEmergencyFreeze {
  /** authority must equal the emergency_authority parameter. */
  authority: string;
  target: string;
  /**
   * reason is the grounds, in words the accused can read. Required, the same
   * as for a validator's case: acting in an emergency is not a reason to leave
   * the record blank, it is the reason the record matters.
   */
  reason: string;
  evidenceUri: string;
  evidenceHash: string;
}

/** MsgEmergencyFreezeResponse returns the new case's id. */
export interface MsgEmergencyFreezeResponse {
  id: string;
}

/**
 * MsgEmergencyRelease lifts a freeze immediately, whoever imposed it.
 *
 * This is the half that makes the emergency authority worth having. A freeze
 * opened on a misread transaction otherwise sits on somebody's account for the
 * whole voting period, and "wait a day, it expires by itself" is not an answer
 * anyone can give a customer whose payroll is stuck.
 */
export interface MsgEmergencyRelease {
  authority: string;
  caseId: string;
  /**
   * reason is why it was released, kept beside the original accusation. The
   * case is not deleted: a freeze that was lifted as a mistake is part of the
   * record of how this power has been used.
   */
  reason: string;
}

/** MsgEmergencyReleaseResponse defines the response structure. */
export interface MsgEmergencyReleaseResponse {
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams = {
  encode(message: MsgUpdateParams, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse = {
  encode(_: MsgUpdateParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgOpenCase(): MsgOpenCase {
  return { opener: "", target: "", action: 0, reason: "", evidenceUri: "", evidenceHash: "" };
}

export const MsgOpenCase = {
  encode(message: MsgOpenCase, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.opener !== "") {
      writer.uint32(10).string(message.opener);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.action !== 0) {
      writer.uint32(24).int32(message.action);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    if (message.evidenceUri !== "") {
      writer.uint32(42).string(message.evidenceUri);
    }
    if (message.evidenceHash !== "") {
      writer.uint32(50).string(message.evidenceHash);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgOpenCase {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgOpenCase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.opener = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.action = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.evidenceUri = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.evidenceHash = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgOpenCase {
    return {
      opener: isSet(object.opener) ? globalThis.String(object.opener) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      action: isSet(object.action) ? caseActionFromJSON(object.action) : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      evidenceUri: isSet(object.evidenceUri) ? globalThis.String(object.evidenceUri) : "",
      evidenceHash: isSet(object.evidenceHash) ? globalThis.String(object.evidenceHash) : "",
    };
  },

  toJSON(message: MsgOpenCase): unknown {
    const obj: any = {};
    if (message.opener !== "") {
      obj.opener = message.opener;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.action !== 0) {
      obj.action = caseActionToJSON(message.action);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.evidenceUri !== "") {
      obj.evidenceUri = message.evidenceUri;
    }
    if (message.evidenceHash !== "") {
      obj.evidenceHash = message.evidenceHash;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgOpenCase>): MsgOpenCase {
    return MsgOpenCase.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgOpenCase>): MsgOpenCase {
    const message = createBaseMsgOpenCase();
    message.opener = object.opener ?? "";
    message.target = object.target ?? "";
    message.action = object.action ?? 0;
    message.reason = object.reason ?? "";
    message.evidenceUri = object.evidenceUri ?? "";
    message.evidenceHash = object.evidenceHash ?? "";
    return message;
  },
};

function createBaseMsgOpenCaseResponse(): MsgOpenCaseResponse {
  return { id: "0" };
}

export const MsgOpenCaseResponse = {
  encode(message: MsgOpenCaseResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgOpenCaseResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgOpenCaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgOpenCaseResponse {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "0" };
  },

  toJSON(message: MsgOpenCaseResponse): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgOpenCaseResponse>): MsgOpenCaseResponse {
    return MsgOpenCaseResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgOpenCaseResponse>): MsgOpenCaseResponse {
    const message = createBaseMsgOpenCaseResponse();
    message.id = object.id ?? "0";
    return message;
  },
};

function createBaseMsgVoteCase(): MsgVoteCase {
  return { voter: "", caseId: "0", option: 0 };
}

export const MsgVoteCase = {
  encode(message: MsgVoteCase, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.voter !== "") {
      writer.uint32(10).string(message.voter);
    }
    if (message.caseId !== "0") {
      writer.uint32(16).uint64(message.caseId);
    }
    if (message.option !== 0) {
      writer.uint32(24).int32(message.option);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgVoteCase {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgVoteCase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.voter = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.option = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgVoteCase {
    return {
      voter: isSet(object.voter) ? globalThis.String(object.voter) : "",
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      option: isSet(object.option) ? voteOptionFromJSON(object.option) : 0,
    };
  },

  toJSON(message: MsgVoteCase): unknown {
    const obj: any = {};
    if (message.voter !== "") {
      obj.voter = message.voter;
    }
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.option !== 0) {
      obj.option = voteOptionToJSON(message.option);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgVoteCase>): MsgVoteCase {
    return MsgVoteCase.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgVoteCase>): MsgVoteCase {
    const message = createBaseMsgVoteCase();
    message.voter = object.voter ?? "";
    message.caseId = object.caseId ?? "0";
    message.option = object.option ?? 0;
    return message;
  },
};

function createBaseMsgVoteCaseResponse(): MsgVoteCaseResponse {
  return {};
}

export const MsgVoteCaseResponse = {
  encode(_: MsgVoteCaseResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgVoteCaseResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgVoteCaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgVoteCaseResponse {
    return {};
  },

  toJSON(_: MsgVoteCaseResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgVoteCaseResponse>): MsgVoteCaseResponse {
    return MsgVoteCaseResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgVoteCaseResponse>): MsgVoteCaseResponse {
    const message = createBaseMsgVoteCaseResponse();
    return message;
  },
};

function createBaseMsgWithdrawCase(): MsgWithdrawCase {
  return { opener: "", caseId: "0" };
}

export const MsgWithdrawCase = {
  encode(message: MsgWithdrawCase, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.opener !== "") {
      writer.uint32(10).string(message.opener);
    }
    if (message.caseId !== "0") {
      writer.uint32(16).uint64(message.caseId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgWithdrawCase {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWithdrawCase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.opener = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgWithdrawCase {
    return {
      opener: isSet(object.opener) ? globalThis.String(object.opener) : "",
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
    };
  },

  toJSON(message: MsgWithdrawCase): unknown {
    const obj: any = {};
    if (message.opener !== "") {
      obj.opener = message.opener;
    }
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgWithdrawCase>): MsgWithdrawCase {
    return MsgWithdrawCase.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgWithdrawCase>): MsgWithdrawCase {
    const message = createBaseMsgWithdrawCase();
    message.opener = object.opener ?? "";
    message.caseId = object.caseId ?? "0";
    return message;
  },
};

function createBaseMsgWithdrawCaseResponse(): MsgWithdrawCaseResponse {
  return {};
}

export const MsgWithdrawCaseResponse = {
  encode(_: MsgWithdrawCaseResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgWithdrawCaseResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWithdrawCaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgWithdrawCaseResponse {
    return {};
  },

  toJSON(_: MsgWithdrawCaseResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgWithdrawCaseResponse>): MsgWithdrawCaseResponse {
    return MsgWithdrawCaseResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgWithdrawCaseResponse>): MsgWithdrawCaseResponse {
    const message = createBaseMsgWithdrawCaseResponse();
    return message;
  },
};

function createBaseMsgSweep(): MsgSweep {
  return { sender: "", caseId: "0" };
}

export const MsgSweep = {
  encode(message: MsgSweep, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.caseId !== "0") {
      writer.uint32(16).uint64(message.caseId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSweep {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSweep();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSweep {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
    };
  },

  toJSON(message: MsgSweep): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSweep>): MsgSweep {
    return MsgSweep.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSweep>): MsgSweep {
    const message = createBaseMsgSweep();
    message.sender = object.sender ?? "";
    message.caseId = object.caseId ?? "0";
    return message;
  },
};

function createBaseMsgSweepResponse(): MsgSweepResponse {
  return { collected: [], complete: false };
}

export const MsgSweepResponse = {
  encode(message: MsgSweepResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.collected) {
      Coin.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.complete !== false) {
      writer.uint32(16).bool(message.complete);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSweepResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSweepResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.collected.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.complete = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSweepResponse {
    return {
      collected: globalThis.Array.isArray(object?.collected) ? object.collected.map((e: any) => Coin.fromJSON(e)) : [],
      complete: isSet(object.complete) ? globalThis.Boolean(object.complete) : false,
    };
  },

  toJSON(message: MsgSweepResponse): unknown {
    const obj: any = {};
    if (message.collected?.length) {
      obj.collected = message.collected.map((e) => Coin.toJSON(e));
    }
    if (message.complete !== false) {
      obj.complete = message.complete;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSweepResponse>): MsgSweepResponse {
    return MsgSweepResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSweepResponse>): MsgSweepResponse {
    const message = createBaseMsgSweepResponse();
    message.collected = object.collected?.map((e) => Coin.fromPartial(e)) || [];
    message.complete = object.complete ?? false;
    return message;
  },
};

function createBaseMsgReverseCase(): MsgReverseCase {
  return { authority: "", caseId: "0", reason: "" };
}

export const MsgReverseCase = {
  encode(message: MsgReverseCase, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.caseId !== "0") {
      writer.uint32(16).uint64(message.caseId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgReverseCase {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgReverseCase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgReverseCase {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: MsgReverseCase): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgReverseCase>): MsgReverseCase {
    return MsgReverseCase.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgReverseCase>): MsgReverseCase {
    const message = createBaseMsgReverseCase();
    message.authority = object.authority ?? "";
    message.caseId = object.caseId ?? "0";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseMsgReverseCaseResponse(): MsgReverseCaseResponse {
  return {};
}

export const MsgReverseCaseResponse = {
  encode(_: MsgReverseCaseResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgReverseCaseResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgReverseCaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgReverseCaseResponse {
    return {};
  },

  toJSON(_: MsgReverseCaseResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgReverseCaseResponse>): MsgReverseCaseResponse {
    return MsgReverseCaseResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgReverseCaseResponse>): MsgReverseCaseResponse {
    const message = createBaseMsgReverseCaseResponse();
    return message;
  },
};

function createBaseMsgEmergencyFreeze(): MsgEmergencyFreeze {
  return { authority: "", target: "", reason: "", evidenceUri: "", evidenceHash: "" };
}

export const MsgEmergencyFreeze = {
  encode(message: MsgEmergencyFreeze, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.evidenceUri !== "") {
      writer.uint32(34).string(message.evidenceUri);
    }
    if (message.evidenceHash !== "") {
      writer.uint32(42).string(message.evidenceHash);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgEmergencyFreeze {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgEmergencyFreeze();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.evidenceUri = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.evidenceHash = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgEmergencyFreeze {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      evidenceUri: isSet(object.evidenceUri) ? globalThis.String(object.evidenceUri) : "",
      evidenceHash: isSet(object.evidenceHash) ? globalThis.String(object.evidenceHash) : "",
    };
  },

  toJSON(message: MsgEmergencyFreeze): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.evidenceUri !== "") {
      obj.evidenceUri = message.evidenceUri;
    }
    if (message.evidenceHash !== "") {
      obj.evidenceHash = message.evidenceHash;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgEmergencyFreeze>): MsgEmergencyFreeze {
    return MsgEmergencyFreeze.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgEmergencyFreeze>): MsgEmergencyFreeze {
    const message = createBaseMsgEmergencyFreeze();
    message.authority = object.authority ?? "";
    message.target = object.target ?? "";
    message.reason = object.reason ?? "";
    message.evidenceUri = object.evidenceUri ?? "";
    message.evidenceHash = object.evidenceHash ?? "";
    return message;
  },
};

function createBaseMsgEmergencyFreezeResponse(): MsgEmergencyFreezeResponse {
  return { id: "0" };
}

export const MsgEmergencyFreezeResponse = {
  encode(message: MsgEmergencyFreezeResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgEmergencyFreezeResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgEmergencyFreezeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgEmergencyFreezeResponse {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "0" };
  },

  toJSON(message: MsgEmergencyFreezeResponse): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgEmergencyFreezeResponse>): MsgEmergencyFreezeResponse {
    return MsgEmergencyFreezeResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgEmergencyFreezeResponse>): MsgEmergencyFreezeResponse {
    const message = createBaseMsgEmergencyFreezeResponse();
    message.id = object.id ?? "0";
    return message;
  },
};

function createBaseMsgEmergencyRelease(): MsgEmergencyRelease {
  return { authority: "", caseId: "0", reason: "" };
}

export const MsgEmergencyRelease = {
  encode(message: MsgEmergencyRelease, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.caseId !== "0") {
      writer.uint32(16).uint64(message.caseId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgEmergencyRelease {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgEmergencyRelease();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgEmergencyRelease {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: MsgEmergencyRelease): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgEmergencyRelease>): MsgEmergencyRelease {
    return MsgEmergencyRelease.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgEmergencyRelease>): MsgEmergencyRelease {
    const message = createBaseMsgEmergencyRelease();
    message.authority = object.authority ?? "";
    message.caseId = object.caseId ?? "0";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseMsgEmergencyReleaseResponse(): MsgEmergencyReleaseResponse {
  return {};
}

export const MsgEmergencyReleaseResponse = {
  encode(_: MsgEmergencyReleaseResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgEmergencyReleaseResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgEmergencyReleaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgEmergencyReleaseResponse {
    return {};
  },

  toJSON(_: MsgEmergencyReleaseResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgEmergencyReleaseResponse>): MsgEmergencyReleaseResponse {
    return MsgEmergencyReleaseResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgEmergencyReleaseResponse>): MsgEmergencyReleaseResponse {
    const message = createBaseMsgEmergencyReleaseResponse();
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  /**
   * UpdateParams defines a (governance) operation for updating the module
   * parameters. The authority defaults to the x/gov module account.
   */
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
  /** OpenCase accuses an address, and freezes it while the validators decide. */
  OpenCase(request: MsgOpenCase): Promise<MsgOpenCaseResponse>;
  /** VoteCase records one validator's judgement. */
  VoteCase(request: MsgVoteCase): Promise<MsgVoteCaseResponse>;
  /** WithdrawCase takes back a case before the vote ends, lifting its freeze. */
  WithdrawCase(request: MsgWithdrawCase): Promise<MsgWithdrawCaseResponse>;
  /**
   * Sweep collects whatever a passed seizure can now reach. Permissionless and
   * repeatable: funds that were staked arrive later, when unbonding matures,
   * and somebody has to be able to collect them without another vote.
   */
  Sweep(request: MsgSweep): Promise<MsgSweepResponse>;
  /**
   * EmergencyFreeze lets the founders' group stop an account in one block,
   * without waiting for a validator to open a case.
   */
  EmergencyFreeze(request: MsgEmergencyFreeze): Promise<MsgEmergencyFreezeResponse>;
  /** EmergencyRelease lets them let it go again, just as fast. */
  EmergencyRelease(request: MsgEmergencyRelease): Promise<MsgEmergencyReleaseResponse>;
  /**
   * ReverseCase is governance overturning a passed case: the appeal. It lifts
   * the freeze and records the reversal, and it is deliberately a slower
   * instrument than the one that imposed the freeze, because it is the one used
   * when the chain got it wrong.
   */
  ReverseCase(request: MsgReverseCase): Promise<MsgReverseCaseResponse>;
}

export const MsgServiceName = "blockchain.enforcement.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.UpdateParams = this.UpdateParams.bind(this);
    this.OpenCase = this.OpenCase.bind(this);
    this.VoteCase = this.VoteCase.bind(this);
    this.WithdrawCase = this.WithdrawCase.bind(this);
    this.Sweep = this.Sweep.bind(this);
    this.EmergencyFreeze = this.EmergencyFreeze.bind(this);
    this.EmergencyRelease = this.EmergencyRelease.bind(this);
    this.ReverseCase = this.ReverseCase.bind(this);
  }
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(_m0.Reader.create(data)));
  }

  OpenCase(request: MsgOpenCase): Promise<MsgOpenCaseResponse> {
    const data = MsgOpenCase.encode(request).finish();
    const promise = this.rpc.request(this.service, "OpenCase", data);
    return promise.then((data) => MsgOpenCaseResponse.decode(_m0.Reader.create(data)));
  }

  VoteCase(request: MsgVoteCase): Promise<MsgVoteCaseResponse> {
    const data = MsgVoteCase.encode(request).finish();
    const promise = this.rpc.request(this.service, "VoteCase", data);
    return promise.then((data) => MsgVoteCaseResponse.decode(_m0.Reader.create(data)));
  }

  WithdrawCase(request: MsgWithdrawCase): Promise<MsgWithdrawCaseResponse> {
    const data = MsgWithdrawCase.encode(request).finish();
    const promise = this.rpc.request(this.service, "WithdrawCase", data);
    return promise.then((data) => MsgWithdrawCaseResponse.decode(_m0.Reader.create(data)));
  }

  Sweep(request: MsgSweep): Promise<MsgSweepResponse> {
    const data = MsgSweep.encode(request).finish();
    const promise = this.rpc.request(this.service, "Sweep", data);
    return promise.then((data) => MsgSweepResponse.decode(_m0.Reader.create(data)));
  }

  EmergencyFreeze(request: MsgEmergencyFreeze): Promise<MsgEmergencyFreezeResponse> {
    const data = MsgEmergencyFreeze.encode(request).finish();
    const promise = this.rpc.request(this.service, "EmergencyFreeze", data);
    return promise.then((data) => MsgEmergencyFreezeResponse.decode(_m0.Reader.create(data)));
  }

  EmergencyRelease(request: MsgEmergencyRelease): Promise<MsgEmergencyReleaseResponse> {
    const data = MsgEmergencyRelease.encode(request).finish();
    const promise = this.rpc.request(this.service, "EmergencyRelease", data);
    return promise.then((data) => MsgEmergencyReleaseResponse.decode(_m0.Reader.create(data)));
  }

  ReverseCase(request: MsgReverseCase): Promise<MsgReverseCaseResponse> {
    const data = MsgReverseCase.encode(request).finish();
    const promise = this.rpc.request(this.service, "ReverseCase", data);
    return promise.then((data) => MsgReverseCaseResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
