// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/enforcement/v1/events.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import {
  CaseAction,
  caseActionFromJSON,
  caseActionToJSON,
  CaseStatus,
  caseStatusFromJSON,
  caseStatusToJSON,
  VoteOption,
  voteOptionFromJSON,
  voteOptionToJSON,
} from "./enforcement.ts";

export const protobufPackage = "blockchain.enforcement.v1";

/** EventCaseOpened is emitted when a case is opened and its target frozen. */
export interface EventCaseOpened {
  caseId: string;
  target: string;
  opener: string;
  action: CaseAction;
  reason: string;
  votingEndsAtHeight: string;
  /**
   * emergency is set when the founders' group opened it directly rather than a
   * validator. Anyone watching for this module's events should be able to tell
   * the two apart without fetching the case.
   */
  emergency: boolean;
}

/** EventCaseVoted is emitted for each validator's vote. */
export interface EventCaseVoted {
  caseId: string;
  validator: string;
  option: VoteOption;
  power: string;
}

/**
 * EventCaseResolved is emitted once, when a case reaches its final status —
 * including when it expires with nobody having voted.
 */
export interface EventCaseResolved {
  caseId: string;
  target: string;
  status: CaseStatus;
  yesPower: string;
  noPower: string;
  requiredPower: string;
}

/**
 * EventSeized is emitted every time a sweep collects something, which for a
 * target with staked funds is more than once.
 */
export interface EventSeized {
  caseId: string;
  target: string;
  destination: string;
  collected: Coin[];
  /**
   * complete is set on the sweep that finishes the job: nothing liquid left,
   * nothing staked, nothing unbonding.
   */
  complete: boolean;
}

/**
 * EventCaseHeld is emitted when a seizure is agreed and starts waiting out its
 * delay. This is the block in which anybody who wants to object still can, so
 * it carries the height the objection window closes at and the value the delay
 * was sized from.
 */
export interface EventCaseHeld {
  caseId: string;
  target: string;
  /**
   * assessed_value is what the target was found to hold: balance, stake and
   * unbonding together.
   */
  assessedValue: Coin[];
  executeAtHeight: string;
  delayBlocks: string;
}

/**
 * EventCaseVetoed is emitted when the ombudsman stops a case.
 *
 * Its own event rather than an EventCaseResolved with a different status,
 * because "the office outside the validator set refused this" is the single
 * most important thing that can happen in this module and it should not have to
 * be inferred from an enum by whoever is watching.
 */
export interface EventCaseVetoed {
  caseId: string;
  target: string;
  ombudsman: string;
  reason: string;
  /**
   * was_held distinguishes a veto that stopped a seizure the validators had
   * already agreed to from one that stopped a case still being argued. They are
   * very different acts by the same office.
   */
  wasHeld: boolean;
}

/**
 * EventSeizureDeferred is emitted when a seizure comes due and the rolling cap
 * refuses it.
 *
 * Emitted every time it is refused, not once. A case that is quietly waiting is
 * indistinguishable from a case that has been forgotten, and the difference
 * matters to the person whose account is still frozen.
 */
export interface EventSeizureDeferred {
  caseId: string;
  target: string;
  /**
   * retry_at_height is the next height at which the window could have room —
   * when the oldest seizure in it falls out.
   */
  retryAtHeight: string;
  /**
   * reason says which limit refused it, in words, so that nobody has to
   * reconstruct the arithmetic from the parameters to find out.
   */
  reason: string;
}

/**
 * EventFreezeLifted is emitted whenever an address becomes able to send again,
 * whatever the reason — expiry, rejection, withdrawal or reversal.
 */
export interface EventFreezeLifted {
  address: string;
  caseId: string;
  status: CaseStatus;
}

function createBaseEventCaseOpened(): EventCaseOpened {
  return { caseId: "0", target: "", opener: "", action: 0, reason: "", votingEndsAtHeight: "0", emergency: false };
}

export const EventCaseOpened = {
  encode(message: EventCaseOpened, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.opener !== "") {
      writer.uint32(26).string(message.opener);
    }
    if (message.action !== 0) {
      writer.uint32(32).int32(message.action);
    }
    if (message.reason !== "") {
      writer.uint32(42).string(message.reason);
    }
    if (message.votingEndsAtHeight !== "0") {
      writer.uint32(48).int64(message.votingEndsAtHeight);
    }
    if (message.emergency !== false) {
      writer.uint32(56).bool(message.emergency);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventCaseOpened {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventCaseOpened();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.opener = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.action = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.votingEndsAtHeight = longToString(reader.int64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.emergency = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventCaseOpened {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      opener: isSet(object.opener) ? globalThis.String(object.opener) : "",
      action: isSet(object.action) ? caseActionFromJSON(object.action) : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      votingEndsAtHeight: isSet(object.votingEndsAtHeight) ? globalThis.String(object.votingEndsAtHeight) : "0",
      emergency: isSet(object.emergency) ? globalThis.Boolean(object.emergency) : false,
    };
  },

  toJSON(message: EventCaseOpened): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.opener !== "") {
      obj.opener = message.opener;
    }
    if (message.action !== 0) {
      obj.action = caseActionToJSON(message.action);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.votingEndsAtHeight !== "0") {
      obj.votingEndsAtHeight = message.votingEndsAtHeight;
    }
    if (message.emergency !== false) {
      obj.emergency = message.emergency;
    }
    return obj;
  },

  create(base?: DeepPartial<EventCaseOpened>): EventCaseOpened {
    return EventCaseOpened.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventCaseOpened>): EventCaseOpened {
    const message = createBaseEventCaseOpened();
    message.caseId = object.caseId ?? "0";
    message.target = object.target ?? "";
    message.opener = object.opener ?? "";
    message.action = object.action ?? 0;
    message.reason = object.reason ?? "";
    message.votingEndsAtHeight = object.votingEndsAtHeight ?? "0";
    message.emergency = object.emergency ?? false;
    return message;
  },
};

function createBaseEventCaseVoted(): EventCaseVoted {
  return { caseId: "0", validator: "", option: 0, power: "0" };
}

export const EventCaseVoted = {
  encode(message: EventCaseVoted, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.validator !== "") {
      writer.uint32(18).string(message.validator);
    }
    if (message.option !== 0) {
      writer.uint32(24).int32(message.option);
    }
    if (message.power !== "0") {
      writer.uint32(32).int64(message.power);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventCaseVoted {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventCaseVoted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.option = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.power = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventCaseVoted {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      option: isSet(object.option) ? voteOptionFromJSON(object.option) : 0,
      power: isSet(object.power) ? globalThis.String(object.power) : "0",
    };
  },

  toJSON(message: EventCaseVoted): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.option !== 0) {
      obj.option = voteOptionToJSON(message.option);
    }
    if (message.power !== "0") {
      obj.power = message.power;
    }
    return obj;
  },

  create(base?: DeepPartial<EventCaseVoted>): EventCaseVoted {
    return EventCaseVoted.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventCaseVoted>): EventCaseVoted {
    const message = createBaseEventCaseVoted();
    message.caseId = object.caseId ?? "0";
    message.validator = object.validator ?? "";
    message.option = object.option ?? 0;
    message.power = object.power ?? "0";
    return message;
  },
};

function createBaseEventCaseResolved(): EventCaseResolved {
  return { caseId: "0", target: "", status: 0, yesPower: "0", noPower: "0", requiredPower: "0" };
}

export const EventCaseResolved = {
  encode(message: EventCaseResolved, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.yesPower !== "0") {
      writer.uint32(32).int64(message.yesPower);
    }
    if (message.noPower !== "0") {
      writer.uint32(40).int64(message.noPower);
    }
    if (message.requiredPower !== "0") {
      writer.uint32(48).int64(message.requiredPower);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventCaseResolved {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventCaseResolved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.yesPower = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.noPower = longToString(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.requiredPower = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventCaseResolved {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      status: isSet(object.status) ? caseStatusFromJSON(object.status) : 0,
      yesPower: isSet(object.yesPower) ? globalThis.String(object.yesPower) : "0",
      noPower: isSet(object.noPower) ? globalThis.String(object.noPower) : "0",
      requiredPower: isSet(object.requiredPower) ? globalThis.String(object.requiredPower) : "0",
    };
  },

  toJSON(message: EventCaseResolved): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.status !== 0) {
      obj.status = caseStatusToJSON(message.status);
    }
    if (message.yesPower !== "0") {
      obj.yesPower = message.yesPower;
    }
    if (message.noPower !== "0") {
      obj.noPower = message.noPower;
    }
    if (message.requiredPower !== "0") {
      obj.requiredPower = message.requiredPower;
    }
    return obj;
  },

  create(base?: DeepPartial<EventCaseResolved>): EventCaseResolved {
    return EventCaseResolved.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventCaseResolved>): EventCaseResolved {
    const message = createBaseEventCaseResolved();
    message.caseId = object.caseId ?? "0";
    message.target = object.target ?? "";
    message.status = object.status ?? 0;
    message.yesPower = object.yesPower ?? "0";
    message.noPower = object.noPower ?? "0";
    message.requiredPower = object.requiredPower ?? "0";
    return message;
  },
};

function createBaseEventSeized(): EventSeized {
  return { caseId: "0", target: "", destination: "", collected: [], complete: false };
}

export const EventSeized = {
  encode(message: EventSeized, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.destination !== "") {
      writer.uint32(26).string(message.destination);
    }
    for (const v of message.collected) {
      Coin.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message.complete !== false) {
      writer.uint32(40).bool(message.complete);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventSeized {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventSeized();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.destination = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.collected.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.complete = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventSeized {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      destination: isSet(object.destination) ? globalThis.String(object.destination) : "",
      collected: globalThis.Array.isArray(object?.collected) ? object.collected.map((e: any) => Coin.fromJSON(e)) : [],
      complete: isSet(object.complete) ? globalThis.Boolean(object.complete) : false,
    };
  },

  toJSON(message: EventSeized): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.destination !== "") {
      obj.destination = message.destination;
    }
    if (message.collected?.length) {
      obj.collected = message.collected.map((e) => Coin.toJSON(e));
    }
    if (message.complete !== false) {
      obj.complete = message.complete;
    }
    return obj;
  },

  create(base?: DeepPartial<EventSeized>): EventSeized {
    return EventSeized.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventSeized>): EventSeized {
    const message = createBaseEventSeized();
    message.caseId = object.caseId ?? "0";
    message.target = object.target ?? "";
    message.destination = object.destination ?? "";
    message.collected = object.collected?.map((e) => Coin.fromPartial(e)) || [];
    message.complete = object.complete ?? false;
    return message;
  },
};

function createBaseEventCaseHeld(): EventCaseHeld {
  return { caseId: "0", target: "", assessedValue: [], executeAtHeight: "0", delayBlocks: "0" };
}

export const EventCaseHeld = {
  encode(message: EventCaseHeld, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    for (const v of message.assessedValue) {
      Coin.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.executeAtHeight !== "0") {
      writer.uint32(32).int64(message.executeAtHeight);
    }
    if (message.delayBlocks !== "0") {
      writer.uint32(40).uint64(message.delayBlocks);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventCaseHeld {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventCaseHeld();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.assessedValue.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.executeAtHeight = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.delayBlocks = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventCaseHeld {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      assessedValue: globalThis.Array.isArray(object?.assessedValue)
        ? object.assessedValue.map((e: any) => Coin.fromJSON(e))
        : [],
      executeAtHeight: isSet(object.executeAtHeight) ? globalThis.String(object.executeAtHeight) : "0",
      delayBlocks: isSet(object.delayBlocks) ? globalThis.String(object.delayBlocks) : "0",
    };
  },

  toJSON(message: EventCaseHeld): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.assessedValue?.length) {
      obj.assessedValue = message.assessedValue.map((e) => Coin.toJSON(e));
    }
    if (message.executeAtHeight !== "0") {
      obj.executeAtHeight = message.executeAtHeight;
    }
    if (message.delayBlocks !== "0") {
      obj.delayBlocks = message.delayBlocks;
    }
    return obj;
  },

  create(base?: DeepPartial<EventCaseHeld>): EventCaseHeld {
    return EventCaseHeld.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventCaseHeld>): EventCaseHeld {
    const message = createBaseEventCaseHeld();
    message.caseId = object.caseId ?? "0";
    message.target = object.target ?? "";
    message.assessedValue = object.assessedValue?.map((e) => Coin.fromPartial(e)) || [];
    message.executeAtHeight = object.executeAtHeight ?? "0";
    message.delayBlocks = object.delayBlocks ?? "0";
    return message;
  },
};

function createBaseEventCaseVetoed(): EventCaseVetoed {
  return { caseId: "0", target: "", ombudsman: "", reason: "", wasHeld: false };
}

export const EventCaseVetoed = {
  encode(message: EventCaseVetoed, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.ombudsman !== "") {
      writer.uint32(26).string(message.ombudsman);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    if (message.wasHeld !== false) {
      writer.uint32(40).bool(message.wasHeld);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventCaseVetoed {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventCaseVetoed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.ombudsman = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.wasHeld = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventCaseVetoed {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      ombudsman: isSet(object.ombudsman) ? globalThis.String(object.ombudsman) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      wasHeld: isSet(object.wasHeld) ? globalThis.Boolean(object.wasHeld) : false,
    };
  },

  toJSON(message: EventCaseVetoed): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.ombudsman !== "") {
      obj.ombudsman = message.ombudsman;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.wasHeld !== false) {
      obj.wasHeld = message.wasHeld;
    }
    return obj;
  },

  create(base?: DeepPartial<EventCaseVetoed>): EventCaseVetoed {
    return EventCaseVetoed.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventCaseVetoed>): EventCaseVetoed {
    const message = createBaseEventCaseVetoed();
    message.caseId = object.caseId ?? "0";
    message.target = object.target ?? "";
    message.ombudsman = object.ombudsman ?? "";
    message.reason = object.reason ?? "";
    message.wasHeld = object.wasHeld ?? false;
    return message;
  },
};

function createBaseEventSeizureDeferred(): EventSeizureDeferred {
  return { caseId: "0", target: "", retryAtHeight: "0", reason: "" };
}

export const EventSeizureDeferred = {
  encode(message: EventSeizureDeferred, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.caseId !== "0") {
      writer.uint32(8).uint64(message.caseId);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.retryAtHeight !== "0") {
      writer.uint32(24).int64(message.retryAtHeight);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventSeizureDeferred {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventSeizureDeferred();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.retryAtHeight = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventSeizureDeferred {
    return {
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      retryAtHeight: isSet(object.retryAtHeight) ? globalThis.String(object.retryAtHeight) : "0",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: EventSeizureDeferred): unknown {
    const obj: any = {};
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.retryAtHeight !== "0") {
      obj.retryAtHeight = message.retryAtHeight;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<EventSeizureDeferred>): EventSeizureDeferred {
    return EventSeizureDeferred.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventSeizureDeferred>): EventSeizureDeferred {
    const message = createBaseEventSeizureDeferred();
    message.caseId = object.caseId ?? "0";
    message.target = object.target ?? "";
    message.retryAtHeight = object.retryAtHeight ?? "0";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseEventFreezeLifted(): EventFreezeLifted {
  return { address: "", caseId: "0", status: 0 };
}

export const EventFreezeLifted = {
  encode(message: EventFreezeLifted, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.caseId !== "0") {
      writer.uint32(16).uint64(message.caseId);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventFreezeLifted {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventFreezeLifted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.caseId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventFreezeLifted {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      caseId: isSet(object.caseId) ? globalThis.String(object.caseId) : "0",
      status: isSet(object.status) ? caseStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: EventFreezeLifted): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.caseId !== "0") {
      obj.caseId = message.caseId;
    }
    if (message.status !== 0) {
      obj.status = caseStatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<EventFreezeLifted>): EventFreezeLifted {
    return EventFreezeLifted.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventFreezeLifted>): EventFreezeLifted {
    const message = createBaseEventFreezeLifted();
    message.address = object.address ?? "";
    message.caseId = object.caseId ?? "0";
    message.status = object.status ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
