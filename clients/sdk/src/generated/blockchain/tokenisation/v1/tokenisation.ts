// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/tokenisation/v1/tokenisation.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { Timestamp } from "../../../google/protobuf/timestamp.ts";

export const protobufPackage = "blockchain.tokenisation.v1";

/**
 * VerificationMode is how the chain learns what an asset sold for.
 *
 * The issuer alone can never set it. An issuer who under-reports steals from
 * every shareholder in one transaction, at the moment of maximum value, with no
 * further obligation to anybody afterwards.
 */
export const VerificationMode = {
  VERIFICATION_UNSPECIFIED: 0,
  /**
   * VERIFY_VALUER - The appointed independent valuer signs the figure. x/oracle already holds
   * this machinery for appraisals.
   */
  VERIFY_VALUER: 1,
  /** VERIFY_ATTESTORS - m-of-n attestors agree, m >= 2. Same shape as x/custody's deposits. */
  VERIFY_ATTESTORS: 2,
  /** VERIFY_GOVERNANCE - Voted, with the evidence attached to the proposal. */
  VERIFY_GOVERNANCE: 3,
  /**
   * VERIFY_SCHEDULE - The redemption amount was fixed when the vehicle was created -- par plus
   * coupon, on a date -- so there is nothing to appraise and nobody to trust.
   * The chain checks whether the expected amount arrived. This is the sovereign
   * case: a state issuing a bond asks its citizens to trust arithmetic rather
   * than a person.
   */
  VERIFY_SCHEDULE: 4,
  UNRECOGNIZED: -1,
} as const;

export type VerificationMode = typeof VerificationMode[keyof typeof VerificationMode];

export namespace VerificationMode {
  export type VERIFICATION_UNSPECIFIED = typeof VerificationMode.VERIFICATION_UNSPECIFIED;
  export type VERIFY_VALUER = typeof VerificationMode.VERIFY_VALUER;
  export type VERIFY_ATTESTORS = typeof VerificationMode.VERIFY_ATTESTORS;
  export type VERIFY_GOVERNANCE = typeof VerificationMode.VERIFY_GOVERNANCE;
  export type VERIFY_SCHEDULE = typeof VerificationMode.VERIFY_SCHEDULE;
  export type UNRECOGNIZED = typeof VerificationMode.UNRECOGNIZED;
}

export function verificationModeFromJSON(object: any): VerificationMode {
  switch (object) {
    case 0:
    case "VERIFICATION_UNSPECIFIED":
      return VerificationMode.VERIFICATION_UNSPECIFIED;
    case 1:
    case "VERIFY_VALUER":
      return VerificationMode.VERIFY_VALUER;
    case 2:
    case "VERIFY_ATTESTORS":
      return VerificationMode.VERIFY_ATTESTORS;
    case 3:
    case "VERIFY_GOVERNANCE":
      return VerificationMode.VERIFY_GOVERNANCE;
    case 4:
    case "VERIFY_SCHEDULE":
      return VerificationMode.VERIFY_SCHEDULE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return VerificationMode.UNRECOGNIZED;
  }
}

export function verificationModeToJSON(object: VerificationMode): string {
  switch (object) {
    case VerificationMode.VERIFICATION_UNSPECIFIED:
      return "VERIFICATION_UNSPECIFIED";
    case VerificationMode.VERIFY_VALUER:
      return "VERIFY_VALUER";
    case VerificationMode.VERIFY_ATTESTORS:
      return "VERIFY_ATTESTORS";
    case VerificationMode.VERIFY_GOVERNANCE:
      return "VERIFY_GOVERNANCE";
    case VerificationMode.VERIFY_SCHEDULE:
      return "VERIFY_SCHEDULE";
    case VerificationMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Status is the vehicle's life. It ends; this is a closed-end instrument, not a
 * perpetual registry.
 */
export const Status = {
  STATUS_UNSPECIFIED: 0,
  /** STATUS_HELD - Title exists, no shareholders yet. */
  STATUS_HELD: 1,
  /** STATUS_ACTIVE - Shareholders exist. Income distributes as it arrives. */
  STATUS_ACTIVE: 2,
  /**
   * STATUS_REPORTED - A sale price has been reported and is inside its challenge window.
   * Redemption is not open yet.
   */
  STATUS_REPORTED: 3,
  /**
   * STATUS_REALISED - The price is final. Proceeds distribute exactly like income -- the sale is
   * not a special case in the accounting, it is the last distribution and it is
   * larger.
   *
   * Transfers of the token stop here. Each token is now a claim on a known
   * fixed pot, and a pool still quoting from its reserves is a free lunch that
   * gets taken within a block.
   */
  STATUS_REALISED: 4,
  /** STATUS_CLOSED - Supply reached zero and the title was burned. */
  STATUS_CLOSED: 5,
  /**
   * STATUS_DISPUTED - A challenge succeeded or the sale fell through. Back to ACTIVE by
   * governance, never automatically.
   */
  STATUS_DISPUTED: 6,
  UNRECOGNIZED: -1,
} as const;

export type Status = typeof Status[keyof typeof Status];

export namespace Status {
  export type STATUS_UNSPECIFIED = typeof Status.STATUS_UNSPECIFIED;
  export type STATUS_HELD = typeof Status.STATUS_HELD;
  export type STATUS_ACTIVE = typeof Status.STATUS_ACTIVE;
  export type STATUS_REPORTED = typeof Status.STATUS_REPORTED;
  export type STATUS_REALISED = typeof Status.STATUS_REALISED;
  export type STATUS_CLOSED = typeof Status.STATUS_CLOSED;
  export type STATUS_DISPUTED = typeof Status.STATUS_DISPUTED;
  export type UNRECOGNIZED = typeof Status.UNRECOGNIZED;
}

export function statusFromJSON(object: any): Status {
  switch (object) {
    case 0:
    case "STATUS_UNSPECIFIED":
      return Status.STATUS_UNSPECIFIED;
    case 1:
    case "STATUS_HELD":
      return Status.STATUS_HELD;
    case 2:
    case "STATUS_ACTIVE":
      return Status.STATUS_ACTIVE;
    case 3:
    case "STATUS_REPORTED":
      return Status.STATUS_REPORTED;
    case 4:
    case "STATUS_REALISED":
      return Status.STATUS_REALISED;
    case 5:
    case "STATUS_CLOSED":
      return Status.STATUS_CLOSED;
    case 6:
    case "STATUS_DISPUTED":
      return Status.STATUS_DISPUTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Status.UNRECOGNIZED;
  }
}

export function statusToJSON(object: Status): string {
  switch (object) {
    case Status.STATUS_UNSPECIFIED:
      return "STATUS_UNSPECIFIED";
    case Status.STATUS_HELD:
      return "STATUS_HELD";
    case Status.STATUS_ACTIVE:
      return "STATUS_ACTIVE";
    case Status.STATUS_REPORTED:
      return "STATUS_REPORTED";
    case Status.STATUS_REALISED:
      return "STATUS_REALISED";
    case Status.STATUS_CLOSED:
      return "STATUS_CLOSED";
    case Status.STATUS_DISPUTED:
      return "STATUS_DISPUTED";
    case Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Collection is a class of real-world asset the chain will record title for:
 * a deeds registry, a warehouse operator's receipts, a vehicle register.
 *
 * Collections are chain-level constructs. There is no permissionless message
 * that creates one, because a registry of deeds is not something a chain grants
 * on request -- governance creates it and appoints who may mint into it.
 */
export interface Collection {
  id: string;
  /**
   * The only address permitted to mint assets into this collection.
   *
   * An unset or revoked authority does not fall back to governance: the
   * collection refuses mints outright. The failure mode of a permissive default
   * here is unlimited issuance of title to things nobody owns.
   */
  authority: string;
  /** How a sale price reported against assets in this collection is checked. */
  verification: VerificationMode;
  /**
   * Attestor threshold when verification is VERIFY_ATTESTORS. Refused below 2:
   * one attestor is not a threshold, it is a single point of unlimited theft.
   */
  attestationThreshold: number;
  /**
   * How long a reported sale price sits before redemption opens. Calibrated to
   * the asset class rather than a chain-wide constant -- a bond's redemption
   * was fixed at issuance and needs days, a unique building needs a month.
   * See docs/guides/tokenisation.md.
   */
  challengeWindowSeconds: string;
  /**
   * Fraction of the reported sale price a challenger must bond, in basis
   * points. Scales with the vehicle rather than being flat: a flat bond is
   * trivial for a large fraud to post and prohibitive for a small holder.
   */
  disputeBondBps: number;
}

/**
 * Asset is title to one real-world thing.
 *
 * Title and shareholding are separate registers. This record says who owns the
 * asset; the tokens say who holds the economic interest. They move
 * independently, so an asset changing operator is a non-event for shareholders.
 */
export interface Asset {
  id: string;
  collectionId: string;
  /**
   * Holder of title. Whoever holds it carries the obligation to pay income into
   * the vault -- an obligation, never a balance they could walk away with.
   */
  owner: string;
  uri: string;
  /**
   * The encumbrance. Non-empty means shareholders exist against this asset.
   *
   * It lives on the asset record rather than in an index built beside it, so
   * there is no path where a stale view sells somebody a warehouse that already
   * has shareholders. That is the failure which costs the chain its credibility
   * as a registry rather than merely costing money.
   */
  fractionDenom: string;
  /**
   * Share of the asset's economics carried by the tokens, in basis points. The
   * sponsor keeps the rest. Fixed at fractionalisation and never editable: an
   * issuer who could revise it would dilute every holder without minting a
   * single token.
   */
  holderShareBps: number;
  status: Status;
  /**
   * The x/land parcel this vehicle is over, or 0 when the asset is not land.
   *
   * Naming the parcel is what makes the registry's authorisation enforceable:
   * fractionalising an asset with a parcel id is refused unless the registry
   * has a live permission for that parcel, the issued share is inside the
   * ceiling it set, and the sponsor is still the parcel's holder. Zero means
   * none of that applies, so a warehouse receipt or a bond is untouched.
   *
   * The parcel is referenced, never moved. Title stays in x/land held by the
   * same account, which is what keeps a fractionalised parcel governed by the
   * registry rather than by whoever accumulated the tokens.
   */
  parcelId: string;
}

/**
 * Vault holds what is owed to shareholders and the running index that says how
 * much each token has earned.
 */
export interface Vault {
  assetId: string;
  /**
   * Income per token, accumulated over the vehicle's life, scaled by 1e18.
   *
   * An index rather than a snapshot because a snapshot is snipeable: buy from
   * the pool at H-1, be holder of record at H, sell at H+1, and collect a
   * quarter's rent for two blocks of exposure with no capital at risk. An index
   * is time-weighted, so two blocks earn two blocks.
   *
   * It also removes a halt: there is no holder set to iterate.
   */
  cumulativePerToken: string;
  /** Total ever paid in, for the solvency view. */
  funded: Coin[];
  /** The denomination income arrives in. */
  denom: string;
}

/**
 * Position is one account's place in the index.
 *
 * Settled whenever the balance moves, so what is owed is always
 * balance * (cumulative_per_token - last_index).
 */
export interface Position {
  assetId: string;
  holder: string;
  lastIndex: string;
  /** Settled but not yet withdrawn. */
  accrued: string;
}

/** SaleReport is a reported price inside or past its challenge window. */
export interface SaleReport {
  assetId: string;
  price: Coin | undefined;
  reporter: string;
  reportedAt:
    | Date
    | undefined;
  /** Opens redemption once passed, absent a dispute. */
  claimableAt:
    | Date
    | undefined;
  /** Attestors who have signed this figure, when the mode requires them. */
  attestors: string[];
  disputed: boolean;
}

function createBaseCollection(): Collection {
  return {
    id: "",
    authority: "",
    verification: 0,
    attestationThreshold: 0,
    challengeWindowSeconds: "0",
    disputeBondBps: 0,
  };
}

export const Collection = {
  encode(message: Collection, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.authority !== "") {
      writer.uint32(18).string(message.authority);
    }
    if (message.verification !== 0) {
      writer.uint32(24).int32(message.verification);
    }
    if (message.attestationThreshold !== 0) {
      writer.uint32(32).uint32(message.attestationThreshold);
    }
    if (message.challengeWindowSeconds !== "0") {
      writer.uint32(40).int64(message.challengeWindowSeconds);
    }
    if (message.disputeBondBps !== 0) {
      writer.uint32(48).uint32(message.disputeBondBps);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Collection {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.verification = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.attestationThreshold = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.challengeWindowSeconds = longToString(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.disputeBondBps = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Collection {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      verification: isSet(object.verification) ? verificationModeFromJSON(object.verification) : 0,
      attestationThreshold: isSet(object.attestationThreshold) ? globalThis.Number(object.attestationThreshold) : 0,
      challengeWindowSeconds: isSet(object.challengeWindowSeconds)
        ? globalThis.String(object.challengeWindowSeconds)
        : "0",
      disputeBondBps: isSet(object.disputeBondBps) ? globalThis.Number(object.disputeBondBps) : 0,
    };
  },

  toJSON(message: Collection): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.verification !== 0) {
      obj.verification = verificationModeToJSON(message.verification);
    }
    if (message.attestationThreshold !== 0) {
      obj.attestationThreshold = Math.round(message.attestationThreshold);
    }
    if (message.challengeWindowSeconds !== "0") {
      obj.challengeWindowSeconds = message.challengeWindowSeconds;
    }
    if (message.disputeBondBps !== 0) {
      obj.disputeBondBps = Math.round(message.disputeBondBps);
    }
    return obj;
  },

  create(base?: DeepPartial<Collection>): Collection {
    return Collection.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Collection>): Collection {
    const message = createBaseCollection();
    message.id = object.id ?? "";
    message.authority = object.authority ?? "";
    message.verification = object.verification ?? 0;
    message.attestationThreshold = object.attestationThreshold ?? 0;
    message.challengeWindowSeconds = object.challengeWindowSeconds ?? "0";
    message.disputeBondBps = object.disputeBondBps ?? 0;
    return message;
  },
};

function createBaseAsset(): Asset {
  return {
    id: "0",
    collectionId: "",
    owner: "",
    uri: "",
    fractionDenom: "",
    holderShareBps: 0,
    status: 0,
    parcelId: "0",
  };
}

export const Asset = {
  encode(message: Asset, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.collectionId !== "") {
      writer.uint32(18).string(message.collectionId);
    }
    if (message.owner !== "") {
      writer.uint32(26).string(message.owner);
    }
    if (message.uri !== "") {
      writer.uint32(34).string(message.uri);
    }
    if (message.fractionDenom !== "") {
      writer.uint32(42).string(message.fractionDenom);
    }
    if (message.holderShareBps !== 0) {
      writer.uint32(48).uint32(message.holderShareBps);
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    if (message.parcelId !== "0") {
      writer.uint32(64).uint64(message.parcelId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Asset {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAsset();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.collectionId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.owner = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.uri = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.fractionDenom = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.holderShareBps = reader.uint32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.parcelId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Asset {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      collectionId: isSet(object.collectionId) ? globalThis.String(object.collectionId) : "",
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      fractionDenom: isSet(object.fractionDenom) ? globalThis.String(object.fractionDenom) : "",
      holderShareBps: isSet(object.holderShareBps) ? globalThis.Number(object.holderShareBps) : 0,
      status: isSet(object.status) ? statusFromJSON(object.status) : 0,
      parcelId: isSet(object.parcelId) ? globalThis.String(object.parcelId) : "0",
    };
  },

  toJSON(message: Asset): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.collectionId !== "") {
      obj.collectionId = message.collectionId;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.fractionDenom !== "") {
      obj.fractionDenom = message.fractionDenom;
    }
    if (message.holderShareBps !== 0) {
      obj.holderShareBps = Math.round(message.holderShareBps);
    }
    if (message.status !== 0) {
      obj.status = statusToJSON(message.status);
    }
    if (message.parcelId !== "0") {
      obj.parcelId = message.parcelId;
    }
    return obj;
  },

  create(base?: DeepPartial<Asset>): Asset {
    return Asset.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Asset>): Asset {
    const message = createBaseAsset();
    message.id = object.id ?? "0";
    message.collectionId = object.collectionId ?? "";
    message.owner = object.owner ?? "";
    message.uri = object.uri ?? "";
    message.fractionDenom = object.fractionDenom ?? "";
    message.holderShareBps = object.holderShareBps ?? 0;
    message.status = object.status ?? 0;
    message.parcelId = object.parcelId ?? "0";
    return message;
  },
};

function createBaseVault(): Vault {
  return { assetId: "0", cumulativePerToken: "", funded: [], denom: "" };
}

export const Vault = {
  encode(message: Vault, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.assetId !== "0") {
      writer.uint32(8).uint64(message.assetId);
    }
    if (message.cumulativePerToken !== "") {
      writer.uint32(18).string(message.cumulativePerToken);
    }
    for (const v of message.funded) {
      Coin.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.denom !== "") {
      writer.uint32(34).string(message.denom);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Vault {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVault();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.assetId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.cumulativePerToken = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.funded.push(Coin.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.denom = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Vault {
    return {
      assetId: isSet(object.assetId) ? globalThis.String(object.assetId) : "0",
      cumulativePerToken: isSet(object.cumulativePerToken) ? globalThis.String(object.cumulativePerToken) : "",
      funded: globalThis.Array.isArray(object?.funded) ? object.funded.map((e: any) => Coin.fromJSON(e)) : [],
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
    };
  },

  toJSON(message: Vault): unknown {
    const obj: any = {};
    if (message.assetId !== "0") {
      obj.assetId = message.assetId;
    }
    if (message.cumulativePerToken !== "") {
      obj.cumulativePerToken = message.cumulativePerToken;
    }
    if (message.funded?.length) {
      obj.funded = message.funded.map((e) => Coin.toJSON(e));
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    return obj;
  },

  create(base?: DeepPartial<Vault>): Vault {
    return Vault.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Vault>): Vault {
    const message = createBaseVault();
    message.assetId = object.assetId ?? "0";
    message.cumulativePerToken = object.cumulativePerToken ?? "";
    message.funded = object.funded?.map((e) => Coin.fromPartial(e)) || [];
    message.denom = object.denom ?? "";
    return message;
  },
};

function createBasePosition(): Position {
  return { assetId: "0", holder: "", lastIndex: "", accrued: "" };
}

export const Position = {
  encode(message: Position, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.assetId !== "0") {
      writer.uint32(8).uint64(message.assetId);
    }
    if (message.holder !== "") {
      writer.uint32(18).string(message.holder);
    }
    if (message.lastIndex !== "") {
      writer.uint32(26).string(message.lastIndex);
    }
    if (message.accrued !== "") {
      writer.uint32(34).string(message.accrued);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Position {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePosition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.assetId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.holder = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.lastIndex = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.accrued = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Position {
    return {
      assetId: isSet(object.assetId) ? globalThis.String(object.assetId) : "0",
      holder: isSet(object.holder) ? globalThis.String(object.holder) : "",
      lastIndex: isSet(object.lastIndex) ? globalThis.String(object.lastIndex) : "",
      accrued: isSet(object.accrued) ? globalThis.String(object.accrued) : "",
    };
  },

  toJSON(message: Position): unknown {
    const obj: any = {};
    if (message.assetId !== "0") {
      obj.assetId = message.assetId;
    }
    if (message.holder !== "") {
      obj.holder = message.holder;
    }
    if (message.lastIndex !== "") {
      obj.lastIndex = message.lastIndex;
    }
    if (message.accrued !== "") {
      obj.accrued = message.accrued;
    }
    return obj;
  },

  create(base?: DeepPartial<Position>): Position {
    return Position.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Position>): Position {
    const message = createBasePosition();
    message.assetId = object.assetId ?? "0";
    message.holder = object.holder ?? "";
    message.lastIndex = object.lastIndex ?? "";
    message.accrued = object.accrued ?? "";
    return message;
  },
};

function createBaseSaleReport(): SaleReport {
  return {
    assetId: "0",
    price: undefined,
    reporter: "",
    reportedAt: undefined,
    claimableAt: undefined,
    attestors: [],
    disputed: false,
  };
}

export const SaleReport = {
  encode(message: SaleReport, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.assetId !== "0") {
      writer.uint32(8).uint64(message.assetId);
    }
    if (message.price !== undefined) {
      Coin.encode(message.price, writer.uint32(18).fork()).ldelim();
    }
    if (message.reporter !== "") {
      writer.uint32(26).string(message.reporter);
    }
    if (message.reportedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.reportedAt), writer.uint32(34).fork()).ldelim();
    }
    if (message.claimableAt !== undefined) {
      Timestamp.encode(toTimestamp(message.claimableAt), writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.attestors) {
      writer.uint32(50).string(v!);
    }
    if (message.disputed !== false) {
      writer.uint32(56).bool(message.disputed);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SaleReport {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaleReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.assetId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.price = Coin.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.reporter = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.reportedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.claimableAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.attestors.push(reader.string());
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.disputed = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SaleReport {
    return {
      assetId: isSet(object.assetId) ? globalThis.String(object.assetId) : "0",
      price: isSet(object.price) ? Coin.fromJSON(object.price) : undefined,
      reporter: isSet(object.reporter) ? globalThis.String(object.reporter) : "",
      reportedAt: isSet(object.reportedAt) ? fromJsonTimestamp(object.reportedAt) : undefined,
      claimableAt: isSet(object.claimableAt) ? fromJsonTimestamp(object.claimableAt) : undefined,
      attestors: globalThis.Array.isArray(object?.attestors)
        ? object.attestors.map((e: any) => globalThis.String(e))
        : [],
      disputed: isSet(object.disputed) ? globalThis.Boolean(object.disputed) : false,
    };
  },

  toJSON(message: SaleReport): unknown {
    const obj: any = {};
    if (message.assetId !== "0") {
      obj.assetId = message.assetId;
    }
    if (message.price !== undefined) {
      obj.price = Coin.toJSON(message.price);
    }
    if (message.reporter !== "") {
      obj.reporter = message.reporter;
    }
    if (message.reportedAt !== undefined) {
      obj.reportedAt = message.reportedAt.toISOString();
    }
    if (message.claimableAt !== undefined) {
      obj.claimableAt = message.claimableAt.toISOString();
    }
    if (message.attestors?.length) {
      obj.attestors = message.attestors;
    }
    if (message.disputed !== false) {
      obj.disputed = message.disputed;
    }
    return obj;
  },

  create(base?: DeepPartial<SaleReport>): SaleReport {
    return SaleReport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SaleReport>): SaleReport {
    const message = createBaseSaleReport();
    message.assetId = object.assetId ?? "0";
    message.price = (object.price !== undefined && object.price !== null) ? Coin.fromPartial(object.price) : undefined;
    message.reporter = object.reporter ?? "";
    message.reportedAt = object.reportedAt ?? undefined;
    message.claimableAt = object.claimableAt ?? undefined;
    message.attestors = object.attestors?.map((e) => e) || [];
    message.disputed = object.disputed ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
