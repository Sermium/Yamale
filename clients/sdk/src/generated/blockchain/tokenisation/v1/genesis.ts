// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/tokenisation/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Params } from "./params.ts";
import { Asset, Collection, Position, SaleReport, Vault } from "./tokenisation.ts";

export const protobufPackage = "blockchain.tokenisation.v1";

export interface GenesisState {
  params: Params | undefined;
  collections: Collection[];
  assets: Asset[];
  vaults: Vault[];
  positions: Position[];
  saleReports: SaleReport[];
  /**
   * Explicit, because derived state must match genesis byte for byte. A
   * counter that recomputes itself from the asset list writes zero where
   * InitGenesis wrote nothing, and the export stops round-tripping.
   */
  nextAssetId: string;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    collections: [],
    assets: [],
    vaults: [],
    positions: [],
    saleReports: [],
    nextAssetId: "0",
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.collections) {
      Collection.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.assets) {
      Asset.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.vaults) {
      Vault.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.positions) {
      Position.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.saleReports) {
      SaleReport.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    if (message.nextAssetId !== "0") {
      writer.uint32(56).uint64(message.nextAssetId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.collections.push(Collection.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.assets.push(Asset.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.vaults.push(Vault.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.positions.push(Position.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.saleReports.push(SaleReport.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.nextAssetId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      collections: globalThis.Array.isArray(object?.collections)
        ? object.collections.map((e: any) => Collection.fromJSON(e))
        : [],
      assets: globalThis.Array.isArray(object?.assets) ? object.assets.map((e: any) => Asset.fromJSON(e)) : [],
      vaults: globalThis.Array.isArray(object?.vaults) ? object.vaults.map((e: any) => Vault.fromJSON(e)) : [],
      positions: globalThis.Array.isArray(object?.positions)
        ? object.positions.map((e: any) => Position.fromJSON(e))
        : [],
      saleReports: globalThis.Array.isArray(object?.saleReports)
        ? object.saleReports.map((e: any) => SaleReport.fromJSON(e))
        : [],
      nextAssetId: isSet(object.nextAssetId) ? globalThis.String(object.nextAssetId) : "0",
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.collections?.length) {
      obj.collections = message.collections.map((e) => Collection.toJSON(e));
    }
    if (message.assets?.length) {
      obj.assets = message.assets.map((e) => Asset.toJSON(e));
    }
    if (message.vaults?.length) {
      obj.vaults = message.vaults.map((e) => Vault.toJSON(e));
    }
    if (message.positions?.length) {
      obj.positions = message.positions.map((e) => Position.toJSON(e));
    }
    if (message.saleReports?.length) {
      obj.saleReports = message.saleReports.map((e) => SaleReport.toJSON(e));
    }
    if (message.nextAssetId !== "0") {
      obj.nextAssetId = message.nextAssetId;
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.collections = object.collections?.map((e) => Collection.fromPartial(e)) || [];
    message.assets = object.assets?.map((e) => Asset.fromPartial(e)) || [];
    message.vaults = object.vaults?.map((e) => Vault.fromPartial(e)) || [];
    message.positions = object.positions?.map((e) => Position.fromPartial(e)) || [];
    message.saleReports = object.saleReports?.map((e) => SaleReport.fromPartial(e)) || [];
    message.nextAssetId = object.nextAssetId ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
