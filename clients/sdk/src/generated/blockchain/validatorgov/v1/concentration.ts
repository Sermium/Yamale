// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/validatorgov/v1/concentration.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.validatorgov.v1";

/**
 * ConcentrationCap is which ceiling a demotion was for. Recorded rather than
 * inferred, because "over the entity cap" and "over the jurisdiction cap" are
 * different facts about the same validator and the remedy is different for
 * each: one entity can be restructured, a jurisdiction cannot.
 */
export const ConcentrationCap = {
  /** CONCENTRATION_CAP_UNSPECIFIED - CONCENTRATION_CAP_UNSPECIFIED is the unset default and is never valid. */
  CONCENTRATION_CAP_UNSPECIFIED: 0,
  /** CONCENTRATION_CAP_ENTITY - CONCENTRATION_CAP_ENTITY is the ceiling on one declared legal entity. */
  CONCENTRATION_CAP_ENTITY: 1,
  /**
   * CONCENTRATION_CAP_BENEFICIAL_OWNER - CONCENTRATION_CAP_BENEFICIAL_OWNER is the ceiling on one declared ultimate
   * beneficial owner, across every entity it owns.
   */
  CONCENTRATION_CAP_BENEFICIAL_OWNER: 2,
  /**
   * CONCENTRATION_CAP_JURISDICTION - CONCENTRATION_CAP_JURISDICTION is the ceiling on the power answering to one
   * national authority.
   */
  CONCENTRATION_CAP_JURISDICTION: 3,
  UNRECOGNIZED: -1,
} as const;

export type ConcentrationCap = typeof ConcentrationCap[keyof typeof ConcentrationCap];

export namespace ConcentrationCap {
  export type CONCENTRATION_CAP_UNSPECIFIED = typeof ConcentrationCap.CONCENTRATION_CAP_UNSPECIFIED;
  export type CONCENTRATION_CAP_ENTITY = typeof ConcentrationCap.CONCENTRATION_CAP_ENTITY;
  export type CONCENTRATION_CAP_BENEFICIAL_OWNER = typeof ConcentrationCap.CONCENTRATION_CAP_BENEFICIAL_OWNER;
  export type CONCENTRATION_CAP_JURISDICTION = typeof ConcentrationCap.CONCENTRATION_CAP_JURISDICTION;
  export type UNRECOGNIZED = typeof ConcentrationCap.UNRECOGNIZED;
}

export function concentrationCapFromJSON(object: any): ConcentrationCap {
  switch (object) {
    case 0:
    case "CONCENTRATION_CAP_UNSPECIFIED":
      return ConcentrationCap.CONCENTRATION_CAP_UNSPECIFIED;
    case 1:
    case "CONCENTRATION_CAP_ENTITY":
      return ConcentrationCap.CONCENTRATION_CAP_ENTITY;
    case 2:
    case "CONCENTRATION_CAP_BENEFICIAL_OWNER":
      return ConcentrationCap.CONCENTRATION_CAP_BENEFICIAL_OWNER;
    case 3:
    case "CONCENTRATION_CAP_JURISDICTION":
      return ConcentrationCap.CONCENTRATION_CAP_JURISDICTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConcentrationCap.UNRECOGNIZED;
  }
}

export function concentrationCapToJSON(object: ConcentrationCap): string {
  switch (object) {
    case ConcentrationCap.CONCENTRATION_CAP_UNSPECIFIED:
      return "CONCENTRATION_CAP_UNSPECIFIED";
    case ConcentrationCap.CONCENTRATION_CAP_ENTITY:
      return "CONCENTRATION_CAP_ENTITY";
    case ConcentrationCap.CONCENTRATION_CAP_BENEFICIAL_OWNER:
      return "CONCENTRATION_CAP_BENEFICIAL_OWNER";
    case ConcentrationCap.CONCENTRATION_CAP_JURISDICTION:
      return "CONCENTRATION_CAP_JURISDICTION";
    case ConcentrationCap.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Demotion is a validator whose power the epoch check took away, and why.
 *
 * It is a record and not a punishment. Nothing is slashed, no delegation is
 * unbonded, and it is undone automatically at the first epoch the breach has
 * cleared — so the entity's remedy is in its own hands and does not require
 * anybody to vote on letting it back. That is the difference between a
 * concentration ceiling and an expulsion.
 */
export interface Demotion {
  /**
   * operator is the address in its account form, matching how the approval
   * allowlist and the rotation records key a validator.
   */
  operator: string;
  cap: ConcentrationCap;
  /**
   * group is the declared value that breached — the entity id, the owner id or
   * the country code — so the record says which set the validator was counted
   * in rather than only that some ceiling was hit.
   */
  group: string;
  /**
   * group_power_bps and cap_bps are what the group held and what it was allowed
   * to hold, at the epoch the demotion happened. Frozen into the record because
   * both figures are recomputed every epoch, and a demotion that could only be
   * explained by numbers that have since moved cannot be audited at all.
   */
  groupPowerBps: string;
  capBps: string;
  demotedAtHeight: string;
  /**
   * jailed_validator records that this demotion is what jailed the validator,
   * so that restoring it un-jails only what it jailed. A validator already
   * jailed for downtime when the epoch check ran must stay jailed when the
   * breach clears: releasing it would turn a concentration ceiling into a way
   * of clearing somebody else's downtime.
   */
  jailedValidator: boolean;
}

/**
 * ConcentrationGroup is one entity, owner or jurisdiction and what it currently
 * holds. It exists for the supervisor's query rather than for the chain: under
 * equal seats a ceiling is a count out of a count, and the point of publishing
 * it this way is that it can be checked against a list by somebody who is not
 * recomputing anything.
 */
export interface ConcentrationGroup {
  cap: ConcentrationCap;
  group: string;
  power: string;
  powerBps: string;
  capBps: string;
  /**
   * over is whether the group is above its ceiling right now, which is not the
   * same as whether anything has been demoted: a breach the set is too small to
   * correct stays reported and uncorrected, because a cap must never be the
   * reason a chain stops producing blocks.
   */
  over: boolean;
}

function createBaseDemotion(): Demotion {
  return {
    operator: "",
    cap: 0,
    group: "",
    groupPowerBps: "0",
    capBps: "0",
    demotedAtHeight: "0",
    jailedValidator: false,
  };
}

export const Demotion = {
  encode(message: Demotion, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.operator !== "") {
      writer.uint32(10).string(message.operator);
    }
    if (message.cap !== 0) {
      writer.uint32(16).int32(message.cap);
    }
    if (message.group !== "") {
      writer.uint32(26).string(message.group);
    }
    if (message.groupPowerBps !== "0") {
      writer.uint32(32).uint64(message.groupPowerBps);
    }
    if (message.capBps !== "0") {
      writer.uint32(40).uint64(message.capBps);
    }
    if (message.demotedAtHeight !== "0") {
      writer.uint32(48).int64(message.demotedAtHeight);
    }
    if (message.jailedValidator !== false) {
      writer.uint32(56).bool(message.jailedValidator);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Demotion {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDemotion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.operator = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.cap = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.group = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.groupPowerBps = longToString(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.capBps = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.demotedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.jailedValidator = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Demotion {
    return {
      operator: isSet(object.operator) ? globalThis.String(object.operator) : "",
      cap: isSet(object.cap) ? concentrationCapFromJSON(object.cap) : 0,
      group: isSet(object.group) ? globalThis.String(object.group) : "",
      groupPowerBps: isSet(object.groupPowerBps) ? globalThis.String(object.groupPowerBps) : "0",
      capBps: isSet(object.capBps) ? globalThis.String(object.capBps) : "0",
      demotedAtHeight: isSet(object.demotedAtHeight) ? globalThis.String(object.demotedAtHeight) : "0",
      jailedValidator: isSet(object.jailedValidator) ? globalThis.Boolean(object.jailedValidator) : false,
    };
  },

  toJSON(message: Demotion): unknown {
    const obj: any = {};
    if (message.operator !== "") {
      obj.operator = message.operator;
    }
    if (message.cap !== 0) {
      obj.cap = concentrationCapToJSON(message.cap);
    }
    if (message.group !== "") {
      obj.group = message.group;
    }
    if (message.groupPowerBps !== "0") {
      obj.groupPowerBps = message.groupPowerBps;
    }
    if (message.capBps !== "0") {
      obj.capBps = message.capBps;
    }
    if (message.demotedAtHeight !== "0") {
      obj.demotedAtHeight = message.demotedAtHeight;
    }
    if (message.jailedValidator !== false) {
      obj.jailedValidator = message.jailedValidator;
    }
    return obj;
  },

  create(base?: DeepPartial<Demotion>): Demotion {
    return Demotion.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Demotion>): Demotion {
    const message = createBaseDemotion();
    message.operator = object.operator ?? "";
    message.cap = object.cap ?? 0;
    message.group = object.group ?? "";
    message.groupPowerBps = object.groupPowerBps ?? "0";
    message.capBps = object.capBps ?? "0";
    message.demotedAtHeight = object.demotedAtHeight ?? "0";
    message.jailedValidator = object.jailedValidator ?? false;
    return message;
  },
};

function createBaseConcentrationGroup(): ConcentrationGroup {
  return { cap: 0, group: "", power: "0", powerBps: "0", capBps: "0", over: false };
}

export const ConcentrationGroup = {
  encode(message: ConcentrationGroup, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.cap !== 0) {
      writer.uint32(8).int32(message.cap);
    }
    if (message.group !== "") {
      writer.uint32(18).string(message.group);
    }
    if (message.power !== "0") {
      writer.uint32(24).int64(message.power);
    }
    if (message.powerBps !== "0") {
      writer.uint32(32).uint64(message.powerBps);
    }
    if (message.capBps !== "0") {
      writer.uint32(40).uint64(message.capBps);
    }
    if (message.over !== false) {
      writer.uint32(48).bool(message.over);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ConcentrationGroup {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConcentrationGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.cap = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.group = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.power = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.powerBps = longToString(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.capBps = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.over = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConcentrationGroup {
    return {
      cap: isSet(object.cap) ? concentrationCapFromJSON(object.cap) : 0,
      group: isSet(object.group) ? globalThis.String(object.group) : "",
      power: isSet(object.power) ? globalThis.String(object.power) : "0",
      powerBps: isSet(object.powerBps) ? globalThis.String(object.powerBps) : "0",
      capBps: isSet(object.capBps) ? globalThis.String(object.capBps) : "0",
      over: isSet(object.over) ? globalThis.Boolean(object.over) : false,
    };
  },

  toJSON(message: ConcentrationGroup): unknown {
    const obj: any = {};
    if (message.cap !== 0) {
      obj.cap = concentrationCapToJSON(message.cap);
    }
    if (message.group !== "") {
      obj.group = message.group;
    }
    if (message.power !== "0") {
      obj.power = message.power;
    }
    if (message.powerBps !== "0") {
      obj.powerBps = message.powerBps;
    }
    if (message.capBps !== "0") {
      obj.capBps = message.capBps;
    }
    if (message.over !== false) {
      obj.over = message.over;
    }
    return obj;
  },

  create(base?: DeepPartial<ConcentrationGroup>): ConcentrationGroup {
    return ConcentrationGroup.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConcentrationGroup>): ConcentrationGroup {
    const message = createBaseConcentrationGroup();
    message.cap = object.cap ?? 0;
    message.group = object.group ?? "";
    message.power = object.power ?? "0";
    message.powerBps = object.powerBps ?? "0";
    message.capBps = object.capBps ?? "0";
    message.over = object.over ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
