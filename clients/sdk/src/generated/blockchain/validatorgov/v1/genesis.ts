// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/validatorgov/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { ApprovedValidator } from "./approved_validator.ts";
import { Params } from "./params.ts";
import { OperatorRotation } from "./rotation.ts";
import { ValidatorApplication } from "./validator_application.ts";

export const protobufPackage = "blockchain.validatorgov.v1";

/** GenesisState defines the validatorgov module's genesis state. */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params: Params | undefined;
  validatorApplicationMap: ValidatorApplication[];
  approvedValidatorMap: ApprovedValidator[];
  /**
   * operator_rotations carries every rotation ever opened, pending ones
   * included. A chain that dropped a pending rotation on export would complete
   * it silently at the next upgrade, or lose a challenge window that delegators
   * were relying on — at exactly the moment nobody is watching.
   */
  operatorRotations: OperatorRotation[];
  /**
   * rotation_count is the next id to hand out. Exported explicitly rather than
   * rebuilt from the highest rotation present, so that a genesis edited by hand
   * says what it means and importing it cannot collide.
   */
  rotationCount: string;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    validatorApplicationMap: [],
    approvedValidatorMap: [],
    operatorRotations: [],
    rotationCount: "0",
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.validatorApplicationMap) {
      ValidatorApplication.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.approvedValidatorMap) {
      ApprovedValidator.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.operatorRotations) {
      OperatorRotation.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message.rotationCount !== "0") {
      writer.uint32(40).uint64(message.rotationCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.validatorApplicationMap.push(ValidatorApplication.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.approvedValidatorMap.push(ApprovedValidator.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.operatorRotations.push(OperatorRotation.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.rotationCount = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      validatorApplicationMap: globalThis.Array.isArray(object?.validatorApplicationMap)
        ? object.validatorApplicationMap.map((e: any) => ValidatorApplication.fromJSON(e))
        : [],
      approvedValidatorMap: globalThis.Array.isArray(object?.approvedValidatorMap)
        ? object.approvedValidatorMap.map((e: any) => ApprovedValidator.fromJSON(e))
        : [],
      operatorRotations: globalThis.Array.isArray(object?.operatorRotations)
        ? object.operatorRotations.map((e: any) => OperatorRotation.fromJSON(e))
        : [],
      rotationCount: isSet(object.rotationCount) ? globalThis.String(object.rotationCount) : "0",
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.validatorApplicationMap?.length) {
      obj.validatorApplicationMap = message.validatorApplicationMap.map((e) => ValidatorApplication.toJSON(e));
    }
    if (message.approvedValidatorMap?.length) {
      obj.approvedValidatorMap = message.approvedValidatorMap.map((e) => ApprovedValidator.toJSON(e));
    }
    if (message.operatorRotations?.length) {
      obj.operatorRotations = message.operatorRotations.map((e) => OperatorRotation.toJSON(e));
    }
    if (message.rotationCount !== "0") {
      obj.rotationCount = message.rotationCount;
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.validatorApplicationMap = object.validatorApplicationMap?.map((e) => ValidatorApplication.fromPartial(e)) ||
      [];
    message.approvedValidatorMap = object.approvedValidatorMap?.map((e) => ApprovedValidator.fromPartial(e)) || [];
    message.operatorRotations = object.operatorRotations?.map((e) => OperatorRotation.fromPartial(e)) || [];
    message.rotationCount = object.rotationCount ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
