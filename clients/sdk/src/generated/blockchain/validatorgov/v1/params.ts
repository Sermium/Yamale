// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/validatorgov/v1/params.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.validatorgov.v1";

/** Params defines the parameters for the module. */
export interface Params {
  /**
   * planned_rotation_delay_blocks is how long a rotation signed by the current
   * operator waits before it takes effect. Short on purpose: whoever submitted
   * it already holds the key, so the delay is not there to prove anything, only
   * to give delegators and the other validators a block window in which to see
   * it happen.
   */
  plannedRotationDelayBlocks: string;
  /**
   * recovery_challenge_window_blocks is how long an approved recovery waits
   * before it takes effect, during which the validator is paused and the
   * current operator can end it by signing anything at all. Measured in days,
   * because the thing it is defending against is somebody who does not hold
   * the key claiming that nobody does.
   */
  recoveryChallengeWindowBlocks: string;
}

function createBaseParams(): Params {
  return { plannedRotationDelayBlocks: "0", recoveryChallengeWindowBlocks: "0" };
}

export const Params = {
  encode(message: Params, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.plannedRotationDelayBlocks !== "0") {
      writer.uint32(8).uint64(message.plannedRotationDelayBlocks);
    }
    if (message.recoveryChallengeWindowBlocks !== "0") {
      writer.uint32(16).uint64(message.recoveryChallengeWindowBlocks);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Params {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.plannedRotationDelayBlocks = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.recoveryChallengeWindowBlocks = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      plannedRotationDelayBlocks: isSet(object.plannedRotationDelayBlocks)
        ? globalThis.String(object.plannedRotationDelayBlocks)
        : "0",
      recoveryChallengeWindowBlocks: isSet(object.recoveryChallengeWindowBlocks)
        ? globalThis.String(object.recoveryChallengeWindowBlocks)
        : "0",
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.plannedRotationDelayBlocks !== "0") {
      obj.plannedRotationDelayBlocks = message.plannedRotationDelayBlocks;
    }
    if (message.recoveryChallengeWindowBlocks !== "0") {
      obj.recoveryChallengeWindowBlocks = message.recoveryChallengeWindowBlocks;
    }
    return obj;
  },

  create(base?: DeepPartial<Params>): Params {
    return Params.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.plannedRotationDelayBlocks = object.plannedRotationDelayBlocks ?? "0";
    message.recoveryChallengeWindowBlocks = object.recoveryChallengeWindowBlocks ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
