// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/validatorgov/v1/tx.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.validatorgov.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /** NOTE: All parameters must be supplied. */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/**
 * MsgApplyValidator defines the MsgApplyValidator message.
 *
 * The three declaration fields are required, and requiring them here rather
 * than collecting them later is the point: an admission vote that cannot see
 * the owner and the jurisdiction behind a candidate is a vote on a moniker, and
 * a set admitted without them can never have a concentration ceiling computed
 * over it at all.
 */
export interface MsgApplyValidator {
  creator: string;
  moniker: string;
  description: string;
  /** legal_entity_id identifies the applying entity. */
  legalEntityId: string;
  /**
   * beneficial_owner_id identifies whoever ultimately owns it. Where an entity
   * has no owner above it, this is the entity's own identifier — stated rather
   * than left blank, so that "nobody owns us" is a claim somebody signed.
   */
  beneficialOwnerId: string;
  /** jurisdiction is an ISO 3166-1 alpha-2 code from the assigned list. */
  jurisdiction: string;
}

/** MsgApplyValidatorResponse defines the MsgApplyValidatorResponse message. */
export interface MsgApplyValidatorResponse {
}

/** MsgApproveValidator is the Msg/ApproveValidator request type. */
export interface MsgApproveValidator {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  candidate: string;
  approve: boolean;
}

/** MsgApproveValidatorResponse defines the MsgApproveValidatorResponse message. */
export interface MsgApproveValidatorResponse {
}

/**
 * MsgRotateOperator moves a validator's operator key, signed by the operator
 * being replaced.
 *
 * There is nothing to prove here beyond the signature. Anyone who can submit
 * this already controls everything the operator address protects, so demanding
 * a vote for it would only push operators towards waiting for a loss — and the
 * path for a loss is the slow one.
 */
export interface MsgRotateOperator {
  /** creator is the current operator address. */
  creator: string;
  /** new_operator is the address that takes over after the delay. */
  newOperator: string;
}

/** MsgRotateOperatorResponse returns the id of the rotation that was opened. */
export interface MsgRotateOperatorResponse {
  rotationId: string;
}

/**
 * MsgProposeOperatorRecovery opens a claim that an operator's key is gone.
 *
 * Anybody may submit it, because the person best placed to notice is rarely the
 * person who lost the key. It does nothing on its own: the validator is not
 * paused and no clock starts until the admission quorum approves it, or a
 * single transaction from anybody would be enough to pause any validator on
 * the chain.
 */
export interface MsgProposeOperatorRecovery {
  /** creator is whoever noticed, which is anybody. */
  creator: string;
  /** current_operator is the operator address whose key is claimed to be lost. */
  currentOperator: string;
  /** new_operator is the address proposed to take over. */
  newOperator: string;
  /**
   * reason is the stated grounds, in plain words. Required: a claim that
   * somebody's key is gone should have to say how that is known.
   */
  reason: string;
}

/**
 * MsgProposeOperatorRecoveryResponse returns the id of the rotation that was
 * opened.
 */
export interface MsgProposeOperatorRecoveryResponse {
  rotationId: string;
}

/**
 * MsgApproveOperatorRecovery is the admission quorum's decision on a recovery.
 * Approving it pauses the validator and starts the challenge window; refusing
 * it closes the rotation.
 */
export interface MsgApproveOperatorRecovery {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /** rotation_id is the recovery being decided. */
  rotationId: string;
  /** approve is the decision. */
  approve: boolean;
}

/**
 * MsgApproveOperatorRecoveryResponse defines the response structure for
 * executing a MsgApproveOperatorRecovery message.
 */
export interface MsgApproveOperatorRecoveryResponse {
}

/**
 * MsgCancelOperatorRotation withdraws a rotation before it takes effect,
 * signed by the operator being replaced.
 *
 * On a recovery this is redundant with the veto — signing anything at all ends
 * it — and it is offered anyway so that an operator who wants to object
 * explicitly has something to sign that says so, rather than having to
 * discover that any transaction would have done.
 */
export interface MsgCancelOperatorRotation {
  /** creator is the current operator address. */
  creator: string;
  /** rotation_id is the rotation being withdrawn. */
  rotationId: string;
}

/**
 * MsgCancelOperatorRotationResponse defines the response structure for
 * executing a MsgCancelOperatorRotation message.
 */
export interface MsgCancelOperatorRotationResponse {
}

/**
 * MsgAttestOwnership is the operator re-signing for its own declaration.
 *
 * It carries the whole declaration rather than only a timestamp, because the
 * event it exists to catch is an ownership change: an operator whose owner
 * changed and who re-attested the old values has made a false statement on the
 * record with its own key, which is a fact a supervisor can act on. A bare
 * heartbeat would have let the same operator keep a stale declaration fresh
 * without ever restating it.
 */
export interface MsgAttestOwnership {
  /** creator is the approved operator address, in its account form. */
  creator: string;
  legalEntityId: string;
  beneficialOwnerId: string;
  jurisdiction: string;
}

/**
 * MsgAttestOwnershipResponse defines the response structure for executing a
 * MsgAttestOwnership message.
 */
export interface MsgAttestOwnershipResponse {
}

/**
 * MsgSetValidatorPower sets how many seats an admitted validator holds.
 *
 * Equal seats is the default a genesis is built with, not a constant the chain
 * enforces: admission is already a governance decision, so governance decides
 * weight directly rather than leaving it to whoever bonded the most. This
 * message is how it does that.
 *
 * It deliberately does not check the concentration ceilings. A power set above
 * a cap is accepted here and trimmed by the epoch check like any other breach,
 * for the same reason the caps are enforced at every epoch rather than at
 * admission: a ceiling that is only tested when power is granted is not a
 * ceiling, and a proposal is one of the ways power arrives. Refusing here as
 * well would have made the ceiling look enforced while leaving the path that
 * matters — growth, merger, nationalisation — unguarded.
 *
 * Seats are moved by delegating from the module's own seat reserve and
 * undelegating back into it, which is the only path that changes consensus
 * power without minting anything and without writing to x/staking behind its
 * back. A reserve with too few seats fails the message rather than the block.
 */
export interface MsgSetValidatorPower {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /** validator is the operator address, in its account form. */
  validator: string;
  /**
   * seats is the power the validator is to carry. Zero is refused: a validator
   * with no seats is one that has been removed, and removing one should say so
   * rather than arrive as a power update that happens to be empty.
   */
  seats: string;
}

/**
 * MsgSetValidatorPowerResponse returns the seats the validator holds after the
 * message, which is what it asked for unless the reserve could not cover it.
 */
export interface MsgSetValidatorPowerResponse {
  seats: string;
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams = {
  encode(message: MsgUpdateParams, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse = {
  encode(_: MsgUpdateParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgApplyValidator(): MsgApplyValidator {
  return { creator: "", moniker: "", description: "", legalEntityId: "", beneficialOwnerId: "", jurisdiction: "" };
}

export const MsgApplyValidator = {
  encode(message: MsgApplyValidator, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.moniker !== "") {
      writer.uint32(18).string(message.moniker);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.legalEntityId !== "") {
      writer.uint32(34).string(message.legalEntityId);
    }
    if (message.beneficialOwnerId !== "") {
      writer.uint32(42).string(message.beneficialOwnerId);
    }
    if (message.jurisdiction !== "") {
      writer.uint32(50).string(message.jurisdiction);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgApplyValidator {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgApplyValidator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.moniker = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.legalEntityId = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.beneficialOwnerId = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.jurisdiction = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgApplyValidator {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      moniker: isSet(object.moniker) ? globalThis.String(object.moniker) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      legalEntityId: isSet(object.legalEntityId) ? globalThis.String(object.legalEntityId) : "",
      beneficialOwnerId: isSet(object.beneficialOwnerId) ? globalThis.String(object.beneficialOwnerId) : "",
      jurisdiction: isSet(object.jurisdiction) ? globalThis.String(object.jurisdiction) : "",
    };
  },

  toJSON(message: MsgApplyValidator): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.moniker !== "") {
      obj.moniker = message.moniker;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.legalEntityId !== "") {
      obj.legalEntityId = message.legalEntityId;
    }
    if (message.beneficialOwnerId !== "") {
      obj.beneficialOwnerId = message.beneficialOwnerId;
    }
    if (message.jurisdiction !== "") {
      obj.jurisdiction = message.jurisdiction;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgApplyValidator>): MsgApplyValidator {
    return MsgApplyValidator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgApplyValidator>): MsgApplyValidator {
    const message = createBaseMsgApplyValidator();
    message.creator = object.creator ?? "";
    message.moniker = object.moniker ?? "";
    message.description = object.description ?? "";
    message.legalEntityId = object.legalEntityId ?? "";
    message.beneficialOwnerId = object.beneficialOwnerId ?? "";
    message.jurisdiction = object.jurisdiction ?? "";
    return message;
  },
};

function createBaseMsgApplyValidatorResponse(): MsgApplyValidatorResponse {
  return {};
}

export const MsgApplyValidatorResponse = {
  encode(_: MsgApplyValidatorResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgApplyValidatorResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgApplyValidatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgApplyValidatorResponse {
    return {};
  },

  toJSON(_: MsgApplyValidatorResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgApplyValidatorResponse>): MsgApplyValidatorResponse {
    return MsgApplyValidatorResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgApplyValidatorResponse>): MsgApplyValidatorResponse {
    const message = createBaseMsgApplyValidatorResponse();
    return message;
  },
};

function createBaseMsgApproveValidator(): MsgApproveValidator {
  return { authority: "", candidate: "", approve: false };
}

export const MsgApproveValidator = {
  encode(message: MsgApproveValidator, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.candidate !== "") {
      writer.uint32(18).string(message.candidate);
    }
    if (message.approve !== false) {
      writer.uint32(24).bool(message.approve);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgApproveValidator {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgApproveValidator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.candidate = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.approve = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgApproveValidator {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      candidate: isSet(object.candidate) ? globalThis.String(object.candidate) : "",
      approve: isSet(object.approve) ? globalThis.Boolean(object.approve) : false,
    };
  },

  toJSON(message: MsgApproveValidator): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.candidate !== "") {
      obj.candidate = message.candidate;
    }
    if (message.approve !== false) {
      obj.approve = message.approve;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgApproveValidator>): MsgApproveValidator {
    return MsgApproveValidator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgApproveValidator>): MsgApproveValidator {
    const message = createBaseMsgApproveValidator();
    message.authority = object.authority ?? "";
    message.candidate = object.candidate ?? "";
    message.approve = object.approve ?? false;
    return message;
  },
};

function createBaseMsgApproveValidatorResponse(): MsgApproveValidatorResponse {
  return {};
}

export const MsgApproveValidatorResponse = {
  encode(_: MsgApproveValidatorResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgApproveValidatorResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgApproveValidatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgApproveValidatorResponse {
    return {};
  },

  toJSON(_: MsgApproveValidatorResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgApproveValidatorResponse>): MsgApproveValidatorResponse {
    return MsgApproveValidatorResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgApproveValidatorResponse>): MsgApproveValidatorResponse {
    const message = createBaseMsgApproveValidatorResponse();
    return message;
  },
};

function createBaseMsgRotateOperator(): MsgRotateOperator {
  return { creator: "", newOperator: "" };
}

export const MsgRotateOperator = {
  encode(message: MsgRotateOperator, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.newOperator !== "") {
      writer.uint32(18).string(message.newOperator);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRotateOperator {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRotateOperator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.newOperator = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRotateOperator {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      newOperator: isSet(object.newOperator) ? globalThis.String(object.newOperator) : "",
    };
  },

  toJSON(message: MsgRotateOperator): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.newOperator !== "") {
      obj.newOperator = message.newOperator;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRotateOperator>): MsgRotateOperator {
    return MsgRotateOperator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRotateOperator>): MsgRotateOperator {
    const message = createBaseMsgRotateOperator();
    message.creator = object.creator ?? "";
    message.newOperator = object.newOperator ?? "";
    return message;
  },
};

function createBaseMsgRotateOperatorResponse(): MsgRotateOperatorResponse {
  return { rotationId: "0" };
}

export const MsgRotateOperatorResponse = {
  encode(message: MsgRotateOperatorResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rotationId !== "0") {
      writer.uint32(8).uint64(message.rotationId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgRotateOperatorResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRotateOperatorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.rotationId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRotateOperatorResponse {
    return { rotationId: isSet(object.rotationId) ? globalThis.String(object.rotationId) : "0" };
  },

  toJSON(message: MsgRotateOperatorResponse): unknown {
    const obj: any = {};
    if (message.rotationId !== "0") {
      obj.rotationId = message.rotationId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRotateOperatorResponse>): MsgRotateOperatorResponse {
    return MsgRotateOperatorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRotateOperatorResponse>): MsgRotateOperatorResponse {
    const message = createBaseMsgRotateOperatorResponse();
    message.rotationId = object.rotationId ?? "0";
    return message;
  },
};

function createBaseMsgProposeOperatorRecovery(): MsgProposeOperatorRecovery {
  return { creator: "", currentOperator: "", newOperator: "", reason: "" };
}

export const MsgProposeOperatorRecovery = {
  encode(message: MsgProposeOperatorRecovery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.currentOperator !== "") {
      writer.uint32(18).string(message.currentOperator);
    }
    if (message.newOperator !== "") {
      writer.uint32(26).string(message.newOperator);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgProposeOperatorRecovery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgProposeOperatorRecovery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.currentOperator = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.newOperator = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgProposeOperatorRecovery {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      currentOperator: isSet(object.currentOperator) ? globalThis.String(object.currentOperator) : "",
      newOperator: isSet(object.newOperator) ? globalThis.String(object.newOperator) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: MsgProposeOperatorRecovery): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.currentOperator !== "") {
      obj.currentOperator = message.currentOperator;
    }
    if (message.newOperator !== "") {
      obj.newOperator = message.newOperator;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgProposeOperatorRecovery>): MsgProposeOperatorRecovery {
    return MsgProposeOperatorRecovery.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgProposeOperatorRecovery>): MsgProposeOperatorRecovery {
    const message = createBaseMsgProposeOperatorRecovery();
    message.creator = object.creator ?? "";
    message.currentOperator = object.currentOperator ?? "";
    message.newOperator = object.newOperator ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseMsgProposeOperatorRecoveryResponse(): MsgProposeOperatorRecoveryResponse {
  return { rotationId: "0" };
}

export const MsgProposeOperatorRecoveryResponse = {
  encode(message: MsgProposeOperatorRecoveryResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rotationId !== "0") {
      writer.uint32(8).uint64(message.rotationId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgProposeOperatorRecoveryResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgProposeOperatorRecoveryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.rotationId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgProposeOperatorRecoveryResponse {
    return { rotationId: isSet(object.rotationId) ? globalThis.String(object.rotationId) : "0" };
  },

  toJSON(message: MsgProposeOperatorRecoveryResponse): unknown {
    const obj: any = {};
    if (message.rotationId !== "0") {
      obj.rotationId = message.rotationId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgProposeOperatorRecoveryResponse>): MsgProposeOperatorRecoveryResponse {
    return MsgProposeOperatorRecoveryResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgProposeOperatorRecoveryResponse>): MsgProposeOperatorRecoveryResponse {
    const message = createBaseMsgProposeOperatorRecoveryResponse();
    message.rotationId = object.rotationId ?? "0";
    return message;
  },
};

function createBaseMsgApproveOperatorRecovery(): MsgApproveOperatorRecovery {
  return { authority: "", rotationId: "0", approve: false };
}

export const MsgApproveOperatorRecovery = {
  encode(message: MsgApproveOperatorRecovery, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.rotationId !== "0") {
      writer.uint32(16).uint64(message.rotationId);
    }
    if (message.approve !== false) {
      writer.uint32(24).bool(message.approve);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgApproveOperatorRecovery {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgApproveOperatorRecovery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.rotationId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.approve = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgApproveOperatorRecovery {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      rotationId: isSet(object.rotationId) ? globalThis.String(object.rotationId) : "0",
      approve: isSet(object.approve) ? globalThis.Boolean(object.approve) : false,
    };
  },

  toJSON(message: MsgApproveOperatorRecovery): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.rotationId !== "0") {
      obj.rotationId = message.rotationId;
    }
    if (message.approve !== false) {
      obj.approve = message.approve;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgApproveOperatorRecovery>): MsgApproveOperatorRecovery {
    return MsgApproveOperatorRecovery.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgApproveOperatorRecovery>): MsgApproveOperatorRecovery {
    const message = createBaseMsgApproveOperatorRecovery();
    message.authority = object.authority ?? "";
    message.rotationId = object.rotationId ?? "0";
    message.approve = object.approve ?? false;
    return message;
  },
};

function createBaseMsgApproveOperatorRecoveryResponse(): MsgApproveOperatorRecoveryResponse {
  return {};
}

export const MsgApproveOperatorRecoveryResponse = {
  encode(_: MsgApproveOperatorRecoveryResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgApproveOperatorRecoveryResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgApproveOperatorRecoveryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgApproveOperatorRecoveryResponse {
    return {};
  },

  toJSON(_: MsgApproveOperatorRecoveryResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgApproveOperatorRecoveryResponse>): MsgApproveOperatorRecoveryResponse {
    return MsgApproveOperatorRecoveryResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgApproveOperatorRecoveryResponse>): MsgApproveOperatorRecoveryResponse {
    const message = createBaseMsgApproveOperatorRecoveryResponse();
    return message;
  },
};

function createBaseMsgCancelOperatorRotation(): MsgCancelOperatorRotation {
  return { creator: "", rotationId: "0" };
}

export const MsgCancelOperatorRotation = {
  encode(message: MsgCancelOperatorRotation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.rotationId !== "0") {
      writer.uint32(16).uint64(message.rotationId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgCancelOperatorRotation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCancelOperatorRotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.rotationId = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCancelOperatorRotation {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      rotationId: isSet(object.rotationId) ? globalThis.String(object.rotationId) : "0",
    };
  },

  toJSON(message: MsgCancelOperatorRotation): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.rotationId !== "0") {
      obj.rotationId = message.rotationId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgCancelOperatorRotation>): MsgCancelOperatorRotation {
    return MsgCancelOperatorRotation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgCancelOperatorRotation>): MsgCancelOperatorRotation {
    const message = createBaseMsgCancelOperatorRotation();
    message.creator = object.creator ?? "";
    message.rotationId = object.rotationId ?? "0";
    return message;
  },
};

function createBaseMsgCancelOperatorRotationResponse(): MsgCancelOperatorRotationResponse {
  return {};
}

export const MsgCancelOperatorRotationResponse = {
  encode(_: MsgCancelOperatorRotationResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgCancelOperatorRotationResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCancelOperatorRotationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgCancelOperatorRotationResponse {
    return {};
  },

  toJSON(_: MsgCancelOperatorRotationResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgCancelOperatorRotationResponse>): MsgCancelOperatorRotationResponse {
    return MsgCancelOperatorRotationResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgCancelOperatorRotationResponse>): MsgCancelOperatorRotationResponse {
    const message = createBaseMsgCancelOperatorRotationResponse();
    return message;
  },
};

function createBaseMsgAttestOwnership(): MsgAttestOwnership {
  return { creator: "", legalEntityId: "", beneficialOwnerId: "", jurisdiction: "" };
}

export const MsgAttestOwnership = {
  encode(message: MsgAttestOwnership, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.legalEntityId !== "") {
      writer.uint32(18).string(message.legalEntityId);
    }
    if (message.beneficialOwnerId !== "") {
      writer.uint32(26).string(message.beneficialOwnerId);
    }
    if (message.jurisdiction !== "") {
      writer.uint32(34).string(message.jurisdiction);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgAttestOwnership {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAttestOwnership();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.legalEntityId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.beneficialOwnerId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.jurisdiction = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAttestOwnership {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      legalEntityId: isSet(object.legalEntityId) ? globalThis.String(object.legalEntityId) : "",
      beneficialOwnerId: isSet(object.beneficialOwnerId) ? globalThis.String(object.beneficialOwnerId) : "",
      jurisdiction: isSet(object.jurisdiction) ? globalThis.String(object.jurisdiction) : "",
    };
  },

  toJSON(message: MsgAttestOwnership): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.legalEntityId !== "") {
      obj.legalEntityId = message.legalEntityId;
    }
    if (message.beneficialOwnerId !== "") {
      obj.beneficialOwnerId = message.beneficialOwnerId;
    }
    if (message.jurisdiction !== "") {
      obj.jurisdiction = message.jurisdiction;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgAttestOwnership>): MsgAttestOwnership {
    return MsgAttestOwnership.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgAttestOwnership>): MsgAttestOwnership {
    const message = createBaseMsgAttestOwnership();
    message.creator = object.creator ?? "";
    message.legalEntityId = object.legalEntityId ?? "";
    message.beneficialOwnerId = object.beneficialOwnerId ?? "";
    message.jurisdiction = object.jurisdiction ?? "";
    return message;
  },
};

function createBaseMsgAttestOwnershipResponse(): MsgAttestOwnershipResponse {
  return {};
}

export const MsgAttestOwnershipResponse = {
  encode(_: MsgAttestOwnershipResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgAttestOwnershipResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAttestOwnershipResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgAttestOwnershipResponse {
    return {};
  },

  toJSON(_: MsgAttestOwnershipResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgAttestOwnershipResponse>): MsgAttestOwnershipResponse {
    return MsgAttestOwnershipResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgAttestOwnershipResponse>): MsgAttestOwnershipResponse {
    const message = createBaseMsgAttestOwnershipResponse();
    return message;
  },
};

function createBaseMsgSetValidatorPower(): MsgSetValidatorPower {
  return { authority: "", validator: "", seats: "0" };
}

export const MsgSetValidatorPower = {
  encode(message: MsgSetValidatorPower, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.validator !== "") {
      writer.uint32(18).string(message.validator);
    }
    if (message.seats !== "0") {
      writer.uint32(24).uint64(message.seats);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSetValidatorPower {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSetValidatorPower();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.seats = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSetValidatorPower {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      seats: isSet(object.seats) ? globalThis.String(object.seats) : "0",
    };
  },

  toJSON(message: MsgSetValidatorPower): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.seats !== "0") {
      obj.seats = message.seats;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSetValidatorPower>): MsgSetValidatorPower {
    return MsgSetValidatorPower.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSetValidatorPower>): MsgSetValidatorPower {
    const message = createBaseMsgSetValidatorPower();
    message.authority = object.authority ?? "";
    message.validator = object.validator ?? "";
    message.seats = object.seats ?? "0";
    return message;
  },
};

function createBaseMsgSetValidatorPowerResponse(): MsgSetValidatorPowerResponse {
  return { seats: "0" };
}

export const MsgSetValidatorPowerResponse = {
  encode(message: MsgSetValidatorPowerResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.seats !== "0") {
      writer.uint32(8).uint64(message.seats);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MsgSetValidatorPowerResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSetValidatorPowerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.seats = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSetValidatorPowerResponse {
    return { seats: isSet(object.seats) ? globalThis.String(object.seats) : "0" };
  },

  toJSON(message: MsgSetValidatorPowerResponse): unknown {
    const obj: any = {};
    if (message.seats !== "0") {
      obj.seats = message.seats;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgSetValidatorPowerResponse>): MsgSetValidatorPowerResponse {
    return MsgSetValidatorPowerResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgSetValidatorPowerResponse>): MsgSetValidatorPowerResponse {
    const message = createBaseMsgSetValidatorPowerResponse();
    message.seats = object.seats ?? "0";
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  /**
   * UpdateParams defines a (governance) operation for updating the module
   * parameters. The authority defaults to the x/gov module account.
   */
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse>;
  /** ApplyValidator defines the ApplyValidator RPC. */
  ApplyValidator(request: MsgApplyValidator): Promise<MsgApplyValidatorResponse>;
  /**
   * ApproveValidator defines the ApproveValidator RPC. It is authority-gated
   * (the x/gov module account) and approves or rejects a pending validator
   * application submitted via ApplyValidator.
   */
  ApproveValidator(request: MsgApproveValidator): Promise<MsgApproveValidatorResponse>;
  /**
   * RotateOperator defines the RotateOperator RPC: the planned path, signed by
   * the operator being replaced.
   */
  RotateOperator(request: MsgRotateOperator): Promise<MsgRotateOperatorResponse>;
  /**
   * ProposeOperatorRecovery defines the ProposeOperatorRecovery RPC: the
   * recovery path, openable by anybody and inert until approved.
   */
  ProposeOperatorRecovery(request: MsgProposeOperatorRecovery): Promise<MsgProposeOperatorRecoveryResponse>;
  /**
   * ApproveOperatorRecovery defines the ApproveOperatorRecovery RPC. It is
   * authority-gated by the same account that admits validators, because
   * recovering one should be exactly as hard as admitting one.
   */
  ApproveOperatorRecovery(request: MsgApproveOperatorRecovery): Promise<MsgApproveOperatorRecoveryResponse>;
  /**
   * CancelOperatorRotation defines the CancelOperatorRotation RPC, which lets
   * the current operator withdraw a rotation before it takes effect.
   */
  CancelOperatorRotation(request: MsgCancelOperatorRotation): Promise<MsgCancelOperatorRotationResponse>;
  /**
   * AttestOwnership defines the AttestOwnership RPC: the operator re-signing
   * for who is behind it, which is what keeps a declaration from going stale.
   */
  AttestOwnership(request: MsgAttestOwnership): Promise<MsgAttestOwnershipResponse>;
  /**
   * SetValidatorPower defines the SetValidatorPower RPC. It is authority-gated
   * and moves how many seats one admitted validator holds.
   */
  SetValidatorPower(request: MsgSetValidatorPower): Promise<MsgSetValidatorPowerResponse>;
}

export const MsgServiceName = "blockchain.validatorgov.v1.Msg";
export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MsgServiceName;
    this.rpc = rpc;
    this.UpdateParams = this.UpdateParams.bind(this);
    this.ApplyValidator = this.ApplyValidator.bind(this);
    this.ApproveValidator = this.ApproveValidator.bind(this);
    this.RotateOperator = this.RotateOperator.bind(this);
    this.ProposeOperatorRecovery = this.ProposeOperatorRecovery.bind(this);
    this.ApproveOperatorRecovery = this.ApproveOperatorRecovery.bind(this);
    this.CancelOperatorRotation = this.CancelOperatorRotation.bind(this);
    this.AttestOwnership = this.AttestOwnership.bind(this);
    this.SetValidatorPower = this.SetValidatorPower.bind(this);
  }
  UpdateParams(request: MsgUpdateParams): Promise<MsgUpdateParamsResponse> {
    const data = MsgUpdateParams.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateParams", data);
    return promise.then((data) => MsgUpdateParamsResponse.decode(_m0.Reader.create(data)));
  }

  ApplyValidator(request: MsgApplyValidator): Promise<MsgApplyValidatorResponse> {
    const data = MsgApplyValidator.encode(request).finish();
    const promise = this.rpc.request(this.service, "ApplyValidator", data);
    return promise.then((data) => MsgApplyValidatorResponse.decode(_m0.Reader.create(data)));
  }

  ApproveValidator(request: MsgApproveValidator): Promise<MsgApproveValidatorResponse> {
    const data = MsgApproveValidator.encode(request).finish();
    const promise = this.rpc.request(this.service, "ApproveValidator", data);
    return promise.then((data) => MsgApproveValidatorResponse.decode(_m0.Reader.create(data)));
  }

  RotateOperator(request: MsgRotateOperator): Promise<MsgRotateOperatorResponse> {
    const data = MsgRotateOperator.encode(request).finish();
    const promise = this.rpc.request(this.service, "RotateOperator", data);
    return promise.then((data) => MsgRotateOperatorResponse.decode(_m0.Reader.create(data)));
  }

  ProposeOperatorRecovery(request: MsgProposeOperatorRecovery): Promise<MsgProposeOperatorRecoveryResponse> {
    const data = MsgProposeOperatorRecovery.encode(request).finish();
    const promise = this.rpc.request(this.service, "ProposeOperatorRecovery", data);
    return promise.then((data) => MsgProposeOperatorRecoveryResponse.decode(_m0.Reader.create(data)));
  }

  ApproveOperatorRecovery(request: MsgApproveOperatorRecovery): Promise<MsgApproveOperatorRecoveryResponse> {
    const data = MsgApproveOperatorRecovery.encode(request).finish();
    const promise = this.rpc.request(this.service, "ApproveOperatorRecovery", data);
    return promise.then((data) => MsgApproveOperatorRecoveryResponse.decode(_m0.Reader.create(data)));
  }

  CancelOperatorRotation(request: MsgCancelOperatorRotation): Promise<MsgCancelOperatorRotationResponse> {
    const data = MsgCancelOperatorRotation.encode(request).finish();
    const promise = this.rpc.request(this.service, "CancelOperatorRotation", data);
    return promise.then((data) => MsgCancelOperatorRotationResponse.decode(_m0.Reader.create(data)));
  }

  AttestOwnership(request: MsgAttestOwnership): Promise<MsgAttestOwnershipResponse> {
    const data = MsgAttestOwnership.encode(request).finish();
    const promise = this.rpc.request(this.service, "AttestOwnership", data);
    return promise.then((data) => MsgAttestOwnershipResponse.decode(_m0.Reader.create(data)));
  }

  SetValidatorPower(request: MsgSetValidatorPower): Promise<MsgSetValidatorPowerResponse> {
    const data = MsgSetValidatorPower.encode(request).finish();
    const promise = this.rpc.request(this.service, "SetValidatorPower", data);
    return promise.then((data) => MsgSetValidatorPowerResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
