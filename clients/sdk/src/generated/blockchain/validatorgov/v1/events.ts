// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/validatorgov/v1/events.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import {
  RotationKind,
  rotationKindFromJSON,
  rotationKindToJSON,
  RotationStatus,
  rotationStatusFromJSON,
  rotationStatusToJSON,
} from "./rotation.ts";

export const protobufPackage = "blockchain.validatorgov.v1";

/** EventRotationProposed is emitted when a rotation is opened, by either path. */
export interface EventRotationProposed {
  rotationId: string;
  currentOperator: string;
  newOperator: string;
  proposer: string;
  kind: RotationKind;
  reason: string;
  /**
   * completes_at_height is zero for a recovery that has not been approved yet,
   * because no clock is running on it.
   */
  completesAtHeight: string;
}

/**
 * EventRecoveryApproved is emitted when the admission quorum agrees to a
 * recovery. This is the moment the validator is paused and the challenge window
 * starts, so it is the moment anybody watching needs to hear about.
 */
export interface EventRecoveryApproved {
  rotationId: string;
  currentOperator: string;
  newOperator: string;
  completesAtHeight: string;
  /**
   * validator_paused is false when there was no validator to pause — an
   * approved candidate who never created one — or when it was already jailed
   * for something else.
   */
  validatorPaused: boolean;
}

/**
 * EventRotationResolved is emitted once, when a rotation reaches its final
 * status, including when it is vetoed by the old key signing.
 */
export interface EventRotationResolved {
  rotationId: string;
  currentOperator: string;
  newOperator: string;
  kind: RotationKind;
  status: RotationStatus;
}

function createBaseEventRotationProposed(): EventRotationProposed {
  return {
    rotationId: "0",
    currentOperator: "",
    newOperator: "",
    proposer: "",
    kind: 0,
    reason: "",
    completesAtHeight: "0",
  };
}

export const EventRotationProposed = {
  encode(message: EventRotationProposed, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rotationId !== "0") {
      writer.uint32(8).uint64(message.rotationId);
    }
    if (message.currentOperator !== "") {
      writer.uint32(18).string(message.currentOperator);
    }
    if (message.newOperator !== "") {
      writer.uint32(26).string(message.newOperator);
    }
    if (message.proposer !== "") {
      writer.uint32(34).string(message.proposer);
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.reason !== "") {
      writer.uint32(50).string(message.reason);
    }
    if (message.completesAtHeight !== "0") {
      writer.uint32(56).int64(message.completesAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventRotationProposed {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventRotationProposed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.rotationId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.currentOperator = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.newOperator = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.proposer = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.completesAtHeight = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventRotationProposed {
    return {
      rotationId: isSet(object.rotationId) ? globalThis.String(object.rotationId) : "0",
      currentOperator: isSet(object.currentOperator) ? globalThis.String(object.currentOperator) : "",
      newOperator: isSet(object.newOperator) ? globalThis.String(object.newOperator) : "",
      proposer: isSet(object.proposer) ? globalThis.String(object.proposer) : "",
      kind: isSet(object.kind) ? rotationKindFromJSON(object.kind) : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      completesAtHeight: isSet(object.completesAtHeight) ? globalThis.String(object.completesAtHeight) : "0",
    };
  },

  toJSON(message: EventRotationProposed): unknown {
    const obj: any = {};
    if (message.rotationId !== "0") {
      obj.rotationId = message.rotationId;
    }
    if (message.currentOperator !== "") {
      obj.currentOperator = message.currentOperator;
    }
    if (message.newOperator !== "") {
      obj.newOperator = message.newOperator;
    }
    if (message.proposer !== "") {
      obj.proposer = message.proposer;
    }
    if (message.kind !== 0) {
      obj.kind = rotationKindToJSON(message.kind);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.completesAtHeight !== "0") {
      obj.completesAtHeight = message.completesAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<EventRotationProposed>): EventRotationProposed {
    return EventRotationProposed.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventRotationProposed>): EventRotationProposed {
    const message = createBaseEventRotationProposed();
    message.rotationId = object.rotationId ?? "0";
    message.currentOperator = object.currentOperator ?? "";
    message.newOperator = object.newOperator ?? "";
    message.proposer = object.proposer ?? "";
    message.kind = object.kind ?? 0;
    message.reason = object.reason ?? "";
    message.completesAtHeight = object.completesAtHeight ?? "0";
    return message;
  },
};

function createBaseEventRecoveryApproved(): EventRecoveryApproved {
  return { rotationId: "0", currentOperator: "", newOperator: "", completesAtHeight: "0", validatorPaused: false };
}

export const EventRecoveryApproved = {
  encode(message: EventRecoveryApproved, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rotationId !== "0") {
      writer.uint32(8).uint64(message.rotationId);
    }
    if (message.currentOperator !== "") {
      writer.uint32(18).string(message.currentOperator);
    }
    if (message.newOperator !== "") {
      writer.uint32(26).string(message.newOperator);
    }
    if (message.completesAtHeight !== "0") {
      writer.uint32(32).int64(message.completesAtHeight);
    }
    if (message.validatorPaused !== false) {
      writer.uint32(40).bool(message.validatorPaused);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventRecoveryApproved {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventRecoveryApproved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.rotationId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.currentOperator = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.newOperator = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.completesAtHeight = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.validatorPaused = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventRecoveryApproved {
    return {
      rotationId: isSet(object.rotationId) ? globalThis.String(object.rotationId) : "0",
      currentOperator: isSet(object.currentOperator) ? globalThis.String(object.currentOperator) : "",
      newOperator: isSet(object.newOperator) ? globalThis.String(object.newOperator) : "",
      completesAtHeight: isSet(object.completesAtHeight) ? globalThis.String(object.completesAtHeight) : "0",
      validatorPaused: isSet(object.validatorPaused) ? globalThis.Boolean(object.validatorPaused) : false,
    };
  },

  toJSON(message: EventRecoveryApproved): unknown {
    const obj: any = {};
    if (message.rotationId !== "0") {
      obj.rotationId = message.rotationId;
    }
    if (message.currentOperator !== "") {
      obj.currentOperator = message.currentOperator;
    }
    if (message.newOperator !== "") {
      obj.newOperator = message.newOperator;
    }
    if (message.completesAtHeight !== "0") {
      obj.completesAtHeight = message.completesAtHeight;
    }
    if (message.validatorPaused !== false) {
      obj.validatorPaused = message.validatorPaused;
    }
    return obj;
  },

  create(base?: DeepPartial<EventRecoveryApproved>): EventRecoveryApproved {
    return EventRecoveryApproved.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventRecoveryApproved>): EventRecoveryApproved {
    const message = createBaseEventRecoveryApproved();
    message.rotationId = object.rotationId ?? "0";
    message.currentOperator = object.currentOperator ?? "";
    message.newOperator = object.newOperator ?? "";
    message.completesAtHeight = object.completesAtHeight ?? "0";
    message.validatorPaused = object.validatorPaused ?? false;
    return message;
  },
};

function createBaseEventRotationResolved(): EventRotationResolved {
  return { rotationId: "0", currentOperator: "", newOperator: "", kind: 0, status: 0 };
}

export const EventRotationResolved = {
  encode(message: EventRotationResolved, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rotationId !== "0") {
      writer.uint32(8).uint64(message.rotationId);
    }
    if (message.currentOperator !== "") {
      writer.uint32(18).string(message.currentOperator);
    }
    if (message.newOperator !== "") {
      writer.uint32(26).string(message.newOperator);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EventRotationResolved {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventRotationResolved();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.rotationId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.currentOperator = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.newOperator = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventRotationResolved {
    return {
      rotationId: isSet(object.rotationId) ? globalThis.String(object.rotationId) : "0",
      currentOperator: isSet(object.currentOperator) ? globalThis.String(object.currentOperator) : "",
      newOperator: isSet(object.newOperator) ? globalThis.String(object.newOperator) : "",
      kind: isSet(object.kind) ? rotationKindFromJSON(object.kind) : 0,
      status: isSet(object.status) ? rotationStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: EventRotationResolved): unknown {
    const obj: any = {};
    if (message.rotationId !== "0") {
      obj.rotationId = message.rotationId;
    }
    if (message.currentOperator !== "") {
      obj.currentOperator = message.currentOperator;
    }
    if (message.newOperator !== "") {
      obj.newOperator = message.newOperator;
    }
    if (message.kind !== 0) {
      obj.kind = rotationKindToJSON(message.kind);
    }
    if (message.status !== 0) {
      obj.status = rotationStatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<EventRotationResolved>): EventRotationResolved {
    return EventRotationResolved.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventRotationResolved>): EventRotationResolved {
    const message = createBaseEventRotationResolved();
    message.rotationId = object.rotationId ?? "0";
    message.currentOperator = object.currentOperator ?? "";
    message.newOperator = object.newOperator ?? "";
    message.kind = object.kind ?? 0;
    message.status = object.status ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
