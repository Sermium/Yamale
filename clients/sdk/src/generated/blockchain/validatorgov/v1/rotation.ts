// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/validatorgov/v1/rotation.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.validatorgov.v1";

/**
 * RotationKind is which of the two paths a rotation took, and it decides
 * everything about how the rotation is treated. The two are separated at the
 * type level rather than inferred from who signed, so that a reader of the
 * record — or of an event — never has to reconstruct which set of rules
 * applied to a rotation that already happened.
 */
export const RotationKind = {
  /** ROTATION_KIND_UNSPECIFIED - ROTATION_KIND_UNSPECIFIED is the unset default and is never valid. */
  ROTATION_KIND_UNSPECIFIED: 0,
  /**
   * ROTATION_KIND_PLANNED - ROTATION_KIND_PLANNED was signed by the operator being replaced.
   * Possession of the key is the whole proof, so nothing else is required of
   * it: anyone able to submit this already controls everything it protects.
   */
  ROTATION_KIND_PLANNED: 1,
  /**
   * ROTATION_KIND_RECOVERY - ROTATION_KIND_RECOVERY was proposed by somebody who does not hold the key,
   * which is also exactly what an attacker would be doing. It carries the
   * admission quorum, the pause and the challenge window because of that.
   */
  ROTATION_KIND_RECOVERY: 2,
  UNRECOGNIZED: -1,
} as const;

export type RotationKind = typeof RotationKind[keyof typeof RotationKind];

export namespace RotationKind {
  export type ROTATION_KIND_UNSPECIFIED = typeof RotationKind.ROTATION_KIND_UNSPECIFIED;
  export type ROTATION_KIND_PLANNED = typeof RotationKind.ROTATION_KIND_PLANNED;
  export type ROTATION_KIND_RECOVERY = typeof RotationKind.ROTATION_KIND_RECOVERY;
  export type UNRECOGNIZED = typeof RotationKind.UNRECOGNIZED;
}

export function rotationKindFromJSON(object: any): RotationKind {
  switch (object) {
    case 0:
    case "ROTATION_KIND_UNSPECIFIED":
      return RotationKind.ROTATION_KIND_UNSPECIFIED;
    case 1:
    case "ROTATION_KIND_PLANNED":
      return RotationKind.ROTATION_KIND_PLANNED;
    case 2:
    case "ROTATION_KIND_RECOVERY":
      return RotationKind.ROTATION_KIND_RECOVERY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RotationKind.UNRECOGNIZED;
  }
}

export function rotationKindToJSON(object: RotationKind): string {
  switch (object) {
    case RotationKind.ROTATION_KIND_UNSPECIFIED:
      return "ROTATION_KIND_UNSPECIFIED";
    case RotationKind.ROTATION_KIND_PLANNED:
      return "ROTATION_KIND_PLANNED";
    case RotationKind.ROTATION_KIND_RECOVERY:
      return "ROTATION_KIND_RECOVERY";
    case RotationKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** RotationStatus is where a rotation has got to. */
export const RotationStatus = {
  /** ROTATION_STATUS_UNSPECIFIED - ROTATION_STATUS_UNSPECIFIED is the unset default and is never valid. */
  ROTATION_STATUS_UNSPECIFIED: 0,
  /**
   * ROTATION_STATUS_PENDING - ROTATION_STATUS_PENDING is open. A planned rotation is counting down its
   * delay; a recovery is either waiting for the admission quorum or counting
   * down its challenge window.
   */
  ROTATION_STATUS_PENDING: 1,
  /**
   * ROTATION_STATUS_COMPLETED - ROTATION_STATUS_COMPLETED means the new operator is now the operator of
   * record.
   */
  ROTATION_STATUS_COMPLETED: 2,
  /**
   * ROTATION_STATUS_CANCELLED - ROTATION_STATUS_CANCELLED means the current operator withdrew it
   * deliberately, by message, before it took effect.
   */
  ROTATION_STATUS_CANCELLED: 3,
  /**
   * ROTATION_STATUS_VETOED - ROTATION_STATUS_VETOED means the current operator signed a transaction
   * while the recovery claiming their key was lost was open. It is deliberately
   * not the same status as cancelled: cancelled is somebody changing their
   * mind, vetoed is a claim about a lost key being disproved, and conflating
   * the two would hide the only evidence that a recovery was false.
   */
  ROTATION_STATUS_VETOED: 4,
  /** ROTATION_STATUS_REJECTED - ROTATION_STATUS_REJECTED means the admission quorum refused the recovery. */
  ROTATION_STATUS_REJECTED: 5,
  UNRECOGNIZED: -1,
} as const;

export type RotationStatus = typeof RotationStatus[keyof typeof RotationStatus];

export namespace RotationStatus {
  export type ROTATION_STATUS_UNSPECIFIED = typeof RotationStatus.ROTATION_STATUS_UNSPECIFIED;
  export type ROTATION_STATUS_PENDING = typeof RotationStatus.ROTATION_STATUS_PENDING;
  export type ROTATION_STATUS_COMPLETED = typeof RotationStatus.ROTATION_STATUS_COMPLETED;
  export type ROTATION_STATUS_CANCELLED = typeof RotationStatus.ROTATION_STATUS_CANCELLED;
  export type ROTATION_STATUS_VETOED = typeof RotationStatus.ROTATION_STATUS_VETOED;
  export type ROTATION_STATUS_REJECTED = typeof RotationStatus.ROTATION_STATUS_REJECTED;
  export type UNRECOGNIZED = typeof RotationStatus.UNRECOGNIZED;
}

export function rotationStatusFromJSON(object: any): RotationStatus {
  switch (object) {
    case 0:
    case "ROTATION_STATUS_UNSPECIFIED":
      return RotationStatus.ROTATION_STATUS_UNSPECIFIED;
    case 1:
    case "ROTATION_STATUS_PENDING":
      return RotationStatus.ROTATION_STATUS_PENDING;
    case 2:
    case "ROTATION_STATUS_COMPLETED":
      return RotationStatus.ROTATION_STATUS_COMPLETED;
    case 3:
    case "ROTATION_STATUS_CANCELLED":
      return RotationStatus.ROTATION_STATUS_CANCELLED;
    case 4:
    case "ROTATION_STATUS_VETOED":
      return RotationStatus.ROTATION_STATUS_VETOED;
    case 5:
    case "ROTATION_STATUS_REJECTED":
      return RotationStatus.ROTATION_STATUS_REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RotationStatus.UNRECOGNIZED;
  }
}

export function rotationStatusToJSON(object: RotationStatus): string {
  switch (object) {
    case RotationStatus.ROTATION_STATUS_UNSPECIFIED:
      return "ROTATION_STATUS_UNSPECIFIED";
    case RotationStatus.ROTATION_STATUS_PENDING:
      return "ROTATION_STATUS_PENDING";
    case RotationStatus.ROTATION_STATUS_COMPLETED:
      return "ROTATION_STATUS_COMPLETED";
    case RotationStatus.ROTATION_STATUS_CANCELLED:
      return "ROTATION_STATUS_CANCELLED";
    case RotationStatus.ROTATION_STATUS_VETOED:
      return "ROTATION_STATUS_VETOED";
    case RotationStatus.ROTATION_STATUS_REJECTED:
      return "ROTATION_STATUS_REJECTED";
    case RotationStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * OperatorRotation is one attempt to move a validator's operator key, and
 * everything the chain did about it. Rotations are never deleted: a recovery
 * that was vetoed is the record of somebody having claimed a key was lost when
 * it was not, and that is precisely the history worth keeping.
 */
export interface OperatorRotation {
  /**
   * id is numbered from one. Zero is reserved because in proto3 an id of 0 is
   * indistinguishable from an unset field, and "rotation 0" would be a record
   * nobody could look up.
   */
  id: string;
  /**
   * current_operator is the address being replaced. It is the same address the
   * ApprovedValidator allowlist is keyed by, which is the account form of the
   * validator's operator address.
   */
  currentOperator: string;
  /** new_operator is the address that takes over. */
  newOperator: string;
  kind: RotationKind;
  status: RotationStatus;
  /**
   * proposer is whoever submitted it. For a planned rotation this equals
   * current_operator; for a recovery it is whoever noticed, which is anybody.
   */
  proposer: string;
  /**
   * reason is the stated grounds. Required on a recovery — a claim that
   * somebody's key is gone should have to say so in words — and unused on a
   * planned rotation, where the signature says everything.
   */
  reason: string;
  /**
   * approved is set once the admission quorum has agreed to a recovery. Until
   * then a recovery is inert: it is a claim on the record, and it neither
   * pauses the validator nor runs a clock. Otherwise anyone could pause any
   * validator on this chain for the price of one transaction.
   */
  approved: boolean;
  openedAtHeight: string;
  /**
   * completes_at_height is the height the rotation takes effect at. Zero means
   * no clock is running yet, which for a recovery is the ordinary state
   * between being proposed and being approved.
   */
  completesAtHeight: string;
  resolvedAtHeight: string;
  /**
   * paused_validator records that this rotation is what jailed the validator,
   * so that resolving it un-jails only what it jailed. A validator already
   * jailed for downtime when the recovery was approved must stay jailed when
   * the recovery ends: releasing it would turn a contested rotation into a way
   * of clearing somebody else's downtime.
   */
  pausedValidator: boolean;
}

function createBaseOperatorRotation(): OperatorRotation {
  return {
    id: "0",
    currentOperator: "",
    newOperator: "",
    kind: 0,
    status: 0,
    proposer: "",
    reason: "",
    approved: false,
    openedAtHeight: "0",
    completesAtHeight: "0",
    resolvedAtHeight: "0",
    pausedValidator: false,
  };
}

export const OperatorRotation = {
  encode(message: OperatorRotation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.currentOperator !== "") {
      writer.uint32(18).string(message.currentOperator);
    }
    if (message.newOperator !== "") {
      writer.uint32(26).string(message.newOperator);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.proposer !== "") {
      writer.uint32(50).string(message.proposer);
    }
    if (message.reason !== "") {
      writer.uint32(58).string(message.reason);
    }
    if (message.approved !== false) {
      writer.uint32(64).bool(message.approved);
    }
    if (message.openedAtHeight !== "0") {
      writer.uint32(72).int64(message.openedAtHeight);
    }
    if (message.completesAtHeight !== "0") {
      writer.uint32(80).int64(message.completesAtHeight);
    }
    if (message.resolvedAtHeight !== "0") {
      writer.uint32(88).int64(message.resolvedAtHeight);
    }
    if (message.pausedValidator !== false) {
      writer.uint32(96).bool(message.pausedValidator);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OperatorRotation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperatorRotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.currentOperator = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.newOperator = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.proposer = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.reason = reader.string();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.approved = reader.bool();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.openedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.completesAtHeight = longToString(reader.int64() as Long);
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.resolvedAtHeight = longToString(reader.int64() as Long);
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.pausedValidator = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OperatorRotation {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      currentOperator: isSet(object.currentOperator) ? globalThis.String(object.currentOperator) : "",
      newOperator: isSet(object.newOperator) ? globalThis.String(object.newOperator) : "",
      kind: isSet(object.kind) ? rotationKindFromJSON(object.kind) : 0,
      status: isSet(object.status) ? rotationStatusFromJSON(object.status) : 0,
      proposer: isSet(object.proposer) ? globalThis.String(object.proposer) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      approved: isSet(object.approved) ? globalThis.Boolean(object.approved) : false,
      openedAtHeight: isSet(object.openedAtHeight) ? globalThis.String(object.openedAtHeight) : "0",
      completesAtHeight: isSet(object.completesAtHeight) ? globalThis.String(object.completesAtHeight) : "0",
      resolvedAtHeight: isSet(object.resolvedAtHeight) ? globalThis.String(object.resolvedAtHeight) : "0",
      pausedValidator: isSet(object.pausedValidator) ? globalThis.Boolean(object.pausedValidator) : false,
    };
  },

  toJSON(message: OperatorRotation): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.currentOperator !== "") {
      obj.currentOperator = message.currentOperator;
    }
    if (message.newOperator !== "") {
      obj.newOperator = message.newOperator;
    }
    if (message.kind !== 0) {
      obj.kind = rotationKindToJSON(message.kind);
    }
    if (message.status !== 0) {
      obj.status = rotationStatusToJSON(message.status);
    }
    if (message.proposer !== "") {
      obj.proposer = message.proposer;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.approved !== false) {
      obj.approved = message.approved;
    }
    if (message.openedAtHeight !== "0") {
      obj.openedAtHeight = message.openedAtHeight;
    }
    if (message.completesAtHeight !== "0") {
      obj.completesAtHeight = message.completesAtHeight;
    }
    if (message.resolvedAtHeight !== "0") {
      obj.resolvedAtHeight = message.resolvedAtHeight;
    }
    if (message.pausedValidator !== false) {
      obj.pausedValidator = message.pausedValidator;
    }
    return obj;
  },

  create(base?: DeepPartial<OperatorRotation>): OperatorRotation {
    return OperatorRotation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OperatorRotation>): OperatorRotation {
    const message = createBaseOperatorRotation();
    message.id = object.id ?? "0";
    message.currentOperator = object.currentOperator ?? "";
    message.newOperator = object.newOperator ?? "";
    message.kind = object.kind ?? 0;
    message.status = object.status ?? 0;
    message.proposer = object.proposer ?? "";
    message.reason = object.reason ?? "";
    message.approved = object.approved ?? false;
    message.openedAtHeight = object.openedAtHeight ?? "0";
    message.completesAtHeight = object.completesAtHeight ?? "0";
    message.resolvedAtHeight = object.resolvedAtHeight ?? "0";
    message.pausedValidator = object.pausedValidator ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
