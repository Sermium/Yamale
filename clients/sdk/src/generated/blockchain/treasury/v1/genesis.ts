// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/treasury/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";
import { Lock } from "./lock.ts";
import { Params } from "./params.ts";
import { SpendPolicy, SpendWindow } from "./spend_policy.ts";
import { RoleAssignment, Treasury, TreasuryBalance } from "./treasury.ts";

export const protobufPackage = "blockchain.treasury.v1";

/**
 * GenesisState defines the treasury module's genesis state.
 *
 * Every field here is also produced by ExportGenesis, so a chain's treasury
 * state survives an export/import round trip intact — including the balance
 * ledger, without which the locked/available accounting would silently reset.
 */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params: Params | undefined;
  treasuryList: Treasury[];
  treasuryCount: string;
  lockList: Lock[];
  lockCount: string;
  balanceList: TreasuryBalance[];
  roleList: RoleAssignment[];
  spendPolicyList: SpendPolicy[];
  spendWindowList: SpendWindow[];
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    treasuryList: [],
    treasuryCount: "0",
    lockList: [],
    lockCount: "0",
    balanceList: [],
    roleList: [],
    spendPolicyList: [],
    spendWindowList: [],
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.treasuryList) {
      Treasury.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.treasuryCount !== "0") {
      writer.uint32(24).uint64(message.treasuryCount);
    }
    for (const v of message.lockList) {
      Lock.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message.lockCount !== "0") {
      writer.uint32(40).uint64(message.lockCount);
    }
    for (const v of message.balanceList) {
      TreasuryBalance.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    for (const v of message.roleList) {
      RoleAssignment.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    for (const v of message.spendPolicyList) {
      SpendPolicy.encode(v!, writer.uint32(66).fork()).ldelim();
    }
    for (const v of message.spendWindowList) {
      SpendWindow.encode(v!, writer.uint32(74).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.treasuryList.push(Treasury.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.treasuryCount = longToString(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.lockList.push(Lock.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.lockCount = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.balanceList.push(TreasuryBalance.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.roleList.push(RoleAssignment.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.spendPolicyList.push(SpendPolicy.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.spendWindowList.push(SpendWindow.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      treasuryList: globalThis.Array.isArray(object?.treasuryList)
        ? object.treasuryList.map((e: any) => Treasury.fromJSON(e))
        : [],
      treasuryCount: isSet(object.treasuryCount) ? globalThis.String(object.treasuryCount) : "0",
      lockList: globalThis.Array.isArray(object?.lockList) ? object.lockList.map((e: any) => Lock.fromJSON(e)) : [],
      lockCount: isSet(object.lockCount) ? globalThis.String(object.lockCount) : "0",
      balanceList: globalThis.Array.isArray(object?.balanceList)
        ? object.balanceList.map((e: any) => TreasuryBalance.fromJSON(e))
        : [],
      roleList: globalThis.Array.isArray(object?.roleList)
        ? object.roleList.map((e: any) => RoleAssignment.fromJSON(e))
        : [],
      spendPolicyList: globalThis.Array.isArray(object?.spendPolicyList)
        ? object.spendPolicyList.map((e: any) => SpendPolicy.fromJSON(e))
        : [],
      spendWindowList: globalThis.Array.isArray(object?.spendWindowList)
        ? object.spendWindowList.map((e: any) => SpendWindow.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.treasuryList?.length) {
      obj.treasuryList = message.treasuryList.map((e) => Treasury.toJSON(e));
    }
    if (message.treasuryCount !== "0") {
      obj.treasuryCount = message.treasuryCount;
    }
    if (message.lockList?.length) {
      obj.lockList = message.lockList.map((e) => Lock.toJSON(e));
    }
    if (message.lockCount !== "0") {
      obj.lockCount = message.lockCount;
    }
    if (message.balanceList?.length) {
      obj.balanceList = message.balanceList.map((e) => TreasuryBalance.toJSON(e));
    }
    if (message.roleList?.length) {
      obj.roleList = message.roleList.map((e) => RoleAssignment.toJSON(e));
    }
    if (message.spendPolicyList?.length) {
      obj.spendPolicyList = message.spendPolicyList.map((e) => SpendPolicy.toJSON(e));
    }
    if (message.spendWindowList?.length) {
      obj.spendWindowList = message.spendWindowList.map((e) => SpendWindow.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.treasuryList = object.treasuryList?.map((e) => Treasury.fromPartial(e)) || [];
    message.treasuryCount = object.treasuryCount ?? "0";
    message.lockList = object.lockList?.map((e) => Lock.fromPartial(e)) || [];
    message.lockCount = object.lockCount ?? "0";
    message.balanceList = object.balanceList?.map((e) => TreasuryBalance.fromPartial(e)) || [];
    message.roleList = object.roleList?.map((e) => RoleAssignment.fromPartial(e)) || [];
    message.spendPolicyList = object.spendPolicyList?.map((e) => SpendPolicy.fromPartial(e)) || [];
    message.spendWindowList = object.spendWindowList?.map((e) => SpendWindow.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
