// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/treasury/v1/lock.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.treasury.v1";

/** LockType selects how a lock releases its funds over time. */
export const LockType = {
  /** LOCK_TYPE_UNSPECIFIED - LOCK_TYPE_UNSPECIFIED is the unset default and is never valid. */
  LOCK_TYPE_UNSPECIFIED: 0,
  /**
   * LOCK_TYPE_TIME - LOCK_TYPE_TIME releases the whole amount at once, when end_time passes.
   * Use it for a simple escrow or a delayed disbursement.
   */
  LOCK_TYPE_TIME: 1,
  /**
   * LOCK_TYPE_VESTING - LOCK_TYPE_VESTING releases progressively between cliff_time and end_time.
   * Use it for grants, payroll and token allocations.
   */
  LOCK_TYPE_VESTING: 2,
  /**
   * LOCK_TYPE_CONDITIONAL - LOCK_TYPE_CONDITIONAL releases when the depositor says so, not when a clock
   * says so. This is escrow: a buyer commits the money, the seller ships
   * knowing it exists, and the funds move on confirmation.
   *
   * Deliberately no deadline. An automatic release rewards precisely the seller
   * who ships nothing and waits, and it lands hardest on buyers who are ill,
   * travelling or offline. Instead either party may open a case, which is why
   * silence is not a weapon: the seller escalates rather than waiting forever.
   */
  LOCK_TYPE_CONDITIONAL: 3,
  UNRECOGNIZED: -1,
} as const;

export type LockType = typeof LockType[keyof typeof LockType];

export namespace LockType {
  export type LOCK_TYPE_UNSPECIFIED = typeof LockType.LOCK_TYPE_UNSPECIFIED;
  export type LOCK_TYPE_TIME = typeof LockType.LOCK_TYPE_TIME;
  export type LOCK_TYPE_VESTING = typeof LockType.LOCK_TYPE_VESTING;
  export type LOCK_TYPE_CONDITIONAL = typeof LockType.LOCK_TYPE_CONDITIONAL;
  export type UNRECOGNIZED = typeof LockType.UNRECOGNIZED;
}

export function lockTypeFromJSON(object: any): LockType {
  switch (object) {
    case 0:
    case "LOCK_TYPE_UNSPECIFIED":
      return LockType.LOCK_TYPE_UNSPECIFIED;
    case 1:
    case "LOCK_TYPE_TIME":
      return LockType.LOCK_TYPE_TIME;
    case 2:
    case "LOCK_TYPE_VESTING":
      return LockType.LOCK_TYPE_VESTING;
    case 3:
    case "LOCK_TYPE_CONDITIONAL":
      return LockType.LOCK_TYPE_CONDITIONAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LockType.UNRECOGNIZED;
  }
}

export function lockTypeToJSON(object: LockType): string {
  switch (object) {
    case LockType.LOCK_TYPE_UNSPECIFIED:
      return "LOCK_TYPE_UNSPECIFIED";
    case LockType.LOCK_TYPE_TIME:
      return "LOCK_TYPE_TIME";
    case LockType.LOCK_TYPE_VESTING:
      return "LOCK_TYPE_VESTING";
    case LockType.LOCK_TYPE_CONDITIONAL:
      return "LOCK_TYPE_CONDITIONAL";
    case LockType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** DisputeState tracks a conditional lock that somebody has escalated. */
export const DisputeState = {
  DISPUTE_STATE_NONE: 0,
  /** DISPUTE_STATE_OPEN - Frozen. Neither release nor refund happens until the moderator decides. */
  DISPUTE_STATE_OPEN: 1,
  DISPUTE_STATE_RESOLVED: 2,
  UNRECOGNIZED: -1,
} as const;

export type DisputeState = typeof DisputeState[keyof typeof DisputeState];

export namespace DisputeState {
  export type DISPUTE_STATE_NONE = typeof DisputeState.DISPUTE_STATE_NONE;
  export type DISPUTE_STATE_OPEN = typeof DisputeState.DISPUTE_STATE_OPEN;
  export type DISPUTE_STATE_RESOLVED = typeof DisputeState.DISPUTE_STATE_RESOLVED;
  export type UNRECOGNIZED = typeof DisputeState.UNRECOGNIZED;
}

export function disputeStateFromJSON(object: any): DisputeState {
  switch (object) {
    case 0:
    case "DISPUTE_STATE_NONE":
      return DisputeState.DISPUTE_STATE_NONE;
    case 1:
    case "DISPUTE_STATE_OPEN":
      return DisputeState.DISPUTE_STATE_OPEN;
    case 2:
    case "DISPUTE_STATE_RESOLVED":
      return DisputeState.DISPUTE_STATE_RESOLVED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DisputeState.UNRECOGNIZED;
  }
}

export function disputeStateToJSON(object: DisputeState): string {
  switch (object) {
    case DisputeState.DISPUTE_STATE_NONE:
      return "DISPUTE_STATE_NONE";
    case DisputeState.DISPUTE_STATE_OPEN:
      return "DISPUTE_STATE_OPEN";
    case DisputeState.DISPUTE_STATE_RESOLVED:
      return "DISPUTE_STATE_RESOLVED";
    case DisputeState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Lock commits treasury funds to a beneficiary on a schedule.
 *
 * Creating a lock does not transfer anything; it moves funds from the
 * treasury's available balance into its locked balance. The beneficiary pulls
 * what has vested by claiming, so the treasury never needs to run a scheduler
 * and a beneficiary who never claims costs the chain nothing.
 */
export interface Lock {
  id: string;
  treasuryId: string;
  /** beneficiary is the only address that may claim from this lock. */
  beneficiary: string;
  denom: string;
  /**
   * total_amount is the full commitment; released_amount is how much of it the
   * beneficiary has already claimed. The difference is what remains locked.
   */
  totalAmount: string;
  releasedAmount: string;
  /** start_time is when the schedule begins, in Unix seconds. */
  startTime: string;
  /**
   * cliff_time is the earliest moment anything may be claimed. Before it, a
   * vesting lock releases nothing at all — that is the whole point of a cliff.
   * Set equal to start_time for no cliff.
   */
  cliffTime: string;
  /** end_time is when the lock is fully released. */
  endTime: string;
  /**
   * release_intervals splits vesting into discrete tranches rather than a
   * continuous drip: 4 over a year means quarterly. Zero or one means
   * continuous, releasing proportionally to elapsed time.
   */
  releaseIntervals: string;
  lockType: LockType;
  /**
   * revocable lets the admin cancel the lock and return the *unreleased*
   * portion to available. Already-vested funds are never clawed back, whether
   * or not the beneficiary has claimed them — a promise that can be withdrawn
   * retroactively is not a promise.
   */
  revocable: boolean;
  /**
   * active is false once the lock is fully claimed or revoked. Inactive locks
   * are retained rather than deleted so the disbursement history stays
   * auditable.
   */
  active: boolean;
  createdAtHeight: string;
  /**
   * depositor funded the lock and is the only account that may release it.
   *
   * Held separately from the treasury's admin on purpose: in an escrow the
   * buyer is not an administrator of anything, and an admin who could release
   * somebody else's escrow would be the trusted party this design exists to
   * remove.
   */
  depositor: string;
  /**
   * moderator decides a disputed lock, and is named when the lock is created
   * rather than chosen afterwards. Both parties see who will judge them before
   * they commit money, which is the whole of what makes this honest.
   *
   * A moderator has exactly one power: deciding an *open* case. They cannot
   * release, refund, or touch a lock nobody has disputed.
   */
  moderator: string;
  dispute: DisputeState;
  /**
   * Why the case was opened, kept because a moderator judging between two
   * strangers has nothing else to go on.
   */
  disputeReason: string;
  /**
   * Which side escalated. "The seller cannot get confirmed" and "the buyer says
   * it never came" are different disputes with the same shape.
   */
  disputeOpenedBy: string;
}

function createBaseLock(): Lock {
  return {
    id: "0",
    treasuryId: "0",
    beneficiary: "",
    denom: "",
    totalAmount: "",
    releasedAmount: "",
    startTime: "0",
    cliffTime: "0",
    endTime: "0",
    releaseIntervals: "0",
    lockType: 0,
    revocable: false,
    active: false,
    createdAtHeight: "0",
    depositor: "",
    moderator: "",
    dispute: 0,
    disputeReason: "",
    disputeOpenedBy: "",
  };
}

export const Lock = {
  encode(message: Lock, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.treasuryId !== "0") {
      writer.uint32(16).uint64(message.treasuryId);
    }
    if (message.beneficiary !== "") {
      writer.uint32(26).string(message.beneficiary);
    }
    if (message.denom !== "") {
      writer.uint32(34).string(message.denom);
    }
    if (message.totalAmount !== "") {
      writer.uint32(42).string(message.totalAmount);
    }
    if (message.releasedAmount !== "") {
      writer.uint32(50).string(message.releasedAmount);
    }
    if (message.startTime !== "0") {
      writer.uint32(56).int64(message.startTime);
    }
    if (message.cliffTime !== "0") {
      writer.uint32(64).int64(message.cliffTime);
    }
    if (message.endTime !== "0") {
      writer.uint32(72).int64(message.endTime);
    }
    if (message.releaseIntervals !== "0") {
      writer.uint32(80).uint64(message.releaseIntervals);
    }
    if (message.lockType !== 0) {
      writer.uint32(88).int32(message.lockType);
    }
    if (message.revocable !== false) {
      writer.uint32(96).bool(message.revocable);
    }
    if (message.active !== false) {
      writer.uint32(104).bool(message.active);
    }
    if (message.createdAtHeight !== "0") {
      writer.uint32(112).uint64(message.createdAtHeight);
    }
    if (message.depositor !== "") {
      writer.uint32(122).string(message.depositor);
    }
    if (message.moderator !== "") {
      writer.uint32(130).string(message.moderator);
    }
    if (message.dispute !== 0) {
      writer.uint32(136).int32(message.dispute);
    }
    if (message.disputeReason !== "") {
      writer.uint32(146).string(message.disputeReason);
    }
    if (message.disputeOpenedBy !== "") {
      writer.uint32(154).string(message.disputeOpenedBy);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Lock {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.treasuryId = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.beneficiary = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.totalAmount = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.releasedAmount = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.startTime = longToString(reader.int64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.cliffTime = longToString(reader.int64() as Long);
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.endTime = longToString(reader.int64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.releaseIntervals = longToString(reader.uint64() as Long);
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.lockType = reader.int32() as any;
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.revocable = reader.bool();
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.active = reader.bool();
          continue;
        case 14:
          if (tag !== 112) {
            break;
          }

          message.createdAtHeight = longToString(reader.uint64() as Long);
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.depositor = reader.string();
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.moderator = reader.string();
          continue;
        case 17:
          if (tag !== 136) {
            break;
          }

          message.dispute = reader.int32() as any;
          continue;
        case 18:
          if (tag !== 146) {
            break;
          }

          message.disputeReason = reader.string();
          continue;
        case 19:
          if (tag !== 154) {
            break;
          }

          message.disputeOpenedBy = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lock {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      treasuryId: isSet(object.treasuryId) ? globalThis.String(object.treasuryId) : "0",
      beneficiary: isSet(object.beneficiary) ? globalThis.String(object.beneficiary) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      totalAmount: isSet(object.totalAmount) ? globalThis.String(object.totalAmount) : "",
      releasedAmount: isSet(object.releasedAmount) ? globalThis.String(object.releasedAmount) : "",
      startTime: isSet(object.startTime) ? globalThis.String(object.startTime) : "0",
      cliffTime: isSet(object.cliffTime) ? globalThis.String(object.cliffTime) : "0",
      endTime: isSet(object.endTime) ? globalThis.String(object.endTime) : "0",
      releaseIntervals: isSet(object.releaseIntervals) ? globalThis.String(object.releaseIntervals) : "0",
      lockType: isSet(object.lockType) ? lockTypeFromJSON(object.lockType) : 0,
      revocable: isSet(object.revocable) ? globalThis.Boolean(object.revocable) : false,
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
      createdAtHeight: isSet(object.createdAtHeight) ? globalThis.String(object.createdAtHeight) : "0",
      depositor: isSet(object.depositor) ? globalThis.String(object.depositor) : "",
      moderator: isSet(object.moderator) ? globalThis.String(object.moderator) : "",
      dispute: isSet(object.dispute) ? disputeStateFromJSON(object.dispute) : 0,
      disputeReason: isSet(object.disputeReason) ? globalThis.String(object.disputeReason) : "",
      disputeOpenedBy: isSet(object.disputeOpenedBy) ? globalThis.String(object.disputeOpenedBy) : "",
    };
  },

  toJSON(message: Lock): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.treasuryId !== "0") {
      obj.treasuryId = message.treasuryId;
    }
    if (message.beneficiary !== "") {
      obj.beneficiary = message.beneficiary;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.totalAmount !== "") {
      obj.totalAmount = message.totalAmount;
    }
    if (message.releasedAmount !== "") {
      obj.releasedAmount = message.releasedAmount;
    }
    if (message.startTime !== "0") {
      obj.startTime = message.startTime;
    }
    if (message.cliffTime !== "0") {
      obj.cliffTime = message.cliffTime;
    }
    if (message.endTime !== "0") {
      obj.endTime = message.endTime;
    }
    if (message.releaseIntervals !== "0") {
      obj.releaseIntervals = message.releaseIntervals;
    }
    if (message.lockType !== 0) {
      obj.lockType = lockTypeToJSON(message.lockType);
    }
    if (message.revocable !== false) {
      obj.revocable = message.revocable;
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    if (message.createdAtHeight !== "0") {
      obj.createdAtHeight = message.createdAtHeight;
    }
    if (message.depositor !== "") {
      obj.depositor = message.depositor;
    }
    if (message.moderator !== "") {
      obj.moderator = message.moderator;
    }
    if (message.dispute !== 0) {
      obj.dispute = disputeStateToJSON(message.dispute);
    }
    if (message.disputeReason !== "") {
      obj.disputeReason = message.disputeReason;
    }
    if (message.disputeOpenedBy !== "") {
      obj.disputeOpenedBy = message.disputeOpenedBy;
    }
    return obj;
  },

  create(base?: DeepPartial<Lock>): Lock {
    return Lock.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Lock>): Lock {
    const message = createBaseLock();
    message.id = object.id ?? "0";
    message.treasuryId = object.treasuryId ?? "0";
    message.beneficiary = object.beneficiary ?? "";
    message.denom = object.denom ?? "";
    message.totalAmount = object.totalAmount ?? "";
    message.releasedAmount = object.releasedAmount ?? "";
    message.startTime = object.startTime ?? "0";
    message.cliffTime = object.cliffTime ?? "0";
    message.endTime = object.endTime ?? "0";
    message.releaseIntervals = object.releaseIntervals ?? "0";
    message.lockType = object.lockType ?? 0;
    message.revocable = object.revocable ?? false;
    message.active = object.active ?? false;
    message.createdAtHeight = object.createdAtHeight ?? "0";
    message.depositor = object.depositor ?? "";
    message.moderator = object.moderator ?? "";
    message.dispute = object.dispute ?? 0;
    message.disputeReason = object.disputeReason ?? "";
    message.disputeOpenedBy = object.disputeOpenedBy ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
