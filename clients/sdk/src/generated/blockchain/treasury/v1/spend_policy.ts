// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/treasury/v1/spend_policy.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.treasury.v1";

/**
 * SpendPolicy constrains what a ROLE_SPENDER may move out of a treasury in one
 * denom, without needing an admin decision per payment.
 *
 * The policy is what makes a spender role safe to hand out: it bounds the blast
 * radius of a compromised operational key to one period's limit, rather than
 * the whole treasury.
 */
export interface SpendPolicy {
  treasuryId: string;
  denom: string;
  /** per_transaction_limit caps a single spend. Empty means no cap. */
  perTransactionLimit: string;
  /**
   * period_limit caps the total spent within one window, and period_seconds is
   * the window length. Empty period_limit means no cap. The window is fixed,
   * not rolling: it resets when period_seconds elapses since the window opened,
   * which keeps the accounting to a single stored value per denom.
   */
  periodLimit: string;
  periodSeconds: string;
  /**
   * allowlist restricts destinations. Empty means any destination is allowed;
   * non-empty means only these are.
   */
  allowlist: string[];
  /**
   * blocklist forbids destinations outright. It is checked after the allowlist,
   * so an address in both is denied — the safer reading of a contradiction.
   */
  blocklist: string[];
}

/** SpendWindow tracks consumption of a SpendPolicy's period_limit. */
export interface SpendWindow {
  treasuryId: string;
  denom: string;
  /** spent is the amount already used in the current window. */
  spent: string;
  /** window_start is when the current window opened, in Unix seconds. */
  windowStart: string;
}

function createBaseSpendPolicy(): SpendPolicy {
  return {
    treasuryId: "0",
    denom: "",
    perTransactionLimit: "",
    periodLimit: "",
    periodSeconds: "0",
    allowlist: [],
    blocklist: [],
  };
}

export const SpendPolicy = {
  encode(message: SpendPolicy, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.treasuryId !== "0") {
      writer.uint32(8).uint64(message.treasuryId);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.perTransactionLimit !== "") {
      writer.uint32(26).string(message.perTransactionLimit);
    }
    if (message.periodLimit !== "") {
      writer.uint32(34).string(message.periodLimit);
    }
    if (message.periodSeconds !== "0") {
      writer.uint32(40).uint64(message.periodSeconds);
    }
    for (const v of message.allowlist) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.blocklist) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SpendPolicy {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpendPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.treasuryId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.perTransactionLimit = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.periodLimit = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.periodSeconds = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.allowlist.push(reader.string());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.blocklist.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpendPolicy {
    return {
      treasuryId: isSet(object.treasuryId) ? globalThis.String(object.treasuryId) : "0",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      perTransactionLimit: isSet(object.perTransactionLimit) ? globalThis.String(object.perTransactionLimit) : "",
      periodLimit: isSet(object.periodLimit) ? globalThis.String(object.periodLimit) : "",
      periodSeconds: isSet(object.periodSeconds) ? globalThis.String(object.periodSeconds) : "0",
      allowlist: globalThis.Array.isArray(object?.allowlist)
        ? object.allowlist.map((e: any) => globalThis.String(e))
        : [],
      blocklist: globalThis.Array.isArray(object?.blocklist)
        ? object.blocklist.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SpendPolicy): unknown {
    const obj: any = {};
    if (message.treasuryId !== "0") {
      obj.treasuryId = message.treasuryId;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.perTransactionLimit !== "") {
      obj.perTransactionLimit = message.perTransactionLimit;
    }
    if (message.periodLimit !== "") {
      obj.periodLimit = message.periodLimit;
    }
    if (message.periodSeconds !== "0") {
      obj.periodSeconds = message.periodSeconds;
    }
    if (message.allowlist?.length) {
      obj.allowlist = message.allowlist;
    }
    if (message.blocklist?.length) {
      obj.blocklist = message.blocklist;
    }
    return obj;
  },

  create(base?: DeepPartial<SpendPolicy>): SpendPolicy {
    return SpendPolicy.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SpendPolicy>): SpendPolicy {
    const message = createBaseSpendPolicy();
    message.treasuryId = object.treasuryId ?? "0";
    message.denom = object.denom ?? "";
    message.perTransactionLimit = object.perTransactionLimit ?? "";
    message.periodLimit = object.periodLimit ?? "";
    message.periodSeconds = object.periodSeconds ?? "0";
    message.allowlist = object.allowlist?.map((e) => e) || [];
    message.blocklist = object.blocklist?.map((e) => e) || [];
    return message;
  },
};

function createBaseSpendWindow(): SpendWindow {
  return { treasuryId: "0", denom: "", spent: "", windowStart: "0" };
}

export const SpendWindow = {
  encode(message: SpendWindow, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.treasuryId !== "0") {
      writer.uint32(8).uint64(message.treasuryId);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.spent !== "") {
      writer.uint32(26).string(message.spent);
    }
    if (message.windowStart !== "0") {
      writer.uint32(32).int64(message.windowStart);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SpendWindow {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpendWindow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.treasuryId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.spent = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.windowStart = longToString(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpendWindow {
    return {
      treasuryId: isSet(object.treasuryId) ? globalThis.String(object.treasuryId) : "0",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      spent: isSet(object.spent) ? globalThis.String(object.spent) : "",
      windowStart: isSet(object.windowStart) ? globalThis.String(object.windowStart) : "0",
    };
  },

  toJSON(message: SpendWindow): unknown {
    const obj: any = {};
    if (message.treasuryId !== "0") {
      obj.treasuryId = message.treasuryId;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.spent !== "") {
      obj.spent = message.spent;
    }
    if (message.windowStart !== "0") {
      obj.windowStart = message.windowStart;
    }
    return obj;
  },

  create(base?: DeepPartial<SpendWindow>): SpendWindow {
    return SpendWindow.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SpendWindow>): SpendWindow {
    const message = createBaseSpendWindow();
    message.treasuryId = object.treasuryId ?? "0";
    message.denom = object.denom ?? "";
    message.spent = object.spent ?? "";
    message.windowStart = object.windowStart ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
