// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/treasury/v1/treasury.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.treasury.v1";

/**
 * Role is what an address may do to a treasury.
 *
 * Proposing, voting and executing are deliberately absent: on this chain that
 * machinery belongs to x/group, which already records every approval and
 * rejection as an attributable on-chain event. Setting a treasury's admin to a
 * group policy composes the two — the group decides, the treasury enforces.
 */
export const Role = {
  /** ROLE_UNSPECIFIED - ROLE_UNSPECIFIED is the unset default and grants nothing. */
  ROLE_UNSPECIFIED: 0,
  /**
   * ROLE_ADMIN - ROLE_ADMIN may manage roles and policies, create locks, and revoke
   * revocable locks. Equivalent to the treasury's admin address.
   */
  ROLE_ADMIN: 1,
  /**
   * ROLE_SPENDER - ROLE_SPENDER may move funds out directly, without an admin decision, so
   * long as the spend policy allows it. This is the role that makes routine
   * operational payments possible without a governance round trip.
   */
  ROLE_SPENDER: 2,
  /**
   * ROLE_PAUSER - ROLE_PAUSER may pause and unpause the treasury. Held separately from admin
   * so an emergency responder can freeze funds without also being able to move
   * them.
   */
  ROLE_PAUSER: 3,
  UNRECOGNIZED: -1,
} as const;

export type Role = typeof Role[keyof typeof Role];

export namespace Role {
  export type ROLE_UNSPECIFIED = typeof Role.ROLE_UNSPECIFIED;
  export type ROLE_ADMIN = typeof Role.ROLE_ADMIN;
  export type ROLE_SPENDER = typeof Role.ROLE_SPENDER;
  export type ROLE_PAUSER = typeof Role.ROLE_PAUSER;
  export type UNRECOGNIZED = typeof Role.UNRECOGNIZED;
}

export function roleFromJSON(object: any): Role {
  switch (object) {
    case 0:
    case "ROLE_UNSPECIFIED":
      return Role.ROLE_UNSPECIFIED;
    case 1:
    case "ROLE_ADMIN":
      return Role.ROLE_ADMIN;
    case 2:
    case "ROLE_SPENDER":
      return Role.ROLE_SPENDER;
    case 3:
    case "ROLE_PAUSER":
      return Role.ROLE_PAUSER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Role.UNRECOGNIZED;
  }
}

export function roleToJSON(object: Role): string {
  switch (object) {
    case Role.ROLE_UNSPECIFIED:
      return "ROLE_UNSPECIFIED";
    case Role.ROLE_ADMIN:
      return "ROLE_ADMIN";
    case Role.ROLE_SPENDER:
      return "ROLE_SPENDER";
    case Role.ROLE_PAUSER:
      return "ROLE_PAUSER";
    case Role.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Treasury is a pool of funds under programmable control.
 *
 * Funds deposited into a treasury are held by the treasury module account, not
 * by the admin's own address. That indirection is what makes the locked/available
 * split enforceable: because no ordinary bank transfer can reach the balance,
 * the only way out is a treasury message, and every treasury message checks the
 * available balance first.
 */
export interface Treasury {
  id: string;
  /** name is a human label, for display only. */
  name: string;
  /**
   * admin manages roles, policies and locks. Point this at an x/group policy
   * address to get M-of-N control with a full on-chain approval trail; a plain
   * account address gives single-key control.
   */
  admin: string;
  /**
   * paused halts spending and claiming without unwinding any state, so a
   * compromise can be contained while the admins decide what to do.
   */
  paused: boolean;
  createdAtHeight: string;
}

/** RoleAssignment grants an address a role over one treasury. */
export interface RoleAssignment {
  treasuryId: string;
  address: string;
  role: Role;
}

/**
 * TreasuryBalance is the module's ledger of what one treasury holds in one
 * denom, and how much of it is already committed to locks.
 *
 * available = total - locked, and it is `available` that every outbound path
 * checks. Once funds back a lock they cannot be spent by anyone, including the
 * admin and including a proposal that clears its signing threshold.
 */
export interface TreasuryBalance {
  treasuryId: string;
  denom: string;
  total: string;
  locked: string;
}

function createBaseTreasury(): Treasury {
  return { id: "0", name: "", admin: "", paused: false, createdAtHeight: "0" };
}

export const Treasury = {
  encode(message: Treasury, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.admin !== "") {
      writer.uint32(26).string(message.admin);
    }
    if (message.paused !== false) {
      writer.uint32(32).bool(message.paused);
    }
    if (message.createdAtHeight !== "0") {
      writer.uint32(40).uint64(message.createdAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Treasury {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTreasury();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.admin = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.paused = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.createdAtHeight = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Treasury {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      admin: isSet(object.admin) ? globalThis.String(object.admin) : "",
      paused: isSet(object.paused) ? globalThis.Boolean(object.paused) : false,
      createdAtHeight: isSet(object.createdAtHeight) ? globalThis.String(object.createdAtHeight) : "0",
    };
  },

  toJSON(message: Treasury): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.admin !== "") {
      obj.admin = message.admin;
    }
    if (message.paused !== false) {
      obj.paused = message.paused;
    }
    if (message.createdAtHeight !== "0") {
      obj.createdAtHeight = message.createdAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<Treasury>): Treasury {
    return Treasury.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Treasury>): Treasury {
    const message = createBaseTreasury();
    message.id = object.id ?? "0";
    message.name = object.name ?? "";
    message.admin = object.admin ?? "";
    message.paused = object.paused ?? false;
    message.createdAtHeight = object.createdAtHeight ?? "0";
    return message;
  },
};

function createBaseRoleAssignment(): RoleAssignment {
  return { treasuryId: "0", address: "", role: 0 };
}

export const RoleAssignment = {
  encode(message: RoleAssignment, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.treasuryId !== "0") {
      writer.uint32(8).uint64(message.treasuryId);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.role !== 0) {
      writer.uint32(24).int32(message.role);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RoleAssignment {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoleAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.treasuryId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.address = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoleAssignment {
    return {
      treasuryId: isSet(object.treasuryId) ? globalThis.String(object.treasuryId) : "0",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      role: isSet(object.role) ? roleFromJSON(object.role) : 0,
    };
  },

  toJSON(message: RoleAssignment): unknown {
    const obj: any = {};
    if (message.treasuryId !== "0") {
      obj.treasuryId = message.treasuryId;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.role !== 0) {
      obj.role = roleToJSON(message.role);
    }
    return obj;
  },

  create(base?: DeepPartial<RoleAssignment>): RoleAssignment {
    return RoleAssignment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RoleAssignment>): RoleAssignment {
    const message = createBaseRoleAssignment();
    message.treasuryId = object.treasuryId ?? "0";
    message.address = object.address ?? "";
    message.role = object.role ?? 0;
    return message;
  },
};

function createBaseTreasuryBalance(): TreasuryBalance {
  return { treasuryId: "0", denom: "", total: "", locked: "" };
}

export const TreasuryBalance = {
  encode(message: TreasuryBalance, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.treasuryId !== "0") {
      writer.uint32(8).uint64(message.treasuryId);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.total !== "") {
      writer.uint32(26).string(message.total);
    }
    if (message.locked !== "") {
      writer.uint32(34).string(message.locked);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TreasuryBalance {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTreasuryBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.treasuryId = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.total = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.locked = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TreasuryBalance {
    return {
      treasuryId: isSet(object.treasuryId) ? globalThis.String(object.treasuryId) : "0",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      total: isSet(object.total) ? globalThis.String(object.total) : "",
      locked: isSet(object.locked) ? globalThis.String(object.locked) : "",
    };
  },

  toJSON(message: TreasuryBalance): unknown {
    const obj: any = {};
    if (message.treasuryId !== "0") {
      obj.treasuryId = message.treasuryId;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.total !== "") {
      obj.total = message.total;
    }
    if (message.locked !== "") {
      obj.locked = message.locked;
    }
    return obj;
  },

  create(base?: DeepPartial<TreasuryBalance>): TreasuryBalance {
    return TreasuryBalance.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TreasuryBalance>): TreasuryBalance {
    const message = createBaseTreasuryBalance();
    message.treasuryId = object.treasuryId ?? "0";
    message.denom = object.denom ?? "";
    message.total = object.total ?? "";
    message.locked = object.locked ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
