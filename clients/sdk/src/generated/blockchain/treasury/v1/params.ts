// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/treasury/v1/params.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.treasury.v1";

/** Params defines the parameters for the module. */
export interface Params {
  /**
   * max_locks_per_treasury bounds how many active locks one treasury may hold.
   * Releasing funds walks a treasury's locks, so an unbounded count would let
   * anyone make that walk arbitrarily expensive.
   */
  maxLocksPerTreasury: string;
  /**
   * max_role_assignments_per_treasury bounds the size of a treasury's access
   * control list, for the same reason.
   */
  maxRoleAssignmentsPerTreasury: string;
  /**
   * min_lock_seconds is the shortest duration a lock may run for. A lock that
   * ends the moment it is created commits nothing and only wastes state.
   */
  minLockSeconds: string;
  /**
   * max_spend_policy_addresses bounds the combined size of a spend policy's
   * allowlist and blocklist. Both are scanned on every spend and stored
   * indefinitely, so leaving them unbounded would let a treasury make its own
   * payments arbitrarily expensive to validate and bloat state while doing it.
   */
  maxSpendPolicyAddresses: string;
  /**
   * fee_operating_treasury_id names the treasury that network fees are paid
   * into on a deployment that has no native token.
   *
   * The destination is a treasury rather than an address because fees are
   * operating income of the deployment, and income that lands in somebody's
   * account is spendable by whoever holds that key. A treasury puts it behind
   * the same roles, spend policies and M-of-N group control as every other
   * committed balance, and leaves the audit trail those produce.
   *
   * Nothing reads this unless route_fees_to_operating_treasury is set.
   */
  feeOperatingTreasuryId: string;
  /**
   * route_fees_to_operating_treasury turns fee routing on.
   *
   * It is a separate field rather than a zero sentinel on the id above, because
   * in proto3 a uint64 of 0 is indistinguishable from a field nobody set, and
   * whether zero names a treasury is x/treasury's numbering convention rather
   * than this parameter's. Ids start at one today, so zero would work; if that
   * ever changed, every chain that had not configured routing would start
   * paying its fees into whichever treasury was created first, and nothing here
   * would have changed to say so.
   */
  routeFeesToOperatingTreasury: boolean;
}

function createBaseParams(): Params {
  return {
    maxLocksPerTreasury: "0",
    maxRoleAssignmentsPerTreasury: "0",
    minLockSeconds: "0",
    maxSpendPolicyAddresses: "0",
    feeOperatingTreasuryId: "0",
    routeFeesToOperatingTreasury: false,
  };
}

export const Params = {
  encode(message: Params, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.maxLocksPerTreasury !== "0") {
      writer.uint32(8).uint64(message.maxLocksPerTreasury);
    }
    if (message.maxRoleAssignmentsPerTreasury !== "0") {
      writer.uint32(16).uint64(message.maxRoleAssignmentsPerTreasury);
    }
    if (message.minLockSeconds !== "0") {
      writer.uint32(24).uint64(message.minLockSeconds);
    }
    if (message.maxSpendPolicyAddresses !== "0") {
      writer.uint32(32).uint64(message.maxSpendPolicyAddresses);
    }
    if (message.feeOperatingTreasuryId !== "0") {
      writer.uint32(40).uint64(message.feeOperatingTreasuryId);
    }
    if (message.routeFeesToOperatingTreasury !== false) {
      writer.uint32(48).bool(message.routeFeesToOperatingTreasury);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Params {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.maxLocksPerTreasury = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.maxRoleAssignmentsPerTreasury = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.minLockSeconds = longToString(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.maxSpendPolicyAddresses = longToString(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.feeOperatingTreasuryId = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.routeFeesToOperatingTreasury = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      maxLocksPerTreasury: isSet(object.maxLocksPerTreasury) ? globalThis.String(object.maxLocksPerTreasury) : "0",
      maxRoleAssignmentsPerTreasury: isSet(object.maxRoleAssignmentsPerTreasury)
        ? globalThis.String(object.maxRoleAssignmentsPerTreasury)
        : "0",
      minLockSeconds: isSet(object.minLockSeconds) ? globalThis.String(object.minLockSeconds) : "0",
      maxSpendPolicyAddresses: isSet(object.maxSpendPolicyAddresses)
        ? globalThis.String(object.maxSpendPolicyAddresses)
        : "0",
      feeOperatingTreasuryId: isSet(object.feeOperatingTreasuryId)
        ? globalThis.String(object.feeOperatingTreasuryId)
        : "0",
      routeFeesToOperatingTreasury: isSet(object.routeFeesToOperatingTreasury)
        ? globalThis.Boolean(object.routeFeesToOperatingTreasury)
        : false,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.maxLocksPerTreasury !== "0") {
      obj.maxLocksPerTreasury = message.maxLocksPerTreasury;
    }
    if (message.maxRoleAssignmentsPerTreasury !== "0") {
      obj.maxRoleAssignmentsPerTreasury = message.maxRoleAssignmentsPerTreasury;
    }
    if (message.minLockSeconds !== "0") {
      obj.minLockSeconds = message.minLockSeconds;
    }
    if (message.maxSpendPolicyAddresses !== "0") {
      obj.maxSpendPolicyAddresses = message.maxSpendPolicyAddresses;
    }
    if (message.feeOperatingTreasuryId !== "0") {
      obj.feeOperatingTreasuryId = message.feeOperatingTreasuryId;
    }
    if (message.routeFeesToOperatingTreasury !== false) {
      obj.routeFeesToOperatingTreasury = message.routeFeesToOperatingTreasury;
    }
    return obj;
  },

  create(base?: DeepPartial<Params>): Params {
    return Params.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.maxLocksPerTreasury = object.maxLocksPerTreasury ?? "0";
    message.maxRoleAssignmentsPerTreasury = object.maxRoleAssignmentsPerTreasury ?? "0";
    message.minLockSeconds = object.minLockSeconds ?? "0";
    message.maxSpendPolicyAddresses = object.maxSpendPolicyAddresses ?? "0";
    message.feeOperatingTreasuryId = object.feeOperatingTreasuryId ?? "0";
    message.routeFeesToOperatingTreasury = object.routeFeesToOperatingTreasury ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
