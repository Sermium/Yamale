// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/oracle/v1/params.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.oracle.v1";

/** Params defines the parameters for the module. */
export interface Params {
  /**
   * vote_period is how many blocks a voting round lasts. Rates are agreed at
   * the end of each round, so this is also how quickly the chain can react to
   * a price move.
   */
  votePeriod: string;
  /**
   * vote_threshold_bps is the share of stake, in basis points, that must report
   * a denom before the chain will agree a rate for it. Below this the previous
   * rate stands and ages, rather than the chain accepting a price set by a
   * minority — a rate agreed by two validators out of a large set is a rate
   * those two chose.
   */
  voteThresholdBps: string;
  /**
   * quote_symbol is what rates are expressed in, e.g. "USD". Every rate answers
   * "one display unit of this denom is worth how many of these".
   */
  quoteSymbol: string;
  /**
   * accepted_denoms are the denoms validators are expected to report. A denom
   * outside this list is ignored rather than counted, so one validator cannot
   * introduce a market by reporting into it.
   */
  acceptedDenoms: string[];
  /**
   * max_rate_age_seconds is how old an exchange rate may be and still be used.
   * Past it, consumers must treat the price as unknown.
   *
   * This is the parameter that decides what happens in a crisis. A feed that
   * goes quiet and a market that has crashed look identical to code that only
   * reads the last value, and acting on a stale price during the exact minutes
   * it matters most is how lending protocols hurt the people using them.
   */
  maxRateAgeSeconds: string;
  /**
   * max_appraisal_age_seconds is the equivalent for real-world valuations,
   * necessarily much longer: a building is appraised quarterly, not every
   * block.
   */
  maxAppraisalAgeSeconds: string;
  /**
   * max_class_ids_per_appraiser bounds an appraiser's scope list, which is
   * scanned on every appraisal they submit.
   */
  maxClassIdsPerAppraiser: string;
}

function createBaseParams(): Params {
  return {
    votePeriod: "0",
    voteThresholdBps: "0",
    quoteSymbol: "",
    acceptedDenoms: [],
    maxRateAgeSeconds: "0",
    maxAppraisalAgeSeconds: "0",
    maxClassIdsPerAppraiser: "0",
  };
}

export const Params = {
  encode(message: Params, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.votePeriod !== "0") {
      writer.uint32(8).uint64(message.votePeriod);
    }
    if (message.voteThresholdBps !== "0") {
      writer.uint32(16).uint64(message.voteThresholdBps);
    }
    if (message.quoteSymbol !== "") {
      writer.uint32(26).string(message.quoteSymbol);
    }
    for (const v of message.acceptedDenoms) {
      writer.uint32(34).string(v!);
    }
    if (message.maxRateAgeSeconds !== "0") {
      writer.uint32(40).uint64(message.maxRateAgeSeconds);
    }
    if (message.maxAppraisalAgeSeconds !== "0") {
      writer.uint32(48).uint64(message.maxAppraisalAgeSeconds);
    }
    if (message.maxClassIdsPerAppraiser !== "0") {
      writer.uint32(56).uint64(message.maxClassIdsPerAppraiser);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Params {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.votePeriod = longToString(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.voteThresholdBps = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.quoteSymbol = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.acceptedDenoms.push(reader.string());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.maxRateAgeSeconds = longToString(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.maxAppraisalAgeSeconds = longToString(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.maxClassIdsPerAppraiser = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      votePeriod: isSet(object.votePeriod) ? globalThis.String(object.votePeriod) : "0",
      voteThresholdBps: isSet(object.voteThresholdBps) ? globalThis.String(object.voteThresholdBps) : "0",
      quoteSymbol: isSet(object.quoteSymbol) ? globalThis.String(object.quoteSymbol) : "",
      acceptedDenoms: globalThis.Array.isArray(object?.acceptedDenoms)
        ? object.acceptedDenoms.map((e: any) => globalThis.String(e))
        : [],
      maxRateAgeSeconds: isSet(object.maxRateAgeSeconds) ? globalThis.String(object.maxRateAgeSeconds) : "0",
      maxAppraisalAgeSeconds: isSet(object.maxAppraisalAgeSeconds)
        ? globalThis.String(object.maxAppraisalAgeSeconds)
        : "0",
      maxClassIdsPerAppraiser: isSet(object.maxClassIdsPerAppraiser)
        ? globalThis.String(object.maxClassIdsPerAppraiser)
        : "0",
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.votePeriod !== "0") {
      obj.votePeriod = message.votePeriod;
    }
    if (message.voteThresholdBps !== "0") {
      obj.voteThresholdBps = message.voteThresholdBps;
    }
    if (message.quoteSymbol !== "") {
      obj.quoteSymbol = message.quoteSymbol;
    }
    if (message.acceptedDenoms?.length) {
      obj.acceptedDenoms = message.acceptedDenoms;
    }
    if (message.maxRateAgeSeconds !== "0") {
      obj.maxRateAgeSeconds = message.maxRateAgeSeconds;
    }
    if (message.maxAppraisalAgeSeconds !== "0") {
      obj.maxAppraisalAgeSeconds = message.maxAppraisalAgeSeconds;
    }
    if (message.maxClassIdsPerAppraiser !== "0") {
      obj.maxClassIdsPerAppraiser = message.maxClassIdsPerAppraiser;
    }
    return obj;
  },

  create(base?: DeepPartial<Params>): Params {
    return Params.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.votePeriod = object.votePeriod ?? "0";
    message.voteThresholdBps = object.voteThresholdBps ?? "0";
    message.quoteSymbol = object.quoteSymbol ?? "";
    message.acceptedDenoms = object.acceptedDenoms?.map((e) => e) || [];
    message.maxRateAgeSeconds = object.maxRateAgeSeconds ?? "0";
    message.maxAppraisalAgeSeconds = object.maxAppraisalAgeSeconds ?? "0";
    message.maxClassIdsPerAppraiser = object.maxClassIdsPerAppraiser ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
