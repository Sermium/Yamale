// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/oracle/v1/genesis.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal.js";
import { Appraisal, Appraiser, ExchangeRate, FeederDelegation, MissCounter } from "./oracle.ts";
import { Params } from "./params.ts";

export const protobufPackage = "blockchain.oracle.v1";

/**
 * GenesisState defines the oracle module's genesis state.
 *
 * Votes in flight are deliberately absent: a voting round that was open when
 * the chain stopped is abandoned rather than resumed, because the prices in it
 * describe a moment that has passed. Restarting mid-round with stale votes
 * would agree a rate nobody currently stands behind.
 */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params: Params | undefined;
  exchangeRates: ExchangeRate[];
  appraisers: Appraiser[];
  appraisals: Appraisal[];
  feederDelegations: FeederDelegation[];
  missCounters: MissCounter[];
  /**
   * appraisal_history carries superseded valuations. They are exported rather
   * than dropped because they cannot be reconstructed from the current
   * valuations, and the record of what an asset was said to be worth over time
   * is the thing an auditor needs after a dispute. Their sequence numbers are
   * not carried: they are contiguous per asset and are rebuilt on import, so an
   * exported genesis cannot disagree with itself.
   */
  appraisalHistory: Appraisal[];
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    exchangeRates: [],
    appraisers: [],
    appraisals: [],
    feederDelegations: [],
    missCounters: [],
    appraisalHistory: [],
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.exchangeRates) {
      ExchangeRate.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.appraisers) {
      Appraiser.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.appraisals) {
      Appraisal.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.feederDelegations) {
      FeederDelegation.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.missCounters) {
      MissCounter.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    for (const v of message.appraisalHistory) {
      Appraisal.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.exchangeRates.push(ExchangeRate.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.appraisers.push(Appraiser.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.appraisals.push(Appraisal.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.feederDelegations.push(FeederDelegation.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.missCounters.push(MissCounter.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.appraisalHistory.push(Appraisal.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      exchangeRates: globalThis.Array.isArray(object?.exchangeRates)
        ? object.exchangeRates.map((e: any) => ExchangeRate.fromJSON(e))
        : [],
      appraisers: globalThis.Array.isArray(object?.appraisers)
        ? object.appraisers.map((e: any) => Appraiser.fromJSON(e))
        : [],
      appraisals: globalThis.Array.isArray(object?.appraisals)
        ? object.appraisals.map((e: any) => Appraisal.fromJSON(e))
        : [],
      feederDelegations: globalThis.Array.isArray(object?.feederDelegations)
        ? object.feederDelegations.map((e: any) => FeederDelegation.fromJSON(e))
        : [],
      missCounters: globalThis.Array.isArray(object?.missCounters)
        ? object.missCounters.map((e: any) => MissCounter.fromJSON(e))
        : [],
      appraisalHistory: globalThis.Array.isArray(object?.appraisalHistory)
        ? object.appraisalHistory.map((e: any) => Appraisal.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.exchangeRates?.length) {
      obj.exchangeRates = message.exchangeRates.map((e) => ExchangeRate.toJSON(e));
    }
    if (message.appraisers?.length) {
      obj.appraisers = message.appraisers.map((e) => Appraiser.toJSON(e));
    }
    if (message.appraisals?.length) {
      obj.appraisals = message.appraisals.map((e) => Appraisal.toJSON(e));
    }
    if (message.feederDelegations?.length) {
      obj.feederDelegations = message.feederDelegations.map((e) => FeederDelegation.toJSON(e));
    }
    if (message.missCounters?.length) {
      obj.missCounters = message.missCounters.map((e) => MissCounter.toJSON(e));
    }
    if (message.appraisalHistory?.length) {
      obj.appraisalHistory = message.appraisalHistory.map((e) => Appraisal.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.exchangeRates = object.exchangeRates?.map((e) => ExchangeRate.fromPartial(e)) || [];
    message.appraisers = object.appraisers?.map((e) => Appraiser.fromPartial(e)) || [];
    message.appraisals = object.appraisals?.map((e) => Appraisal.fromPartial(e)) || [];
    message.feederDelegations = object.feederDelegations?.map((e) => FeederDelegation.fromPartial(e)) || [];
    message.missCounters = object.missCounters?.map((e) => MissCounter.fromPartial(e)) || [];
    message.appraisalHistory = object.appraisalHistory?.map((e) => Appraisal.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
