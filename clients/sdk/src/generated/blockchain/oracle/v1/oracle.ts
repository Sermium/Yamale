// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/oracle/v1/oracle.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.oracle.v1";

/**
 * AppraiserStatus mirrors the application lifecycle used elsewhere on the
 * chain.
 */
export const AppraiserStatus = {
  /** APPRAISER_STATUS_UNSPECIFIED - APPRAISER_STATUS_UNSPECIFIED is the unset default. */
  APPRAISER_STATUS_UNSPECIFIED: 0,
  /** APPRAISER_STATUS_PENDING - APPRAISER_STATUS_PENDING is awaiting a governance decision. */
  APPRAISER_STATUS_PENDING: 1,
  /** APPRAISER_STATUS_APPROVED - APPRAISER_STATUS_APPROVED may sign appraisals within its scope. */
  APPRAISER_STATUS_APPROVED: 2,
  /** APPRAISER_STATUS_REJECTED - APPRAISER_STATUS_REJECTED was refused, and may apply again. */
  APPRAISER_STATUS_REJECTED: 3,
  UNRECOGNIZED: -1,
} as const;

export type AppraiserStatus = typeof AppraiserStatus[keyof typeof AppraiserStatus];

export namespace AppraiserStatus {
  export type APPRAISER_STATUS_UNSPECIFIED = typeof AppraiserStatus.APPRAISER_STATUS_UNSPECIFIED;
  export type APPRAISER_STATUS_PENDING = typeof AppraiserStatus.APPRAISER_STATUS_PENDING;
  export type APPRAISER_STATUS_APPROVED = typeof AppraiserStatus.APPRAISER_STATUS_APPROVED;
  export type APPRAISER_STATUS_REJECTED = typeof AppraiserStatus.APPRAISER_STATUS_REJECTED;
  export type UNRECOGNIZED = typeof AppraiserStatus.UNRECOGNIZED;
}

export function appraiserStatusFromJSON(object: any): AppraiserStatus {
  switch (object) {
    case 0:
    case "APPRAISER_STATUS_UNSPECIFIED":
      return AppraiserStatus.APPRAISER_STATUS_UNSPECIFIED;
    case 1:
    case "APPRAISER_STATUS_PENDING":
      return AppraiserStatus.APPRAISER_STATUS_PENDING;
    case 2:
    case "APPRAISER_STATUS_APPROVED":
      return AppraiserStatus.APPRAISER_STATUS_APPROVED;
    case 3:
    case "APPRAISER_STATUS_REJECTED":
      return AppraiserStatus.APPRAISER_STATUS_REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AppraiserStatus.UNRECOGNIZED;
  }
}

export function appraiserStatusToJSON(object: AppraiserStatus): string {
  switch (object) {
    case AppraiserStatus.APPRAISER_STATUS_UNSPECIFIED:
      return "APPRAISER_STATUS_UNSPECIFIED";
    case AppraiserStatus.APPRAISER_STATUS_PENDING:
      return "APPRAISER_STATUS_PENDING";
    case AppraiserStatus.APPRAISER_STATUS_APPROVED:
      return "APPRAISER_STATUS_APPROVED";
    case AppraiserStatus.APPRAISER_STATUS_REJECTED:
      return "APPRAISER_STATUS_REJECTED";
    case AppraiserStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ExchangeRate is the chain's agreed price for one denom, in terms of the
 * quote symbol set in the module's parameters.
 *
 * The rate prices one *display* unit, not one base unit — 1 YML, not 1 uyml —
 * because that is the number a person quotes and the number a feeder reads off
 * a market. Consumers scale by the denom's exponent.
 */
export interface ExchangeRate {
  denom: string;
  /** rate is a decimal string, e.g. "0.4213". */
  rate: string;
  /**
   * updated_at is when this rate was agreed, in Unix seconds. Staleness is
   * judged from here, so a feed that stops reporting stops being usable rather
   * than silently freezing at its last value.
   */
  updatedAt: string;
  updatedHeight: string;
  /**
   * voting_power is the share of stake, in basis points, that contributed to
   * this median. A rate agreed by a bare quorum deserves less trust than one
   * agreed by everybody, and consumers can see the difference.
   */
  votingPowerBps: string;
}

/** ExchangeRateVote is one validator's report for the current vote period. */
export interface ExchangeRateVote {
  validator: string;
  denom: string;
  rate: string;
}

/**
 * FeederDelegation lets a validator nominate a hot key to submit its votes.
 *
 * Without this a validator would have to sign with its operator key every vote
 * period, which means keeping that key online — the one key whose compromise
 * costs the most. The delegate can only vote; it cannot move stake, change
 * commission, or do anything else on the validator's behalf.
 */
export interface FeederDelegation {
  validator: string;
  feeder: string;
}

/**
 * MissCounter records how many vote periods a validator failed to report in.
 *
 * It is recorded rather than punished. On a small permissioned network an
 * automatic slash mostly punishes the operator whose VM rebooted, and the
 * social layer — a known set of accountable validators — handles absence
 * better than an incentive that fires during an outage. Governance can add a
 * penalty later; it cannot easily undo one that fired wrongly.
 */
export interface MissCounter {
  validator: string;
  misses: string;
  windows: string;
}

/**
 * Appraiser is an independent party that governance has admitted to value
 * real-world assets.
 *
 * Independence is the whole point: the party that values the collateral must
 * not be the party that benefits from the valuation. The chain cannot verify
 * independence, so it does the next best thing — it records who governance
 * admitted, what they were admitted to value, and attributes every number they
 * sign.
 */
export interface Appraiser {
  address: string;
  /** name is the firm or individual, for the audit trail. */
  name: string;
  /**
   * credentials points at the licence, registration or accreditation that
   * governance relied on when admitting them.
   */
  credentials: string;
  /**
   * class_ids limits what they may value: an NFT class each corresponds to an
   * asset type. Empty means every class, which governance should grant rarely.
   */
  classIds: string[];
  status: AppraiserStatus;
  approvedAtHeight: string;
}

/**
 * Appraisal is one valuer's signed opinion of what a specific tokenised asset
 * is worth.
 *
 * It is bound to an NFT — the token that represents the asset — rather than to
 * a denom, because each real-world asset is individual. Two invoices from the
 * same issuer are not interchangeable, and a fungible price for them would be
 * a fiction.
 */
export interface Appraisal {
  /** class_id and nft_id identify the asset in x/nft. */
  classId: string;
  nftId: string;
  /** value is what the asset is worth, in base units of value_denom. */
  value: string;
  valueDenom: string;
  /** appraiser is the address that signed this valuation. */
  appraiser: string;
  /**
   * valued_at is the date the valuation reflects, in Unix seconds. This is the
   * date of the inspection or the NAV, which is not the same as when it was
   * submitted — a monthly NAV published late is still a month-end number, and
   * treating the two as the same would overstate how current it is.
   */
  valuedAt: string;
  /** submitted_at is when the chain recorded it. */
  submittedAt: string;
  submittedHeight: string;
  /**
   * method describes how the number was reached, e.g. "RICS Red Book",
   * "discounted cash flow", "administrator NAV".
   */
  method: string;
  /**
   * report_uri points at the signed valuation document, and report_hash pins
   * its contents so the on-chain number and the off-chain report cannot drift
   * apart unnoticed.
   */
  reportUri: string;
  reportHash: string;
  /**
   * superseded is true once a newer appraisal for the same asset exists. Old
   * appraisals are retained rather than overwritten, because the history of
   * what an asset was said to be worth, and by whom, is exactly what an auditor
   * needs after a dispute.
   */
  superseded: boolean;
}

function createBaseExchangeRate(): ExchangeRate {
  return { denom: "", rate: "", updatedAt: "0", updatedHeight: "0", votingPowerBps: "0" };
}

export const ExchangeRate = {
  encode(message: ExchangeRate, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.denom !== "") {
      writer.uint32(10).string(message.denom);
    }
    if (message.rate !== "") {
      writer.uint32(18).string(message.rate);
    }
    if (message.updatedAt !== "0") {
      writer.uint32(24).int64(message.updatedAt);
    }
    if (message.updatedHeight !== "0") {
      writer.uint32(32).int64(message.updatedHeight);
    }
    if (message.votingPowerBps !== "0") {
      writer.uint32(40).uint64(message.votingPowerBps);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExchangeRate {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeRate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.rate = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.updatedAt = longToString(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.updatedHeight = longToString(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.votingPowerBps = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeRate {
    return {
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      rate: isSet(object.rate) ? globalThis.String(object.rate) : "",
      updatedAt: isSet(object.updatedAt) ? globalThis.String(object.updatedAt) : "0",
      updatedHeight: isSet(object.updatedHeight) ? globalThis.String(object.updatedHeight) : "0",
      votingPowerBps: isSet(object.votingPowerBps) ? globalThis.String(object.votingPowerBps) : "0",
    };
  },

  toJSON(message: ExchangeRate): unknown {
    const obj: any = {};
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.rate !== "") {
      obj.rate = message.rate;
    }
    if (message.updatedAt !== "0") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.updatedHeight !== "0") {
      obj.updatedHeight = message.updatedHeight;
    }
    if (message.votingPowerBps !== "0") {
      obj.votingPowerBps = message.votingPowerBps;
    }
    return obj;
  },

  create(base?: DeepPartial<ExchangeRate>): ExchangeRate {
    return ExchangeRate.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExchangeRate>): ExchangeRate {
    const message = createBaseExchangeRate();
    message.denom = object.denom ?? "";
    message.rate = object.rate ?? "";
    message.updatedAt = object.updatedAt ?? "0";
    message.updatedHeight = object.updatedHeight ?? "0";
    message.votingPowerBps = object.votingPowerBps ?? "0";
    return message;
  },
};

function createBaseExchangeRateVote(): ExchangeRateVote {
  return { validator: "", denom: "", rate: "" };
}

export const ExchangeRateVote = {
  encode(message: ExchangeRateVote, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.validator !== "") {
      writer.uint32(10).string(message.validator);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.rate !== "") {
      writer.uint32(26).string(message.rate);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExchangeRateVote {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeRateVote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.rate = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeRateVote {
    return {
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      rate: isSet(object.rate) ? globalThis.String(object.rate) : "",
    };
  },

  toJSON(message: ExchangeRateVote): unknown {
    const obj: any = {};
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.rate !== "") {
      obj.rate = message.rate;
    }
    return obj;
  },

  create(base?: DeepPartial<ExchangeRateVote>): ExchangeRateVote {
    return ExchangeRateVote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExchangeRateVote>): ExchangeRateVote {
    const message = createBaseExchangeRateVote();
    message.validator = object.validator ?? "";
    message.denom = object.denom ?? "";
    message.rate = object.rate ?? "";
    return message;
  },
};

function createBaseFeederDelegation(): FeederDelegation {
  return { validator: "", feeder: "" };
}

export const FeederDelegation = {
  encode(message: FeederDelegation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.validator !== "") {
      writer.uint32(10).string(message.validator);
    }
    if (message.feeder !== "") {
      writer.uint32(18).string(message.feeder);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FeederDelegation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeederDelegation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.feeder = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeederDelegation {
    return {
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      feeder: isSet(object.feeder) ? globalThis.String(object.feeder) : "",
    };
  },

  toJSON(message: FeederDelegation): unknown {
    const obj: any = {};
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.feeder !== "") {
      obj.feeder = message.feeder;
    }
    return obj;
  },

  create(base?: DeepPartial<FeederDelegation>): FeederDelegation {
    return FeederDelegation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FeederDelegation>): FeederDelegation {
    const message = createBaseFeederDelegation();
    message.validator = object.validator ?? "";
    message.feeder = object.feeder ?? "";
    return message;
  },
};

function createBaseMissCounter(): MissCounter {
  return { validator: "", misses: "0", windows: "0" };
}

export const MissCounter = {
  encode(message: MissCounter, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.validator !== "") {
      writer.uint32(10).string(message.validator);
    }
    if (message.misses !== "0") {
      writer.uint32(16).uint64(message.misses);
    }
    if (message.windows !== "0") {
      writer.uint32(24).uint64(message.windows);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MissCounter {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMissCounter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.validator = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.misses = longToString(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.windows = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MissCounter {
    return {
      validator: isSet(object.validator) ? globalThis.String(object.validator) : "",
      misses: isSet(object.misses) ? globalThis.String(object.misses) : "0",
      windows: isSet(object.windows) ? globalThis.String(object.windows) : "0",
    };
  },

  toJSON(message: MissCounter): unknown {
    const obj: any = {};
    if (message.validator !== "") {
      obj.validator = message.validator;
    }
    if (message.misses !== "0") {
      obj.misses = message.misses;
    }
    if (message.windows !== "0") {
      obj.windows = message.windows;
    }
    return obj;
  },

  create(base?: DeepPartial<MissCounter>): MissCounter {
    return MissCounter.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MissCounter>): MissCounter {
    const message = createBaseMissCounter();
    message.validator = object.validator ?? "";
    message.misses = object.misses ?? "0";
    message.windows = object.windows ?? "0";
    return message;
  },
};

function createBaseAppraiser(): Appraiser {
  return { address: "", name: "", credentials: "", classIds: [], status: 0, approvedAtHeight: "0" };
}

export const Appraiser = {
  encode(message: Appraiser, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.credentials !== "") {
      writer.uint32(26).string(message.credentials);
    }
    for (const v of message.classIds) {
      writer.uint32(34).string(v!);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.approvedAtHeight !== "0") {
      writer.uint32(48).uint64(message.approvedAtHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Appraiser {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppraiser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.credentials = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.classIds.push(reader.string());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.approvedAtHeight = longToString(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Appraiser {
    return {
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      credentials: isSet(object.credentials) ? globalThis.String(object.credentials) : "",
      classIds: globalThis.Array.isArray(object?.classIds) ? object.classIds.map((e: any) => globalThis.String(e)) : [],
      status: isSet(object.status) ? appraiserStatusFromJSON(object.status) : 0,
      approvedAtHeight: isSet(object.approvedAtHeight) ? globalThis.String(object.approvedAtHeight) : "0",
    };
  },

  toJSON(message: Appraiser): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.credentials !== "") {
      obj.credentials = message.credentials;
    }
    if (message.classIds?.length) {
      obj.classIds = message.classIds;
    }
    if (message.status !== 0) {
      obj.status = appraiserStatusToJSON(message.status);
    }
    if (message.approvedAtHeight !== "0") {
      obj.approvedAtHeight = message.approvedAtHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<Appraiser>): Appraiser {
    return Appraiser.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Appraiser>): Appraiser {
    const message = createBaseAppraiser();
    message.address = object.address ?? "";
    message.name = object.name ?? "";
    message.credentials = object.credentials ?? "";
    message.classIds = object.classIds?.map((e) => e) || [];
    message.status = object.status ?? 0;
    message.approvedAtHeight = object.approvedAtHeight ?? "0";
    return message;
  },
};

function createBaseAppraisal(): Appraisal {
  return {
    classId: "",
    nftId: "",
    value: "",
    valueDenom: "",
    appraiser: "",
    valuedAt: "0",
    submittedAt: "0",
    submittedHeight: "0",
    method: "",
    reportUri: "",
    reportHash: "",
    superseded: false,
  };
}

export const Appraisal = {
  encode(message: Appraisal, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.classId !== "") {
      writer.uint32(10).string(message.classId);
    }
    if (message.nftId !== "") {
      writer.uint32(18).string(message.nftId);
    }
    if (message.value !== "") {
      writer.uint32(26).string(message.value);
    }
    if (message.valueDenom !== "") {
      writer.uint32(34).string(message.valueDenom);
    }
    if (message.appraiser !== "") {
      writer.uint32(42).string(message.appraiser);
    }
    if (message.valuedAt !== "0") {
      writer.uint32(48).int64(message.valuedAt);
    }
    if (message.submittedAt !== "0") {
      writer.uint32(56).int64(message.submittedAt);
    }
    if (message.submittedHeight !== "0") {
      writer.uint32(64).uint64(message.submittedHeight);
    }
    if (message.method !== "") {
      writer.uint32(74).string(message.method);
    }
    if (message.reportUri !== "") {
      writer.uint32(82).string(message.reportUri);
    }
    if (message.reportHash !== "") {
      writer.uint32(90).string(message.reportHash);
    }
    if (message.superseded !== false) {
      writer.uint32(96).bool(message.superseded);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Appraisal {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppraisal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.classId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nftId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.value = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.valueDenom = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.appraiser = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.valuedAt = longToString(reader.int64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.submittedAt = longToString(reader.int64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.submittedHeight = longToString(reader.uint64() as Long);
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.method = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.reportUri = reader.string();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.reportHash = reader.string();
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.superseded = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Appraisal {
    return {
      classId: isSet(object.classId) ? globalThis.String(object.classId) : "",
      nftId: isSet(object.nftId) ? globalThis.String(object.nftId) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      valueDenom: isSet(object.valueDenom) ? globalThis.String(object.valueDenom) : "",
      appraiser: isSet(object.appraiser) ? globalThis.String(object.appraiser) : "",
      valuedAt: isSet(object.valuedAt) ? globalThis.String(object.valuedAt) : "0",
      submittedAt: isSet(object.submittedAt) ? globalThis.String(object.submittedAt) : "0",
      submittedHeight: isSet(object.submittedHeight) ? globalThis.String(object.submittedHeight) : "0",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      reportUri: isSet(object.reportUri) ? globalThis.String(object.reportUri) : "",
      reportHash: isSet(object.reportHash) ? globalThis.String(object.reportHash) : "",
      superseded: isSet(object.superseded) ? globalThis.Boolean(object.superseded) : false,
    };
  },

  toJSON(message: Appraisal): unknown {
    const obj: any = {};
    if (message.classId !== "") {
      obj.classId = message.classId;
    }
    if (message.nftId !== "") {
      obj.nftId = message.nftId;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.valueDenom !== "") {
      obj.valueDenom = message.valueDenom;
    }
    if (message.appraiser !== "") {
      obj.appraiser = message.appraiser;
    }
    if (message.valuedAt !== "0") {
      obj.valuedAt = message.valuedAt;
    }
    if (message.submittedAt !== "0") {
      obj.submittedAt = message.submittedAt;
    }
    if (message.submittedHeight !== "0") {
      obj.submittedHeight = message.submittedHeight;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.reportUri !== "") {
      obj.reportUri = message.reportUri;
    }
    if (message.reportHash !== "") {
      obj.reportHash = message.reportHash;
    }
    if (message.superseded !== false) {
      obj.superseded = message.superseded;
    }
    return obj;
  },

  create(base?: DeepPartial<Appraisal>): Appraisal {
    return Appraisal.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Appraisal>): Appraisal {
    const message = createBaseAppraisal();
    message.classId = object.classId ?? "";
    message.nftId = object.nftId ?? "";
    message.value = object.value ?? "";
    message.valueDenom = object.valueDenom ?? "";
    message.appraiser = object.appraiser ?? "";
    message.valuedAt = object.valuedAt ?? "0";
    message.submittedAt = object.submittedAt ?? "0";
    message.submittedHeight = object.submittedHeight ?? "0";
    message.method = object.method ?? "";
    message.reportUri = object.reportUri ?? "";
    message.reportHash = object.reportHash ?? "";
    message.superseded = object.superseded ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
