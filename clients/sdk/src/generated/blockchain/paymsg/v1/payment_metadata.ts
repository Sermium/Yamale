// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: blockchain/paymsg/v1/payment_metadata.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal.js";

export const protobufPackage = "blockchain.paymsg.v1";

/**
 * PaymentMetadata is the ISO 20022 detail that used to travel in
 * MsgSendPayment's purpose_code and remittance_information fields.
 *
 * It is never sent to the chain and is never stored by it. The chain sees only
 * SHA-256 over this message's encoding, in MsgSendPayment.metadata_hash; the
 * message itself is held off-chain by the parties. It is declared here, rather
 * than in each client, so that the payer, the payee, the regulator and the node
 * all hash the same bytes — three hand-written serialisers in three languages
 * would agree until the first field anybody added, and a hash that disagrees
 * proves nothing while looking exactly like a hash that works.
 *
 * Encoding is the protobuf encoding: fields in ascending number, defaults
 * omitted. That is the same determinism the chain already stakes transaction
 * signing on, so it is not a new assumption.
 */
export interface PaymentMetadata {
  /**
   * salt is 32 random bytes, regenerated for every payment.
   *
   * Without it this whole exercise publishes what it set out to hide. A purpose
   * code is four characters drawn from a published list, so the unsalted hash
   * of one is not a fingerprint — it is a lookup table anybody can build in a
   * second. Remittance text is barely better: invoice numbers and names are
   * guessable, and the ledger is public and permanent, so a guess can be tested
   * for as long as the chain exists.
   *
   * Regenerated per payment, not per account, because a reused salt means two
   * payments carrying the same detail hash to the same value, which tells an
   * observer they are the same detail without ever revealing what it is.
   */
  salt: Uint8Array;
  purposeCode: string;
  remittanceInformation: string;
}

function createBasePaymentMetadata(): PaymentMetadata {
  return { salt: new Uint8Array(0), purposeCode: "", remittanceInformation: "" };
}

export const PaymentMetadata = {
  encode(message: PaymentMetadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.salt.length !== 0) {
      writer.uint32(10).bytes(message.salt);
    }
    if (message.purposeCode !== "") {
      writer.uint32(18).string(message.purposeCode);
    }
    if (message.remittanceInformation !== "") {
      writer.uint32(26).string(message.remittanceInformation);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PaymentMetadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaymentMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.salt = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.purposeCode = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.remittanceInformation = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PaymentMetadata {
    return {
      salt: isSet(object.salt) ? bytesFromBase64(object.salt) : new Uint8Array(0),
      purposeCode: isSet(object.purposeCode) ? globalThis.String(object.purposeCode) : "",
      remittanceInformation: isSet(object.remittanceInformation) ? globalThis.String(object.remittanceInformation) : "",
    };
  },

  toJSON(message: PaymentMetadata): unknown {
    const obj: any = {};
    if (message.salt.length !== 0) {
      obj.salt = base64FromBytes(message.salt);
    }
    if (message.purposeCode !== "") {
      obj.purposeCode = message.purposeCode;
    }
    if (message.remittanceInformation !== "") {
      obj.remittanceInformation = message.remittanceInformation;
    }
    return obj;
  },

  create(base?: DeepPartial<PaymentMetadata>): PaymentMetadata {
    return PaymentMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PaymentMetadata>): PaymentMetadata {
    const message = createBasePaymentMetadata();
    message.salt = object.salt ?? new Uint8Array(0);
    message.purposeCode = object.purposeCode ?? "";
    message.remittanceInformation = object.remittanceInformation ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
