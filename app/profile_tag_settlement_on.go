//go:build settlement

package app

// profileTagSettlement is the tag's own name, carried into ProfileName so an
// error message can say which build the operator is running.
const profileTagSettlement = "settlement"
